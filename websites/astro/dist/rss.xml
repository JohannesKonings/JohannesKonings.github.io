<?xml version="1.0" encoding="UTF-8"?><rss version="2.0" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Johannes Konings</title><description>Contact, notes and some posts</description><link>https://johanneskonings.dev/</link><item><title>TanStack AI with AWS Bedrock on TanStack Start (simple example)</title><link>https://johanneskonings.dev/blog/2026-02-02-tanstack-ai-bedrock-simple/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2026-02-02-tanstack-ai-bedrock-simple/</guid><description>Add TanStack AI with AWS Bedrock to your TanStack Start on AWS setup: Bedrock usage, IAM permissions, and a minimal chat example.</description><pubDate>Mon, 02 Feb 2026 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Introduction&lt;/h2&gt;
&lt;p&gt;In &lt;a href=&quot;/blog/2025-11-30-tanstack-start-aws-serverless/&quot;&gt;Deploying TanStack Start on AWS with Lambda Function URLs&lt;/a&gt;, I describe how to deploy TanStack Start serverless on AWS. This post shows how to add &lt;a href=&quot;https://tanstack.com/ai/latest&quot;&gt;TanStack AI&lt;/a&gt; with &lt;a href=&quot;https://aws.amazon.com/bedrock/&quot;&gt;AWS Bedrock&lt;/a&gt; to that setup: Bedrock usage, IAM wiring, and a minimal chat example.&lt;/p&gt;
&lt;p&gt;The implementation is available in the &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws&quot;&gt;tanstack-aws repository&lt;/a&gt; at the tag &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/tree/2026-02-02-tanstack-ai-bedrock-simple&quot;&gt;2026-02-02-tanstack-ai-bedrock-simple&lt;/a&gt;.&lt;/p&gt;
&lt;h2&gt;Disclaimer&lt;/h2&gt;
&lt;p&gt;This is a minimal example to get you started. It is not production-ready; restrict Bedrock resources by model ARN, add auth, and harden error handling as needed.&lt;/p&gt;
&lt;h2&gt;TanStack AI and Bedrock&lt;/h2&gt;
&lt;p&gt;TanStack AI provides a unified API for AI features (chat, completion, streaming) and supports multiple backends via providers. AWS Bedrock is the model backend here: you configure a Bedrock provider with region and model id, and the app uses it for streaming chat.&lt;/p&gt;
&lt;p&gt;TanStack AI is a good fit for this stack: it integrates with &lt;strong&gt;TanStack DevTools&lt;/strong&gt; so you can inspect requests and streams during development. &lt;strong&gt;Deeper integration&lt;/strong&gt; with the router, SSR, or data layer is thinkable for future posts.&lt;/p&gt;
&lt;p&gt;An official Bedrock adapter is not yet in TanStack AI, but it is on the &lt;a href=&quot;https://tanstack.com/blog/tanstack-ai-alpha-2#whats-next&quot;&gt;roadmap&lt;/a&gt;, and &lt;a href=&quot;https://github.com/TanStack/ai/pull/220&quot;&gt;pull requests&lt;/a&gt; already exist.&lt;/p&gt;
&lt;h2&gt;Custom Bedrock adapter&lt;/h2&gt;
&lt;p&gt;Until the official adapter lands, the tanstack-aws repo uses a &lt;strong&gt;custom Bedrock adapter&lt;/strong&gt; that plugs into TanStack AI. It wraps the AWS Bedrock Runtime client (Converse / ConverseStream) and exposes the interface TanStack AI expects for chat and streaming.&lt;/p&gt;
&lt;p&gt;Key files at the &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/tree/2026-02-02-tanstack-ai-bedrock-simple&quot;&gt;2026-02-02-tanstack-ai-bedrock-simple&lt;/a&gt; tag:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Adapter&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2026-02-02-tanstack-ai-bedrock-simple/src/webapp/integrations/bedrock-adapter/index.ts&quot;&gt;src/webapp/integrations/bedrock-adapter/index.ts&lt;/a&gt; — custom Bedrock provider for TanStack AI&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Chat API route&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2026-02-02-tanstack-ai-bedrock-simple/src/webapp/routes/demo/api.tanchat.ts&quot;&gt;src/webapp/routes/demo/api.tanchat.ts&lt;/a&gt; — server route that uses the adapter and streams the response&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Chat UI&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2026-02-02-tanstack-ai-bedrock-simple/src/webapp/routes/demo/tanchat.tsx&quot;&gt;src/webapp/routes/demo/tanchat.tsx&lt;/a&gt; — demo chat page that calls the API and renders the stream&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;A small &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2026-02-02-tanstack-ai-bedrock-simple/scripts/test-bedrock-chat.ts&quot;&gt;test script&lt;/a&gt; (&lt;code&gt;pnpm test:bedrock&lt;/code&gt;) calls the same Bedrock model (Converse and ConverseStream) without the web app, for quick verification.&lt;/p&gt;
&lt;h2&gt;Bedrock and IAM in CDK&lt;/h2&gt;
&lt;p&gt;The Lambda that runs the TanStack Start server (and thus the AI route) must be allowed to call Bedrock. In CDK, add a policy to the Lambda’s role with &lt;code&gt;bedrock:InvokeModel&lt;/code&gt; and &lt;code&gt;bedrock:InvokeModelWithResponseStream&lt;/code&gt;. The example below uses &lt;code&gt;resources: [&quot;*&quot;]&lt;/code&gt;; for production, scope &lt;code&gt;resources&lt;/code&gt; to the specific Bedrock model ARNs you use.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { Code, Function, Runtime } from &quot;aws-cdk-lib/aws-lambda&quot;;
import { Duration, Tags } from &quot;aws-cdk-lib&quot;;
import { Effect, PolicyStatement } from &quot;aws-cdk-lib/aws-iam&quot;;
import { Construct } from &quot;constructs&quot;;
import path from &quot;node:path&quot;;

export class WebappServer extends Construct {
  readonly webappServer: Function;

  constructor(scope: Construct, id: string) {
    super(scope, id);

    this.webappServer = new Function(this, &quot;WebappServer&quot;, {
      code: Code.fromAsset(
        path.join(
          path.dirname(new URL(import.meta.url).pathname),
          &quot;../../.output/server&quot;,
        ),
      ),
      handler: &quot;index.handler&quot;,
      memorySize: 2048,
      runtime: Runtime.NODEJS_24_X,
      timeout: Duration.seconds(60),
    });
    Tags.of(this.webappServer).add(&quot;IsWebAppServer&quot;, &quot;true&quot;);

    this.webappServer.addToRolePolicy(
      new PolicyStatement({
        actions: [
          &quot;bedrock:InvokeModel&quot;,
          &quot;bedrock:InvokeModelWithResponseStream&quot;,
        ],
        effect: Effect.ALLOW,
        resources: [&quot;*&quot;],
      }),
    );
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;https://github.com/JohannesKonings/tanstack-aws/blob/2026-02-02-tanstack-ai-bedrock-simple/lib/constructs/WebappServer.ts&lt;/p&gt;
&lt;p&gt;The Lambda runs in your AWS account and uses its execution role; no API keys are required in the app. Bedrock is invoked from the server (e.g. in a TanStack Start API route or server handler), so credentials stay off the client.&lt;/p&gt;
&lt;h2&gt;Using the Bedrock provider in the app&lt;/h2&gt;
&lt;p&gt;In the app, create a Bedrock provider (region, model id, and optionally env-driven config) and use it where you need AI—for example in a server route that handles chat and streams the response. The &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/tree/2026-02-02-tanstack-ai-bedrock-simple&quot;&gt;tanstack-aws&lt;/a&gt; repo at the tag above implements a demo chat with streaming; see the Bedrock provider and chat route there for the full code.&lt;/p&gt;
&lt;p&gt;The flow is: client calls your API route → server uses the Bedrock provider and TanStack AI to call the model and stream back → client renders the stream. TanStack AI’s streaming API and optional DevTools help with debugging and UX.&lt;/p&gt;
&lt;h2&gt;Checking a budget with metrics&lt;/h2&gt;
&lt;p&gt;AWS Bedrock publishes usage to &lt;strong&gt;CloudWatch&lt;/strong&gt; in the &lt;code&gt;AWS/Bedrock&lt;/code&gt; namespace: metrics such as &lt;code&gt;InputTokenCount&lt;/code&gt; and &lt;code&gt;OutputTokenCount&lt;/code&gt; with dimension &lt;code&gt;ModelId&lt;/code&gt;. You can use these to estimate daily cost and enforce a simple budget.&lt;/p&gt;
&lt;p&gt;In the tanstack-aws repo at the tag:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Get today’s token usage&lt;/strong&gt; — Call CloudWatch &lt;code&gt;GetMetricStatistics&lt;/code&gt; for the model’s &lt;code&gt;InputTokenCount&lt;/code&gt; and &lt;code&gt;OutputTokenCount&lt;/code&gt; over the current UTC day and sum the datapoints.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Estimate cost&lt;/strong&gt; — Multiply input/output token counts by your model’s per‑million‑token prices (e.g. Nova Pro; configurable via env) to get an estimated cost in USD.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Compare to a limit&lt;/strong&gt; — Define a daily limit (e.g. $2) and return &lt;code&gt;overBudget&lt;/code&gt; when estimated cost ≥ limit. The chat route can then block or warn before calling Bedrock.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Key files:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2026-02-02-tanstack-ai-bedrock-simple/src/webapp/lib/bedrock-budget-config.ts&quot;&gt;src/webapp/lib/bedrock-budget-config.ts&lt;/a&gt; — daily limit (e.g. &lt;code&gt;DAILY_LIMIT_USD&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2026-02-02-tanstack-ai-bedrock-simple/src/webapp/lib/bedrock-budget.ts&quot;&gt;src/webapp/lib/bedrock-budget.ts&lt;/a&gt; — CloudWatch client, &lt;code&gt;GetMetricStatistics&lt;/code&gt; for tokens, cost estimation, and a server function for the UI&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2026-02-02-tanstack-ai-bedrock-simple/src/webapp/routes/demo/api.bedrock-budget.ts&quot;&gt;src/webapp/routes/demo/api.bedrock-budget.ts&lt;/a&gt; — API route that returns budget status (tokens, estimated cost, overBudget)&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The Lambda needs &lt;strong&gt;CloudWatch read&lt;/strong&gt; permissions for these metrics: &lt;code&gt;cloudwatch:GetMetricStatistics&lt;/code&gt; and &lt;code&gt;cloudwatch:ListMetrics&lt;/code&gt;. The &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2026-02-02-tanstack-ai-bedrock-simple/lib/constructs/WebappServer.ts&quot;&gt;WebappServer&lt;/a&gt; at the tag adds this policy alongside the Bedrock one.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Disclaimer:&lt;/strong&gt; The budget estimate is not accurate — CloudWatch metrics have a delay, so recent usage may not yet appear. It is fine for a simple use case (e.g. a soft cap or rough visibility); for stricter controls, consider other mechanisms.&lt;/p&gt;
&lt;h2&gt;Result&lt;/h2&gt;
&lt;p&gt;The template repository includes a demo chat powered by AWS Bedrock (Claude) with streaming. For a screenshot and deployment overview, see the &lt;a href=&quot;/blog/2025-11-30-tanstack-start-aws-serverless/&quot;&gt;TanStack Start serverless&lt;/a&gt; post.&lt;/p&gt;
&lt;h2&gt;Conclusion&lt;/h2&gt;
&lt;p&gt;This post showed how to add TanStack AI with AWS Bedrock to TanStack Start on AWS: grant the Lambda role &lt;code&gt;bedrock:InvokeModel&lt;/code&gt; and &lt;code&gt;bedrock:InvokeModelWithResponseStream&lt;/code&gt;, use a Bedrock provider in the app, and optionally rely on TanStack DevTools and deeper framework integration later. Next steps include scoping IAM to model ARNs, adding auth to the chat route, and exploring deeper TanStack integration.&lt;/p&gt;
&lt;h2&gt;Sources and References&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;tanstack-aws (implementation at tag)&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/tree/2026-02-02-tanstack-ai-bedrock-simple&quot;&gt;github.com/JohannesKonings/tanstack-aws/tree/2026-02-02-tanstack-ai-bedrock-simple&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;TanStack AI&lt;/strong&gt;: &lt;a href=&quot;https://tanstack.com/ai/latest&quot;&gt;tanstack.com/ai/latest&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;AWS Bedrock&lt;/strong&gt;: &lt;a href=&quot;https://docs.aws.amazon.com/bedrock/&quot;&gt;docs.aws.amazon.com/bedrock&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>Tag log buckets created by AWS CDK for third party tools</title><link>https://johanneskonings.dev/blog/2026-01-11-aws-cdk-nag-log-bucket-tagger/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2026-01-11-aws-cdk-nag-log-bucket-tagger/</guid><description>How to tag S3 log buckets created by the AWS CDK so third-party scanners can recognize them.</description><pubDate>Sun, 11 Jan 2026 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Overview&lt;/h2&gt;
&lt;p&gt;How you secure cloud configuration often starts with cdk-nag, but many teams also use third-party tools after deployments.
Using the “server access logs not configured” example, this post shows how cdk-nag can interact with those tools by tagging log buckets created by the AWS CDK.&lt;/p&gt;
&lt;p&gt;This example focuses on the AwsSolutions-S1 rule from the AWS Solutions ruleset (https://github.com/cdklabs/cdk-nag/blob/main/RULES.md#awssolutions).&lt;/p&gt;
&lt;p&gt;As of &lt;a href=&quot;https://github.com/JohannesKonings/cdk-nag-custom-nag-pack/releases/tag/v0.0.11&quot;&gt;@jaykingson/cdk-nag-custom-nag-pack v0.0.11&lt;/a&gt;, this behavior is available via a log bucket tagger and an AwsSolutions-S1 suppression helper.&lt;/p&gt;
&lt;h2&gt;The cdk-nag log bucket behavior&lt;/h2&gt;
&lt;p&gt;If the log bucket and the source buckets are created in the same AWS CDK app, cdk-nag can identify that relationship and won’t raise AwsSolutions-S1 for the log bucket.
If the app only provides the log bucket for usage outside the app, cdk-nag will raise AwsSolutions-S1.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;s1-log-bucket-outside-app.png&quot; alt=&quot;log bucket outside app&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;The bridge to third party tools&lt;/h2&gt;
&lt;p&gt;If a third-party tool doesn’t check bucket dependencies and only checks whether the log bucket has server access logs enabled, it will still raise findings.
One universal approach is to suppress those findings based on tags on the bucket itself.&lt;/p&gt;
&lt;h2&gt;The extension for cdk-nag to handle log bucket tagging&lt;/h2&gt;
&lt;p&gt;Because cdk-nag can identify the dependency within the app, it can also tag the log bucket to indicate that it is used for server access logs.
e.g. &lt;code&gt;isLogBucket=true&lt;/code&gt; and for better visibility an additional tag &lt;code&gt;LogBucketTaggedBy=cdk-nag&lt;/code&gt; (or however you want the source to look).&lt;/p&gt;
&lt;h2&gt;The result&lt;/h2&gt;
&lt;p&gt;This app contains different usage patterns for log buckets and shows how the custom checks suppress and tag the log bucket.&lt;/p&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;TypeScript example&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;#!/usr/bin/env node
import { App, Aspects, Stack } from &quot;aws-cdk-lib&quot;;
import {
  CustomChecks,
  CustomChecksSuppressions,
} from &quot;@jaykingson/cdk-nag-custom-nag-pack&quot;;
import { Bucket } from &quot;aws-cdk-lib/aws-s3&quot;;

const app = new App();

// Create two separate CloudFormation stacks to demonstrate cross-stack resource references
// Stack 1 will contain the primary log bucket and a test bucket
const stack = new Stack(app, &quot;Stack&quot;);
// Stack 2 will demonstrate referencing log buckets from different stacks
const stack2 = new Stack(app, &quot;Stack2&quot;);

// Create a dedicated log bucket that will serve as the central logging destination
// for other application buckets across the infrastructure
const logBucketForOtherAppsBuckets = new Bucket(
  stack,
  &quot;LogBucketForOtherAppsBuckets&quot;,
  {
    enforceSSL: true,
  },
);
// Usage of the new supression, which also tag as an log bucket
CustomChecksSuppressions.addLogBucketS1Suppression(
  logBucketForOtherAppsBuckets,
);

// Reference an existing log bucket in Stack2 by its ARN (Amazon Resource Name)
// This demonstrates importing external resources that may already exist in your AWS account
const logBucketLookup = Bucket.fromBucketArn(
  stack2,
  &quot;LogBucketLookup&quot;,
  &quot;arn:aws:s3:::log-bucket-lookup&quot;,
);

// Create another log bucket in Stack 1 for demonstration purposes
// This bucket will also serve as a logging destination for application buckets
const logBucket = new Bucket(stack, &quot;LogBucket&quot;, {
  enforceSSL: true,
});

// Create a test application bucket in Stack 1 that logs access to the local log bucket
// This demonstrates the standard pattern: application bucket → log bucket (same stack)
new Bucket(stack, &quot;TestBucket&quot;, {
  enforceSSL: true,
  serverAccessLogsBucket: logBucket,
});

// Create a test bucket in Stack2 that references the imported log bucket
// This shows how buckets can log to external/imported logging destinations
new Bucket(stack2, &quot;TestBucketLookup&quot;, {
  enforceSSL: true,
  serverAccessLogsBucket: logBucketLookup,
});

// Create another test bucket in Stack2, but this time referencing the log bucket from Stack 1
// This demonstrates CROSS-STACK resource references - a powerful CDK feature
// CDK automatically creates the necessary CloudFormation exports/imports
new Bucket(stack2, &quot;TestBucket2&quot;, {
  enforceSSL: true,
  serverAccessLogsBucket: logBucket,
});

// Apply custom CDK Nag checks to the entire application using the Aspects pattern
// Aspects run validation rules against all resources in the app before synthesis
Aspects.of(app).add(
  new CustomChecks({
    enableAwsSolutionChecks: true, // Enable AWS Solutions construct library compliance checks
    enableLogBucketTagger: true, // Automatically tag log buckets for third-party security scanners
  }),
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;p&gt;&lt;code&gt;LogBucketForOtherAppsBucketsF118033B&lt;/code&gt; has the suppression and the tags applied, and &lt;code&gt;LogBucketCC3B17E8&lt;/code&gt; is treated as a log bucket because it is created and referenced within the app.&lt;/p&gt;
&lt;p&gt;The synthesized CloudFormation template&lt;/p&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;CloudFormation template excerpt&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
 &quot;Resources&quot;: {
  &quot;LogBucketForOtherAppsBucketsF118033B&quot;: {
   &quot;Type&quot;: &quot;AWS::S3::Bucket&quot;,
   &quot;Properties&quot;: {
    &quot;Tags&quot;: [
     {
      &quot;Key&quot;: &quot;isLogBucket&quot;,
      &quot;Value&quot;: &quot;true&quot;
     },
     {
      &quot;Key&quot;: &quot;LogBucketTaggedBy&quot;,
      &quot;Value&quot;: &quot;cdkNagCustomChecks&quot;
     }
    ]
   },
   &quot;UpdateReplacePolicy&quot;: &quot;Retain&quot;,
   &quot;DeletionPolicy&quot;: &quot;Retain&quot;,
   &quot;Metadata&quot;: {
    &quot;aws:cdk:path&quot;: &quot;Stack/LogBucketForOtherAppsBuckets/Resource&quot;,
    &quot;cdk_nag&quot;: {
     &quot;rules_to_suppress&quot;: [
      {
       &quot;reason&quot;: &quot;This is intended to be a log bucket. Log bucket does not require access logging to prevent infinite loop&quot;,
       &quot;id&quot;: &quot;AwsSolutions-S1&quot;
      }
     ]
    }
   }
  },
 ...
  &quot;LogBucketCC3B17E8&quot;: {
   &quot;Type&quot;: &quot;AWS::S3::Bucket&quot;,
   &quot;Properties&quot;: {
    &quot;Tags&quot;: [
     {
      &quot;Key&quot;: &quot;isLogBucket&quot;,
      &quot;Value&quot;: &quot;true&quot;
     },
     {
      &quot;Key&quot;: &quot;LogBucketTaggedBy&quot;,
      &quot;Value&quot;: &quot;cdkNagCustomChecks&quot;
     }
    ]
   },
   &quot;UpdateReplacePolicy&quot;: &quot;Retain&quot;,
   &quot;DeletionPolicy&quot;: &quot;Retain&quot;,
   &quot;Metadata&quot;: {
    &quot;aws:cdk:path&quot;: &quot;Stack/LogBucket/Resource&quot;,
    &quot;cdk_nag&quot;: {
     &quot;rules_to_suppress&quot;: [
      {
       &quot;reason&quot;: &quot;This is intended to be a log bucket. Log bucket does not require access logging to prevent infinite loop&quot;,
       &quot;id&quot;: &quot;AwsSolutions-S1&quot;
      }
     ]
    }
   }
  },
  ...
  &quot;TestBucket560B80BC&quot;: {
   &quot;Type&quot;: &quot;AWS::S3::Bucket&quot;,
   &quot;Properties&quot;: {
    &quot;LoggingConfiguration&quot;: {
     &quot;DestinationBucketName&quot;: {
      &quot;Ref&quot;: &quot;LogBucketCC3B17E8&quot;
     }
    }
   },
   &quot;UpdateReplacePolicy&quot;: &quot;Retain&quot;,
   &quot;DeletionPolicy&quot;: &quot;Retain&quot;,
   &quot;Metadata&quot;: {
    &quot;aws:cdk:path&quot;: &quot;Stack/TestBucket/Resource&quot;
   }
  },
  ...
  &quot;CDKMetadata&quot;: {
   &quot;Type&quot;: &quot;AWS::CDK::Metadata&quot;,
   &quot;Properties&quot;: {
    &quot;Analytics&quot;: &quot;v2:deflate64:H4sIAAAAAAAA/0WISw5AMBBAz2LfDoYTcAHhAFJVySiVmJaIuLtFJVbvg4AFQp6ok6UerVxogLvzSluhTu65gLsK2hov6sl9FtFsC+nr37GfR7htNDBzemAJOUKWzEwk9+A8rQbayBeDG/QVeAAAAA==&quot;
   },
   &quot;Metadata&quot;: {
    &quot;aws:cdk:path&quot;: &quot;Stack/CDKMetadata/Default&quot;
   },
   &quot;Condition&quot;: &quot;CDKMetadataAvailable&quot;
  }
 },
 &quot;Outputs&quot;: {
  &quot;ExportsOutputRefLogBucketCC3B17E818DCEC53&quot;: {
   &quot;Value&quot;: {
    &quot;Ref&quot;: &quot;LogBucketCC3B17E8&quot;
   },
   &quot;Export&quot;: {
    &quot;Name&quot;: &quot;Stack:ExportsOutputRefLogBucketCC3B17E818DCEC53&quot;
   }
  }
 },
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;h2&gt;Sources and links&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;https://github.com/cdklabs/cdk-nag/blob/main/RULES.md#awssolutions&lt;/li&gt;
&lt;li&gt;https://github.com/JohannesKonings/cdk-nag-custom-nag-pack/releases/tag/v0.0.11&lt;/li&gt;
&lt;li&gt;https://github.com/cdklabs/cdk-nag&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>Using Server Sent Events (SSE) to sync Tanstack Db from AWS DynamoDB</title><link>https://johanneskonings.dev/blog/2026-01-08-tanstack-start-aws-db-multiple-entities-sse/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2026-01-08-tanstack-start-aws-db-multiple-entities-sse/</guid><description>Build real-time data synchronization between AWS DynamoDB and TanStack DB using Server-Sent Events. Learn how to stream database changes via DynamoDB Streams, implement SSE endpoints with TanStack Start/Router.</description><pubDate>Thu, 08 Jan 2026 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Introduction&lt;/h2&gt;
&lt;p&gt;As described in &lt;a href=&quot;./2025-12-27-tanstack-start-aws-db-multiple-entities&quot;&gt;Simple example of TanStack DB with DynamoDB on AWS with multiple entities&lt;/a&gt;, we set up a multi-entity data model using ElectroDB and TanStack DB collections. In this post, we will explore how to keep our TanStack DB in sync with AWS DynamoDB using Server Sent Events (SSE).&lt;/p&gt;
&lt;p&gt;TanStack DB has the advantage that it can update specific database entries without needing to refetch all the data. This capability enables real-time synchronization across multiple sessions when another user or process modifies the data. Instead of polling the database constantly or refreshing entire datasets, SSE allows the server to push only the changes that occurred, making the application more efficient and responsive.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;⚠️ Disclaimer&lt;/strong&gt;: This implementation is somewhat hacky and is intended to prove the approach and demonstrate the concept. It has not been battle-tested in production environments and may contain errors or edge cases that haven&apos;t been fully addressed.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;Demo Video&lt;/h2&gt;
&lt;p&gt;&amp;lt;iframe width=&quot;560&quot; height=&quot;315&quot; src=&quot;https://www.youtube.com/embed/ZOf52x9ehYg&quot; title=&quot;TanStack DB SSE Demo&quot; frameborder=&quot;0&quot; allow=&quot;accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share&quot; allowfullscreen&amp;gt;&amp;lt;/iframe&amp;gt;&lt;/p&gt;
&lt;h2&gt;Architecture Overview&lt;/h2&gt;
&lt;p&gt;The synchronization architecture consists of three main components working together:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;DynamoDB Streams&lt;/strong&gt;: Captures all data changes from the main DynamoDB table&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Event Table&lt;/strong&gt;: Stores a change log of all modifications for historical tracking&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;SSE Endpoint&lt;/strong&gt;: Streams these changes to connected clients in real-time&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This architecture enables multiple clients to stay synchronized with minimal latency while maintaining a complete audit trail of all data modifications.&lt;/p&gt;
&lt;h2&gt;DynamoDB Stream and Event Table&lt;/h2&gt;
&lt;p&gt;AWS DynamoDB Streams is a powerful feature that captures a time-ordered sequence of item-level modifications in any DynamoDB table. When enabled, DynamoDB Streams records every change (insert, update, delete) made to the table in near real-time.&lt;/p&gt;
&lt;h3&gt;How It Works&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Stream Capture&lt;/strong&gt;: When data changes in the main DynamoDB table, DynamoDB Streams captures these modifications&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Lambda Processing&lt;/strong&gt;: A Lambda function is triggered by the stream and processes each change event&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Event Storage&lt;/strong&gt;: The Lambda writes these events to a separate &quot;Events&quot; table, creating a persistent change log&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Query by Clients&lt;/strong&gt;: The SSE endpoint can query this Events table to find changes since a specific timestamp&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;This approach provides several benefits:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Historical Audit Trail&lt;/strong&gt;: All changes are permanently stored in the Events table&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Efficient Polling&lt;/strong&gt;: Clients only need to query for events after their last known event ID&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Decoupling&lt;/strong&gt;: The stream processing is separate from client synchronization logic&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;For more information, see the &lt;a href=&quot;https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Streams.html&quot;&gt;AWS DynamoDB Streams documentation&lt;/a&gt;.&lt;/p&gt;
&lt;h3&gt;Implementation Links&lt;/h3&gt;
&lt;p&gt;The complete implementation is available in the repository:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2026-01-08-tanstack-start-aws-db-multiple-entities-sse/lib/constructs/dynamo-table.ts&quot;&gt;DynamoDB stream setup&lt;/a&gt; - Table configuration with streams enabled&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2026-01-08-tanstack-start-aws-db-multiple-entities-sse/lambda/dynamodb-stream/index.ts&quot;&gt;Lambda function for stream processing&lt;/a&gt; - Processes DynamoDB stream events&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2026-01-08-tanstack-start-aws-db-multiple-entities-sse/app/entities/events.ts&quot;&gt;Events table schema and setup&lt;/a&gt; - ElectroDB entity definition for events&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Server Sent Events (SSE) Setup&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://en.wikipedia.org/wiki/Server-sent_events&quot;&gt;Server-Sent Events (SSE)&lt;/a&gt; is a standard web technology that enables servers to push real-time updates to clients over a single HTTP connection. Unlike WebSockets which provide bidirectional communication, SSE is unidirectional (server to client) and uses simple HTTP, making it easier to implement and more compatible with existing infrastructure like load balancers and proxies.&lt;/p&gt;
&lt;h3&gt;Why SSE for Database Synchronization?&lt;/h3&gt;
&lt;p&gt;SSE is ideal for this use case because:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Automatic Reconnection&lt;/strong&gt;: The browser&apos;s native &lt;code&gt;EventSource&lt;/code&gt; API handles reconnection automatically&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Simple Protocol&lt;/strong&gt;: Uses standard HTTP, no special server infrastructure needed&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Efficient&lt;/strong&gt;: Keeps a single connection open, pushing updates only when changes occur&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Last-Event-ID&lt;/strong&gt;: Built-in support for resuming from the last received event after reconnection&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;SSE Endpoint Implementation&lt;/h3&gt;
&lt;p&gt;A Server-Sent Events endpoint is created using TanStack Router&apos;s &lt;code&gt;createFileRoute&lt;/code&gt; API. This endpoint streams changes from the Events table to connected clients. Here&apos;s how the implementation works:&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Key Features:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Polling Mechanism&lt;/strong&gt;: Queries the Events table every 500ms for new changes&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Heartbeat&lt;/strong&gt;: Sends keepalive messages every 15 seconds to prevent connection timeout&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Graceful Timeout&lt;/strong&gt;: Closes the connection before Lambda timeout (14 minutes), allowing automatic reconnection&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Last-Event-ID Support&lt;/strong&gt;: Clients can reconnect and resume from their last received event&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Efficient Queries&lt;/strong&gt;: Only fetches events newer than the client&apos;s last known event&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Click to view the SSE endpoint implementation&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// oxlint-disable no-magic-numbers
// oxlint-disable init-declarations
// oxlint-disable no-ternary
// oxlint-disable max-statements
// oxlint-disable no-console
import { DynamoDBClient } from &quot;@aws-sdk/client-dynamodb&quot;;
import { DynamoDBDocumentClient, QueryCommand } from &quot;@aws-sdk/lib-dynamodb&quot;;
import { createFileRoute } from &quot;@tanstack/react-router&quot;;
import { TIMEOUT_IN_SECONDS } from &quot;../../../../lib/constructs/type&quot;;

// =============================================================================
// Constants
// =============================================================================

const STREAM_DURATION_MS = (TIMEOUT_IN_SECONDS - 4) * 1000;
const RETRY_MS = 1000;
const POLL_INTERVAL_MS = 500;
const HEARTBEAT_INTERVAL_MS = 15000; // Send heartbeat every 15 seconds to prevent connection timeout
const EVENTS_TABLE = process.env.EVENTS_TABLE ?? &quot;&quot;;

// =============================================================================
// DynamoDB Client
// =============================================================================

const ddbClient = DynamoDBDocumentClient.from(new DynamoDBClient({}));

// =============================================================================
// Helper Functions
// =============================================================================

/** Query events from the Events table since a given sort key */
const queryEventsSince = async (
  lastEventSk: string | null,
): Promise&amp;lt;Array&amp;lt;Record&amp;lt;string, unknown&amp;gt;&amp;gt;&amp;gt; =&amp;gt; {
  if (!EVENTS_TABLE) {
    console.log(&quot;Persons SSE: EVENTS_TABLE not configured&quot;);
    return [];
  }

  const params = {
    TableName: EVENTS_TABLE,
    KeyConditionExpression: lastEventSk ? &quot;pk = :pk AND sk &amp;gt; :sk&quot; : &quot;pk = :pk&quot;,
    ExpressionAttributeValues: lastEventSk
      ? { &quot;:pk&quot;: &quot;EVENTS&quot;, &quot;:sk&quot;: lastEventSk }
      : { &quot;:pk&quot;: &quot;EVENTS&quot; },
    ScanIndexForward: true,
    Limit: 100,
  };

  console.log(
    &quot;Persons SSE: Querying events table:&quot;,
    EVENTS_TABLE,
    &quot;cursor:&quot;,
    lastEventSk,
  );
  const result = await ddbClient.send(new QueryCommand(params));
  console.log(
    &quot;Persons SSE: Query returned&quot;,
    result.Items?.length ?? 0,
    &quot;events&quot;,
  );
  return (result.Items ?? []) as Array&amp;lt;Record&amp;lt;string, unknown&amp;gt;&amp;gt;;
};

/** Format an event as SSE data */
const formatSseEvent = (event: Record&amp;lt;string, unknown&amp;gt;): string =&amp;gt; {
  const data = {
    timestamp: event.createdAt,
    eventType: event.eventType,
    entityType: event.entityType,
    entity: event.entity,
    oldEntity: event.oldEntity,
  };
  return `event: change\nid: ${event.sk}\ndata: ${JSON.stringify(data)}\n\n`;
};

// =============================================================================
// SSE Stream Route for Persons DB
// =============================================================================

/**
 * SSE Stream API Route for Persons Database
 *
 * Streams entity change events (persons, addresses, bank accounts, etc.)
 * to connected clients using Server-Sent Events.
 *
 * Based on the simple /api/events blueprint pattern.
 */
export const Route = createFileRoute(&quot;/api/persons-stream&quot;)({
  server: {
    handlers: {
      GET: async ({ request }) =&amp;gt; {
        // Check if Events table is configured
        if (!EVENTS_TABLE) {
          return new Response(&quot;SSE not configured - EVENTS_TABLE not set&quot;, {
            status: 503,
          });
        }

        // Get last event ID from header for reconnection
        const lastEventId = request.headers.get(&quot;Last-Event-ID&quot;);
        let lastSk = lastEventId;

        const encoder = new TextEncoder();
        let pollIntervalId: ReturnType&amp;lt;typeof setInterval&amp;gt;;
        let heartbeatIntervalId: ReturnType&amp;lt;typeof setInterval&amp;gt;;
        let timeoutId: ReturnType&amp;lt;typeof setTimeout&amp;gt;;

        const stream = new ReadableStream({
          async start(controller) {
            // Send retry directive so EventSource knows how long to wait before reconnecting
            controller.enqueue(encoder.encode(`retry: ${RETRY_MS}\n\n`));

            // Send initial connected event
            controller.enqueue(
              encoder.encode(
                &apos;event: connected\ndata: {&quot;status&quot;:&quot;connected&quot;}\n\n&apos;,
              ),
            );
            console.log(
              &quot;Persons SSE: Client connected, EVENTS_TABLE:&quot;,
              EVENTS_TABLE,
            );

            // Define the poll function
            const poll = async (): Promise&amp;lt;void&amp;gt; =&amp;gt; {
              try {
                // Query for new events
                const events = await queryEventsSince(lastSk);

                // Send each event
                for (const event of events) {
                  controller.enqueue(encoder.encode(formatSseEvent(event)));
                  lastSk = event.sk as string;
                  console.log(
                    &quot;Persons SSE: Sent event:&quot;,
                    event.eventType,
                    event.entityType,
                  );
                }
              } catch (error) {
                console.error(&quot;Persons SSE: Poll error:&quot;, error);
              }
            };

            // Define the heartbeat function
            const sendHeartbeat = (): void =&amp;gt; {
              try {
                controller.enqueue(encoder.encode(&quot;: heartbeat\n\n&quot;));
                console.log(&quot;Persons SSE: Heartbeat sent&quot;);
              } catch (error) {
                console.error(&quot;Persons SSE: Heartbeat error:&quot;, error);
              }
            };

            // Do an immediate first poll
            await poll();

            // Then poll at regular intervals
            pollIntervalId = setInterval(() =&amp;gt; {
              poll();
            }, POLL_INTERVAL_MS);

            // Send heartbeats at regular intervals to keep connection alive
            heartbeatIntervalId = setInterval(
              sendHeartbeat,
              HEARTBEAT_INTERVAL_MS,
            );

            // Gracefully close the stream before Lambda timeout
            // EventSource will automatically reconnect
            timeoutId = setTimeout(() =&amp;gt; {
              clearInterval(pollIntervalId);
              clearInterval(heartbeatIntervalId);
              // Send a final event to signal graceful close
              controller.enqueue(
                encoder.encode(
                  &apos;event: reconnect\ndata: {&quot;reason&quot;:&quot;timeout&quot;}\n\n&apos;,
                ),
              );
              controller.close();
              console.log(
                &quot;Persons SSE: Stream closed gracefully before timeout&quot;,
              );
            }, STREAM_DURATION_MS);
          },
          cancel() {
            clearInterval(pollIntervalId);
            clearInterval(heartbeatIntervalId);
            clearTimeout(timeoutId);
            console.log(&quot;Persons SSE: Client disconnected&quot;);
          },
        });

        return new Response(stream, {
          headers: {
            &quot;Content-Type&quot;: &quot;text/event-stream&quot;,
            &quot;Cache-Control&quot;: &quot;no-cache&quot;,
            Connection: &quot;keep-alive&quot;,
            &quot;X-Accel-Buffering&quot;: &quot;no&quot;,
          },
        });
      },
    },
  },
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;h3&gt;Understanding the SSE Flow&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Client Connection&lt;/strong&gt;: When a client connects, the endpoint sends a &lt;code&gt;retry&lt;/code&gt; directive and a &lt;code&gt;connected&lt;/code&gt; event&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Initial Poll&lt;/strong&gt;: Immediately queries for any events since the client&apos;s &lt;code&gt;Last-Event-ID&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Continuous Polling&lt;/strong&gt;: Sets up an interval to poll for new events every 500ms&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Event Streaming&lt;/strong&gt;: Each new event is formatted and sent to the client with a unique ID&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Heartbeat&lt;/strong&gt;: Sends comment lines (&lt;code&gt;: heartbeat&lt;/code&gt;) to keep the connection alive&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Graceful Close&lt;/strong&gt;: Before Lambda timeout, sends a &lt;code&gt;reconnect&lt;/code&gt; event and closes the stream&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The client&apos;s browser automatically reconnects using the built-in &lt;code&gt;EventSource&lt;/code&gt; retry mechanism, and the server resumes streaming from the last event ID.&lt;/p&gt;
&lt;h3&gt;ElectroDB Integration (Future Enhancement)&lt;/h3&gt;
&lt;p&gt;ElectroDB, the library used for DynamoDB entity management, has community discussions about built-in change tracking. This could potentially simplify the stream processing in the future. See &lt;a href=&quot;https://github.com/tywalch/electrodb/issues/74&quot;&gt;ElectroDB issue #74&lt;/a&gt; for more details.&lt;/p&gt;
&lt;h2&gt;The SSE Hook&lt;/h2&gt;
&lt;p&gt;The client-side implementation uses a custom React hook (&lt;code&gt;useSseSync&lt;/code&gt;) that manages the SSE connection and applies changes to TanStack DB collections. This hook abstracts away the complexity of SSE connection management and provides a clean API for components to use.&lt;/p&gt;
&lt;h3&gt;Hook Architecture&lt;/h3&gt;
&lt;p&gt;The hook is structured into several key sections:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Constants &amp;amp; Configuration&lt;/strong&gt;: Defines the SSE endpoint and flag for preventing loops&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Type Definitions&lt;/strong&gt;: TypeScript interfaces for entity types, event types, and state&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Collection Update Functions&lt;/strong&gt;: Specialized functions for INSERT, MODIFY, and REMOVE operations&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Change Application Logic&lt;/strong&gt;: Routes events to the correct collection and operation&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Connection Management&lt;/strong&gt;: Handles EventSource lifecycle and reconnection&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Key Features&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Automatic Reconnection&lt;/strong&gt;: Uses native &lt;code&gt;EventSource&lt;/code&gt; which handles reconnections automatically&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Loop Prevention&lt;/strong&gt;: Uses a flag to prevent SSE updates from triggering writes back to DynamoDB&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Type-Safe Updates&lt;/strong&gt;: Strongly typed entity handlers for all five entity types (Person, Address, BankAccount, ContactInfo, Employment)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;State Management&lt;/strong&gt;: Tracks connection status and last sync time&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Manual Reconnection&lt;/strong&gt;: Provides a &lt;code&gt;reconnect()&lt;/code&gt; function for forced reconnection&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Click to view the SSE synchronization hook implementation&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// oxlint-disable no-ternary
import type {
  Address,
  BankAccount,
  ContactInfo,
  Employment,
  Person,
} from &quot;#src/webapp/types/person&quot;;
import {
  addressesCollection,
  bankAccountsCollection,
  contactsCollection,
  employmentsCollection,
  personsCollection,
} from &quot;#src/webapp/db-collections/persons&quot;;
import { useCallback, useEffect, useRef, useState } from &quot;react&quot;;

// =============================================================================
// Constants
// =============================================================================

const SSE_ENDPOINT = &quot;/api/persons-stream&quot;;

// Flag to track when we&apos;re applying SSE changes
// This prevents SSE updates from triggering DynamoDB writes
let isApplyingSseChange = false;

/**
 * Check if currently applying an SSE change
 * Collection handlers can use this to skip server writes
 */
export const isFromSse = (): boolean =&amp;gt; isApplyingSseChange;

// =============================================================================
// Types
// =============================================================================

type EntityType =
  | &quot;person&quot;
  | &quot;address&quot;
  | &quot;bankAccount&quot;
  | &quot;contactInfo&quot;
  | &quot;employment&quot;;
type EventType = &quot;INSERT&quot; | &quot;MODIFY&quot; | &quot;REMOVE&quot;;

interface ChangeEventData {
  timestamp?: string;
  eventType?: EventType;
  entityType?: EntityType;
  entity?: object | null;
  oldEntity?: object;
}

interface SseSyncState {
  isConnected: boolean;
  lastSyncTime: Date | null;
}

// =============================================================================
// Collection Update Functions
// =============================================================================

/**
 * Apply an INSERT change to the collection
 */
const applyInsert = (
  entityType: EntityType,
  entity: object | null | undefined,
): void =&amp;gt; {
  if (!entity) {
    return;
  }

  // Note: TanStack DB collections handle duplicates gracefully
  // If the entity already exists, this becomes an update operation
  switch (entityType) {
    case &quot;person&quot;:
      personsCollection.insert(entity as Person);
      break;
    case &quot;address&quot;:
      addressesCollection.insert(entity as Address);
      break;
    case &quot;bankAccount&quot;:
      bankAccountsCollection.insert(entity as BankAccount);
      break;
    case &quot;contactInfo&quot;:
      contactsCollection.insert(entity as ContactInfo);
      break;
    case &quot;employment&quot;:
      employmentsCollection.insert(entity as Employment);
      break;
  }
};

/**
 * Apply a MODIFY change to the collection
 */
const applyModify = (
  entityType: EntityType,
  entity: object | null | undefined,
): void =&amp;gt; {
  if (!entity) {
    return;
  }

  const entityWithId = entity as { id: string };

  switch (entityType) {
    case &quot;person&quot;:
      personsCollection.update(entityWithId.id, (draft) =&amp;gt; {
        Object.assign(draft, entity);
      });
      break;
    case &quot;address&quot;:
      addressesCollection.update(entityWithId.id, (draft) =&amp;gt; {
        Object.assign(draft, entity);
      });
      break;
    case &quot;bankAccount&quot;:
      bankAccountsCollection.update(entityWithId.id, (draft) =&amp;gt; {
        Object.assign(draft, entity);
      });
      break;
    case &quot;contactInfo&quot;:
      contactsCollection.update(entityWithId.id, (draft) =&amp;gt; {
        Object.assign(draft, entity);
      });
      break;
    case &quot;employment&quot;:
      employmentsCollection.update(entityWithId.id, (draft) =&amp;gt; {
        Object.assign(draft, entity);
      });
      break;
  }
};

/**
 * Apply a REMOVE change to the collection
 */
const applyRemove = (
  entityType: EntityType,
  entity: object | null | undefined,
  oldEntity: object | undefined,
): void =&amp;gt; {
  const entityWithId = entity as { id: string } | null | undefined;
  const oldEntityWithId = oldEntity as { id: string } | undefined;
  const removeId = entityWithId?.id ?? oldEntityWithId?.id;

  if (!removeId) {
    return;
  }

  switch (entityType) {
    case &quot;person&quot;:
      personsCollection.delete(removeId);
      break;
    case &quot;address&quot;:
      addressesCollection.delete(removeId);
      break;
    case &quot;bankAccount&quot;:
      bankAccountsCollection.delete(removeId);
      break;
    case &quot;contactInfo&quot;:
      contactsCollection.delete(removeId);
      break;
    case &quot;employment&quot;:
      employmentsCollection.delete(removeId);
      break;
  }
};

/**
 * Apply a change event to the appropriate TanStack DB collection.
 * Only applies changes if they differ from current collection state.
 */
const applyChangeToCollection = (data: ChangeEventData): void =&amp;gt; {
  if (!data.entityType || !data.eventType) {
    return;
  }

  switch (data.eventType) {
    case &quot;INSERT&quot;:
      applyInsert(data.entityType, data.entity);
      break;
    case &quot;MODIFY&quot;:
      applyModify(data.entityType, data.entity);
      break;
    case &quot;REMOVE&quot;:
      applyRemove(data.entityType, data.entity, data.oldEntity);
      break;
  }
};

// =============================================================================
// Hook
// =============================================================================

/**
 * Hook for real-time SSE synchronization with TanStack DB.
 * Uses native EventSource to receive entity changes from the persons-stream endpoint.
 *
 * **IMPORTANT: Preventing Endless Loops**
 * This hook sets a global `isApplyingSseChange` flag when applying SSE changes.
 * Collection mutation handlers (onInsert/onUpdate/onDelete) should check `isFromSse()`
 * and skip DynamoDB writes when true. This breaks the loop:
 *
 * - SSE event -&amp;gt; isApplyingSseChange=true -&amp;gt; collection mutation -&amp;gt; onInsert checks isFromSse()
 *   -&amp;gt; returns true -&amp;gt; ✅ skips DynamoDB write
 * - Local user mutation -&amp;gt; isApplyingSseChange=false -&amp;gt; onInsert checks isFromSse()
 *   -&amp;gt; returns false -&amp;gt; ✅ writes to DynamoDB -&amp;gt; stream -&amp;gt; SSE event (handled above)
 *
 * This approach allows SSE to update local state without triggering server writes,
 * while user-initiated changes still persist to DynamoDB as expected.
 *
 * EventSource automatically handles reconnection using the `retry` directive from
 * the server. The server also sends a `reconnect` event before graceful timeout
 * to signal the client that a reconnection is expected.
 *
 * Based on the simple /api/events blueprint pattern.
 *
 * @returns Object with isConnected, lastSyncTime, and reconnect function
 */
export const useSseSync = (): SseSyncState &amp;amp; { reconnect: () =&amp;gt; void } =&amp;gt; {
  const eventSourceRef = useRef&amp;lt;EventSource | null&amp;gt;(null);
  const [state, setState] = useState&amp;lt;SseSyncState&amp;gt;({
    isConnected: false,
    lastSyncTime: null,
  });

  const connect = useCallback(() =&amp;gt; {
    // Close existing connection if any
    if (eventSourceRef.current) {
      eventSourceRef.current.close();
    }

    // Create new EventSource connection
    const eventSource = new EventSource(SSE_ENDPOINT);
    eventSourceRef.current = eventSource;

    // Handle connection open
    eventSource.onopen = () =&amp;gt; {
      setState((prev) =&amp;gt; ({ ...prev, isConnected: true }));
    };

    // Handle connection error - EventSource will automatically reconnect
    // Based on the retry directive from the server
    eventSource.onerror = () =&amp;gt; {
      setState((prev) =&amp;gt; ({ ...prev, isConnected: false }));
    };

    // Handle &apos;connected&apos; event from server
    eventSource.addEventListener(&quot;connected&quot;, () =&amp;gt; {
      setState((prev) =&amp;gt; ({ ...prev, isConnected: true }));
    });

    // Handle &apos;change&apos; events (entity changes)
    eventSource.addEventListener(&quot;change&quot;, (event: MessageEvent) =&amp;gt; {
      try {
        const data = JSON.parse(event.data) as ChangeEventData;

        // Set flag to indicate we&apos;re applying SSE changes
        // This prevents mutation handlers from writing back to DynamoDB
        isApplyingSseChange = true;
        try {
          applyChangeToCollection(data);
        } finally {
          // Always reset the flag, even if an error occurs
          isApplyingSseChange = false;
        }

        if (data.timestamp) {
          setState((prev) =&amp;gt; ({
            ...prev,
            lastSyncTime: new Date(data.timestamp as string),
          }));
        }
      } catch {
        // Ignore parse errors
      }
    });

    // Handle &apos;reconnect&apos; event (server signaling graceful close before timeout)
    // EventSource will automatically reconnect using the retry directive
    eventSource.addEventListener(&quot;reconnect&quot;, () =&amp;gt; {
      setState((prev) =&amp;gt; ({ ...prev, isConnected: false }));
    });
  }, []);

  const disconnect = useCallback(() =&amp;gt; {
    if (eventSourceRef.current) {
      eventSourceRef.current.close();
      eventSourceRef.current = null;
    }
    setState((prev) =&amp;gt; ({ ...prev, isConnected: false }));
  }, []);

  const reconnect = useCallback(() =&amp;gt; {
    disconnect();
    connect();
  }, [connect, disconnect]);

  useEffect(() =&amp;gt; {
    // Connect on mount
    connect();

    // Disconnect on unmount
    return () =&amp;gt; {
      disconnect();
    };
  }, [connect, disconnect]);

  return {
    ...state,
    reconnect,
  };
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;h2&gt;Avoiding Infinite Loops&lt;/h2&gt;
&lt;p&gt;One of the most critical challenges when implementing real-time synchronization is preventing infinite loops. Without proper safeguards, the following cycle can occur:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;SSE receives a change event from the server&lt;/li&gt;
&lt;li&gt;Hook applies the change to the TanStack DB collection&lt;/li&gt;
&lt;li&gt;Collection&apos;s &lt;code&gt;onUpdate&lt;/code&gt;/&lt;code&gt;onInsert&lt;/code&gt;/&lt;code&gt;onDelete&lt;/code&gt; handler triggers&lt;/li&gt;
&lt;li&gt;Handler writes the change back to DynamoDB&lt;/li&gt;
&lt;li&gt;DynamoDB Stream captures this &quot;new&quot; change&lt;/li&gt;
&lt;li&gt;Lambda writes it to the Events table&lt;/li&gt;
&lt;li&gt;SSE endpoint streams it back to the client&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Loop repeats infinitely&lt;/strong&gt; ♾️&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;The Solution: Global Flag Pattern&lt;/h3&gt;
&lt;p&gt;The implementation uses a module-level flag (&lt;code&gt;isApplyingSseChange&lt;/code&gt;) to break this cycle. Here&apos;s how it works:&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;When SSE Updates Occur:&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;SSE event received
→ Set isApplyingSseChange = true
→ Apply change to collection
→ Collection handler checks isFromSse()
→ Returns true, so SKIP DynamoDB write ✅
→ Reset isApplyingSseChange = false
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;When User Makes Changes:&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;User modifies data
→ isApplyingSseChange = false
→ Apply change to collection
→ Collection handler checks isFromSse()
→ Returns false, so WRITE to DynamoDB ✅
→ DynamoDB Stream → Events table → SSE → Other clients
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Implementation in Collection Handlers&lt;/h3&gt;
&lt;p&gt;Your collection mutation handlers should implement this check:&lt;/p&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Click to view example collection handler with loop prevention&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { isFromSse } from &quot;./useSseSync&quot;;

personsCollection = createCollection({
  // ... other config
  onInsert: async (person) =&amp;gt; {
    // Check if this change came from SSE
    if (isFromSse()) {
      console.log(&quot;Skipping DynamoDB write - change from SSE&quot;);
      return; // Don&apos;t write back to DynamoDB
    }

    // This is a user-initiated change, write to DynamoDB
    await writeToDynamoDB(person);
  },
  onUpdate: async (person) =&amp;gt; {
    if (isFromSse()) return;
    await updateDynamoDB(person);
  },
  onDelete: async (id) =&amp;gt; {
    if (isFromSse()) return;
    await deleteFromDynamoDB(id);
  },
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;p&gt;This pattern ensures that:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;✅ SSE updates modify local state without server writes&lt;/li&gt;
&lt;li&gt;✅ User changes persist to DynamoDB and propagate to other clients&lt;/li&gt;
&lt;li&gt;✅ No infinite loops or duplicate operations&lt;/li&gt;
&lt;li&gt;✅ Clean separation between sync and user-initiated changes&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Using the Hook in Components&lt;/h2&gt;
&lt;p&gt;To use the SSE synchronization in your React components, simply call the &lt;code&gt;useSseSync&lt;/code&gt; hook:&lt;/p&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Click to view example component usage&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { useSseSync } from &quot;./useSseSync&quot;;

function PersonsList() {
  const { isConnected, lastSyncTime, reconnect } = useSseSync();

  return (
    &amp;lt;div&amp;gt;
      &amp;lt;div className=&quot;sync-status&quot;&amp;gt;
        {isConnected ? (
          &amp;lt;span className=&quot;connected&quot;&amp;gt;🟢 Connected&amp;lt;/span&amp;gt;
        ) : (
          &amp;lt;span className=&quot;disconnected&quot;&amp;gt;🔴 Disconnected&amp;lt;/span&amp;gt;
        )}
        {lastSyncTime &amp;amp;&amp;amp; (
          &amp;lt;span&amp;gt;Last sync: {lastSyncTime.toLocaleTimeString()}&amp;lt;/span&amp;gt;
        )}
        &amp;lt;button onClick={reconnect}&amp;gt;Reconnect&amp;lt;/button&amp;gt;
      &amp;lt;/div&amp;gt;

      {/* Your persons list component */}
    &amp;lt;/div&amp;gt;
  );
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;p&gt;The hook automatically:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Establishes the SSE connection on mount&lt;/li&gt;
&lt;li&gt;Applies incoming changes to TanStack DB collections&lt;/li&gt;
&lt;li&gt;Handles reconnection on errors or timeouts&lt;/li&gt;
&lt;li&gt;Cleans up the connection on unmount&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Cost Considerations&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Long-Running Connections&lt;/strong&gt;: Each SSE connection keeps a Lambda instance running for up to 14 minutes&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Concurrent Connections&lt;/strong&gt;: Each connected client requires a separate Lambda instance&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Polling Overhead&lt;/strong&gt;: The 500ms polling interval means continuous DynamoDB queries, even when no changes occur&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Other Options&lt;/h2&gt;
&lt;p&gt;While SSE provides a good balance of simplicity and functionality, other approaches may be better suited for specific use cases:&lt;/p&gt;
&lt;h3&gt;1. WebSocket (Bidirectional)&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;When to use:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Need bidirectional communication (client can send messages to server)&lt;/li&gt;
&lt;li&gt;High-frequency updates (many changes per second)&lt;/li&gt;
&lt;li&gt;Lower latency requirements&lt;/li&gt;
&lt;li&gt;Building chat or collaborative editing features&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Implementation:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;AWS API Gateway WebSocket API&lt;/li&gt;
&lt;li&gt;Requires connection state management (connection IDs in DynamoDB)&lt;/li&gt;
&lt;li&gt;More complex than SSE but more flexible&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;2. Short Polling&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;When to use:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Simple use case with infrequent updates&lt;/li&gt;
&lt;li&gt;Need to support older browsers without SSE&lt;/li&gt;
&lt;li&gt;Want explicit control over refresh timing&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Implementation:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Standard HTTP requests on an interval (e.g., every 5-10 seconds)&lt;/li&gt;
&lt;li&gt;Simpler than SSE but less efficient&lt;/li&gt;
&lt;li&gt;Higher latency than push-based approaches&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;3. GraphQL Subscriptions&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;When to use:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Already using GraphQL (AWS AppSync)&lt;/li&gt;
&lt;li&gt;Need fine-grained subscription filtering&lt;/li&gt;
&lt;li&gt;Want managed infrastructure&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Implementation:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;AWS AppSync with DynamoDB resolvers&lt;/li&gt;
&lt;li&gt;Built-in authorization and subscription management&lt;/li&gt;
&lt;li&gt;Higher per-request cost than Lambda&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;4. Custom WebSocket Collection (TanStack DB)&lt;/h3&gt;
&lt;p&gt;TanStack DB supports creating custom collections with bidirectional WebSocket synchronization. This is the most integrated approach but requires the most implementation effort.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;When to use:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Need full bidirectional sync (client changes immediately propagate)&lt;/li&gt;
&lt;li&gt;Want conflict resolution at the collection level&lt;/li&gt;
&lt;li&gt;Building offline-first applications&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Resources:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://tanstack.com/db/latest/docs/guides/collection-options-creator#complete-example-websocket-collection&quot;&gt;TanStack DB Collection Options Guide&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;Requires implementing custom WebSocket server and protocol&lt;/li&gt;
&lt;li&gt;Most complex option but provides the richest synchronization features&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Conclusion&lt;/h2&gt;
&lt;p&gt;This implementation demonstrates how to build real-time data synchronization between AWS DynamoDB and TanStack DB using Server-Sent Events. The key takeaways are:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;DynamoDB Streams + Events Table&lt;/strong&gt;: Provides a reliable, queryable change log&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;SSE with TanStack Router&lt;/strong&gt;: Simple, standards-based approach to server push&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Loop Prevention&lt;/strong&gt;: Critical pattern using a global flag to prevent infinite updates&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Automatic Reconnection&lt;/strong&gt;: Built-in browser support makes SSE resilient&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Cost Awareness&lt;/strong&gt;: Monitor Lambda and DynamoDB costs, especially with many concurrent connections&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The SSE approach works well for:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;✅ Applications with moderate update frequency (seconds to minutes)&lt;/li&gt;
&lt;li&gt;✅ Primarily server-to-client updates&lt;/li&gt;
&lt;li&gt;✅ Standard web browsers&lt;/li&gt;
&lt;li&gt;✅ Simple implementation requirements&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;For applications requiring bidirectional real-time communication, lower latency, or offline support, consider WebSocket or TanStack DB&apos;s custom collection approach.&lt;/p&gt;
&lt;h2&gt;Source Code&lt;/h2&gt;
&lt;p&gt;The complete implementation is available on GitHub:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/tree/2026-01-08-tanstack-start-aws-db-multiple-entities-sse&quot;&gt;Repository Tag: 2026-01-08-tanstack-start-aws-db-multiple-entities-sse&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/pull/13&quot;&gt;Pull Request #13 - Implementation&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;References&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Streams.html&quot;&gt;AWS DynamoDB Streams Documentation&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://en.wikipedia.org/wiki/Server-sent_events&quot;&gt;Server-Sent Events (Wikipedia)&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://developer.mozilla.org/en-US/docs/Web/API/EventSource&quot;&gt;MDN: EventSource API&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://tanstack.com/db/latest/docs/guides/collection-options-creator&quot;&gt;TanStack DB Collection Options&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/tywalch/electrodb/issues/74&quot;&gt;ElectroDB Issue #74 - Change Tracking&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://tanstack.com/router/latest/docs/framework/react/guide/file-based-routing&quot;&gt;TanStack Router File Routes&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>Use a customized CDK bootstrap template</title><link>https://johanneskonings.dev/blog/2025-12-31-cdk-bootstrap-customized-template/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2025-12-31-cdk-bootstrap-customized-template/</guid><description>Learn how to customize the AWS CDK bootstrap template to add server access logging and KMS encryption to the staging bucket. Use the CDK Toolkit library to orchestrate multi-region deployments, extract environments from CDK apps, and validate CloudFormation templates with cdk-nag.</description><pubDate>Wed, 31 Dec 2025 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Introduction&lt;/h2&gt;
&lt;p&gt;In some cases, the CDK bootstrap resources need changes beyond what&apos;s possible with the standard bootstrap parameters. While the CDK provides &lt;a href=&quot;https://docs.aws.amazon.com/cdk/v2/guide/bootstrapping-customizing.html&quot;&gt;customization options&lt;/a&gt;, certain configurations require customizing of the template.&lt;/p&gt;
&lt;p&gt;This post demonstrates how to:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Encrypt the staging bucket with a custom KMS key&lt;/li&gt;
&lt;li&gt;Enable server access logs for the staging bucket&lt;/li&gt;
&lt;li&gt;Validate CloudFormation templates with cdk-nag before deployment&lt;/li&gt;
&lt;li&gt;Use the CDK Toolkit library to orchestrate the entire process in TypeScript&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;While the KMS key can be configured via bootstrap parameters, server access logging requires template customization. Since TypeScript is used for the CDK setup, all scripting will also be in TypeScript using the &lt;a href=&quot;https://docs.aws.amazon.com/cdk/api/toolkit-lib/&quot;&gt;AWS CDK Toolkit library&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;Additionally, we&apos;ll use cdk-nag to bridge the gap between CloudFormation templates and CDK validation rules.&lt;/p&gt;
&lt;h2&gt;Create the Resources for Bootstrap via CloudFormation&lt;/h2&gt;
&lt;p&gt;Before customizing the bootstrap template, we need to create supporting resources via CloudFormation:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;A KMS key for encrypting the staging bucket&lt;/li&gt;
&lt;li&gt;A log bucket for storing server access logs&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;These resources will be referenced during the bootstrap process via CloudFormation exports.&lt;/p&gt;
&lt;h3&gt;KMS Key Template&lt;/h3&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Show KMS template&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;AWSTemplateFormatVersion: &quot;2010-09-09&quot;
Description: KMS Key for encrypting the CDK bootstrap bucket
Resources:
  CdkBootstrapKmsKey:
    Type: AWS::KMS::Key
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      Description: KMS Key for CDK Bootstrap Bucket
      EnableKeyRotation: true
      KeyPolicy:
        Version: &quot;2012-10-17&quot;
        Statement:
          - Sid: Enable IAM User Permissions
            Effect: Allow
            Principal:
              AWS: !Sub &quot;arn:${AWS::Partition}:iam::${AWS::AccountId}:root&quot;
            Action: &quot;kms:*&quot;
            Resource: &quot;*&quot;
          - Sid: Allow CDK roles to use the key
            Effect: Allow
            Principal:
              AWS: !Sub &quot;arn:${AWS::Partition}:iam::${AWS::AccountId}:root&quot;
            Action:
              - &quot;kms:Encrypt&quot;
              - &quot;kms:Decrypt&quot;
              - &quot;kms:ReEncrypt*&quot;
              - &quot;kms:GenerateDataKey*&quot;
              - &quot;kms:DescribeKey&quot;
            Resource: &quot;*&quot;
            Condition:
              StringLike:
                &quot;aws:PrincipalArn&quot;:
                  - !Sub &quot;arn:${AWS::Partition}:iam::${AWS::AccountId}:role/cdk-*-cfn-exec-role-*&quot;
                  - !Sub &quot;arn:${AWS::Partition}:iam::${AWS::AccountId}:role/cdk-*-file-publishing-role-*&quot;
                  - !Sub &quot;arn:${AWS::Partition}:iam::${AWS::AccountId}:role/cdk-*-image-publishing-role-*&quot;
                  - !Sub &quot;arn:${AWS::Partition}:iam::${AWS::AccountId}:role/cdk-*-lookup-role-*&quot;
                  - !Sub &quot;arn:${AWS::Partition}:iam::${AWS::AccountId}:role/cdk-*-deploy-role-*&quot;
                &quot;kms:ViaService&quot;:
                  - !Sub &quot;s3.${AWS::Region}.amazonaws.com&quot;
                  - !Sub &quot;ecr.${AWS::Region}.amazonaws.com&quot;
              ArnLike:
                &quot;kms:EncryptionContext:aws:s3:arn&quot;:
                  - !Sub &quot;arn:${AWS::Partition}:s3:::cdk-*-assets-${AWS::AccountId}-${AWS::Region}/*&quot;
                &quot;kms:EncryptionContext:aws:ecr:arn&quot;:
                  - !Sub &quot;arn:${AWS::Partition}:ecr:${AWS::Region}:${AWS::AccountId}:repository/cdk-*-container-assets-*&quot;
  CdkBootstrapKmsKeyAlias:
    Type: AWS::KMS::Alias
    Properties:
      AliasName: alias/cdk-bootstrap-key
      TargetKeyId: !Ref CdkBootstrapKmsKey
Outputs:
  CdkBootstrapKmsKeyId:
    Value: !Ref CdkBootstrapKmsKey
    Description: ID of the KMS key for CDK bootstrap
    Export:
      Name: cdk-bootstrap-kms-key-id
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;h3&gt;S3 Log Bucket Template&lt;/h3&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Show S3 log bucket template&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;AWSTemplateFormatVersion: &quot;2010-09-09&quot;
Description: &quot;CloudFormation template for S3 log bucket with encryption and lifecycle policies&quot;

Resources:
  LogBucket:
    Type: AWS::S3::Bucket
    Properties:
      VersioningConfiguration:
        Status: Enabled
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      LifecycleConfiguration:
        Rules:
          - Id: DeleteLogsAfterRetention
            Status: Enabled
            ExpirationInDays: 90
          - Id: DeleteOldVersions
            Status: Enabled
            NoncurrentVersionExpiration:
              NoncurrentDays: 30
      Tags:
        - Key: Purpose
          Value: ApplicationLogs
        - Key: Environment
          Value: Bootstrap

  LogBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref LogBucket
      PolicyDocument:
        Version: &quot;2012-10-17&quot;
        Statement:
          # AWS Best Practice: Restrict log delivery to buckets in the same account
          # See: https://docs.aws.amazon.com/AmazonS3/latest/userguide/enable-server-access-logging.html
          - Sid: AllowS3LogDeliveryWrite
            Effect: Allow
            Principal:
              Service: logging.s3.amazonaws.com
            Action: s3:PutObject
            Resource: !Sub &quot;${LogBucket.Arn}/*&quot;
            Condition:
              # Restrict to buckets in the same account
              StringEquals:
                aws:SourceAccount: !Ref AWS::AccountId
          - Sid: AllowS3LogDeliveryAclCheck
            Effect: Allow
            Principal:
              Service: logging.s3.amazonaws.com
            Action: s3:GetBucketAcl
            Resource: !GetAtt LogBucket.Arn
          # Note: DenyUnencryptedObjectUploads removed per AWS best practices
          # Bucket default encryption (SSE-S3) ensures all objects are encrypted
          # The S3 logging service doesn&apos;t set explicit encryption headers
          - Sid: DenyInsecureTransport
            Effect: Deny
            Principal: &quot;*&quot;
            Action: s3:*
            Resource:
              - !GetAtt LogBucket.Arn
              - !Sub &quot;${LogBucket.Arn}/*&quot;
            Condition:
              Bool:
                aws:SecureTransport: &quot;false&quot;

Outputs:
  LogBucketName:
    Description: Name of the log bucket
    Value: !Ref LogBucket
    Export:
      Name: log-bucket-name
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;h3&gt;The Deployment Script&lt;/h3&gt;
&lt;p&gt;A CDK app can have stacks deployed to different regions. Each region needs to be bootstrapped, so the KMS key and log bucket must be created in every region.&lt;/p&gt;
&lt;p&gt;Using the CDK Toolkit library, we can extract the regions from the CDK app and deploy the CloudFormation templates accordingly.&lt;/p&gt;
&lt;p&gt;The script creates CloudFormation outputs to make the resource values retrievable during bootstrap.&lt;/p&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Show deploy script&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import * as fs from &quot;node:fs&quot;;
import * as path from &quot;node:path&quot;;
import {
  CloudFormationClient,
  CreateStackCommand,
  type CreateStackCommandInput,
  UpdateStackCommand,
  type UpdateStackCommandInput,
  type Tag,
} from &quot;@aws-sdk/client-cloudformation&quot;;
import { Toolkit } from &quot;@aws-cdk/toolkit-lib&quot;;

// Parse command line arguments
const args = process.argv.slice(2);
const DRY_RUN = args.includes(&quot;--dry-run&quot;);

interface TemplateConfig {
  filename: string;
  stackName: string;
  description: string;
}

const getRegions = async (): Promise&amp;lt;string[]&amp;gt; =&amp;gt; {
  const toolkit = new Toolkit();
  const appPath = path.join(import.meta.dirname, &quot;../../bin/app.ts&quot;);
  const cloudAssemblySource = await toolkit.fromCdkApp(`pnpx tsx ${appPath}`);

  const cloudAssembly = await toolkit.synth(cloudAssemblySource);
  const list = await toolkit.list(cloudAssembly);
  const regions = list.map((stack) =&amp;gt; stack.environment.region);
  return regions;
};

const templates: TemplateConfig[] = [
  {
    filename: &quot;bootstrap-kms-key.yaml&quot;,
    stackName: &quot;cdk-bootstrap-kms-key&quot;,
    description: &quot;KMS Key for encrypting the CDK bootstrap bucket&quot;,
  },
  {
    filename: &quot;log-bucket.yaml&quot;,
    stackName: &quot;cdk-bootstrap-log-bucket&quot;,
    description: &quot;S3 log bucket with encryption and lifecycle policies&quot;,
  },
];

// Tags to apply to all stacks
const commonTags: Tag[] = [
  { Key: &quot;Environment&quot;, Value: &quot;bootstrap&quot; },
  { Key: &quot;ManagedBy&quot;, Value: &quot;cdk-bootstrap&quot; },
  { Key: &quot;Purpose&quot;, Value: &quot;cdk-deployment&quot; },
];

async function loadTemplateContent(templatePath: string): Promise&amp;lt;string&amp;gt; {
  try {
    const content = fs.readFileSync(templatePath, &quot;utf-8&quot;);
    console.log(`✓ Loaded template: ${templatePath}`);
    return content;
  } catch (error) {
    console.error(`✗ Failed to load template: ${templatePath}`);
    throw error;
  }
}

async function deployOrUpdateStack(
  config: TemplateConfig,
  templateContent: string,
  region: string,
): Promise&amp;lt;string&amp;gt; {
  const cfClient = new CloudFormationClient({ region });

  if (DRY_RUN) {
    console.log(`✓ [DRY RUN] Would deploy/update stack`);
    return &quot;dry-run-stack-id&quot;;
  }

  // Try to update first
  try {
    const updateParams: UpdateStackCommandInput = {
      StackName: config.stackName,
      TemplateBody: templateContent,
      Tags: commonTags,
    };

    const updateCommand = new UpdateStackCommand(updateParams);
    const response = await cfClient.send(updateCommand);
    console.log(`✓ Stack updated`);
    return response.StackId!;
  } catch (error: any) {
    // If stack doesn&apos;t exist, create it
    if (error.message?.includes(&quot;does not exist&quot;)) {
      const createParams: CreateStackCommandInput = {
        StackName: config.stackName,
        TemplateBody: templateContent,
        Tags: commonTags,
        OnFailure: &quot;DELETE&quot;,
        TimeoutInMinutes: 10,
        EnableTerminationProtection: true,
      };

      const createCommand = new CreateStackCommand(createParams);
      const response = await cfClient.send(createCommand);
      console.log(`✓ Stack created`);
      return response.StackId!;
    }

    // If no changes are detected during update, it&apos;s still successful
    if (error.message?.includes(&quot;No updates are to be performed&quot;)) {
      console.log(`✓ Stack is up to date (no changes needed)`);
      return &quot;&quot;;
    }

    throw error;
  }
}

async function deployTemplate(
  config: TemplateConfig,
  templatePath: string,
  region: string,
): Promise&amp;lt;void&amp;gt; {
  try {
    const templateContent = await loadTemplateContent(templatePath);

    console.log(`\n📦 Deploying stack: ${config.stackName} to ${region}`);
    const stackId = await deployOrUpdateStack(config, templateContent, region);

    if (stackId) {
      console.log(`  Stack ID: ${stackId}`);
    }
  } catch (error: any) {
    console.error(`✗ Failed to deploy stack ${config.stackName}:`);
    console.error(`  ${error.message}`);
    throw error;
  }
}

async function main(): Promise&amp;lt;void&amp;gt; {
  const cfTemplatesDir = path.join(import.meta.dirname, &quot;../cf-templates&quot;);
  const regions = await getRegions();

  console.log(&quot;🚀 Starting CloudFormation template deployment...\n&quot;);
  if (DRY_RUN) {
    console.log(&quot;⚠️  DRY RUN MODE - No actual changes will be made\n&quot;);
  }
  console.log(`Template directory: ${cfTemplatesDir}`);
  console.log(`Regions: ${regions.join(&quot;, &quot;)}\n`);

  try {
    for (const region of regions) {
      console.log(`\n🌍 Deploying to region: ${region}`);

      for (const template of templates) {
        const templatePath = path.join(cfTemplatesDir, template.filename);
        await deployTemplate(template, templatePath, region);
      }
    }

    if (DRY_RUN) {
      console.log(&quot;\n✅ Dry run completed successfully! No changes were made.&quot;);
    } else {
      console.log(&quot;\n✅ All templates deployed successfully!&quot;);
    }
  } catch (error) {
    console.error(&quot;\n❌ Deployment failed&quot;);
    process.exit(1);
  }
}

main();
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;h2&gt;Validate CloudFormation Templates with cdk-nag&lt;/h2&gt;
&lt;p&gt;Resources within a CDK app can be validated with &lt;a href=&quot;https://github.com/cdklabs/cdk-nag&quot;&gt;cdk-nag&lt;/a&gt;. To validate CloudFormation templates outside the CDK app, we can create a helper app that applies the same rule set to our CloudFormation templates.&lt;/p&gt;
&lt;p&gt;As described in the &lt;a href=&quot;https://github.com/cdklabs/cdk-nag?tab=readme-ov-file#using-on-cloudformation-templates&quot;&gt;cdk-nag documentation&lt;/a&gt;, CloudFormation templates can be included via the &lt;code&gt;CfnInclude&lt;/code&gt; construct and validated using cdk-nag aspects.&lt;/p&gt;
&lt;p&gt;This approach ensures:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;The same validation rules are applied consistently&lt;/li&gt;
&lt;li&gt;The same suppression mechanisms can be used&lt;/li&gt;
&lt;li&gt;All infrastructure code follows the same security standards&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Run the validation with this command:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;pnpm exec tsx bootstrap/app.ts
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Show cdk-nag validation script&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { CfnInclude } from &quot;aws-cdk-lib/cloudformation-include&quot;;
import { Stack, StackProps } from &quot;aws-cdk-lib&quot;;
import { Toolkit } from &quot;@aws-cdk/toolkit-lib&quot;;
import { App, Aspects } from &quot;aws-cdk-lib&quot;;
import { AwsSolutionsChecks, NagSuppressions } from &quot;cdk-nag&quot;;

// Set region and avoid IMDS lookup for local checks
process.env.AWS_REGION = process.env.AWS_REGION || &quot;us-east-1&quot;;
process.env.AWS_SDK_LOAD_CONFIG = &quot;false&quot;; // Prevent credential lookup
process.env.AWS_EC2_METADATA_SERVICE_ENDPOINT_MODE = &quot;IPv4&quot;; // Prevent IMDS timeout

const toolkit = new Toolkit();

const cloudAssemblySource = await toolkit.fromAssemblyBuilder(async () =&amp;gt; {
  const app = new App();
  const stack = new Stack(app, &quot;CdkNagCheckStack&quot;);
  new CfnInclude(stack, &quot;BootstrapKmsKey&quot;, {
    templateFile: &quot;./bootstrap/cf-templates/bootstrap-kms-key.yaml&quot;,
  });
  const logBucket = new CfnInclude(stack, &quot;LogBucket&quot;, {
    templateFile: &quot;./bootstrap/cf-templates/log-bucket.yaml&quot;,
  });
  NagSuppressions.addResourceSuppressions(
    logBucket,
    [
      {
        id: &quot;AwsSolutions-S1&quot;,
        reason: &quot;Log bucket does not have server access logs enabled&quot;,
      },
    ],
    true,
  );

  Aspects.of(app).add(new AwsSolutionsChecks({ verbose: true }));
  return app.synth();
});

try {
  await toolkit.synth(cloudAssemblySource);
  console.log(&quot;\n✓ Bootstrap templates synthesized successfully&quot;);
} catch (error) {
  console.error(&quot;\n✗ CDK Nag violations or synthesis errors found:&quot;);
  console.error(error);
  process.exit(1);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;h2&gt;Create the Customized CDK Bootstrap Template&lt;/h2&gt;
&lt;h3&gt;Get the Default Template&lt;/h3&gt;
&lt;p&gt;Customizations should be based on the standard CDK bootstrap template to ensure all necessary resources are included and the process remains compatible with future CDK versions.
First, retrieve the default template using the CDK CLI.&lt;/p&gt;
&lt;p&gt;Since the Toolkit library doesn&apos;t provide functionality to retrieve the standard template, we use the CDK CLI via a child process.&lt;/p&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Show script to get default template&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { execSync } from &quot;node:child_process&quot;;
import { mkdirSync, writeFileSync } from &quot;node:fs&quot;;
import { join } from &quot;node:path&quot;;
import { CDK_STANDDARD_TEMPLATE_FILE_NAME } from &quot;./bootstrap&quot;;

const resultCdkStandardTemplate = execSync(
  &quot;pnpm exec cdk bootstrap --show-template&quot;,
  {
    encoding: &quot;utf8&quot;,
  },
);

const generatedDir = join(import.meta.dirname, &quot;..&quot;, &quot;generated&quot;);
mkdirSync(generatedDir, { recursive: true });

writeFileSync(
  join(generatedDir, CDK_STANDDARD_TEMPLATE_FILE_NAME),
  resultCdkStandardTemplate,
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;h3&gt;Customize the Template&lt;/h3&gt;
&lt;p&gt;Using a YAML parser, the standard template is loaded and modified to add the logging configuration to the staging bucket.&lt;/p&gt;
&lt;p&gt;The log bucket configuration is embedded into the template using the CloudFormation &lt;code&gt;Fn::ImportValue&lt;/code&gt; function, which references the log bucket created earlier. This approach is necessary because there&apos;s no way to pass the log bucket name as a parameter during the bootstrap process itself.&lt;/p&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Show template customization script&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { mkdirSync, readFileSync, writeFileSync } from &quot;node:fs&quot;;
import { join } from &quot;node:path&quot;;
import { parse, stringify } from &quot;yaml&quot;;
import {
  CDK_CUSTOMIZED_TEMPLATE_FILE_NAME,
  CDK_STANDDARD_TEMPLATE_FILE_NAME,
} from &quot;./bootstrap&quot;;

const cdkStandardTemplate = readFileSync(
  join(
    import.meta.dirname,
    &quot;..&quot;,
    &quot;generated&quot;,
    CDK_STANDDARD_TEMPLATE_FILE_NAME,
  ),
  {
    encoding: &quot;utf8&quot;,
  },
);

const cdkBootstrapTemplate = parse(cdkStandardTemplate);
if (!cdkBootstrapTemplate) {
  throw new Error(&quot;Failed to load cdk bootstrap template&quot;);
}

// Add LoggingConfiguration to StagingBucket using Fn::ImportValue
if (cdkBootstrapTemplate.Resources?.StagingBucket?.Properties) {
  cdkBootstrapTemplate.Resources.StagingBucket.Properties.LoggingConfiguration =
    {
      DestinationBucketName: { &quot;Fn::ImportValue&quot;: &quot;log-bucket-name&quot; },
      LogFilePrefix: &quot;staging-bucket-logs/&quot;,
    };
}

const generatedDir = join(import.meta.dirname, &quot;..&quot;, &quot;generated&quot;);
mkdirSync(generatedDir, { recursive: true });

writeFileSync(
  join(generatedDir, CDK_CUSTOMIZED_TEMPLATE_FILE_NAME),
  stringify(cdkBootstrapTemplate),
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;h2&gt;Bootstrap with the Customized Template&lt;/h2&gt;
&lt;p&gt;Finally, execute the CDK bootstrap process with the customized template using the Toolkit library.&lt;/p&gt;
&lt;p&gt;Since the KMS key was created via CloudFormation, its ID must be retrieved using the CloudFormation Stack Outputs. The script:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Extracts environments (account/region pairs) from the CDK app&lt;/li&gt;
&lt;li&gt;Retrieves the KMS key ID for each region from CloudFormation outputs&lt;/li&gt;
&lt;li&gt;Passes the KMS key ID as a parameter to the bootstrap process&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Note that the log bucket configuration is already embedded in the customized template via &lt;code&gt;Fn::ImportValue&lt;/code&gt;, while the KMS key ID is passed as a parameter.&lt;/p&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Show bootstrap script&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import {
  BootstrapEnvironments,
  BootstrapStackParameters,
  Toolkit,
} from &quot;@aws-cdk/toolkit-lib&quot;;
import {
  CloudFormationClient,
  DescribeStacksCommand,
} from &quot;@aws-sdk/client-cloudformation&quot;;
import path from &quot;node:path&quot;;
import { join } from &quot;node:path&quot;;

export const CDK_STANDDARD_TEMPLATE_FILE_NAME =
  &quot;resultCdkStandardTemplate.yaml&quot;;
export const CDK_CUSTOMIZED_TEMPLATE_FILE_NAME =
  &quot;resultCdkCustomizedTemplate.yaml&quot;;

const toolkit = new Toolkit();

const templateFilePath = join(
  import.meta.dirname,
  &quot;..&quot;,
  &quot;generated&quot;,
  &quot;resultCdkCustomizedTemplate.yaml&quot;,
);

const appPath = path.join(import.meta.dirname, &quot;../../bin/app.ts&quot;);

const cloudAssemblySource = await toolkit.fromCdkApp(`pnpx tsx ${appPath}`);
// const environments: BootstrapEnvironments =
//   BootstrapEnvironments.fromCloudAssemblySource(cloudAssemblySource);
const cloudAssembly = await toolkit.synth(cloudAssemblySource);
const list = await toolkit.list(cloudAssembly);
const environmentsFromApp = list.map((stack) =&amp;gt; {
  return {
    account: stack.environment.account,
    region: stack.environment.region,
  };
});

for (const environmentFromApp of environmentsFromApp) {
  // Get KMS Key ID from stack output
  const cfnClient = new CloudFormationClient({
    region: environmentFromApp.region,
  });
  const describeStacksResponse = await cfnClient.send(
    new DescribeStacksCommand({
      StackName: &quot;cdk-bootstrap-kms-key&quot;,
    }),
  );

  const kmsKeyOutput = describeStacksResponse.Stacks?.[0]?.Outputs?.find(
    (output) =&amp;gt; output.ExportName === &quot;cdk-bootstrap-kms-key-id&quot;,
  );

  const kmsKeyId = kmsKeyOutput?.OutputValue;

  const environments: BootstrapEnvironments = BootstrapEnvironments.fromList([
    `aws://${environmentFromApp.account}/${environmentFromApp.region}`,
  ]);
  console.log(
    `Bootstrapping environment ${environmentFromApp.account}/${environmentFromApp.region} with KMS Key ID: ${kmsKeyId}`,
  );
  await toolkit.bootstrap(environments, {
    parameters: {
      parameters: {
        kmsKeyId,
      },
      keepExistingParameters: true,
    },
    source: {
      source: &quot;custom&quot;,
      templateFile: templateFilePath,
    },
  });
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;p&gt;After executing this script, the staging bucket will have:&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Custom KMS encryption:&lt;/strong&gt;
&lt;img src=&quot;./staging-bucket-custom-kms-encrypted.png&quot; alt=&quot;staging-bucket-custom-kms-encrypted&quot; /&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Server access logging configuration:&lt;/strong&gt;
&lt;img src=&quot;./staging-bucket-server-access-logging.png&quot; alt=&quot;staging-bucket-server-access-logging&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Conclusion&lt;/h2&gt;
&lt;p&gt;With a customized CDK bootstrap template, you can extend the bootstrap resources beyond what&apos;s possible with standard parameters. The CDK Toolkit library is an excellent tool for orchestrating the entire workflow in TypeScript, from extracting environments to deploying customized bootstrap stacks.&lt;/p&gt;
&lt;p&gt;This approach enables:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Advanced security configurations like custom KMS encryption and access logging&lt;/li&gt;
&lt;li&gt;Consistent validation using cdk-nag across both CDK and CloudFormation resources&lt;/li&gt;
&lt;li&gt;Type-safe, programmatic control over the bootstrap process&lt;/li&gt;
&lt;li&gt;Multi-region deployments with region-specific resource management&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Sources and References&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.aws.amazon.com/cdk/v2/guide/bootstrapping-customizing.html&quot;&gt;AWS CDK Bootstrapping - Customizing&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.aws.amazon.com/cdk/api/toolkit-lib/&quot;&gt;AWS CDK Toolkit Library&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/cdklabs/cdk-nag&quot;&gt;cdk-nag GitHub Repository&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/cdklabs/cdk-nag?tab=readme-ov-file#using-on-cloudformation-templates&quot;&gt;Using cdk-nag on CloudFormation Templates&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>Simple example of TanStack DB with DynamoDB on AWS with multiple entities</title><link>https://johanneskonings.dev/blog/2025-12-27-tanstack-start-aws-db-multiple-entities/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2025-12-27-tanstack-start-aws-db-multiple-entities/</guid><description>Implement a multi-entity data model with TanStack DB and DynamoDB using single table design. Learn how to manage Person, Address, BankAccount, ContactInfo, and Employment entities with ElectroDB and TanStack DB collections. Includes performance optimization with global collections for instant navigation.</description><pubDate>Sat, 27 Dec 2025 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Introduction&lt;/h2&gt;
&lt;p&gt;In &lt;a href=&quot;/blog/2025-12-20-tanstack-start-aws-db-simple/&quot;&gt;Simple example of TanStack DB with DynamoDB on AWS&lt;/a&gt;, I describe how to use TanStack DB with a single entity in combination with DynamoDB.
This post contains a simple example with multiple entities and how to sync single table design with TanStack DB collections.&lt;/p&gt;
&lt;p&gt;At a high level, we’ll go from infrastructure (a DynamoDB table), to a small DynamoDB client, to a TanStack Start server route, and finally to a TanStack DB collection that powers a simple UI.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Update:&lt;/strong&gt; This post has been updated to include performance optimizations using global collections instead of factory-based per-person collections. This enables instant sub-millisecond navigation between person details without network requests. See the &lt;a href=&quot;#improved-data-retrieval-global-collections&quot;&gt;Improved data retrieval&lt;/a&gt; section for details.&lt;/p&gt;
&lt;p&gt;The complete implementation is available in the &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws&quot;&gt;tanstack-aws repository&lt;/a&gt;, which serves as a working example and template for this deployment pattern.&lt;/p&gt;
&lt;h2&gt;Architecture Overview&lt;/h2&gt;
&lt;p&gt;&lt;img src=&quot;./tanstack-db-architecture.png&quot; alt=&quot;TanStack DB Architecture&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Disclaimer&lt;/h2&gt;
&lt;p&gt;This is an enhancement of a very simple example to get you started with TanStack DB and DynamoDB on AWS with multiple entities, but still simple. It is not production-ready and lacks features like error handling, security, and optimizations.&lt;/p&gt;
&lt;h2&gt;Demo Video&lt;/h2&gt;
&lt;p&gt;The following video demonstrates the improved data retrieval with global collections, showing instant navigation between person details:&lt;/p&gt;
&lt;p&gt;&amp;lt;iframe width=&quot;560&quot; height=&quot;315&quot; src=&quot;https://www.youtube.com/embed/TP4k8dHPL7w&quot; title=&quot;TanStack DB Global Collections Demo&quot; frameborder=&quot;0&quot; allow=&quot;accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share&quot; allowfullscreen&amp;gt;&amp;lt;/iframe&amp;gt;&lt;/p&gt;
&lt;h2&gt;The data model&lt;/h2&gt;
&lt;p&gt;This example implements a multi-entity data model for managing persons with related information. The schemas are defined using Zod as the single source of truth, with ElectroDB entities derived from these schemas to ensure type safety throughout the stack.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;erDiagram
    PERSON ||--o{ ADDRESS : has
    PERSON ||--o{ BANKACCOUNT : has
    PERSON ||--o{ CONTACTINFO : has
    PERSON ||--o{ EMPLOYMENT : has

    PERSON {
        string id PK
        string firstName
        string lastName
        string dateOfBirth
        string gender
        string createdAt
        string updatedAt
    }

    ADDRESS {
        string id PK
        string personId FK
        string type
        string street
        string city
        string state
        string postalCode
        string country
        boolean isPrimary
    }

    BANKACCOUNT {
        string id PK
        string personId FK
        string bankName
        string accountType
        string accountNumberLast4
        string iban
        string bic
        boolean isPrimary
    }

    CONTACTINFO {
        string id PK
        string personId FK
        string type
        string value
        boolean isPrimary
        boolean isVerified
    }

    EMPLOYMENT {
        string id PK
        string personId FK
        string companyName
        string position
        string department
        string startDate
        string endDate
        boolean isCurrent
        number salary
        string currency
    }
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;DynamoDB single table design&lt;/h2&gt;
&lt;p&gt;All entities are stored in a single DynamoDB table using a carefully designed key structure that enables efficient querying patterns.&lt;/p&gt;
&lt;h3&gt;Key Structure&lt;/h3&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Entity&lt;/th&gt;
&lt;th&gt;Partition Key (pk)&lt;/th&gt;
&lt;th&gt;Sort Key (sk)&lt;/th&gt;
&lt;th&gt;Description&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Person&lt;/td&gt;
&lt;td&gt;&lt;code&gt;PERSON#&amp;lt;personId&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;PROFILE&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Person profile data&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Address&lt;/td&gt;
&lt;td&gt;&lt;code&gt;PERSON#&amp;lt;personId&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;ADDRESS#&amp;lt;addressId&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Person&apos;s addresses&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;BankAccount&lt;/td&gt;
&lt;td&gt;&lt;code&gt;PERSON#&amp;lt;personId&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;BANK#&amp;lt;bankId&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Person&apos;s bank accounts&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;ContactInfo&lt;/td&gt;
&lt;td&gt;&lt;code&gt;PERSON#&amp;lt;personId&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;CONTACT#&amp;lt;contactId&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Person&apos;s contact info&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Employment&lt;/td&gt;
&lt;td&gt;&lt;code&gt;PERSON#&amp;lt;personId&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;EMPLOYMENT#&amp;lt;employmentId&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Person&apos;s employment history&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;This structure groups all data for a single person under the same partition key, allowing efficient retrieval of a person and all their related entities in a single query.&lt;/p&gt;
&lt;h3&gt;Access Patterns&lt;/h3&gt;
&lt;p&gt;The design supports the following access patterns:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Access Pattern&lt;/th&gt;
&lt;th&gt;Key Condition&lt;/th&gt;
&lt;th&gt;Description&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Get all persons&lt;/td&gt;
&lt;td&gt;GSI1: &lt;code&gt;gsi1pk = PERSONS&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;List all persons&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Get all addresses&lt;/td&gt;
&lt;td&gt;GSI1: &lt;code&gt;gsi1pk = ADDRESSES&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;List all addresses (for global collection)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Get all bank accounts&lt;/td&gt;
&lt;td&gt;GSI1: &lt;code&gt;gsi1pk = BANKACCOUNTS&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;List all bank accounts (for global collection)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Get all contacts&lt;/td&gt;
&lt;td&gt;GSI1: &lt;code&gt;gsi1pk = CONTACTS&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;List all contacts (for global collection)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Get all employments&lt;/td&gt;
&lt;td&gt;GSI1: &lt;code&gt;gsi1pk = EMPLOYMENTS&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;List all employments (for global collection)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Get person by ID&lt;/td&gt;
&lt;td&gt;&lt;code&gt;pk = PERSON#&amp;lt;id&amp;gt;&lt;/code&gt;, &lt;code&gt;sk = PROFILE&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Single person lookup&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Get person with all data&lt;/td&gt;
&lt;td&gt;&lt;code&gt;pk = PERSON#&amp;lt;id&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Get person + all related entities (collection query)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Get person&apos;s addresses&lt;/td&gt;
&lt;td&gt;&lt;code&gt;pk = PERSON#&amp;lt;id&amp;gt;&lt;/code&gt;, &lt;code&gt;sk begins_with ADDRESS#&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;All addresses for a person&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Get person&apos;s bank accounts&lt;/td&gt;
&lt;td&gt;&lt;code&gt;pk = PERSON#&amp;lt;id&amp;gt;&lt;/code&gt;, &lt;code&gt;sk begins_with BANK#&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;All bank accounts for a person&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Get person&apos;s contacts&lt;/td&gt;
&lt;td&gt;&lt;code&gt;pk = PERSON#&amp;lt;id&amp;gt;&lt;/code&gt;, &lt;code&gt;sk begins_with CONTACT#&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;All contacts for a person&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Get person&apos;s employment&lt;/td&gt;
&lt;td&gt;&lt;code&gt;pk = PERSON#&amp;lt;id&amp;gt;&lt;/code&gt;, &lt;code&gt;sk begins_with EMPLOYMENT#&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;All employment records for a person&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;The table entries will look a little bit different as ElectroDB will take care of the attribute mapping.&lt;/p&gt;
&lt;h3&gt;Global Secondary Indexes&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;GSI1: Multi-Entity Type Index&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;GSI1 is shared by ALL entity types using different partition key templates. This single GSI handles all &quot;get all entities of type X&quot; queries efficiently without table scans.&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Entity&lt;/th&gt;
&lt;th&gt;gsi1pk Template&lt;/th&gt;
&lt;th&gt;gsi1sk&lt;/th&gt;
&lt;th&gt;Query Method&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Person&lt;/td&gt;
&lt;td&gt;&lt;code&gt;PERSONS&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;lastName#firstName#id&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;PersonEntity.query.allPersons({})&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Address&lt;/td&gt;
&lt;td&gt;&lt;code&gt;ADDRESSES&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;personId#id&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;AddressEntity.query.allAddresses({})&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;BankAccount&lt;/td&gt;
&lt;td&gt;&lt;code&gt;BANKACCOUNTS&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;personId#id&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;BankAccountEntity.query.allBankAccounts({})&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;ContactInfo&lt;/td&gt;
&lt;td&gt;&lt;code&gt;CONTACTS&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;personId#id&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;ContactInfoEntity.query.allContacts({})&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Employment&lt;/td&gt;
&lt;td&gt;&lt;code&gt;EMPLOYMENTS&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;personId#id&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;EmploymentEntity.query.allEmployments({})&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;&lt;strong&gt;Benefits of Single GSI1 for All Entities:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;✅ &lt;strong&gt;No scans&lt;/strong&gt; - Each entity type query uses an efficient Query operation&lt;/li&gt;
&lt;li&gt;✅ &lt;strong&gt;Single GSI&lt;/strong&gt; - Reduces infrastructure complexity and costs&lt;/li&gt;
&lt;li&gt;✅ &lt;strong&gt;Template-based partitioning&lt;/strong&gt; - Clean separation by entity type&lt;/li&gt;
&lt;li&gt;✅ &lt;strong&gt;ElectroDB auto-populates&lt;/strong&gt; - gsi1pk/gsi1sk populated automatically on write&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;This approach enables the global collections pattern for TanStack DB, where all entities are loaded once at startup and filtered client-side for instant navigation.&lt;/p&gt;
&lt;h2&gt;ElectroDB as DynamoDB client&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://electrodb.dev/&quot;&gt;ElectroDB&lt;/a&gt; simplifies working with single-table designs that contain multiple entities. In this example, a single DynamoDB table stores Person, Address, BankAccount, ContactInfo, and Employment entities using composite keys. ElectroDB handles the complexity of query building, attribute mapping, and relationship management across these entities, eliminating the need to manually construct DynamoDB expressions. It provides type-safe entity definitions (derived from Zod schemas), automatic key generation, and collection queries that efficiently retrieve related entities together—making single-table designs more maintainable and less error-prone than raw DynamoDB client code.&lt;/p&gt;
&lt;p&gt;The schema looks like this:&lt;/p&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Click to expand ElectroDB schema&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { getDdbDocClient } from &quot;#src/webapp/integrations/ddb-client/ddbClient.ts&quot;;
import { zodToElectroDBAttributes } from &quot;#src/webapp/integrations/electrodb/zod-to-electrodb.ts&quot;;
import {
  AddressSchema,
  BankAccountSchema,
  ContactInfoSchema,
  EmploymentSchema,
  PersonSchema,
} from &quot;#src/webapp/types/person.ts&quot;;
import { Entity, type EntityConfiguration, Service } from &quot;electrodb&quot;;

// =============================================================================
// Table Configuration
// =============================================================================

const TABLE_NAME =
  process.env.DDB_PERSONS_TABLE_NAME ?? &quot;TanstackAwsStack-db-persons&quot;;

const getEntityConfig = (): EntityConfiguration =&amp;gt; ({
  client: getDdbDocClient(),
  table: TABLE_NAME,
});

// =============================================================================
// Derived ElectroDB Attributes from Zod Schemas (Single Source of Truth)
// =============================================================================

const personAttributes = zodToElectroDBAttributes(PersonSchema);
const addressAttributes = zodToElectroDBAttributes(AddressSchema);
const bankAccountAttributes = zodToElectroDBAttributes(BankAccountSchema);
const contactInfoAttributes = zodToElectroDBAttributes(ContactInfoSchema);
const employmentAttributes = zodToElectroDBAttributes(EmploymentSchema);

// =============================================================================
// Person Entity
// =============================================================================

export const PersonEntity = new Entity(
  {
    model: {
      entity: &quot;Person&quot;,
      version: &quot;1&quot;,
      service: &quot;persons&quot;,
    },
    attributes: personAttributes,
    indexes: {
      primary: {
        pk: { field: &quot;pk&quot;, composite: [&quot;id&quot;] },
        sk: { field: &quot;sk&quot;, composite: [] },
      },
      // GSI1: List all persons
      allPersons: {
        index: &quot;GSI1&quot;,
        pk: { field: &quot;gsi1pk&quot;, composite: [], template: &quot;PERSONS&quot; },
        sk: { field: &quot;gsi1sk&quot;, composite: [&quot;lastName&quot;, &quot;firstName&quot;, &quot;id&quot;] },
      },
    },
  },
  getEntityConfig(),
);

// =============================================================================
// Address Entity
// =============================================================================

export const AddressEntity = new Entity(
  {
    model: {
      entity: &quot;Address&quot;,
      version: &quot;1&quot;,
      service: &quot;persons&quot;,
    },
    attributes: addressAttributes,
    indexes: {
      primary: {
        pk: { field: &quot;pk&quot;, composite: [&quot;personId&quot;] },
        sk: { field: &quot;sk&quot;, composite: [&quot;id&quot;] },
      },
      // GSI1: Query all addresses
      allAddresses: {
        index: &quot;GSI1&quot;,
        pk: { field: &quot;gsi1pk&quot;, composite: [], template: &quot;ADDRESSES&quot; },
        sk: { field: &quot;gsi1sk&quot;, composite: [&quot;personId&quot;, &quot;id&quot;] },
      },
    },
  },
  getEntityConfig(),
);

// =============================================================================
// BankAccount Entity
// =============================================================================

export const BankAccountEntity = new Entity(
  {
    model: {
      entity: &quot;BankAccount&quot;,
      version: &quot;1&quot;,
      service: &quot;persons&quot;,
    },
    attributes: bankAccountAttributes,
    indexes: {
      primary: {
        pk: { field: &quot;pk&quot;, composite: [&quot;personId&quot;] },
        sk: { field: &quot;sk&quot;, composite: [&quot;id&quot;] },
      },
      // GSI1: Query all bank accounts
      allBankAccounts: {
        index: &quot;GSI1&quot;,
        pk: { field: &quot;gsi1pk&quot;, composite: [], template: &quot;BANKACCOUNTS&quot; },
        sk: { field: &quot;gsi1sk&quot;, composite: [&quot;personId&quot;, &quot;id&quot;] },
      },
    },
  },
  getEntityConfig(),
);

// =============================================================================
// ContactInfo Entity
// =============================================================================

export const ContactInfoEntity = new Entity(
  {
    model: {
      entity: &quot;ContactInfo&quot;,
      version: &quot;1&quot;,
      service: &quot;persons&quot;,
    },
    attributes: contactInfoAttributes,
    indexes: {
      primary: {
        pk: { field: &quot;pk&quot;, composite: [&quot;personId&quot;] },
        sk: { field: &quot;sk&quot;, composite: [&quot;id&quot;] },
      },
      // GSI1: Query all contacts
      allContacts: {
        index: &quot;GSI1&quot;,
        pk: { field: &quot;gsi1pk&quot;, composite: [], template: &quot;CONTACTS&quot; },
        sk: { field: &quot;gsi1sk&quot;, composite: [&quot;personId&quot;, &quot;id&quot;] },
      },
    },
  },
  getEntityConfig(),
);

// =============================================================================
// Employment Entity
// =============================================================================

export const EmploymentEntity = new Entity(
  {
    model: {
      entity: &quot;Employment&quot;,
      version: &quot;1&quot;,
      service: &quot;persons&quot;,
    },
    attributes: employmentAttributes,
    indexes: {
      primary: {
        pk: { field: &quot;pk&quot;, composite: [&quot;personId&quot;] },
        sk: { field: &quot;sk&quot;, composite: [&quot;id&quot;] },
      },
      // GSI1: Query all employments
      allEmployments: {
        index: &quot;GSI1&quot;,
        pk: { field: &quot;gsi1pk&quot;, composite: [], template: &quot;EMPLOYMENTS&quot; },
        sk: { field: &quot;gsi1sk&quot;, composite: [&quot;personId&quot;, &quot;id&quot;] },
      },
    },
  },
  getEntityConfig(),
);

// =============================================================================
// Persons Service - Collection Queries
// =============================================================================

new Service(
  {
    person: PersonEntity,
    address: AddressEntity,
    bankAccount: BankAccountEntity,
    contactInfo: ContactInfoEntity,
    employment: EmploymentEntity,
  },
  getEntityConfig(),
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;p&gt;Certain fields are derived from Zod schemas to ensure type safety throughout the stack and have a single source of truth&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Full implementation&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2025-12-27-tanstack-start-aws-db-multiple-entities-update/src/webapp/types/person.ts&quot;&gt;person.ts (Zod Schemas)&lt;/a&gt; | &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2025-12-27-tanstack-start-aws-db-multiple-entities-update/src/webapp/integrations/electrodb/entities.ts&quot;&gt;entities.ts (ElectroDB Entities)&lt;/a&gt; | &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2025-12-27-tanstack-start-aws-db-multiple-entities-update/src/webapp/integrations/electrodb/zod-to-electrodb.ts&quot;&gt;zod-to-electrodb.ts&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;The persons client&lt;/h2&gt;
&lt;p&gt;This wrapper around ElectroDB entities provides type-safe methods for performing CRUD operations on persons and their related entities. The updated version includes &lt;code&gt;getAllX&lt;/code&gt; methods for each entity type that query GSI1 to support global collections.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Full implementation&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2025-12-27-tanstack-start-aws-db-multiple-entities-update/src/webapp/integrations/electrodb/personsClient.ts&quot;&gt;personsClient.ts&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Click to expand personsClient.ts&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;/**
 * ElectroDB-based Persons Client
 *
 * Uses ElectroDB entities for type-safe DynamoDB operations.
 */

import type {
  Address,
  BankAccount,
  ContactInfo,
  Employment,
  Person,
} from &quot;#src/webapp/types/person.ts&quot;;
import {
  AddressEntity,
  BankAccountEntity,
  ContactInfoEntity,
  EmploymentEntity,
  PersonEntity,
} from &quot;#src/webapp/integrations/electrodb/entities.ts&quot;;

// =============================================================================
// Person Operations
// =============================================================================

/**
 * Get all persons using GSI1
 */
export const getAllPersons = async (): Promise&amp;lt;Person[]&amp;gt; =&amp;gt; {
  const result = await PersonEntity.query.allPersons({}).go();
  return result.data.map((item) =&amp;gt; ({
    id: item.id,
    firstName: item.firstName,
    lastName: item.lastName,
    dateOfBirth: item.dateOfBirth,
    gender: item.gender,
    createdAt: item.createdAt,
    updatedAt: item.updatedAt,
  }));
};

/**
 * Create a new person
 */
export const createPerson = async (person: Person): Promise&amp;lt;Person&amp;gt; =&amp;gt; {
  const result = await PersonEntity.put(person).go();
  return result.data as Person;
};

/**
 * Update a person
 * Uses put with merged data to ensure GSI1 composite keys are properly formatted
 */
export const updatePerson = async (
  personId: string,
  updates: Partial&amp;lt;Person&amp;gt;,
): Promise&amp;lt;Person&amp;gt; =&amp;gt; {
  // First, get the current person to merge with updates
  const current = await PersonEntity.get({ id: personId }).go();
  if (!current.data) {
    throw new Error(`Person with id ${personId} not found`);
  }

  // Merge current data with updates and use put to replace the item
  const updatedPerson = {
    id: personId,
    firstName: updates.firstName ?? current.data.firstName,
    lastName: updates.lastName ?? current.data.lastName,
    dateOfBirth: updates.dateOfBirth ?? current.data.dateOfBirth,
    gender: updates.gender ?? current.data.gender,
    createdAt: current.data.createdAt,
    updatedAt: updates.updatedAt ?? new Date().toISOString(),
  };

  const result = await PersonEntity.put(updatedPerson).go();
  return result.data as Person;
};

/**
 * Delete a person and all related entities
 */
export const deletePerson = async (personId: string): Promise&amp;lt;void&amp;gt; =&amp;gt; {
  // Delete all related entities first
  const [addresses, bankAccounts, contacts, employments] = await Promise.all([
    AddressEntity.query.primary({ personId }).go(),
    BankAccountEntity.query.primary({ personId }).go(),
    ContactInfoEntity.query.primary({ personId }).go(),
    EmploymentEntity.query.primary({ personId }).go(),
  ]);

  // Delete all related items
  await Promise.all([
    ...addresses.data.map((addr) =&amp;gt;
      AddressEntity.delete({ personId, id: addr.id }).go(),
    ),
    ...bankAccounts.data.map((bank) =&amp;gt;
      BankAccountEntity.delete({ personId, id: bank.id }).go(),
    ),
    ...contacts.data.map((contact) =&amp;gt;
      ContactInfoEntity.delete({ personId, id: contact.id }).go(),
    ),
    ...employments.data.map((emp) =&amp;gt;
      EmploymentEntity.delete({ personId, id: emp.id }).go(),
    ),
    PersonEntity.delete({ id: personId }).go(),
  ]);
};

// =============================================================================
// Address Operations
// =============================================================================

/**
 * Get all addresses using GSI1 (allAddresses)
 * Uses partition key template &apos;ADDRESSES&apos; for efficient querying
 */
export const getAllAddresses = async (): Promise&amp;lt;Address[]&amp;gt; =&amp;gt; {
  const result = await AddressEntity.query.allAddresses({}).go();
  return result.data as Address[];
};

export const createAddress = async (address: Address): Promise&amp;lt;Address&amp;gt; =&amp;gt; {
  const result = await AddressEntity.put(address).go();
  return result.data as Address;
};

export const updateAddress = async (address: Address): Promise&amp;lt;Address&amp;gt; =&amp;gt; {
  const result = await AddressEntity.put(address).go();
  return result.data as Address;
};

export const deleteAddress = async (
  personId: string,
  addressId: string,
): Promise&amp;lt;void&amp;gt; =&amp;gt; {
  await AddressEntity.delete({ personId, id: addressId }).go();
};

// =============================================================================
// BankAccount Operations
// =============================================================================

/**
 * Get all bank accounts using GSI1 (allBankAccounts)
 */
export const getAllBankAccounts = async (): Promise&amp;lt;BankAccount[]&amp;gt; =&amp;gt; {
  const result = await BankAccountEntity.query.allBankAccounts({}).go();
  return result.data as BankAccount[];
};

export const createBankAccount = async (
  bankAccount: BankAccount,
): Promise&amp;lt;BankAccount&amp;gt; =&amp;gt; {
  const result = await BankAccountEntity.put(bankAccount).go();
  return result.data as BankAccount;
};

export const updateBankAccount = async (
  bankAccount: BankAccount,
): Promise&amp;lt;BankAccount&amp;gt; =&amp;gt; {
  const result = await BankAccountEntity.put(bankAccount).go();
  return result.data as BankAccount;
};

export const deleteBankAccount = async (
  personId: string,
  bankAccountId: string,
): Promise&amp;lt;void&amp;gt; =&amp;gt; {
  await BankAccountEntity.delete({ personId, id: bankAccountId }).go();
};

// =============================================================================
// ContactInfo Operations
// =============================================================================

/**
 * Get all contacts using GSI1 (allContacts)
 */
export const getAllContacts = async (): Promise&amp;lt;ContactInfo[]&amp;gt; =&amp;gt; {
  const result = await ContactInfoEntity.query.allContacts({}).go();
  return result.data as ContactInfo[];
};

export const createContact = async (
  contact: ContactInfo,
): Promise&amp;lt;ContactInfo&amp;gt; =&amp;gt; {
  const result = await ContactInfoEntity.put(contact).go();
  return result.data as ContactInfo;
};

export const updateContact = async (
  contact: ContactInfo,
): Promise&amp;lt;ContactInfo&amp;gt; =&amp;gt; {
  const result = await ContactInfoEntity.put(contact).go();
  return result.data as ContactInfo;
};

export const deleteContact = async (
  personId: string,
  contactId: string,
): Promise&amp;lt;void&amp;gt; =&amp;gt; {
  await ContactInfoEntity.delete({ personId, id: contactId }).go();
};

// =============================================================================
// Employment Operations
// =============================================================================

// Helper to convert null to undefined for ElectroDB compatibility
const normalizeEmployment = (employment: Employment) =&amp;gt; ({
  ...employment,
  endDate: employment.endDate ?? undefined,
});

/**
 * Get all employments using GSI1 (allEmployments)
 */
export const getAllEmployments = async (): Promise&amp;lt;Employment[]&amp;gt; =&amp;gt; {
  const result = await EmploymentEntity.query.allEmployments({}).go();
  return result.data as Employment[];
};

export const createEmployment = async (
  employment: Employment,
): Promise&amp;lt;Employment&amp;gt; =&amp;gt; {
  const result = await EmploymentEntity.put(
    normalizeEmployment(employment),
  ).go();
  return result.data as Employment;
};

export const updateEmployment = async (
  employment: Employment,
): Promise&amp;lt;Employment&amp;gt; =&amp;gt; {
  const result = await EmploymentEntity.put(
    normalizeEmployment(employment),
  ).go();
  return result.data as Employment;
};

export const deleteEmployment = async (
  personId: string,
  employmentId: string,
): Promise&amp;lt;void&amp;gt; =&amp;gt; {
  await EmploymentEntity.delete({ personId, id: employmentId }).go();
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;h2&gt;The new GSI for DynamoDB&lt;/h2&gt;
&lt;p&gt;The DynamoDB table uses a single GSI that serves all entity types with different partition key templates. This enables efficient querying of all entities of a specific type without table scans.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Full implementation&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2025-12-27-tanstack-start-aws-db-multiple-entities-update/lib/constructs/DatabasePersons.ts&quot;&gt;DatabasePersons.ts&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Click to expand DatabasePersons.ts&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import {
  AttributeType,
  BillingMode,
  ProjectionType,
  Table,
} from &quot;aws-cdk-lib/aws-dynamodb&quot;;
import { Construct } from &quot;constructs&quot;;

export class DatabasePersons extends Construct {
  public readonly dbPersons: Table;
  constructor(scope: Construct, id: string) {
    super(scope, id);

    this.dbPersons = new Table(this, &quot;Persons&quot;, {
      partitionKey: { name: &quot;pk&quot;, type: AttributeType.STRING },
      sortKey: { name: &quot;sk&quot;, type: AttributeType.STRING },
      billingMode: BillingMode.PAY_PER_REQUEST,
    });

    // GSI1: Multi-entity type index
    // Serves all entity types with different gsi1pk templates:
    // - PERSONS, ADDRESSES, BANKACCOUNTS, CONTACTS, EMPLOYMENTS
    this.dbPersons.addGlobalSecondaryIndex({
      indexName: &quot;GSI1&quot;,
      partitionKey: { name: &quot;gsi1pk&quot;, type: AttributeType.STRING },
      sortKey: { name: &quot;gsi1sk&quot;, type: AttributeType.STRING },
      projectionType: ProjectionType.ALL,
    });
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;h2&gt;The TanStack DB collection with server functions&lt;/h2&gt;
&lt;p&gt;The updated implementation uses &lt;strong&gt;global collections&lt;/strong&gt; instead of factory-based per-person collections. This approach loads all entities once at app startup and enables instant sub-millisecond navigation between person details without network requests.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Full implementation&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2025-12-27-tanstack-start-aws-db-multiple-entities-update/src/webapp/db-collections/persons.ts&quot;&gt;persons.ts (collections)&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Click to expand persons.ts (TanStack DB global collections)&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import * as electrodbClient from &quot;#src/webapp/integrations/electrodb/personsClient&quot;;
import { getContext } from &quot;#src/webapp/integrations/tanstack-query/root-provider&quot;;
import {
  type Address,
  AddressSchema,
  type BankAccount,
  BankAccountSchema,
  type ContactInfo,
  ContactInfoSchema,
  type Employment,
  EmploymentSchema,
  type Person,
  PersonSchema,
} from &quot;#src/webapp/types/person&quot;;
import { queryCollectionOptions } from &quot;@tanstack/query-db-collection&quot;;
import { createCollection } from &quot;@tanstack/react-db&quot;;
import { createServerFn } from &quot;@tanstack/react-start&quot;;

// =============================================================================
// Server Functions - Global Fetchers
// =============================================================================

/**
 * Get all persons (profile only)
 */
const fetchPersons = createServerFn({ method: &quot;GET&quot; }).handler(async () =&amp;gt;
  electrodbClient.getAllPersons(),
);

/**
 * Get all addresses (global)
 */
const fetchAllAddresses = createServerFn({ method: &quot;GET&quot; }).handler(async () =&amp;gt;
  electrodbClient.getAllAddresses(),
);

/**
 * Get all bank accounts (global)
 */
const fetchAllBankAccounts = createServerFn({ method: &quot;GET&quot; }).handler(
  async () =&amp;gt; electrodbClient.getAllBankAccounts(),
);

/**
 * Get all contacts (global)
 */
const fetchAllContacts = createServerFn({ method: &quot;GET&quot; }).handler(async () =&amp;gt;
  electrodbClient.getAllContacts(),
);

/**
 * Get all employments (global)
 */
const fetchAllEmployments = createServerFn({ method: &quot;GET&quot; }).handler(
  async () =&amp;gt; electrodbClient.getAllEmployments(),
);

// ... mutation server functions (createPersonFn, updatePersonFn, etc.)

// =============================================================================
// TanStack DB Global Collections
// =============================================================================

/**
 * Persons Collection - Main collection for person profiles
 *
 * Uses eager sync mode (default) - all persons loaded upfront.
 * Good for datasets &amp;lt; 10k rows.
 */
export const personsCollection = createCollection(
  queryCollectionOptions&amp;lt;Person&amp;gt;({
    queryKey: [&quot;persons&quot;],
    queryFn: () =&amp;gt; fetchPersons(),
    queryClient: getContext().queryClient,
    getKey: (item) =&amp;gt; item.id,
    onInsert: async ({ transaction }) =&amp;gt; {
      await Promise.all(
        transaction.mutations.map((mutation) =&amp;gt;
          createPersonFn({ data: mutation.modified as Person }),
        ),
      );
    },
    onUpdate: async ({ transaction }) =&amp;gt; {
      await Promise.all(
        transaction.mutations.map((mutation) =&amp;gt;
          updatePersonFn({
            data: {
              personId: mutation.key as string,
              updates: mutation.changes as Partial&amp;lt;Person&amp;gt;,
            },
          }),
        ),
      );
    },
    onDelete: async ({ transaction }) =&amp;gt; {
      await Promise.all(
        transaction.mutations.map((mutation) =&amp;gt;
          deletePersonFn({ data: mutation.key as string }),
        ),
      );
    },
  }),
);

/**
 * Global Addresses Collection
 *
 * All addresses loaded once, joined client-side with persons.
 * Enables instant navigation between person details without network calls.
 */
export const addressesCollection = createCollection(
  queryCollectionOptions&amp;lt;Address&amp;gt;({
    queryKey: [&quot;addresses&quot;],
    queryFn: () =&amp;gt; fetchAllAddresses(),
    queryClient: getContext().queryClient,
    getKey: (item) =&amp;gt; item.id,
    // ... onInsert, onUpdate, onDelete handlers
  }),
);

/**
 * Global Bank Accounts Collection
 */
export const bankAccountsCollection = createCollection(
  queryCollectionOptions&amp;lt;BankAccount&amp;gt;({
    queryKey: [&quot;bankAccounts&quot;],
    queryFn: () =&amp;gt; fetchAllBankAccounts(),
    queryClient: getContext().queryClient,
    getKey: (item) =&amp;gt; item.id,
    // ... onInsert, onUpdate, onDelete handlers
  }),
);

/**
 * Global Contacts Collection
 */
export const contactsCollection = createCollection(
  queryCollectionOptions&amp;lt;ContactInfo&amp;gt;({
    queryKey: [&quot;contacts&quot;],
    queryFn: () =&amp;gt; fetchAllContacts(),
    queryClient: getContext().queryClient,
    getKey: (item) =&amp;gt; item.id,
    // ... onInsert, onUpdate, onDelete handlers
  }),
);

/**
 * Global Employments Collection
 */
export const employmentsCollection = createCollection(
  queryCollectionOptions&amp;lt;Employment&amp;gt;({
    queryKey: [&quot;employments&quot;],
    queryFn: () =&amp;gt; fetchAllEmployments(),
    queryClient: getContext().queryClient,
    getKey: (item) =&amp;gt; item.id,
    // ... onInsert, onUpdate, onDelete handlers
  }),
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;p&gt;The key difference from the previous factory-based approach is that all collections are now &lt;strong&gt;global singletons&lt;/strong&gt; that load all entities once at app startup. The client-side filtering using TanStack DB&apos;s &lt;code&gt;eq()&lt;/code&gt; operator provides instant navigation between persons.&lt;/p&gt;
&lt;h2&gt;The hook&lt;/h2&gt;
&lt;p&gt;Custom React hooks provide a clean interface for components to interact with the TanStack DB collections. The updated &lt;code&gt;usePersons&lt;/code&gt; hook now also prefetches all entity data for instant person detail loading. The &lt;code&gt;usePersonDetail&lt;/code&gt; hook uses TanStack DB&apos;s &lt;code&gt;eq()&lt;/code&gt; operator to filter the global collections client-side, providing sub-millisecond navigation.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Full implementation&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2025-12-27-tanstack-start-aws-db-multiple-entities-update/src/webapp/hooks/useDbPersons.ts&quot;&gt;useDbPersons.ts&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Click to expand useDbPersons.ts&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import type {
  Address,
  BankAccount,
  ContactInfo,
  Employment,
  Person,
} from &quot;#src/webapp/types/person&quot;;
import {
  addressesCollection,
  bankAccountsCollection,
  contactsCollection,
  employmentsCollection,
  personsCollection,
} from &quot;#src/webapp/db-collections/persons&quot;;
import { eq, useLiveQuery } from &quot;@tanstack/react-db&quot;;

// =============================================================================
// Prefetch Person Entities Hook
// =============================================================================

/**
 * Hook to prefetch all person-related entity collections.
 * Call this after persons are loaded to ensure entity data is available
 * immediately when a user selects a person.
 */
export function usePrefetchPersonEntities() {
  const addressesQuery = useLiveQuery(addressesCollection);
  const bankAccountsQuery = useLiveQuery(bankAccountsCollection);
  const contactsQuery = useLiveQuery(contactsCollection);
  const employmentsQuery = useLiveQuery(employmentsCollection);

  return {
    isLoading:
      addressesQuery.isLoading ||
      bankAccountsQuery.isLoading ||
      contactsQuery.isLoading ||
      employmentsQuery.isLoading,
    isReady:
      !addressesQuery.isLoading &amp;amp;&amp;amp;
      !bankAccountsQuery.isLoading &amp;amp;&amp;amp;
      !contactsQuery.isLoading &amp;amp;&amp;amp;
      !employmentsQuery.isLoading,
  };
}

// =============================================================================
// Persons List Hook
// =============================================================================

/**
 * Hook for accessing and mutating the persons collection.
 * Also prefetches entity data for instant person detail loading.
 */
export function usePersons() {
  const query = useLiveQuery(personsCollection);

  // Prefetch entity data once persons start loading
  const entitiesPrefetch = usePrefetchPersonEntities();

  const addPerson = (person: Person) =&amp;gt; {
    personsCollection.insert(person);
  };

  const updatePerson = (id: string, changes: Partial&amp;lt;Person&amp;gt;) =&amp;gt; {
    personsCollection.update(id, (draft) =&amp;gt; {
      Object.assign(draft, changes, { updatedAt: new Date().toISOString() });
    });
  };

  const deletePerson = (id: string) =&amp;gt; {
    personsCollection.delete(id);
  };

  return {
    persons: query.data ?? [],
    isLoading: query.isLoading,
    isError: query.isError,
    isEntitiesReady: entitiesPrefetch.isReady,
    addPerson,
    updatePerson,
    deletePerson,
  };
}

// =============================================================================
// Combined Person Detail Hook (Using TanStack DB Joins)
// =============================================================================

/**
 * Hook for accessing a person with all related entities using global collections.
 *
 * Benefits over factory-based collections:
 * - All data loaded once at app startup
 * - Navigation between persons is instant (sub-millisecond)
 * - No network requests when switching between person details
 * - Differential dataflow updates only what changed
 */
export function usePersonDetail(personId: string) {
  // Query person by ID using eq() from global collection
  const personQuery = useLiveQuery(
    (query) =&amp;gt;
      query
        .from({ persons: personsCollection })
        .where(({ persons }) =&amp;gt; eq(persons.id, personId)),
    [personId],
  );

  // Query addresses for this person from global collection
  const addressesQuery = useLiveQuery(
    (query) =&amp;gt;
      query
        .from({ addresses: addressesCollection })
        .where(({ addresses }) =&amp;gt; eq(addresses.personId, personId)),
    [personId],
  );

  // Query bank accounts for this person from global collection
  const bankAccountsQuery = useLiveQuery(
    (query) =&amp;gt;
      query
        .from({ bankAccounts: bankAccountsCollection })
        .where(({ bankAccounts }) =&amp;gt; eq(bankAccounts.personId, personId)),
    [personId],
  );

  // Query contacts for this person from global collection
  const contactsQuery = useLiveQuery(
    (query) =&amp;gt;
      query
        .from({ contacts: contactsCollection })
        .where(({ contacts }) =&amp;gt; eq(contacts.personId, personId)),
    [personId],
  );

  // Query employments for this person from global collection
  const employmentsQuery = useLiveQuery(
    (query) =&amp;gt;
      query
        .from({ employments: employmentsCollection })
        .where(({ employments }) =&amp;gt; eq(employments.personId, personId)),
    [personId],
  );

  const isLoading =
    personQuery.isLoading ||
    addressesQuery.isLoading ||
    bankAccountsQuery.isLoading ||
    contactsQuery.isLoading ||
    employmentsQuery.isLoading;

  // Person mutations
  const updatePerson = (changes: Partial&amp;lt;Person&amp;gt;) =&amp;gt; {
    personsCollection.update(personId, (draft) =&amp;gt; {
      Object.assign(draft, changes, { updatedAt: new Date().toISOString() });
    });
  };

  const deletePerson = () =&amp;gt; {
    personsCollection.delete(personId);
  };

  // ... mutation functions for addresses, bank accounts, contacts, employments

  return {
    person: personQuery.data?.[0],
    addresses: addressesQuery.data ?? [],
    bankAccounts: bankAccountsQuery.data ?? [],
    contacts: contactsQuery.data ?? [],
    employments: employmentsQuery.data ?? [],
    isLoading,
    updatePerson,
    deletePerson,
    // ... other mutation functions
  };
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;h2&gt;Improved data retrieval: Global collections&lt;/h2&gt;
&lt;p&gt;The key improvement in this update is the shift from &lt;strong&gt;factory-based per-person collections&lt;/strong&gt; to &lt;strong&gt;global collections&lt;/strong&gt;. This architectural change provides significant performance benefits:&lt;/p&gt;
&lt;h3&gt;Previous approach (Factory Collections)&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;// Created new collection for each personId - multiple network requests
const createAddressesCollection = (personId: string) =&amp;gt;
  createCollection({
    queryKey: [&quot;persons&quot;, personId, &quot;addresses&quot;],
    queryFn: () =&amp;gt; fetchAddresses(personId), // Network request per person
  });
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;New approach (Global Collections)&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;// Single global collection - one network request, client-side filtering
export const addressesCollection = createCollection({
  queryKey: [&quot;addresses&quot;],
  queryFn: () =&amp;gt; fetchAllAddresses(), // Uses GSI1: gsi1pk = &apos;ADDRESSES&apos;
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Performance comparison&lt;/h3&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Metric&lt;/th&gt;
&lt;th&gt;Factory Collections&lt;/th&gt;
&lt;th&gt;Global Collections&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Network requests per navigation&lt;/td&gt;
&lt;td&gt;4-5 (one per entity)&lt;/td&gt;
&lt;td&gt;0 (data already loaded)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Time to show person details&lt;/td&gt;
&lt;td&gt;100-500ms&lt;/td&gt;
&lt;td&gt;&amp;lt;1ms&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Memory efficiency&lt;/td&gt;
&lt;td&gt;Duplicate per person&lt;/td&gt;
&lt;td&gt;Normalized, shared data&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Cache reuse&lt;/td&gt;
&lt;td&gt;None&lt;/td&gt;
&lt;td&gt;Full TanStack Query cache&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h3&gt;When to use which approach&lt;/h3&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Data Size&lt;/th&gt;
&lt;th&gt;Recommended Mode&lt;/th&gt;
&lt;th&gt;Reason&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&amp;lt; 10k rows&lt;/td&gt;
&lt;td&gt;Eager (global)&lt;/td&gt;
&lt;td&gt;Load everything upfront, instant queries&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;10k-50k rows&lt;/td&gt;
&lt;td&gt;Progressive&lt;/td&gt;
&lt;td&gt;Fast first paint, background sync&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&amp;gt; 50k rows&lt;/td&gt;
&lt;td&gt;On-demand&lt;/td&gt;
&lt;td&gt;Query-driven loading with predicate push&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;Our persons example uses &lt;strong&gt;Eager mode&lt;/strong&gt; since typical datasets are well under 10k entities.&lt;/p&gt;
&lt;h2&gt;Seed the database&lt;/h2&gt;
&lt;p&gt;A seed script populates the DynamoDB table with realistic test data using Faker. It creates persons with all related entities, handles batching to avoid throttling, and supports clearing existing data before seeding.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Full implementation&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/2025-12-27-tanstack-start-aws-db-multiple-entities-update/scripts/seed-persons.ts&quot;&gt;seed-persons.ts&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&amp;lt;details&amp;gt;
&amp;lt;summary&amp;gt;Click to expand seed-persons.ts&amp;lt;/summary&amp;gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;#!/usr/bin/env node
// oxlint-disable max-statements
/* oxlint-disable no-console, no-await-in-loop, no-magic-numbers */
/**
 * Seed Script for DB Persons
 *
 * Populates the DynamoDB Persons table with fake data.
 * Uses ElectroDB client and faker data generators.
 *
 * Usage:
 *   pnpm seed:persons
 *   pnpm seed:persons 100     # Seed 100 persons
 *   pnpm seed:persons --clear # Clear existing data first
 */

import type {
  ContactInfo,
  Employment,
  PersonWithRelations,
} from &quot;#src/webapp/types/person.ts&quot;;
import { generatePersons, initFaker } from &quot;#src/webapp/data/fake-persons.ts&quot;;
import {
  createAddress,
  createBankAccount,
  createContact,
  createEmployment,
  createPerson,
  deletePerson,
  getAllPersons,
} from &quot;#src/webapp/integrations/electrodb/personsClient.ts&quot;;

// =============================================================================
// Configuration
// =============================================================================

const DEFAULT_COUNT = 50;
const BATCH_SIZE = 10;
const LOG_INTERVAL = 10;

// =============================================================================
// Helpers
// =============================================================================

const normalizeContactInfo = (
  contact: ContactInfo,
): Omit&amp;lt;ContactInfo, &quot;isVerified&quot;&amp;gt; &amp;amp; { isVerified: boolean } =&amp;gt; ({
  ...contact,
  isVerified: contact.isVerified ?? false,
});

const normalizeEmployment = (
  employment: Employment,
): Omit&amp;lt;Employment, &quot;endDate&quot;&amp;gt; &amp;amp; { endDate: string | undefined } =&amp;gt; ({
  ...employment,
  endDate: employment.endDate ?? undefined,
});

const sleep = (ms: number) =&amp;gt; new Promise((resolve) =&amp;gt; setTimeout(resolve, ms));

// =============================================================================
// Seed Functions
// =============================================================================

const seedOnePerson = async (
  personData: PersonWithRelations,
): Promise&amp;lt;void&amp;gt; =&amp;gt; {
  // Create the person
  await createPerson({
    id: personData.id,
    firstName: personData.firstName,
    lastName: personData.lastName,
    dateOfBirth: personData.dateOfBirth,
    gender: personData.gender,
    createdAt: personData.createdAt,
    updatedAt: personData.updatedAt,
  });

  // Create related entities in parallel
  await Promise.all([
    ...personData.addresses.map((addr) =&amp;gt; createAddress(addr)),
    ...personData.bankAccounts.map((bank) =&amp;gt; createBankAccount(bank)),
    ...personData.contacts.map((contact) =&amp;gt;
      createContact(normalizeContactInfo(contact)),
    ),
    ...personData.employments.map((emp) =&amp;gt;
      createEmployment(normalizeEmployment(emp)),
    ),
  ]);
};

const seedPersonsBatch = async (
  persons: PersonWithRelations[],
): Promise&amp;lt;number&amp;gt; =&amp;gt; {
  let successCount = 0;

  for (const person of persons) {
    try {
      await seedOnePerson(person);
      successCount++;
    } catch (error) {
      console.error(`Failed to seed person ${person.id}:`, error);
    }
  }

  return successCount;
};

const clearAllPersons = async (): Promise&amp;lt;number&amp;gt; =&amp;gt; {
  console.log(&quot;Clearing existing persons...&quot;);

  const existingPersons = await getAllPersons();
  let deletedCount = 0;

  for (const person of existingPersons) {
    try {
      await deletePerson(person.id);
      deletedCount++;
      if (deletedCount % LOG_INTERVAL === 0) {
        console.log(
          `  Deleted ${deletedCount}/${existingPersons.length} persons...`,
        );
      }
    } catch (error) {
      console.error(`Failed to delete person ${person.id}:`, error);
    }
  }

  console.log(`Cleared ${deletedCount} persons`);
  return deletedCount;
};

// =============================================================================
// Main
// =============================================================================

const main = async () =&amp;gt; {
  // Parse arguments
  const args = process.argv.slice(2);
  const shouldClear = args.includes(&quot;--clear&quot;);
  const countArg = args.find((arg) =&amp;gt; !arg.startsWith(&quot;--&quot;));
  let count = DEFAULT_COUNT;
  if (countArg) {
    count = parseInt(countArg, 10);
  }

  console.log(&quot;=&quot;.repeat(60));
  console.log(&quot;DB Persons Seed Script&quot;);
  console.log(&quot;=&quot;.repeat(60));

  // Check for table name
  const tableName = process.env.DDB_PERSONS_TABLE_NAME;
  if (!tableName) {
    console.error(&quot;Error: DDB_PERSONS_TABLE_NAME environment variable not set&quot;);
    console.log(&quot;\nRun: export DDB_PERSONS_TABLE_NAME=&amp;lt;your-table-name&amp;gt;&quot;);
    process.exit(1);
  }
  console.log(`Table: ${tableName}`);

  // Clear if requested
  if (shouldClear) {
    await clearAllPersons();
    console.log(&quot;&quot;);
  }

  // Generate fake data
  console.log(`Generating ${count} persons...`);
  initFaker();
  const persons = generatePersons(count);
  console.log(`Generated ${persons.length} persons with relations`);

  // Seed in batches
  console.log(`\nSeeding in batches of ${BATCH_SIZE}...`);
  let totalSeeded = 0;

  for (let idx = 0; idx &amp;lt; persons.length; idx += BATCH_SIZE) {
    const batch = persons.slice(idx, idx + BATCH_SIZE);
    const seeded = await seedPersonsBatch(batch);
    totalSeeded += seeded;

    console.log(`  Seeded ${totalSeeded}/${persons.length} persons...`);

    // Small delay to avoid throttling
    await sleep(100);
  }

  console.log(&quot;&quot;);
  console.log(&quot;=&quot;.repeat(60));
  console.log(`Seed Complete: ${totalSeeded} persons created`);
  console.log(&quot;=&quot;.repeat(60));
};

main().catch((error) =&amp;gt; {
  console.error(&quot;Seed script failed:&quot;, error);
  process.exit(1);
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&amp;lt;/details&amp;gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./result-ddb.png&quot; alt=&quot;result ddb&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Result&lt;/h2&gt;
&lt;p&gt;The implementation demonstrates a multi-entity data model with DynamoDB&apos;s single table design. The DynamoDB table shows the hierarchical key structure with person profiles and related entities stored efficiently. The UI displays the person list with all associated data managed through TanStack DB collections.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./result-page.png&quot; alt=&quot;result page&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Conclusion&lt;/h2&gt;
&lt;p&gt;Because ElectroDB handles the entities and single table design, it&apos;s more or less a direct mapping from the ElectroDB client to TanStack DB collections.
At least for the described data model and access patterns.&lt;/p&gt;
&lt;p&gt;The update to global collections demonstrates how TanStack DB&apos;s architecture enables significant performance improvements with minimal code changes. By loading all entities once at startup and filtering client-side, navigation between person details becomes instant—no network requests needed.&lt;/p&gt;
&lt;p&gt;At this size of around 10-50 persons, this approach works well. With much more data (&amp;gt;10k rows), you may need to consider progressive or on-demand loading strategies. TanStack DB&apos;s query-driven sync patterns support these scenarios as your data grows.&lt;/p&gt;
&lt;h2&gt;Sources and References&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Examples (full implementation on GitHub)&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws&quot;&gt;github.com/JohannesKonings/tanstack-aws&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Original tag&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/releases/tag/2025-12-27-tanstack-start-aws-db-multiple-entities&quot;&gt;2025-12-27-tanstack-start-aws-db-multiple-entities&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Updated tag (global collections)&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/releases/tag/2025-12-27-tanstack-start-aws-db-multiple-entities-update&quot;&gt;2025-12-27-tanstack-start-aws-db-multiple-entities-update&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;TanStack DB documentation&lt;/strong&gt;: &lt;a href=&quot;https://tanstack.com/db/latest&quot;&gt;tanstack.com/db/latest&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;TanStack DB 0.5 Query-Driven Sync&lt;/strong&gt;: &lt;a href=&quot;https://tanstack.com/blog/tanstack-db-0.5-query-driven-sync&quot;&gt;tanstack.com/blog/tanstack-db-0.5-query-driven-sync&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;ElectroDB documentation&lt;/strong&gt;: &lt;a href=&quot;https://electrodb.dev/&quot;&gt;electrodb.dev&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>Simple example of TanStack DB with DynamoDB on AWS</title><link>https://johanneskonings.dev/blog/2025-12-20-tanstack-start-aws-db-simple/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2025-12-20-tanstack-start-aws-db-simple/</guid><description>Deploy a simple TanStack DB + DynamoDB setup on AWS using CDK, with a TanStack Start API route and a query collection powering a small todo UI.</description><pubDate>Sat, 20 Dec 2025 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Introduction&lt;/h2&gt;
&lt;p&gt;In &lt;a href=&quot;/blog/2025-11-30-tanstack-start-aws-serverless/&quot;&gt;Deploying TanStack Start on AWS with Lambda Function URLs&lt;/a&gt;, I describe how to deploy TanStack Start serverless on AWS.
This post contains a simple example of using TanStack DB (https://tanstack.com/db/latest) with DynamoDB on AWS.&lt;/p&gt;
&lt;p&gt;At a high level, we’ll go from infrastructure (a DynamoDB table), to a small DynamoDB client, to a TanStack Start server route, and finally to a TanStack DB collection that powers a simple UI.&lt;/p&gt;
&lt;p&gt;The complete implementation is available in the &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws&quot;&gt;tanstack-aws repository&lt;/a&gt;, which serves as a working example and template for this deployment pattern.&lt;/p&gt;
&lt;h2&gt;Disclaimer&lt;/h2&gt;
&lt;p&gt;This is a very simple example to get you started with TanStack DB and DynamoDB on AWS. It is not production-ready and lacks features like error handling, security, and optimizations.&lt;/p&gt;
&lt;h2&gt;TanStack DB&lt;/h2&gt;
&lt;p&gt;This (https://frontendatscale.com/blog/tanstack-db/) blog post explains the concepts of TanStack DB very well.
In short, TanStack DB is a client-first database that runs in the browser or in a server environment. It provides a simple API to store, query, and sync data.
TanStack DB is based on collections, with existing sync engines like ElectricSQL (https://tanstack.com/db/latest/docs/collections/electric-collection).&lt;/p&gt;
&lt;p&gt;This post uses the Query Collection to wire it via an API to DynamoDB (https://tanstack.com/db/latest/docs/collections/query-collection).&lt;/p&gt;
&lt;p&gt;The idea is straightforward: TanStack DB remains the place where your app reads and writes “records” (in this case, todos), while the collection’s fetch and mutation hooks delegate persistence to your own backend API.&lt;/p&gt;
&lt;h2&gt;DynamoDB in CDK&lt;/h2&gt;
&lt;p&gt;This is a very simple configuration, just to store the todos.&lt;/p&gt;
&lt;p&gt;To keep the example focused, the table uses a generic partition key and sort key (&lt;code&gt;pk&lt;/code&gt;/&lt;code&gt;sk&lt;/code&gt;). That makes it easy to evolve into a single-table design later, while keeping the code here minimal.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { AttributeType, BillingMode, Table } from &quot;aws-cdk-lib/aws-dynamodb&quot;;
import { Construct } from &quot;constructs&quot;;

export class DatabaseTodos extends Construct {
  public readonly dbTodos: Table;
  constructor(scope: Construct, id: string) {
    super(scope, id);

    this.dbTodos = new Table(this, &quot;Todos&quot;, {
      partitionKey: { name: &quot;pk&quot;, type: AttributeType.STRING },
      sortKey: { name: &quot;sk&quot;, type: AttributeType.STRING },
      billingMode: BillingMode.PAY_PER_REQUEST,
    });
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;https://github.com/JohannesKonings/tanstack-aws/blob/main/lib/constructs/DatabaseTodos.ts&lt;/p&gt;
&lt;p&gt;This table needs to be wired to the web app server Lambda.&lt;/p&gt;
&lt;p&gt;In the web app construct, the table construct is added.
The Lambda is allowed to read and write data to the table.&lt;/p&gt;
&lt;p&gt;Concretely, that means (1) creating the table, (2) passing the table name into the server function, and (3) granting the function read/write permissions.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const databaseTodos = new DatabaseTodos(this, &quot;DatabaseTodos&quot;);

const webappServer = new WebappServer(this, &quot;WebappServer&quot;, {
  tableName: databaseTodos.dbTodos.tableName,
});

databaseTodos.dbTodos.grantReadWriteData(webappServer.webappServer);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;https://github.com/JohannesKonings/tanstack-aws/blob/main/lib/constructs/Webapp.ts&lt;/p&gt;
&lt;p&gt;The passed table name is exposed as an environment variable in the Lambda function.&lt;/p&gt;
&lt;p&gt;This keeps the application code decoupled from CDK: the Lambda only needs to know an env var, and CDK is responsible for wiring that value at deploy time.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;this.webappServer = new Function(this, &quot;WebappServer&quot;, {
  code: Code.fromAsset(
    path.join(
      path.dirname(new URL(import.meta.url).pathname),
      &quot;../../.output/server&quot;,
    ),
  ),
  // functionName: PhysicalName.GENERATE_IF_NEEDED,
  handler: &quot;index.handler&quot;,
  memorySize: 2048,
  runtime: Runtime.NODEJS_24_X,
  // oxlint-disable-next-line no-magic-numbers
  timeout: Duration.seconds(60),
  environment: {
    DDB_TODOS_TABLE_NAME: tableName,
  },
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;https://github.com/JohannesKonings/tanstack-aws/blob/main/lib/constructs/WebappServer.ts&lt;/p&gt;
&lt;h2&gt;A DynamoDB Client&lt;/h2&gt;
&lt;p&gt;In this example, a simple wrapper around the DynamoDB client is used. In larger projects, something like https://electrodb.dev/ can help.
This is the bridge from the API behind TanStack DB to DynamoDB.&lt;/p&gt;
&lt;p&gt;The client below implements the four operations the demo needs: list todos, create a todo, update one or more todos, and delete todos in batches.&lt;/p&gt;
&lt;p&gt;Here is the environment variable used for the table name.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { DynamoDBClient } from &quot;@aws-sdk/client-dynamodb&quot;;
import {
  BatchWriteCommand,
  DynamoDBDocumentClient,
  PutCommand,
  QueryCommand,
  UpdateCommand,
} from &quot;@aws-sdk/lib-dynamodb&quot;;
import { type Todo, todoSchema, type TodoUpdate } from &quot;@/webapp/types/todo&quot;;

const TODOS_PK = &quot;TODO&quot;;
const TODOS_TABLE_ENV = &quot;DDB_TODOS_TABLE_NAME&quot;;
const EMPTY_LENGTH = 0;
const INITIAL_ATTEMPT = 0;
const NEXT_ATTEMPT_INCREMENT = 1;
const MAX_RETRY_ATTEMPTS = 3;
const MAX_BATCH_WRITE_ITEMS = 25;

let ddbDocSingleton: DynamoDBDocumentClient | null = null;

export const getDdbDocClient = (): DynamoDBDocumentClient =&amp;gt; {
  if (!ddbDocSingleton) {
    ddbDocSingleton = DynamoDBDocumentClient.from(new DynamoDBClient({}), {
      marshallOptions: {
        removeUndefinedValues: true,
      },
    });
  }

  return ddbDocSingleton;
};

export const requireEnvVar = (name: string): string =&amp;gt; {
  const value = process.env[name];
  if (!value) {
    throw new Error(`Missing env var: ${name}`);
  }

  return value;
};

export const getTodosTableName = (): string =&amp;gt; requireEnvVar(TODOS_TABLE_ENV);

const todoSortKey = (id: number): string =&amp;gt; `TODO#${id}`;

const parseTodoItem = (item: Record&amp;lt;string, unknown&amp;gt;): Todo | null =&amp;gt; {
  const parsed = todoSchema.safeParse({
    id: item.id,
    name: item.name,
    status: item.status,
  });

  if (!parsed.success) {
    return null;
  }

  return parsed.data;
};

const chunkItems = &amp;lt;Item&amp;gt;(items: Item[], chunkSize: number): Item[][] =&amp;gt; {
  const out: Item[][] = [];
  for (let index = EMPTY_LENGTH; index &amp;lt; items.length; index += chunkSize) {
    out.push(items.slice(index, index + chunkSize));
  }
  return out;
};

export type TodosDdbClient = {
  getTodos: () =&amp;gt; Promise&amp;lt;Todo[]&amp;gt;;
  putTodo: (todo: Todo) =&amp;gt; Promise&amp;lt;Todo&amp;gt;;
  updateTodos: (updates: TodoUpdate[]) =&amp;gt; Promise&amp;lt;void&amp;gt;;
  deleteTodos: (ids: number[]) =&amp;gt; Promise&amp;lt;void&amp;gt;;
};

const retryBatchWrite = async (args: {
  ddbDoc: DynamoDBDocumentClient;
  requestItems: unknown;
  attempt: number;
}): Promise&amp;lt;void&amp;gt; =&amp;gt; {
  const response = await args.ddbDoc.send(
    new BatchWriteCommand({
      RequestItems: args.requestItems as never,
    }),
  );

  const unprocessedItems = response.UnprocessedItems;
  if (
    !unprocessedItems ||
    Object.keys(unprocessedItems).length === EMPTY_LENGTH
  ) {
    return;
  }

  if (args.attempt + NEXT_ATTEMPT_INCREMENT &amp;gt;= MAX_RETRY_ATTEMPTS) {
    return;
  }

  await retryBatchWrite({
    ddbDoc: args.ddbDoc,
    requestItems: unprocessedItems,
    attempt: args.attempt + NEXT_ATTEMPT_INCREMENT,
  });
};

export const createTodosDdbClient = (): TodosDdbClient =&amp;gt; {
  const ddbDoc = getDdbDocClient();

  return {
    getTodos: async () =&amp;gt; {
      const tableName = getTodosTableName();
      const result = await ddbDoc.send(
        new QueryCommand({
          TableName: tableName,
          KeyConditionExpression: &quot;#pk = :pk&quot;,
          ExpressionAttributeNames: {
            &quot;#pk&quot;: &quot;pk&quot;,
          },
          ExpressionAttributeValues: {
            &quot;:pk&quot;: TODOS_PK,
          },
        }),
      );

      const items = (result.Items ?? [])
        .map((item) =&amp;gt; parseTodoItem(item))
        .filter((todo): todo is Todo =&amp;gt; todo !== null);
      return items;
    },

    putTodo: async (todo: Todo) =&amp;gt; {
      const tableName = getTodosTableName();
      await ddbDoc.send(
        new PutCommand({
          TableName: tableName,
          Item: {
            pk: TODOS_PK,
            sk: todoSortKey(todo.id),
            id: todo.id,
            name: todo.name,
            status: todo.status,
          },
        }),
      );

      return todo;
    },

    updateTodos: async (updates: TodoUpdate[]) =&amp;gt; {
      const tableName = getTodosTableName();

      await Promise.all(
        updates.map(async (update) =&amp;gt; {
          const sets: string[] = [];
          const names: Record&amp;lt;string, string&amp;gt; = {
            &quot;#pk&quot;: &quot;pk&quot;,
            &quot;#sk&quot;: &quot;sk&quot;,
          };
          const values: Record&amp;lt;string, unknown&amp;gt; = {};

          if (update.changes.name !== undefined) {
            names[&quot;#name&quot;] = &quot;name&quot;;
            values[&quot;:name&quot;] = update.changes.name;
            sets.push(&quot;#name = :name&quot;);
          }

          if (update.changes.status !== undefined) {
            names[&quot;#status&quot;] = &quot;status&quot;;
            values[&quot;:status&quot;] = update.changes.status;
            sets.push(&quot;#status = :status&quot;);
          }

          if (sets.length === EMPTY_LENGTH) {
            return;
          }

          try {
            await ddbDoc.send(
              new UpdateCommand({
                TableName: tableName,
                Key: {
                  pk: TODOS_PK,
                  sk: todoSortKey(update.id),
                },
                UpdateExpression: `SET ${sets.join(&quot;, &quot;)}`,
                ConditionExpression:
                  &quot;attribute_exists(#pk) AND attribute_exists(#sk)&quot;,
                ExpressionAttributeNames: names,
                ExpressionAttributeValues: values,
              }),
            );
          } catch (error) {
            const errorName = (error as { name?: string } | undefined)?.name;
            if (errorName !== &quot;ConditionalCheckFailedException&quot;) {
              throw error;
            }
          }
        }),
      );
    },

    deleteTodos: async (ids: number[]) =&amp;gt; {
      const tableName = getTodosTableName();
      const groups = chunkItems(ids, MAX_BATCH_WRITE_ITEMS);

      await Promise.all(
        groups.map(async (group) =&amp;gt; {
          const requestItems = {
            [tableName]: group.map((id) =&amp;gt; ({
              DeleteRequest: {
                Key: {
                  pk: TODOS_PK,
                  sk: todoSortKey(id),
                },
              },
            })),
          };

          await retryBatchWrite({
            ddbDoc,
            requestItems,
            attempt: INITIAL_ATTEMPT,
          });
        }),
      );
    },
  };
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;https://github.com/JohannesKonings/tanstack-aws/blob/main/src/webapp/integrations/ddb-client/ddbClient.ts&lt;/p&gt;
&lt;h2&gt;The API connection to DynamoDB&lt;/h2&gt;
&lt;p&gt;With the DynamoDB client created above, you can create an API that connects TanStack DB to DynamoDB.&lt;/p&gt;
&lt;p&gt;In TanStack Start, this is a server route with standard HTTP verbs. The route validates input, calls the DynamoDB client, and returns JSON responses.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// oxlint-disable func-style
import { createFileRoute } from &quot;@tanstack/react-router&quot;;
import { createTodosDdbClient } from &quot;@/webapp/integrations/ddb-client/ddbClient&quot;;
import {
  createTodoRequestSchema,
  deleteTodosRequestSchema,
  updateTodosRequestSchema,
} from &quot;@/webapp/types/todo&quot;;

const todosClient = createTodosDdbClient();

export const Route = createFileRoute(&quot;/demo/api/ddb-todos&quot;)({
  server: {
    handlers: {
      // oxlint-disable-next-line arrow-body-style
      GET: async () =&amp;gt; {
        const items = await todosClient.getTodos();
        return Response.json(items);
      },
      POST: async ({ request }) =&amp;gt; {
        const requestJson = await request.json();
        const todoParsed = createTodoRequestSchema.parse(requestJson);

        const saved = await todosClient.putTodo(todoParsed);
        return Response.json(saved);
      },
      PUT: async ({ request }) =&amp;gt; {
        const requestJson = await request.json();
        const updates = updateTodosRequestSchema.parse(requestJson);

        await todosClient.updateTodos(updates);

        return Response.json({ ok: true });
      },
      DELETE: async ({ request }) =&amp;gt; {
        const requestJson = await request.json();
        const ids = deleteTodosRequestSchema.parse(requestJson);

        await todosClient.deleteTodos(ids);

        return Response.json({ ok: true });
      },
    },
  },
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;https://github.com/JohannesKonings/tanstack-aws/blob/main/src/webapp/routes/demo/api.ddb-todos.ts&lt;/p&gt;
&lt;h2&gt;The TanStack DB collection&lt;/h2&gt;
&lt;p&gt;The collection uses the API to fetch and mutate data in DynamoDB.&lt;/p&gt;
&lt;p&gt;This is the key integration point: the collection fetches via the &lt;code&gt;queryFn&lt;/code&gt;, and the transaction callbacks (&lt;code&gt;onInsert&lt;/code&gt;, &lt;code&gt;onUpdate&lt;/code&gt;, &lt;code&gt;onDelete&lt;/code&gt;) translate local writes into API calls.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { queryCollectionOptions } from &quot;@tanstack/query-db-collection&quot;;
import { createCollection } from &quot;@tanstack/react-db&quot;;
import { getContext } from &quot;@/webapp/integrations/tanstack-query/root-provider&quot;;
import { type Todo, todoSchema } from &quot;../types/todo&quot;;

// const todoApiPath = &apos;/demo/api/tq-todos&apos;;
const todoApiPath = &quot;/demo/api/ddb-todos&quot;;
const api = {
  async fetchTodos(): Promise&amp;lt;Todo[]&amp;gt; {
    const response = await fetch(todoApiPath);
    if (!response.ok) {
      throw new Error(&quot;Failed to fetch todos&quot;);
    }
    const data = await response.json();
    return todoSchema.array().parse(data);
  },

  async createTodos(todo: Omit&amp;lt;Todo, &quot;id&quot;&amp;gt;) {
    await fetch(todoApiPath, {
      method: &quot;POST&quot;,
      headers: {
        &quot;Content-Type&quot;: &quot;application/json&quot;,
      },
      body: JSON.stringify(todo),
    });
  },

  async updateTodos(
    updates: { id: number; changes: Partial&amp;lt;Omit&amp;lt;Todo, &quot;id&quot;&amp;gt;&amp;gt; }[],
  ) {
    await fetch(todoApiPath, {
      method: &quot;PUT&quot;,
      headers: {
        &quot;Content-Type&quot;: &quot;application/json&quot;,
      },
      body: JSON.stringify(updates),
    });
  },

  async deleteTodos(ids: number[]) {
    await fetch(todoApiPath, {
      method: &quot;DELETE&quot;,
      headers: {
        &quot;Content-Type&quot;: &quot;application/json&quot;,
      },
      body: JSON.stringify(ids),
    });
  },
};

export const todosCollection = createCollection(
  queryCollectionOptions&amp;lt;Todo&amp;gt;({
    queryKey: [&quot;todos&quot;],
    queryFn: () =&amp;gt; api.fetchTodos(),
    queryClient: getContext().queryClient,
    getKey: (item) =&amp;gt; item.id,
    onInsert: async ({ transaction }) =&amp;gt; {
      const newItems = transaction.mutations.map((mutation) =&amp;gt; ({
        id: Math.random(), // Temporary ID; real ID should be assigned by the server
        name: mutation.modified.name,
        status: mutation.modified.status,
      }));
      for (const item of newItems) {
        api.createTodos(item);
      }
    },
    onUpdate: async ({ transaction }) =&amp;gt; {
      const updates = transaction.mutations.map((mutation) =&amp;gt; ({
        id: mutation.key,
        changes: mutation.changes,
      }));
      await api.updateTodos(updates);
    },
    onDelete: async ({ transaction }) =&amp;gt; {
      const ids = transaction.mutations.map((mutation) =&amp;gt; mutation.key);
      await api.deleteTodos(ids);
    },
  }),
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;https://github.com/JohannesKonings/tanstack-aws/blob/main/src/webapp/db-collections/todos.ts&lt;/p&gt;
&lt;h2&gt;The hook&lt;/h2&gt;
&lt;p&gt;These hooks provide a small, UI-friendly API on top of the collection. Components can call &lt;code&gt;addTodo&lt;/code&gt;/&lt;code&gt;toggleTodoStatus&lt;/code&gt;/&lt;code&gt;deleteTodo&lt;/code&gt;, and &lt;code&gt;useTodos&lt;/code&gt; subscribes to the live query for rendering.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { useLiveQuery } from &quot;@tanstack/react-db&quot;;
// oxlint-disable func-style
import { todosCollection } from &quot;@/webapp/db-collections/todos&quot;;
import type { Todo } from &quot;../types/todo&quot;;

export function useTodo() {
  const addTodo = async ({ name, status }: Omit&amp;lt;Todo, &quot;id&quot;&amp;gt;) =&amp;gt; {
    // oxlint-disable-next-line no-magic-numbers
    const randomId = Math.floor(Math.random() * 1000000);
    todosCollection.insert({ id: randomId, name, status });
  };

  const toggleTodoStatus = (id: number, status: &quot;pending&quot; | &quot;completed&quot;) =&amp;gt; {
    todosCollection.update(id, (draft) =&amp;gt; {
      if (draft) {
        draft.status = status;
      }
    });
  };

  const deleteTodo = (id: number) =&amp;gt; {
    todosCollection.delete(id);
  };

  return { addTodo, toggleTodoStatus, deleteTodo };
}

export function useTodos() {
  const { data: todos } = useLiveQuery((todoQuery) =&amp;gt;
    todoQuery.from({ todo: todosCollection }).select(({ todo }) =&amp;gt; ({
      ...todo,
    })),
  );

  return todos as Todo[];
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;https://github.com/JohannesKonings/tanstack-aws/blob/main/src/webapp/hooks/useDbTodos.ts&lt;/p&gt;
&lt;h2&gt;The component&lt;/h2&gt;
&lt;p&gt;Finally, the UI component is just a thin layer over the hooks: render the list, submit a new todo, toggle completion, and delete items.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// oxlint-disable func-style
import { createFileRoute } from &quot;@tanstack/react-router&quot;;
import { Trash2 } from &quot;lucide-react&quot;;
import { useState } from &quot;react&quot;;
import { useTodo, useTodos } from &quot;@/webapp/hooks/useDbTodos&quot;;

export const Route = createFileRoute(&quot;/demo/db-todo&quot;)({
  ssr: false,
  component: DbTodos,
});

function DbTodos() {
  const todos = useTodos();
  const { addTodo, toggleTodoStatus, deleteTodo } = useTodo();

  const [todo, setTodo] = useState&amp;lt;string&amp;gt;(&quot;&quot;);

  const submitTodo = () =&amp;gt; {
    if (todo.trim() !== &quot;&quot;) {
      addTodo({ name: todo, status: &quot;pending&quot; });
      setTodo(&quot;&quot;);
    }
  };

  const handleTodoStatusToggle = (todoItem: (typeof todos)[number]) =&amp;gt; {
    if (todoItem.status === &quot;completed&quot;) {
      toggleTodoStatus(todoItem.id, &quot;pending&quot;);
    } else {
      toggleTodoStatus(todoItem.id, &quot;completed&quot;);
    }
  };

  return (
    &amp;lt;div
      className=&quot;flex items-center justify-center min-h-screen bg-linear-to-br from-purple-100 to-blue-100 p-4 text-white&quot;
      style={{
        backgroundImage:
          &quot;radial-gradient(50% 50% at 95% 5%, #4a90c2 0%, #317eb9 50%, #1e4d72 100%)&quot;,
      }}
    &amp;gt;
      &amp;lt;div className=&quot;w-full max-w-2xl p-8 rounded-xl backdrop-blur-md bg-black/50 shadow-xl border-8 border-black/10&quot;&amp;gt;
        &amp;lt;h1 className=&quot;text-2xl mb-4&quot;&amp;gt;DB Todo list&amp;lt;/h1&amp;gt;
        &amp;lt;ul className=&quot;mb-4 space-y-2&quot;&amp;gt;
          {todos?.map((todoItem) =&amp;gt; {
            const isCompleted = todoItem.status === &quot;completed&quot;;
            let textClasses = &quot;&quot;;
            if (isCompleted) {
              textClasses = &quot;line-through opacity-60&quot;;
            }
            return (
              &amp;lt;li
                key={todoItem.id}
                className=&quot;bg-white/10 border border-white/20 rounded-lg p-3 backdrop-blur-sm shadow-md flex items-center gap-3&quot;
              &amp;gt;
                &amp;lt;input
                  type=&quot;checkbox&quot;
                  checked={isCompleted}
                  onChange={() =&amp;gt; handleTodoStatusToggle(todoItem)}
                  className=&quot;w-5 h-5 cursor-pointer accent-blue-400&quot;
                /&amp;gt;
                &amp;lt;span className={`text-lg text-white flex-1 ${textClasses}`}&amp;gt;
                  {todoItem.name}
                &amp;lt;/span&amp;gt;
                &amp;lt;button
                  type=&quot;button&quot;
                  onClick={() =&amp;gt; deleteTodo(todoItem.id)}
                  className=&quot;text-white/80 hover:text-red-300 p-2 rounded-full hover:bg-white/10 transition-colors&quot;
                  aria-label={`Delete todo ${todoItem.name}`}
                &amp;gt;
                  &amp;lt;Trash2 className=&quot;w-5 h-5&quot; aria-hidden=&quot;true&quot; /&amp;gt;
                &amp;lt;/button&amp;gt;
              &amp;lt;/li&amp;gt;
            );
          })}
        &amp;lt;/ul&amp;gt;
        &amp;lt;div className=&quot;flex flex-col gap-2&quot;&amp;gt;
          &amp;lt;input
            type=&quot;text&quot;
            value={todo}
            onChange={(event) =&amp;gt; setTodo(event.target.value)}
            onKeyDown={(event) =&amp;gt; {
              if (event.key === &quot;Enter&quot;) {
                submitTodo();
              }
            }}
            placeholder=&quot;Enter a new todo...&quot;
            className=&quot;w-full px-4 py-3 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:border-transparent&quot;
          /&amp;gt;
          &amp;lt;button
            // oxlint-disable-next-line no-magic-numbers
            disabled={todo.trim().length === 0}
            onClick={submitTodo}
            className=&quot;bg-blue-500 hover:bg-blue-600 disabled:bg-blue-500/50 disabled:cursor-not-allowed text-white font-bold py-3 px-4 rounded-lg transition-colors&quot;
          &amp;gt;
            Add todo
          &amp;lt;/button&amp;gt;
        &amp;lt;/div&amp;gt;
      &amp;lt;/div&amp;gt;
    &amp;lt;/div&amp;gt;
  );
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;https://github.com/JohannesKonings/tanstack-aws/blob/main/src/webapp/routes/demo/db-todo.tsx&lt;/p&gt;
&lt;h2&gt;Result&lt;/h2&gt;
&lt;p&gt;&lt;img src=&quot;./result.png&quot; alt=&quot;result&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Conclusion&lt;/h2&gt;
&lt;p&gt;This is a simple example and implementation of TanStack DB with DynamoDB on AWS.
In a more complete example, you’d need to figure out how to map a single-table design in DynamoDB to multiple collections in TanStack DB.&lt;/p&gt;
&lt;p&gt;From here, the next natural steps are adding authentication/authorization on the API, better id generation (server-assigned ids), and a more explicit data model to support multiple entity types.&lt;/p&gt;
&lt;h2&gt;Sources and References&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Examples (full implementation on GitHub)&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws&quot;&gt;github.com/JohannesKonings/tanstack-aws&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;TanStack DB documentation&lt;/strong&gt;: &lt;a href=&quot;https://tanstack.com/db/latest&quot;&gt;tanstack.com/db/latest&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>Monitor multiple resources using a single CloudWatch Alarm (with CDK)</title><link>https://johanneskonings.dev/blog/2025-12-13-cdk-monitor-multiple-resources-using-single-alarm/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2025-12-13-cdk-monitor-multiple-resources-using-single-alarm/</guid><description>Use CloudWatch Metrics Insights query alarms (with tags) to monitor many resources with one alarm in AWS CDK.</description><pubDate>Sat, 13 Dec 2025 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Introduction&lt;/h2&gt;
&lt;p&gt;CloudWatch &lt;strong&gt;Metrics Insights query alarms&lt;/strong&gt; (aka “multi-metric alarms”) let one alarm evaluate many &lt;em&gt;individual&lt;/em&gt; resources.
You write a Metrics Insights SQL query (with &lt;code&gt;GROUP BY&lt;/code&gt;), and CloudWatch keeps the alarm’s contributor set up to date as resources are created/deleted.&lt;/p&gt;
&lt;p&gt;Before this announcement, you had two imperfect choices:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Create one alarm per resource (granular, but high maintenance as resources change).&lt;/li&gt;
&lt;li&gt;Use aggregated metrics (low maintenance, but you lose per-resource visibility and actions).&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The September 2025 feature (https://aws.amazon.com/about-aws/whats-new/2025/09/amazon-cloudwatch-alarm-multiple-metrics/) adds query alarms that evaluate many individual metrics with one alarm, preserving per-resource visibility and actions while removing the per-resource alarm sprawl.&lt;/p&gt;
&lt;p&gt;Pubudu Jayawardana already described how to use it here https://pubudu.dev/posts/cloudwatch-multi-metric-alarms/, this post focuses on how to implement the pattern in AWS CDK with concise, working examples (SQS DLQs with tag-based queries, and SFN across all state machines).&lt;/p&gt;
&lt;h1&gt;Account Configuration&lt;/h1&gt;
&lt;p&gt;If you want &lt;code&gt;WHERE tag.X = &apos;Y&apos;&lt;/code&gt; filters in Metrics Insights queries, enable &lt;em&gt;resource tags on telemetry&lt;/em&gt;:
https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/EnableResourceTagsOnTelemetry.html&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./cloudwatch-settings-tags.png&quot; alt=&quot;cloudwatch settings tags&quot; /&gt;&lt;/p&gt;
&lt;p&gt;As of 2025-12, this setting isn’t exposed as a CloudFormation resource. CloudWatch may take up to a few hours to fully enrich all metrics after enabling.&lt;/p&gt;
&lt;p&gt;CLI equivalent:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;aws observabilityadmin start-telemetry-enrichment
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If you want this in CDK, use a Lambda-backed custom resource that calls the same API:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// https://docs.aws.amazon.com/AWSJavaScriptSDK/v3/latest/client/observabilityadmin/command/StartTelemetryEnrichmentCommand/
// https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/EnableResourceTagsOnTelemetry.html
new AwsCustomResource(this, &quot;StartTelemetryEnrichment&quot;, {
  onCreate: {
    service: &quot;@aws-sdk/client-observabilityadmin&quot;,
    action: &quot;StartTelemetryEnrichment&quot;,
    physicalResourceId: PhysicalResourceId.of(&quot;StartTelemetryEnrichment&quot;),
  },
  installLatestAwsSdk: true,
  // policy: AwsCustomResourcePolicy.fromSdkCalls({ resources: AwsCustomResourcePolicy.ANY_RESOURCE }),
  policy: AwsCustomResourcePolicy.fromStatements([
    new PolicyStatement({
      effect: Effect.ALLOW,
      actions: [
        &quot;observabilityadmin:StartTelemetryEnrichment&quot;,
        &quot;iam:CreateServiceLinkedRole&quot;,
        &quot;resource-explorer-2:CreateIndex&quot;,
        &quot;resource-explorer-2:CreateManagedView&quot;,
        &quot;resource-explorer-2:CreateStreamingAccessForService&quot;,
      ],
      resources: [&quot;*&quot;],
    }),
  ]),
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&quot;./cloudwatch-settings-tags-activated.png&quot; alt=&quot;cloudwatch settings tags activated&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Example 1: one alarm for all DLQs (tag filtered)&lt;/h2&gt;
&lt;p&gt;Tag only the DLQs you want included (opt-in):&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const dlq1 = new Queue(this, &quot;DLQ1&quot;);
Tags.of(dlq1).add(&quot;isDlq&quot;, &quot;true&quot;);
Tags.of(dlq1).add(&quot;monitor&quot;, &quot;true&quot;);
new Queue(this, &quot;Queue1&quot;, {
  deadLetterQueue: {
    queue: dlq1,
    maxReceiveCount: 3,
  },
});

const dlq2 = new Queue(this, &quot;DLQ2&quot;);
Tags.of(dlq2).add(&quot;isDlq&quot;, &quot;true&quot;);
Tags.of(dlq2).add(&quot;monitor&quot;, &quot;true&quot;);
new Queue(this, &quot;Queue2&quot;, {
  deadLetterQueue: {
    queue: dlq2,
    maxReceiveCount: 5,
  },
});

const dlq3 = new Queue(this, &quot;DLQ3&quot;);
Tags.of(dlq3).add(&quot;isDlq&quot;, &quot;true&quot;);
Tags.of(dlq3).add(&quot;monitor&quot;, &quot;false&quot;);
new Queue(this, &quot;Queue3&quot;, {
  deadLetterQueue: {
    queue: dlq3,
    maxReceiveCount: 5,
  },
});
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code&gt;const expressionDlq = new MathExpression({
  expression: `SELECT MAX(ApproximateNumberOfMessagesVisible)
            FROM SCHEMA(&quot;AWS/SQS&quot;, QueueName)
            WHERE tag.monitor = &apos;true&apos;
            AND tag.isDlq = &apos;true&apos;
            GROUP BY QueueName
            ORDER BY COUNT() DESC`,
  usingMetrics: {},
  period: Duration.minutes(1),
  label: &quot;DLQ messages&quot;,
});

new Alarm(this, &quot;DlqAlarm&quot;, {
  metric: expressionDlq,
  threshold: 1,
  evaluationPeriods: 1,
  comparisonOperator: ComparisonOperator.GREATER_THAN_OR_EQUAL_TO_THRESHOLD,
  alarmDescription: &quot;Alarm if any tagged DLQ has visible messages&quot;,
  alarmName: &quot;DLQ-Alarm&quot;,
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&quot;./dlq-alarm.png&quot; alt=&quot;cloudwatch dlq alarm&quot; /&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./dlq-ok.png&quot; alt=&quot;cloudwatch dlq ok&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Example 2: one alarm for failed Step Functions executions&lt;/h2&gt;
&lt;p&gt;Tag filtering only works for resource types supported by &lt;em&gt;resource tags for telemetry&lt;/em&gt; (SQS is supported; Step Functions isn’t listed):
https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/UsingResourceTagsForTelemetry.html#SupportedAWSInfrastructureMetrics&lt;/p&gt;
&lt;p&gt;So for Step Functions, this alarms across all state machines in the account/region.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const sfn1 = new StateMachine(this, &quot;StateMachine1&quot;, {
  definitionBody: DefinitionBody.fromChainable(new Fail(this, &quot;FailState1&quot;)),
});

const sfn2 = new StateMachine(this, &quot;StateMachine2&quot;, {
  definitionBody: DefinitionBody.fromChainable(new Fail(this, &quot;FailState2&quot;)),
});
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code&gt;const expressionSfn = new MathExpression({
  expression: `SELECT MAX(ExecutionsFailed)
            FROM SCHEMA(&quot;AWS/States&quot;, StateMachineName)
            GROUP BY StateMachineName
            ORDER BY COUNT() DESC`,
  usingMetrics: {},
  period: Duration.minutes(1),
  label: &quot;Sfn failed executions&quot;,
});

new Alarm(this, &quot;SfnAlarm&quot;, {
  metric: expressionSfn,
  threshold: 1,
  evaluationPeriods: 1,
  comparisonOperator: ComparisonOperator.GREATER_THAN_OR_EQUAL_TO_THRESHOLD,
  alarmDescription: &quot;Alarm if any state machine has failed executions&quot;,
  alarmName: &quot;SFN-Alarm&quot;,
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&quot;./sfn-alarm.png&quot; alt=&quot;cloudwatch sfn alarm&quot; /&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./sfn-ok.png&quot; alt=&quot;cloudwatch sfn ok&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Sources&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Feature announcement: https://aws.amazon.com/about-aws/whats-new/2025/09/amazon-cloudwatch-alarm-multiple-metrics/&lt;/li&gt;
&lt;li&gt;CloudWatch docs (alarms on Metrics Insights queries): https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/cloudwatch-metrics-insights-alarms.html&lt;/li&gt;
&lt;li&gt;CloudWatch docs (create a Metrics Insights alarm): https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/cloudwatch-metrics-insights-alarm-create.html&lt;/li&gt;
&lt;li&gt;CloudWatch docs (enable resource tags on telemetry): https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/EnableResourceTagsOnTelemetry.html&lt;/li&gt;
&lt;li&gt;CloudWatch docs (supported resources for tag enrichment): https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/UsingResourceTagsForTelemetry.html#SupportedAWSInfrastructureMetrics&lt;/li&gt;
&lt;li&gt;CloudWatch docs (Metrics Insights quotas, incl. 500 time series limit): https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/cloudwatch-metrics-insights-limits.html&lt;/li&gt;
&lt;li&gt;Background article: https://pubudu.dev/posts/cloudwatch-multi-metric-alarms/&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>Use cdk-notifier to check CloudFormation predeployment validations in pull requests</title><link>https://johanneskonings.dev/blog/2025-12-06-cdk-notifier-feature-stacks-change-set/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2025-12-06-cdk-notifier-feature-stacks-change-set/</guid><description>Extend cdk-notifier with CloudFormation change sets to validate changes before merging pull requests</description><pubDate>Sat, 06 Dec 2025 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Overview&lt;/h2&gt;
&lt;p&gt;This post builds on the previous example on &lt;a href=&quot;/blog/2023-12-16-cdk-notifier-feature-stacks&quot;&gt;using cdk-notifier to compare changes in pull requests&lt;/a&gt;. It demonstrates how to extend the workflow by adding CloudFormation change set validation alongside &lt;code&gt;cdk diff&lt;/code&gt; to provide more comprehensive pre-deployment validation.&lt;/p&gt;
&lt;p&gt;This approach leverages CloudFormation&apos;s pre-deployment validation capabilities, which are detailed in AWS&apos;s blog post on &lt;a href=&quot;https://aws.amazon.com/blogs/devops/accelerate-infrastructure-development-with-cloudformation-pre-deployment-validation-and-simplified-troubleshooting/&quot;&gt;Accelerate infrastructure development with CloudFormation pre-deployment validation and simplified troubleshooting&lt;/a&gt;.&lt;/p&gt;
&lt;h2&gt;Use Case&lt;/h2&gt;
&lt;p&gt;The cdk diff via the cdk-notifier in pull requests helps a lot to understand what will happen after the merge, but not all deployment issues can be identified with the diff alone.&lt;/p&gt;
&lt;p&gt;For example, adding a role with the same name as an existing role in the account will lead to a deployment failure, but this is not visible in the diff.&lt;/p&gt;
&lt;p&gt;Also, adding multiple Global Secondary Indexes (GSIs) to an existing DynamoDB table in one deployment will fail, but the diff does not show this limitation.
⚠️ Unfortunately, I didn&apos;t manage to raise this problem in the deployment validation.&lt;/p&gt;
&lt;h2&gt;The Extended Example&lt;/h2&gt;
&lt;p&gt;This example enhances the original feature stacks scenario with two key additions:&lt;/p&gt;
&lt;h3&gt;Infrastructure Changes&lt;/h3&gt;
&lt;p&gt;The example adds:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Global Secondary Indexes (GSIs)&lt;/strong&gt; to the DynamoDB table&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;CloudWatch LogGroup&lt;/strong&gt; to the stack&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code&gt;import { App, Stack, StackProps, aws_dynamodb as dynamodb } from &quot;aws-cdk-lib&quot;;
import { LogGroup } from &quot;aws-cdk-lib/aws-logs&quot;;
import { Construct } from &quot;constructs&quot;;

export class CdkNotfifierFeatureStackExample extends Stack {
  constructor(scope: Construct, id: string, props: StackProps = {}) {
    super(scope, id, props);

    const table = new dynamodb.Table(this, &quot;Table&quot;, {
      tableName: `Table-${branchName}`,
      partitionKey: { name: &quot;id&quot;, type: dynamodb.AttributeType.STRING },
      billingMode: dynamodb.BillingMode.PAY_PER_REQUEST,
    });

    // Add CloudWatch LogGroup
    new LogGroup(this, &quot;LogGroup&quot;, {
      logGroupName: &quot;alreadyexisting&quot;,
    });

    // Add GSI 1
    table.addGlobalSecondaryIndex({
      indexName: &quot;GSI1&quot;,
      partitionKey: { name: &quot;gsi1pk&quot;, type: dynamodb.AttributeType.STRING },
      sortKey: { name: &quot;gsi1sk&quot;, type: dynamodb.AttributeType.STRING },
    });

    // Add GSI 2
    table.addGlobalSecondaryIndex({
      indexName: &quot;GSI2&quot;,
      partitionKey: { name: &quot;gsi2pk&quot;, type: dynamodb.AttributeType.STRING },
      sortKey: { name: &quot;gsi2sk&quot;, type: dynamodb.AttributeType.STRING },
    });
  }
}

const app = new App();

const branchName = process.env.BRANCH_NAME || &quot;dev&quot;;
console.log(`Deploying with stack postfix ${branchName}`);

new CdkNotfifierFeatureStackExample(
  app,
  `cdk-notifier-feature-stacks-${branchName}`,
);

app.synth();
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Extended GitHub Action Workflow&lt;/h3&gt;
&lt;p&gt;The workflow now performs both &lt;code&gt;cdk diff&lt;/code&gt; and &lt;code&gt;cdk deploy --method prepare-change-set&lt;/code&gt;.
These command results are aggregated into a single log file, which is then processed by cdk-notifier to create a comprehensive report.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;- name: Check diff and change-set to main
  run: |
    pnpm install --frozen-lockfile  
    ECHO=$(echo &quot;## Check the diff to main&quot;)
    echo $ECHO | tee -a cdk.log
    NPM_CONFIG_LOGLEVEL=error BRANCH_NAME=main pnpm exec cdk diff --progress=events &amp;amp;&amp;gt; &amp;gt;(tee -a cdk.log)
    ECHO=$(echo &quot;## Check the change-set to main&quot;)
    echo $ECHO | tee -a cdk.log
    NPM_CONFIG_LOGLEVEL=error BRANCH_NAME=main pnpm exec cdk deploy --all --method prepare-change-set &amp;amp;&amp;gt; &amp;gt;(tee -a cdk.log) || true
    echo &quot;create cdk-notifier report&quot;
    echo &quot;BRANCH_NAME: $BRANCH_NAME&quot;
    echo &quot;GITHUB_OWNER: $GITHUB_OWNER&quot;
    echo &quot;GITHUB_REPO: $GITHUB_REPO&quot;
    cdk-notifier \
    --owner ${{ env.GITHUB_OWNER }} \
    --repo ${{ env.GITHUB_REPO }} \
    --token ${{ secrets.GITHUB_TOKEN }} \
    --log-file ./cdk.log \
    --tag-id diff-to-main \
    --pull-request-id ${{ env.PULL_REQUEST_ID }} \
    --vcs github \
    --ci circleci \
    --template extendedWithResources
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Results in the Pull Request:&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./pull-request-report.png&quot; alt=&quot;pull-request-report&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Deployment Error with Multiple GSIs&lt;/h2&gt;
&lt;p&gt;When trying to deploy multiple GSIs to an existing table, the deployment validation will show the error:&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./dynamodb-gsi-error.png&quot; alt=&quot;dynamodb-gsi-error&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Further Enhancements&lt;/h2&gt;
&lt;p&gt;Future improvements could include:
checking the change set via the AWS CLI to describe the change set to get more detailed information about the changes planned by CloudFormation.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;aws cloudformation describe-change-set&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./deployment-validation.png&quot; alt=&quot;deployment-validation-report&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Nice to Have (if not overlooked)&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;The cdk diff creates by default a change set, which could be reported back in the diff&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Code and References&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Full example repository: &lt;a href=&quot;https://github.com/JohannesKonings/cdk-notifier-examples&quot;&gt;https://github.com/JohannesKonings/cdk-notifier-examples&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;Related PR with changes: &lt;a href=&quot;https://github.com/JohannesKonings/cdk-notifier-examples/pull/12&quot;&gt;https://github.com/JohannesKonings/cdk-notifier-examples/pull/12&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;cdk-notifier project: &lt;a href=&quot;https://github.com/karlderkaefer/cdk-notifier&quot;&gt;https://github.com/karlderkaefer/cdk-notifier&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Previous Post&lt;/h2&gt;
&lt;p&gt;For the foundational concepts behind this example, refer to the earlier post: &lt;a href=&quot;/blog/2023-12-16-cdk-notifier-feature-stacks&quot;&gt;Use cdk-notifier to compare changes in pull requests&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>Deploy TanStack Start serverless on AWS</title><link>https://johanneskonings.dev/blog/2025-11-30-tanstack-start-aws-serverless/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2025-11-30-tanstack-start-aws-serverless/</guid><description>Deploy TanStack Start on AWS with streaming API Gateway using AWS CDK</description><pubDate>Sun, 30 Nov 2025 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Introduction&lt;/h2&gt;
&lt;p&gt;This guide demonstrates how to deploy a &lt;a href=&quot;https://tanstack.com/start&quot;&gt;TanStack Start&lt;/a&gt; application on AWS using a serverless architecture. TanStack Start is a full-stack React framework that provides server-side rendering, routing, and data fetching capabilities. We&apos;ll deploy it using AWS Lambda, API Gateway with streaming support, CloudFront for CDN, and S3 for static assets.&lt;/p&gt;
&lt;p&gt;The complete implementation is available in the &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws&quot;&gt;tanstack-aws repository&lt;/a&gt;, which serves as a working example and template for this deployment pattern.&lt;/p&gt;
&lt;h2&gt;Disclaimer&lt;/h2&gt;
&lt;p&gt;This is a straightforward implementation guide focused on the core deployment architecture. It does not cover production best practices such as WAF, comprehensive monitoring, structured logging, automated CI/CD pipelines, or security hardening. Use this as a starting point and extend it with production-grade features as needed.&lt;/p&gt;
&lt;h2&gt;Architecture&lt;/h2&gt;
&lt;p&gt;The deployment architecture consists of the following AWS services:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;AWS Lambda&lt;/strong&gt;: Runs the TanStack Start server-side rendering with Node.js 24.x runtime&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;API Gateway (REST API)&lt;/strong&gt;: Provides HTTP endpoint with streaming support for Lambda responses&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;CloudFront&lt;/strong&gt;: CDN for distributing both dynamic and static content globally&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;S3&lt;/strong&gt;: Hosts static assets (JavaScript, CSS, images) generated during build&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;IAM&lt;/strong&gt;: Manages permissions for Lambda to access AWS Bedrock (for AI features)&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;img src=&quot;./architechture.png&quot; alt=&quot;architecture&quot; /&gt;&lt;/p&gt;
&lt;p&gt;The architecture leverages streaming responses through API Gateway, which is essential for server-side rendering and AI-powered features that stream content to the client.&lt;/p&gt;
&lt;h2&gt;Implementation Steps&lt;/h2&gt;
&lt;h3&gt;Create the TanStack Start App&lt;/h3&gt;
&lt;p&gt;Start by creating a new TanStack Start application using the official CLI as described in the &lt;a href=&quot;https://tanstack.com/start/latest/docs/framework/react/quick-start&quot;&gt;TanStack Start Quick Start&lt;/a&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;npm create @tanstack/start
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;When prompted, select &lt;strong&gt;Nitro&lt;/strong&gt; as the deployment adapter since it provides a built-in AWS Lambda preset with streaming support.&lt;/p&gt;
&lt;h4&gt;Organize Project Structure&lt;/h4&gt;
&lt;p&gt;Move the generated TanStack application files to a &lt;code&gt;src/webapp&lt;/code&gt; directory to separate the web application from the infrastructure code. This allows you to maintain both the CDK infrastructure and the application in the same repository.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Reference&lt;/strong&gt;: See the project structure in &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws&quot;&gt;tanstack-aws repository&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h4&gt;Configure Vite and Nitro&lt;/h4&gt;
&lt;p&gt;Update your &lt;code&gt;vite.config.ts&lt;/code&gt; to configure Nitro for AWS Lambda deployment with streaming enabled. This configuration is crucial for enabling response streaming:&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Reference&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/main/vite.config.ts&quot;&gt;&lt;code&gt;vite.config.ts&lt;/code&gt;&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;pre&gt;&lt;code&gt;const config = defineConfig({
  plugins: [
    devtools(),
    nitro({
      awsLambda: { streaming: true },
      preset: &quot;aws-lambda&quot;,
    }),
    viteTsConfigPaths({
      projects: [&quot;./tsconfig.json&quot;],
    }),
    tailwindcss(),
    tanstackStart({
      srcDirectory: &quot;src/webapp&quot;,
    }),
    viteReact(),
  ],
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Key configuration points:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;awsLambda: { streaming: true }&lt;/code&gt;: Enables response streaming for Lambda&lt;/li&gt;
&lt;li&gt;&lt;code&gt;preset: &apos;aws-lambda&apos;&lt;/code&gt;: Uses Nitro&apos;s AWS Lambda adapter&lt;/li&gt;
&lt;li&gt;&lt;code&gt;srcDirectory: &apos;src/webapp&apos;&lt;/code&gt;: Points to the application source directory&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Set Up AWS CDK Infrastructure&lt;/h3&gt;
&lt;p&gt;You can maintain the CDK infrastructure code in the same repository as your application. The infrastructure uses several custom constructs to deploy the complete stack.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Reference&lt;/strong&gt;: All constructs are in &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/tree/main/lib/constructs&quot;&gt;&lt;code&gt;lib/constructs/&lt;/code&gt;&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3&gt;Webapp Server Lambda&lt;/h3&gt;
&lt;p&gt;Create a Lambda function construct that deploys the Nitro build output. The Lambda function runs the server-side rendering and API endpoints.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Reference&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/main/lib/constructs/WebappServer.ts&quot;&gt;&lt;code&gt;lib/constructs/WebappServer.ts&lt;/code&gt;&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;pre&gt;&lt;code&gt;import { Code, Function, Runtime } from &quot;aws-cdk-lib/aws-lambda&quot;;
import { Duration, Tags } from &quot;aws-cdk-lib&quot;;
import { Effect, PolicyStatement } from &quot;aws-cdk-lib/aws-iam&quot;;
import { Construct } from &quot;constructs&quot;;
import path from &quot;node:path&quot;;

export class WebappServer extends Construct {
  readonly webappServer: Function;

  constructor(scope: Construct, id: string) {
    super(scope, id);

    this.webappServer = new Function(this, &quot;WebappServer&quot;, {
      code: Code.fromAsset(
        path.join(
          path.dirname(new URL(import.meta.url).pathname),
          &quot;../../.output/server&quot;,
        ),
      ),
      handler: &quot;index.handler&quot;,
      memorySize: 2048,
      runtime: Runtime.NODEJS_24_X,
      timeout: Duration.seconds(60),
    });
    Tags.of(this.webappServer).add(&quot;IsWebAppServer&quot;, &quot;true&quot;);

    this.webappServer.addToRolePolicy(
      new PolicyStatement({
        actions: [
          &quot;bedrock:InvokeModel&quot;,
          &quot;bedrock:InvokeModelWithResponseStream&quot;,
        ],
        effect: Effect.ALLOW,
        resources: [&quot;*&quot;],
      }),
    );
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Important details:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Code source&lt;/strong&gt;: Points to &lt;code&gt;.output/server&lt;/code&gt; generated by Nitro&apos;s build process (&lt;code&gt;nitro build&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Handler&lt;/strong&gt;: &lt;code&gt;index.handler&lt;/code&gt; is the default entry point created by Nitro&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Memory&lt;/strong&gt;: 2048 MB provides adequate resources for SSR operations&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Runtime&lt;/strong&gt;: Node.js 24.x for latest features and performance&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Bedrock permissions&lt;/strong&gt;: Added for AI chat functionality (optional, remove if not needed)&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Assets Bucket and Deployment&lt;/h3&gt;
&lt;p&gt;Create an S3 bucket to host static assets generated during the build process:&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Reference&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/main/lib/constructs/WebappAssetsBucket.ts&quot;&gt;&lt;code&gt;lib/constructs/WebappAssetsBucket.ts&lt;/code&gt;&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;pre&gt;&lt;code&gt;import { Bucket } from &quot;aws-cdk-lib/aws-s3&quot;;
import { Construct } from &quot;constructs&quot;;

export class WebappAssetsBucket extends Construct {
  readonly assetsBucket: Bucket;
  constructor(scope: Construct, id: string) {
    super(scope, id);

    this.assetsBucket = new Bucket(this, &quot;AssetsBucket&quot;);
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Use the &lt;code&gt;BucketDeployment&lt;/code&gt; construct to automatically upload static assets to S3 after each build and invalidate the CloudFront cache:&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Reference&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/main/lib/constructs/WebappAssetsDeployment.ts&quot;&gt;&lt;code&gt;lib/constructs/WebappAssetsDeployment.ts&lt;/code&gt;&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;pre&gt;&lt;code&gt;import { BucketDeployment, Source } from &quot;aws-cdk-lib/aws-s3-deployment&quot;;
import { Bucket } from &quot;aws-cdk-lib/aws-s3&quot;;
import { Construct } from &quot;constructs&quot;;
import type { Distribution } from &quot;aws-cdk-lib/aws-cloudfront&quot;;
import path from &quot;node:path&quot;;

type WebappAssetsDeploymentProps = {
  assetsBucket: Bucket;
  distribution: Distribution;
};

export class WebappAssetsDeployment extends Construct {
  constructor(
    scope: Construct,
    id: string,
    props: WebappAssetsDeploymentProps,
  ) {
    super(scope, id);

    const { assetsBucket, distribution } = props;

    const sourcePath = path.join(
      path.dirname(new URL(import.meta.url).pathname),
      &quot;../../.output/public&quot;,
    );

    new BucketDeployment(this, &quot;AssetBucketDeployment&quot;, {
      destinationBucket: assetsBucket,
      distribution,
      distributionPaths: [&quot;/*&quot;],
      sources: [Source.asset(sourcePath)],
    });
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Key features:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source&lt;/strong&gt;: Points to &lt;code&gt;.output/public&lt;/code&gt; where Nitro generates static assets&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Cache invalidation&lt;/strong&gt;: &lt;code&gt;distributionPaths: [&quot;/*&quot;]&lt;/code&gt; invalidates CloudFront cache on deployment&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Automatic deployment&lt;/strong&gt;: Runs during CDK deployment to sync assets&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;API Gateway with Streaming&lt;/h3&gt;
&lt;p&gt;Create a REST API Gateway that integrates with the Lambda function and enables streaming responses:&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Reference&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/main/lib/constructs/WebappApi.ts&quot;&gt;&lt;code&gt;lib/constructs/WebappApi.ts&lt;/code&gt;&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;pre&gt;&lt;code&gt;import {
  EndpointType,
  LambdaRestApi,
  ResponseTransferMode,
} from &quot;aws-cdk-lib/aws-apigateway&quot;;
import { Construct } from &quot;constructs&quot;;
import { Function } from &quot;aws-cdk-lib/aws-lambda&quot;;

type WebappApiProps = {
  webappServer: Function;
};

export class WebappApi extends Construct {
  readonly webappApi: LambdaRestApi;

  constructor(scope: Construct, id: string, props: WebappApiProps) {
    super(scope, id);

    const { webappServer } = props;

    this.webappApi = new LambdaRestApi(this, &quot;WebappApi&quot;, {
      endpointConfiguration: {
        types: [EndpointType.REGIONAL],
      },
      handler: webappServer,
      integrationOptions: {
        responseTransferMode: ResponseTransferMode.STREAM,
      },
    });
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Critical configuration:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;ResponseTransferMode.STREAM&lt;/code&gt;&lt;/strong&gt;: Enables streaming responses from Lambda, essential for SSR and AI streaming&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;LambdaRestApi&lt;/code&gt;&lt;/strong&gt;: Automatically creates proxy integration for all routes&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;CloudFront Distribution&lt;/h3&gt;
&lt;p&gt;Set up CloudFront to serve both dynamic content (from API Gateway) and static assets (from S3):&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Reference&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/main/lib/constructs/WebappDistribution.ts&quot;&gt;&lt;code&gt;lib/constructs/WebappDistribution.ts&lt;/code&gt;&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;pre&gt;&lt;code&gt;import {
  AllowedMethods,
  CachePolicy,
  Distribution,
  OriginRequestPolicy,
  PriceClass,
  ResponseHeadersPolicy,
  ViewerProtocolPolicy,
} from &quot;aws-cdk-lib/aws-cloudfront&quot;;
import { Construct } from &quot;constructs&quot;;
import {
  RestApiOrigin,
  S3BucketOrigin,
} from &quot;aws-cdk-lib/aws-cloudfront-origins&quot;;
import type { Bucket } from &quot;aws-cdk-lib/aws-s3&quot;;
import type { RestApi } from &quot;aws-cdk-lib/aws-apigateway&quot;;

type DistributionProps = {
  webappServerApi: RestApi;
  assetsBucket: Bucket;
};

export class WebappDistribution extends Construct {
  public readonly distribution: Distribution;

  constructor(scope: Construct, id: string, props: DistributionProps) {
    super(scope, id);

    const { webappServerApi, assetsBucket } = props;

    const s3BucketOrigin = S3BucketOrigin.withOriginAccessControl(assetsBucket);

    const defaultBehavior = {
      allowedMethods: AllowedMethods.ALLOW_ALL,
      cachePolicy: CachePolicy.CACHING_DISABLED,
      origin: new RestApiOrigin(webappServerApi),
      originRequestPolicy: OriginRequestPolicy.ALL_VIEWER_EXCEPT_HOST_HEADER,
      responseHeadersPolicy: ResponseHeadersPolicy.SECURITY_HEADERS,
      viewerProtocolPolicy: ViewerProtocolPolicy.REDIRECT_TO_HTTPS,
    };

    const staticAssetBehavior = {
      cachePolicy: CachePolicy.CACHING_OPTIMIZED,
      origin: s3BucketOrigin,
      viewerProtocolPolicy: ViewerProtocolPolicy.REDIRECT_TO_HTTPS,
    };

    this.distribution = new Distribution(this, &quot;Distribution&quot;, {
      additionalBehaviors: {
        &quot;/assets/*&quot;: staticAssetBehavior,
        &quot;/favicon.ico&quot;: staticAssetBehavior,
        &quot;/images/*&quot;: staticAssetBehavior,
        &quot;/site.webmanifest&quot;: staticAssetBehavior,
      },
      defaultBehavior,
      priceClass: PriceClass.PRICE_CLASS_100,
    });
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Key configuration:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Default behavior&lt;/strong&gt;: Routes all requests to API Gateway with caching disabled (for dynamic SSR content)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Additional behaviors&lt;/strong&gt;: Routes specific paths (&lt;code&gt;/assets/*&lt;/code&gt;, &lt;code&gt;/images/*&lt;/code&gt;, etc.) to S3 with optimized caching&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Origin Access Control&lt;/strong&gt;: Uses OAC (recommended over OAI) for secure S3 access&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Security headers&lt;/strong&gt;: Automatically adds security headers to responses&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Price class&lt;/strong&gt;: &lt;code&gt;PRICE_CLASS_100&lt;/code&gt; uses edge locations in North America and Europe (adjust as needed)&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Wire Everything Together&lt;/h3&gt;
&lt;p&gt;Create a main construct that integrates all the components:&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Reference&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/main/lib/constructs/Webapp.ts&quot;&gt;&lt;code&gt;lib/constructs/Webapp.ts&lt;/code&gt;&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;pre&gt;&lt;code&gt;import { Construct } from &quot;constructs&quot;;
import { WebappDistribution } from &quot;./WebappDistribution.ts&quot;;
import { WebappServer } from &quot;./WebappServer.ts&quot;;
import { WebappAssetsDeployment } from &quot;./WebappAssetsDeployment.ts&quot;;
import { WebappAssetsBucket } from &quot;./WebappAssetsBucket.ts&quot;;
import { WebappApi } from &quot;./WebappApi.ts&quot;;

export class Webapp extends Construct {
  constructor(scope: Construct, id: string) {
    super(scope, id);

    const webappServer = new WebappServer(this, &quot;WebappServer&quot;);

    const webappApi = new WebappApi(this, &quot;WebappApi&quot;, {
      webappServer: webappServer.webappServer,
    });

    const assetsBucket = new WebappAssetsBucket(this, &quot;WebappAssetsBucket&quot;);

    const distribution = new WebappDistribution(this, &quot;WebappDistribution&quot;, {
      assetsBucket: assetsBucket.assetsBucket,
      webappServerApi: webappApi.webappApi,
    });

    new WebappAssetsDeployment(this, &quot;WebappAssetsDeployment&quot;, {
      assetsBucket: assetsBucket.assetsBucket,
      distribution: distribution.distribution,
    });
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This construct orchestrates the deployment by:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Creating the Lambda function with the server code&lt;/li&gt;
&lt;li&gt;Setting up API Gateway to route requests to Lambda&lt;/li&gt;
&lt;li&gt;Creating the S3 bucket for static assets&lt;/li&gt;
&lt;li&gt;Configuring CloudFront distribution with proper routing&lt;/li&gt;
&lt;li&gt;Deploying static assets and invalidating the cache&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Build and Deploy&lt;/h3&gt;
&lt;p&gt;To build and deploy the application:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# Install dependencies
pnpm install

# Build the TanStack Start application
pnpm webapp:build

# Deploy the CDK stack
pnpm cdk deploy
&lt;/code&gt;&lt;/pre&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Reference&lt;/strong&gt;: See &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/main/package.json&quot;&gt;&lt;code&gt;package.json&lt;/code&gt;&lt;/a&gt; for all available scripts&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;strong&gt;Build process:&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;pnpm webapp:build&lt;/code&gt; runs &lt;code&gt;vite build&lt;/code&gt;, which uses Nitro to compile the app&lt;/li&gt;
&lt;li&gt;Nitro generates two outputs:
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;.output/server/&lt;/code&gt; - Lambda function code&lt;/li&gt;
&lt;li&gt;&lt;code&gt;.output/public/&lt;/code&gt; - Static assets&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;CDK deployment packages the Lambda code and uploads static assets to S3&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Results&lt;/h2&gt;
&lt;p&gt;After deployment, you&apos;ll receive a CloudFront URL where your application is accessible. The architecture provides:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Fast global delivery&lt;/strong&gt;: CloudFront CDN serves content from edge locations&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Efficient caching&lt;/strong&gt;: Static assets cached at edge, dynamic content from Lambda&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Streaming support&lt;/strong&gt;: Real-time responses for SSR and AI features&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Scalability&lt;/strong&gt;: Auto-scaling Lambda functions handle traffic spikes&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Example Deployment&lt;/h3&gt;
&lt;p&gt;&lt;img src=&quot;./deployed-start-page.png&quot; alt=&quot;deployed start page&quot; /&gt;&lt;/p&gt;
&lt;p&gt;The template repository includes a demo chat application powered by AWS Bedrock (Claude AI) with streaming responses:&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./tanchat.gif&quot; alt=&quot;tanchat&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Key Dependencies&lt;/h2&gt;
&lt;p&gt;The implementation relies on these main packages (see &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws/blob/main/package.json&quot;&gt;&lt;code&gt;package.json&lt;/code&gt;&lt;/a&gt; for exact versions):&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;TanStack ecosystem:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;@tanstack/react-start&lt;/code&gt; - Full-stack React framework&lt;/li&gt;
&lt;li&gt;&lt;code&gt;@tanstack/react-router&lt;/code&gt; - File-based routing&lt;/li&gt;
&lt;li&gt;&lt;code&gt;@tanstack/react-query&lt;/code&gt; - Data fetching and caching&lt;/li&gt;
&lt;li&gt;&lt;code&gt;@tanstack/store&lt;/code&gt; - State management&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Build tools:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;nitro&lt;/code&gt; - Universal JavaScript server with AWS Lambda preset&lt;/li&gt;
&lt;li&gt;&lt;code&gt;vite&lt;/code&gt; - Frontend build tool&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tailwindcss&lt;/code&gt; - Utility-first CSS framework&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;AWS infrastructure:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;aws-cdk-lib&lt;/code&gt; - AWS Cloud Development Kit&lt;/li&gt;
&lt;li&gt;&lt;code&gt;constructs&lt;/code&gt; - CDK construct library&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Alternative Deployment Options&lt;/h2&gt;
&lt;p&gt;While this guide focuses on API Gateway, the template repository also demonstrates Lambda Function URLs as an alternative origin. Function URLs provide a simpler setup but may require additional configuration for authentication (e.g., Lambda@Edge for SigV4 signing).&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Note&lt;/strong&gt;: The commented code in the reference implementation shows the Function URL approach for comparison.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;Sources and References&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Template repository&lt;/strong&gt;: &lt;a href=&quot;https://github.com/JohannesKonings/tanstack-aws&quot;&gt;github.com/JohannesKonings/tanstack-aws&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;TanStack Start&lt;/strong&gt;: &lt;a href=&quot;https://tanstack.com/start&quot;&gt;tanstack.com/start&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Nitro framework&lt;/strong&gt;: &lt;a href=&quot;https://nitro.unjs.io&quot;&gt;nitro.unjs.io&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;AWS CDK&lt;/strong&gt;: &lt;a href=&quot;https://docs.aws.amazon.com/cdk&quot;&gt;docs.aws.amazon.com/cdk&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Inspired by&lt;/strong&gt;: &lt;a href=&quot;https://github.com/zhe/tanstack-start-aws-cdk&quot;&gt;github.com/zhe/tanstack-start-aws-cdk&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>Encrypt All Lambda Environment Variables with AWS CDK Aspects/Mixins</title><link>https://johanneskonings.dev/blog/2025-11-28-aws-cdk-encrypt-all-lambda-envs/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2025-11-28-aws-cdk-encrypt-all-lambda-envs/</guid><description>A guide on how to encrypt all AWS Lambda environment variables using AWS CDK aspects/mixins. Also from third party libraries.</description><pubDate>Fri, 28 Nov 2025 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Introduction&lt;/h2&gt;
&lt;p&gt;If you need to ensure that all AWS Lambda environment variables are encrypted with a customer-managed KMS key for compliance or security requirements, you can achieve this using AWS CDK aspects or mixins.&lt;/p&gt;
&lt;p&gt;While Lambda functions that you create directly can be configured with the &lt;code&gt;environmentEncryption&lt;/code&gt; property, third-party libraries or construct libraries may create Lambda functions where you don&apos;t have direct control over their configuration.&lt;/p&gt;
&lt;p&gt;In such cases, you can use aspects or mixins to enforce encryption on all Lambda functions in your CDK app, regardless of how they were created.&lt;/p&gt;
&lt;h2&gt;Aspect, Property Injection or Mixin?&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://docs.aws.amazon.com/cdk/v2/guide/aspects.html&quot;&gt;Aspects&lt;/a&gt; are a way to modify the CDK synth result on a L1 level after the constructs have been created. They are executed during the Prepare phase of the CDK lifecycle, after all constructs are created.&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://docs.aws.amazon.com/cdk/v2/guide/blueprints.html&quot;&gt;Property Injection&lt;/a&gt; is a way to modify the properties of L2 constructs before they are instantiated. Property injection happens during the Construct phase, at the time each construct is created.&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/aws/aws-cdk/blob/main/packages/%40aws-cdk/mixins-preview/README.md&quot;&gt;Mixins&lt;/a&gt; can be used to modify constructs at any level (L1, L2, or L3). They are executed immediately when applied, unlike Aspects which execute later during synthesis.&lt;/p&gt;
&lt;h3&gt;Implementation&lt;/h3&gt;
&lt;p&gt;The stack look like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;export class StackMain extends Stack {
  readonly encryptionKey: Key;

  constructor(scope: Construct, id: string, props: StackMainProps) {
    super(scope, id, props);

    this.encryptionKey = new Key(this, &quot;EncryptionKey&quot;, {
      enableKeyRotation: true,
    });

    // Direct Lambda - environment encryption can be set directly
    new Function(this, &quot;MyFunction&quot;, {
      code: Code.fromInline(
        &apos;exports.handler = async function(event, context) { return &quot;Hello World&quot;; };&apos;,
      ),
      handler: &quot;index.handler&quot;,
      runtime: Runtime.NODEJS_LATEST,
      environmentEncryption: this.encryptionKey,
      environment: {
        DUMMY: &quot;dummy&quot;,
      },
    });

    // Indirect Lambda from third-party library - environment encryption cannot be controlled directly
    // Note: This is a hypothetical third-party construct for demonstration purposes
    new Lambda(this, &quot;DummyThirdPartyLambda&quot;);
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The app could look like this (using either Aspect or Mixin):&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const app = new App();
const stack = new StackMain(app, &quot;StackMain&quot;, {});

// Option 1: Use Aspect (recommended for production - stable API)
Aspects.of(app).add(
  new LambdaEnvEncryptionSetterAspect(stack.encryptionKey.keyArn),
);

// Option 2: Use Mixin (developer preview - more flexible but API may change)
// Mixins.of(app).apply(
//   new LambdaEnvEncryptionSetterMixin(stack.encryptionKey.keyArn),
// );

// could not be used because the functions are created before the property injection happens, but the key is needed for the injection
// PropertyInjectors.of(app).add(
//   new LambdaEnvEncryptionSetterAspectPropertyInjection(stack.encryptionKey),
// );
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Aspect&lt;/h3&gt;
&lt;p&gt;Here is an example of an aspect that encrypts all Lambda environment variables. This aspect operates on L1 (CfnFunction) constructs and sets the &lt;code&gt;kmsKeyArn&lt;/code&gt; property during the synthesis Prepare phase:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import type { IAspect } from &quot;aws-cdk-lib&quot;;
import { CfnFunction } from &quot;aws-cdk-lib/aws-lambda&quot;;
import type { IConstruct } from &quot;constructs&quot;;

export class LambdaEnvEncryptionSetterAspect implements IAspect {
  public readonly kmsKeyArn: string;

  constructor(kmsKeyArn: string) {
    this.kmsKeyArn = kmsKeyArn;
  }

  visit(node: IConstruct): void {
    if (node instanceof CfnFunction) {
      node.kmsKeyArn = this.kmsKeyArn;
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Property Injector&lt;/h3&gt;
&lt;p&gt;Property Injection cannot be used in this specific case because:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Property Injection only works with L2 constructs (like &lt;code&gt;lambda.Function&lt;/code&gt;), not L1 constructs (like &lt;code&gt;lambda.CfnFunction&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;Third-party libraries might create L1 constructs directly, bypassing the L2 construct layer&lt;/li&gt;
&lt;li&gt;Creating the KMS key in the same stack while trying to inject it creates a timing issue, as injection happens during construct instantiation&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Here&apos;s what the code would look like if you could use it (for reference only):&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { type InjectionContext, type IPropertyInjector } from &quot;aws-cdk-lib&quot;;
import { IKey } from &quot;aws-cdk-lib/aws-kms&quot;;
import { Function, FunctionProps } from &quot;aws-cdk-lib/aws-lambda&quot;;

export class LambdaEnvEncryptionSetterPropertyInjector
  implements IPropertyInjector
{
  public readonly constructUniqueId: string;

  constructor(private readonly key: IKey) {
    this.constructUniqueId = Function.PROPERTY_INJECTION_ID;
  }

  public inject(
    originalProps: FunctionProps,
    _context: InjectionContext,
  ): FunctionProps {
    return {
      ...originalProps,
      environmentEncryption: this.key,
    };
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Mixin&lt;/h3&gt;
&lt;p&gt;Mixins are currently in developer preview (as of November 2025). Unlike Aspects, Mixins are applied immediately when called, giving you more control over when and how changes are applied. Here is an example of a mixin that encrypts all Lambda environment variables:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { Mixin } from &quot;@aws-cdk/mixins-preview/core&quot;;
import &quot;@aws-cdk/mixins-preview/with&quot;;
import { IConstruct } from &quot;constructs&quot;;
import { CfnFunction } from &quot;aws-cdk-lib/aws-lambda&quot;;

export class LambdaEnvEncryptionSetterMixin implements Mixin {
  private readonly kmsKeyArn: string;

  constructor(kmsKeyArn: string) {
    this.kmsKeyArn = kmsKeyArn;
  }

  public supports(construct: IConstruct): boolean {
    return construct instanceof CfnFunction;
  }

  public applyTo(construct: IConstruct): IConstruct {
    if (construct instanceof CfnFunction) {
      construct.kmsKeyArn = this.kmsKeyArn;
    }

    return construct;
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Conclusion&lt;/h2&gt;
&lt;p&gt;Using AWS CDK aspects or mixins, you can enforce encryption on all AWS Lambda environment variables in your CDK app, even when using third-party libraries that create Lambda functions. This approach ensures that your Lambda functions adhere to security best practices without requiring direct control over their configuration.&lt;/p&gt;
&lt;h3&gt;Which Approach to Use?&lt;/h3&gt;
&lt;p&gt;Based on the &lt;a href=&quot;https://github.com/aws/aws-cdk-rfcs/pull/824&quot;&gt;AWS CDK Mixins RFC&lt;/a&gt;, the recommended usage is:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Aspects&lt;/strong&gt;: Use for validation and compliance enforcement. This is the stable, production-ready approach.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Mixins&lt;/strong&gt;: Use for making changes to constructs. However, since Mixins are still in Developer Preview, the API may change.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Recommendation for Production&lt;/strong&gt;: Use &lt;strong&gt;Aspects&lt;/strong&gt; for now, as they provide a stable API and are specifically designed for this type of cross-cutting concern. Once Mixins reach general availability, they may offer additional benefits for construct composition.&lt;/p&gt;
&lt;h2&gt;Sources&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;https://docs.aws.amazon.com/cdk/v2/guide/aspects.html&lt;/li&gt;
&lt;li&gt;https://docs.aws.amazon.com/cdk/v2/guide/blueprints.html&lt;/li&gt;
&lt;li&gt;https://github.com/aws/aws-cdk/blob/main/packages/%40aws-cdk/mixins-preview/README.md&lt;/li&gt;
&lt;li&gt;https://dev.to/aws-heroes/using-aws-cdk-property-injectors-2mo5&lt;/li&gt;
&lt;li&gt;https://dev.to/aws-heroes/aws-cdk-introduces-mixins-a-major-feature-for-flexible-construct-composition-developer-preview-583d&lt;/li&gt;
&lt;li&gt;https://dev.to/aws/announcing-aws-cdk-mixins-composable-abstractions-for-aws-resources-588m&lt;/li&gt;
&lt;li&gt;https://docs.aws.amazon.com/lambda/latest/dg/configuration-envvars.html#configuration-envvars-encryption&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>Granular statement cdk-nag AwsSolutions-IAM5 Suppressions</title><link>https://johanneskonings.dev/blog/2025-11-27-aws-cdk-nag-iam5-granular-statement-suppresions/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2025-11-27-aws-cdk-nag-iam5-granular-statement-suppresions/</guid><description>How to document and suppress only the wildcard permissions that are required by AWS services, while keeping the rest of AwsSolutions-IAM5 findings active.</description><pubDate>Thu, 27 Nov 2025 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Overview&lt;/h2&gt;
&lt;p&gt;cdk-nag’s AwsSolutions-IAM5 rule is one of the most frequent findings in real-world stacks. It flags wildcard permissions in both &lt;code&gt;Action&lt;/code&gt; (e.g., &lt;code&gt;kms:GenerateDataKey*&lt;/code&gt;) and &lt;code&gt;Resource&lt;/code&gt; (e.g., &lt;code&gt;*&lt;/code&gt;) when there is no evidence-backed suppression. The challenge is that some AWS APIs are designed to require &lt;code&gt;*&lt;/code&gt; (e.g., &lt;a href=&quot;https://docs.aws.amazon.com/xray/latest/devguide/security_iam_service-with-iam.html&quot;&gt;AWS X-Ray&lt;/a&gt;). Blanket suppressions hide too much. This post demonstrates how to record granular evidence specifically for actions that require &lt;code&gt;*&lt;/code&gt;, ensuring that all other findings remain active.&lt;/p&gt;
&lt;h2&gt;Understanding the Problem&lt;/h2&gt;
&lt;h3&gt;Why IAM5 triggers&lt;/h3&gt;
&lt;p&gt;You’ll hit AwsSolutions-IAM5 when a policy uses an asterisk (&lt;code&gt;&quot;*&quot;&lt;/code&gt;) in the resource or wildcards in actions. This is beneficial because least privilege is critical. However, several AWS APIs require &lt;code&gt;*&lt;/code&gt; by design, or you might initially group actions broadly before refining the scope. Common examples include:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;new PolicyStatement({
  actions: [&quot;xray:PutTelemetryRecords&quot;, &quot;xray:PutTraceSegments&quot;],
  resources: [&quot;*&quot;],
  effect: Effect.ALLOW,
});
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code&gt;new PolicyStatement({
        actions: [&quot;kms:GenerateDataKey*&quot;, &quot;kms:ReEncrypt*&quot;],
        resources: [
          &quot;arn:aws:kms:eu-central-1:471112809534:key/a096e96c-780e-48eb-993b-5b8cc46d8fd7&quot;,
        ],
        effect: Effect.ALLOW,
      }),
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Example Failure&lt;/h3&gt;
&lt;p&gt;An IAM5 failure typically looks like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;[Error at /StackMain/MyRole/Policy/Resource] AwsSolutions-IAM5[Resource::*]: The IAM entity contains wildcard permissions and does not have a cdk-nag rule suppression with evidence for those permissions.
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Why blanket suppression isn&apos;t enough&lt;/h3&gt;
&lt;p&gt;Suppressing the entire resource or the entire rule hides legitimate issues and loses the evidence that auditors expect. The goal is to:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Keep the rule active for statements that can be scoped.&lt;/li&gt;
&lt;li&gt;Provide explicit evidence for the few actions that require &lt;code&gt;*&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;// Bad: over-broad, hides everything
NagSuppressions.addResourceSuppressions(role, [
  { id: &quot;AwsSolutions-IAM5&quot;, reason: &quot;needs wildcard&quot; },
]);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This approach makes reviews harder and weakens the principle of least privilege.&lt;/p&gt;
&lt;h2&gt;The Solution&lt;/h2&gt;
&lt;h3&gt;Overview&lt;/h3&gt;
&lt;p&gt;The solution involves using granular suppressions that document exactly which actions require &lt;code&gt;*&lt;/code&gt; and why. Everything else remains flagged by cdk-nag. The
&lt;a href=&quot;https://github.com/JohannesKonings/cdk-nag-custom-nag-pack&quot;&gt;cdk-nag-custom-nag-pack&lt;/a&gt; automates this by:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Inspecting policy statements with &lt;code&gt; *&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Adding a single AwsSolutions-IAM5 suppression per resource with &lt;code&gt;appliesTo&lt;/code&gt; evidence for only those actions.&lt;/li&gt;
&lt;li&gt;Leaving non-allowed wildcard actions reported so you can scope them properly.&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Implementation&lt;/h3&gt;
&lt;p&gt;The repository includes a dedicated helper (&lt;code&gt;iam5NagSuppressions.ts&lt;/code&gt;) that applies these granular suppressions automatically. The essential components are:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Statement scanning&lt;/strong&gt;: Iterates IAM policy statements and selects those with &lt;code&gt;Resource: *&lt;/code&gt; (including arrays that contain &lt;code&gt;*&lt;/code&gt;).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Evidence builder&lt;/strong&gt;: Produces &lt;code&gt;appliesTo&lt;/code&gt; entries for &lt;code&gt;Resource::*&lt;/code&gt; and only the allow-listed &lt;code&gt;Action::&amp;lt;service:operation&amp;gt;&lt;/code&gt; values present in the statement.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Targeting&lt;/strong&gt;: Attaches a single &lt;code&gt;AwsSolutions-IAM5&lt;/code&gt; suppression to each IAM Policy construct via its path, leaving any non-allowed wildcard actions unsuppressed (so they continue to fail until scoped).&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;You can find the source code here: &lt;a href=&quot;https://github.com/JohannesKonings/cdk-nag-custom-nag-pack/blob/main/src/iam5NagSuppressions.ts&quot;&gt;iam5NagSuppressions.ts&lt;/a&gt;&lt;/p&gt;
&lt;h3&gt;Critical Difference: String-Based vs. Statement-Based Suppressions&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Important:&lt;/strong&gt; There are two fundamentally different approaches to granular IAM5 suppressions, each offering different levels of safety:&lt;/p&gt;
&lt;h4&gt;1. cdk-nag&apos;s Built-in &lt;code&gt;appliesTo&lt;/code&gt;&lt;/h4&gt;
&lt;p&gt;The standard cdk-nag approach (see Example 6 in the &lt;a href=&quot;https://github.com/cdklabs/cdk-nag?tab=readme-ov-file#suppressing-a-rule&quot;&gt;cdk-nag docs&lt;/a&gt;) relies on string patterns:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;NagSuppressions.addResourceSuppressions(
  lambda,
  [
    {
      id: &quot;AwsSolutions-IAM5&quot;,
      reason: &quot;X-Ray requires wildcard&quot;,
      appliesTo: [&quot;Resource::*&quot;],
    },
  ],
  true,
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;The Problem:&lt;/strong&gt; This suppresses &lt;code&gt;Resource::*&lt;/code&gt; and the specified actions &lt;strong&gt;anywhere they appear&lt;/strong&gt; in the construct tree. If you later add a new policy statement like:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// This will be silently suppressed!
new PolicyStatement({
  actions: [&quot;xray:PutTelemetryRecords&quot;], // Matches the suppression
  resources: [&quot;*&quot;], // Matches the suppression
  effect: Effect.ALLOW,
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The suppression hides it because the &lt;strong&gt;strings match&lt;/strong&gt;, even though this might be an unintended permission you added months later.&lt;/p&gt;
&lt;h4&gt;2. cdk-nag-custom-nag-pack&apos;s Statement Validation&lt;/h4&gt;
&lt;p&gt;The &lt;a href=&quot;https://github.com/JohannesKonings/cdk-nag-custom-nag-pack?tab=readme-ov-file#granular-awssolutions-iam5-suppressions&quot;&gt;cdk-nag-custom-nag-pack approach&lt;/a&gt; requires exact statement matching:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const policyStatementForSuppression: PolicyStatementProps = {
  actions: [&quot;xray:PutTelemetryRecords&quot;, &quot;xray:PutTraceSegments&quot;],
  resources: [&quot;*&quot;],
  effect: Effect.ALLOW,
};

Iam5NagSuppressions.addIam5StatementResourceSuppressions(
  lambda,
  {
    id: &quot;AwsSolutions-IAM5&quot;,
    reason: &quot;X-Ray requires wildcard&quot;,
    appliesTo: [policyStatementForSuppression],
  },
  true,
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;The Benefit:&lt;/strong&gt; The suppression applies only if there is an &lt;strong&gt;exact match&lt;/strong&gt; of the entire policy statement (Effect, Actions, Resources). If you add a new statement with different actions or resources later:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// This will NOT be suppressed - it will be flagged!
new PolicyStatement({
  actions: [&quot;xray:PutTelemetryRecords&quot;, &quot;s3:*&quot;], // Different actions
  resources: [&quot;*&quot;],
  effect: Effect.ALLOW,
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;It will be flagged because the statement does not match exactly. This prevents the accidental hiding of new wildcards added over time.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Key Takeaway:&lt;/strong&gt; The &lt;code&gt;cdk-nag-custom-nag-pack&lt;/code&gt; method validates the complete policy statement structure, whereas cdk-nag&apos;s built-in &lt;code&gt;appliesTo&lt;/code&gt; relies on string patterns that may inadvertently suppress unrelated future wildcards.&lt;/p&gt;
&lt;h3&gt;Benefits&lt;/h3&gt;
&lt;p&gt;This statement-based approach:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Documents exactly why &lt;code&gt;*&lt;/code&gt; is needed for specific AWS APIs&lt;/li&gt;
&lt;li&gt;Keeps AwsSolutions-IAM5 active for everything else&lt;/li&gt;
&lt;li&gt;Produces review-ready evidence via &lt;code&gt;appliesTo&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Prevents accidental suppression of future wildcard permissions&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Lambda Construct Wildcard Scenario&lt;/h3&gt;
&lt;p&gt;The &lt;code&gt;aws-lambda.Function&lt;/code&gt; construct (and its default execution role or managed policies) often introduces unavoidable wildcard permissions, such as those required for CloudWatch Logs creation or X-Ray tracing.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;The Risk:&lt;/strong&gt; If you suppress &lt;code&gt;AwsSolutions-IAM5&lt;/code&gt; broadly at the Function or Role level, you inadvertently hide &lt;em&gt;every&lt;/em&gt; future wildcard you might add (e.g., &lt;code&gt;s3:*&lt;/code&gt; or &lt;code&gt;dynamodb:*&lt;/code&gt;).&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;The Granular Strategy:&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Isolate:&lt;/strong&gt; Put truly unavoidable wildcard actions (e.g., &lt;code&gt;logs:CreateLogGroup&lt;/code&gt;, &lt;code&gt;xray:PutTraceSegments&lt;/code&gt;) in their own dedicated &lt;code&gt;PolicyStatement&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Suppress:&lt;/strong&gt; Apply the suppression &lt;em&gt;only&lt;/em&gt; to that specific statement, citing the AWS service limitation as the reason.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Scope:&lt;/strong&gt; Ensure all other actions are scoped to specific ARNs (log streams, tables, buckets, queues, etc.).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Avoid:&lt;/strong&gt; Do not use parent-level suppressions, as they mask future configuration drift.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;Result:&lt;/strong&gt; Required wildcards are documented and allowed, while accidental broad permissions continue to be reported.&lt;/p&gt;
&lt;h2&gt;Additional Resources&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/JohannesKonings/cdk-nag-custom-nag-pack?tab=readme-ov-file#granular-awssolutions-iam5-suppressions&quot;&gt;cdk-nag-custom-nag-pack - Granular IAM5 suppressions&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/cdklabs/cdk-nag&quot;&gt;cdk-nag documentation&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>Cleanup Resources from Ephemeral Stacks in AWS CDK with Aspects and Property Injectors</title><link>https://johanneskonings.dev/blog/2025-06-14-aws-cdk-ephemeral-stacks-cleanup/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2025-06-14-aws-cdk-ephemeral-stacks-cleanup/</guid><description>When developing features with ephemeral CDK stacks, the resource retention configuration needs to differ from test and production stacks. This article explains how to use CDK Aspects and Property Injectors to automatically clean up resources from ephemeral stacks.</description><pubDate>Sat, 14 Jun 2025 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Overview&lt;/h2&gt;
&lt;p&gt;When using AWS CDK, Aspects allow us to apply a &lt;code&gt;RemovalPolicy.DESTROY&lt;/code&gt; to all resources within an ephemeral stack. This works well for many resources but isn&apos;t sufficient for S3 buckets that require objects to be automatically deleted before the bucket itself can be removed.
With the newer Property Injectors feature, we can centrally set the &lt;code&gt;autoDeleteObjects&lt;/code&gt; property for S3 buckets based on the stack type (e.g., ephemeral vs. production). This enables the use of the same CDK code for test, production, and ephemeral stacks while ensuring appropriate cleanup or retention.&lt;/p&gt;
&lt;p&gt;CDK Property Injectors, in general, are described &lt;a href=&quot;https://dev.to/aws/aws-cdk-in-action-may-2025-empowered-deployments-governance-and-community-1gdb&quot;&gt;here&lt;/a&gt;.
CDK Aspects, particularly for setting the removal policy, are detailed &lt;a href=&quot;https://dev.to/aws-builders/enforcing-compliance-with-aws-cdk-aspects-1goo&quot;&gt;here&lt;/a&gt;.&lt;/p&gt;
&lt;h2&gt;Implementation&lt;/h2&gt;
&lt;p&gt;This post will demonstrate an example using a CloudWatch Log Group and an S3 Bucket. For long-lived stacks (like test and production), both resources should be retained even if the stack is destroyed. For ephemeral stacks, this default retention behavior will be overridden to ensure complete cleanup.&lt;/p&gt;
&lt;h2&gt;Aspects&lt;/h2&gt;
&lt;p&gt;Aspects are ideal for applying broad changes across constructs in a stack. Here, we&apos;ll use an Aspect to set the &lt;code&gt;RemovalPolicy&lt;/code&gt; for all resources.&lt;/p&gt;
&lt;p&gt;The Aspect looks like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { CfnResource, type IAspect, RemovalPolicy } from &quot;aws-cdk-lib&quot;; // Combined RemovalPolicy import
import type { IConstruct } from &quot;constructs&quot;;

export class DeletionPolicySetter implements IAspect {
  constructor(private readonly policy: RemovalPolicy) {}
  visit(node: IConstruct): void {
    if (node instanceof CfnResource) {
      node.applyRemovalPolicy(this.policy);
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;It will be applied in your CDK application like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Aspects.of(app).add(new DeletionPolicySetter(RemovalPolicy.DESTROY));
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Property Injection&lt;/h2&gt;
&lt;p&gt;While Aspects handle the general removal policy, S3 buckets need the &lt;code&gt;autoDeleteObjects&lt;/code&gt; property set to &lt;code&gt;true&lt;/code&gt; for automatic cleanup. Property Injectors are perfect for such targeted modifications.&lt;/p&gt;
&lt;p&gt;The Property Injector for S3 buckets looks like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import {
  InjectionContext,
  IPropertyInjector,
  RemovalPolicy,
} from &quot;aws-cdk-lib&quot;;
import { Bucket, BucketProps } from &quot;aws-cdk-lib/aws-s3&quot;;

export class BucketAutoDeletionSetter implements IPropertyInjector {
  public readonly constructUniqueId: string;

  constructor(private readonly autoDeleteObjects: boolean) {
    this.constructUniqueId = Bucket.PROPERTY_INJECTION_ID;
  }

  public inject(
    originalProps: BucketProps,
    _context: InjectionContext,
  ): BucketProps {
    return {
      ...originalProps,
      autoDeleteObjects: this.autoDeleteObjects,
      // If autoDeleteObjects is true, RemovalPolicy must be DESTROY.
      // Otherwise, retain the original policy.
      removalPolicy: this.autoDeleteObjects
        ? RemovalPolicy.DESTROY
        : originalProps.removalPolicy,
    };
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If &lt;code&gt;autoDeleteObjects&lt;/code&gt; is set to &lt;code&gt;true&lt;/code&gt;, the &lt;code&gt;removalPolicy&lt;/code&gt; must also be set to &lt;code&gt;RemovalPolicy.DESTROY&lt;/code&gt;; otherwise, the CDK will throw a validation error during synthesis.&lt;/p&gt;
&lt;p&gt;It will be applied in your CDK application like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;PropertyInjectors.of(app).add(new BucketAutoDeletionSetter(true));
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Example Stack Definition&lt;/h2&gt;
&lt;p&gt;The CDK constructs for the Log Group and the S3 Bucket are instantiated within a stack as follows:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { Stack, StackProps } from &quot;aws-cdk-lib&quot;;
import type { Construct } from &quot;constructs&quot;;
import { LogGroup, LogGroupProps } from &quot;aws-cdk-lib/aws-logs&quot;;
import { Bucket, BucketProps } from &quot;aws-cdk-lib/aws-s3&quot;;

export type StackMainProps = StackProps; // Can be extended with custom props

export class StackMain extends Stack {
  constructor(scope: Construct, id: string, props?: StackMainProps) {
    super(scope, id, props);

    // By default, LogGroup and Bucket have RemovalPolicy.RETAIN if not specified otherwise.
    // For production/test, this is desired. For ephemeral, Aspects/Injectors will override.
    new LogGroup(this, &quot;LogGroup&quot;); // Consider adding default props if needed, e.g., retention

    new Bucket(this, &quot;Bucket&quot;);
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;By default, &lt;code&gt;LogGroup&lt;/code&gt; and &lt;code&gt;Bucket&lt;/code&gt; constructs (when not further configured) often default to a &lt;code&gt;RemovalPolicy&lt;/code&gt; of &lt;code&gt;RETAIN&lt;/code&gt; or have specific behaviors (like S3 buckets not deleting if non-empty). This is generally what we want for test and production stacks. The Aspects and Property Injectors will override these for ephemeral stacks.&lt;/p&gt;
&lt;h2&gt;App Configuration&lt;/h2&gt;
&lt;p&gt;The &lt;code&gt;app.ts&lt;/code&gt; will check an environment variable, &lt;code&gt;EPHEMERAL_STACKS&lt;/code&gt;. If this variable is set to &lt;code&gt;&quot;true&quot;&lt;/code&gt;, it will apply the Aspects and Property Injectors via the &lt;code&gt;ephemeralStacksCleanup&lt;/code&gt; function to configure the stacks for automatic cleanup. This variable is typically set in CI/CD pipelines for feature branches or locally by developers.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;#!/usr/bin/env node
import type { Environment } from &quot;aws-cdk-lib&quot;;
import { App, Aspects, PropertyInjectors, RemovalPolicy } from &quot;aws-cdk-lib&quot;;
import { DeletionPolicySetter } from &quot;../lib/aspects/DeletionPolicySetter&quot;;
import { AwsSolutionsChecks } from &quot;cdk-nag&quot;; // Assuming cdk-nag is used for compliance checks
import { StackMain } from &quot;../lib/stacks/stack&quot;;
import { BucketAutoDeletionSetter } from &quot;../lib/propertyInjectors/BucketAutoDeletionSetter&quot;;

const app = new App();

const env: Environment = {
  account: process.env.APP_ACCOUNT,
  region: process.env.APP_REGION,
};

// Function to apply cleanup settings for ephemeral stacks
const ephemeralStacksCleanup = (targetApp: App) =&amp;gt; {
  // Pass app to scope Aspects/Injectors
  Aspects.of(targetApp).add(new DeletionPolicySetter(RemovalPolicy.DESTROY));
  PropertyInjectors.of(targetApp).add(new BucketAutoDeletionSetter(true));
};

const appMain = (isEphemeral: boolean) =&amp;gt; {
  if (isEphemeral) {
    ephemeralStacksCleanup(app); // Apply to the current app instance
  }

  new StackMain(app, &quot;StackMain&quot;, { env }); // Pass env to the stack

  // Apply general compliance checks (e.g., cdk-nag) after all other modifications
  Aspects.of(app).add(new AwsSolutionsChecks());
};

// Determine if building for ephemeral stacks
appMain(process.env.EPHEMERAL_STACKS === &quot;true&quot;);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;CloudFormation Results&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Ephemeral Stacks:&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;When &lt;code&gt;EPHEMERAL_STACKS&lt;/code&gt; is true, the generated CloudFormation will include &lt;code&gt;DeletionPolicy: Delete&lt;/code&gt; and auto-delete configurations for S3:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
  &quot;Resources&quot;: {
    &quot;LogGroupF5B46931&quot;: {
      &quot;Type&quot;: &quot;AWS::Logs::LogGroup&quot;,
      &quot;Properties&quot;: {
        &quot;RetentionInDays&quot;: 731 // Default retention, can also be configured via props or aspects
      },
      &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;, // This is also influenced by the RemovalPolicy
      &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/LogGroup/Resource&quot;
      }
    },
    &quot;Bucket83908E77&quot;: {
      &quot;Type&quot;: &quot;AWS::S3::Bucket&quot;,
      &quot;Properties&quot;: {
        &quot;Tags&quot;: [
          {
            &quot;Key&quot;: &quot;aws-cdk:auto-delete-objects&quot;,
            &quot;Value&quot;: &quot;true&quot;
          }
          // Other bucket properties...
        ]
      },
      &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;, // This is also influenced by the RemovalPolicy
      &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/Bucket/Resource&quot;
      }
    },
    &quot;BucketPolicyE9A3008A&quot;: {
      &quot;Type&quot;: &quot;AWS::S3::BucketPolicy&quot;,
      &quot;Properties&quot;: {
        &quot;Bucket&quot;: {
          &quot;Ref&quot;: &quot;Bucket83908E77&quot;
        },
        &quot;PolicyDocument&quot;: {
          &quot;Statement&quot;: [
            {
              &quot;Action&quot;: [
                &quot;s3:DeleteObject*&quot;,
                &quot;s3:GetBucket*&quot;,
                &quot;s3:List*&quot;,
                &quot;s3:PutBucketPolicy&quot;
              ],
              &quot;Effect&quot;: &quot;Allow&quot;,
              &quot;Principal&quot;: {
                &quot;AWS&quot;: {
                  &quot;Fn::GetAtt&quot;: [
                    &quot;CustomS3AutoDeleteObjectsCustomResourceProviderRole3B1BD092&quot;,
                    &quot;Arn&quot;
                  ]
                }
              },
              &quot;Resource&quot;: [
                {
                  &quot;Fn::GetAtt&quot;: [&quot;Bucket83908E77&quot;, &quot;Arn&quot;]
                },
                {
                  &quot;Fn::Join&quot;: [
                    &quot;&quot;,
                    [
                      {
                        &quot;Fn::GetAtt&quot;: [&quot;Bucket83908E77&quot;, &quot;Arn&quot;]
                      },
                      &quot;/*&quot;
                    ]
                  ]
                }
              ]
            }
          ],
          &quot;Version&quot;: &quot;2012-10-17&quot;
        }
      },
      &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;,
      &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/Bucket/Policy/Resource&quot;
      }
    },
    &quot;BucketAutoDeleteObjectsCustomResourceBAFD23C2&quot;: {
      &quot;Type&quot;: &quot;Custom::S3AutoDeleteObjects&quot;,
      &quot;Properties&quot;: {
        &quot;ServiceToken&quot;: {
          &quot;Fn::GetAtt&quot;: [
            &quot;CustomS3AutoDeleteObjectsCustomResourceProviderHandler9D90184F&quot;,
            &quot;Arn&quot;
          ]
        },
        &quot;BucketName&quot;: {
          &quot;Ref&quot;: &quot;Bucket83908E77&quot;
        }
      },
      &quot;DependsOn&quot;: [&quot;BucketPolicyE9A3008A&quot;],
      &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;,
      &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/Bucket/AutoDeleteObjectsCustomResource/Default&quot;
      }
    },
    &quot;CustomS3AutoDeleteObjectsCustomResourceProviderRole3B1BD092&quot;: {
      &quot;Type&quot;: &quot;AWS::IAM::Role&quot;,
      &quot;Properties&quot;: {
        &quot;AssumeRolePolicyDocument&quot;: {
          &quot;Version&quot;: &quot;2012-10-17&quot;,
          &quot;Statement&quot;: [
            {
              &quot;Action&quot;: &quot;sts:AssumeRole&quot;,
              &quot;Effect&quot;: &quot;Allow&quot;,
              &quot;Principal&quot;: {
                &quot;Service&quot;: &quot;lambda.amazonaws.com&quot;
              }
            }
          ]
        },
        &quot;ManagedPolicyArns&quot;: [
          {
            &quot;Fn::Sub&quot;: &quot;arn:${AWS::Partition}:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole&quot;
          }
        ]
      },
      &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;,
      &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/Custom::S3AutoDeleteObjectsCustomResourceProvider/Role&quot;
      }
    },
    &quot;CustomS3AutoDeleteObjectsCustomResourceProviderHandler9D90184F&quot;: {
      &quot;Type&quot;: &quot;AWS::Lambda::Function&quot;,
      &quot;Properties&quot;: {
        &quot;Code&quot;: {
          &quot;S3Bucket&quot;: {
            &quot;Fn::Sub&quot;: &quot;cdk-hnb659fds-assets-${AWS::AccountId}-${AWS::Region}&quot;
          },
          &quot;S3Key&quot;: &quot;faa95a81ae7d7373f3e1f242268f904eb748d8d0fdd306e8a6fe515a1905a7d6.zip&quot;
        },
        &quot;Timeout&quot;: 900,
        &quot;MemorySize&quot;: 128,
        &quot;Handler&quot;: &quot;index.handler&quot;,
        &quot;Role&quot;: {
          &quot;Fn::GetAtt&quot;: [
            &quot;CustomS3AutoDeleteObjectsCustomResourceProviderRole3B1BD092&quot;,
            &quot;Arn&quot;
          ]
        },
        &quot;Runtime&quot;: &quot;nodejs22.x&quot;,
        &quot;Description&quot;: {
          &quot;Fn::Join&quot;: [
            &quot;&quot;,
            [
              &quot;Lambda function for auto-deleting objects in &quot;,
              {
                &quot;Ref&quot;: &quot;Bucket83908E77&quot;
              },
              &quot; S3 bucket.&quot;
            ]
          ]
        }
      },
      &quot;DependsOn&quot;: [
        &quot;CustomS3AutoDeleteObjectsCustomResourceProviderRole3B1BD092&quot;
      ],
      &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;,
      &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/Custom::S3AutoDeleteObjectsCustomResourceProvider/Handler&quot;,
        &quot;aws:asset:path&quot;: &quot;asset.faa95a81ae7d7373f3e1f242268f904eb748d8d0fdd306e8a6fe515a1905a7d6&quot;,
        &quot;aws:asset:property&quot;: &quot;Code&quot;
      }
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The test and production stacks will have the same resources but with the removal policy set to &lt;code&gt;RemovalPolicy.RETAIN&lt;/code&gt; and the S3 bucket without the auto deletion.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
  &quot;Resources&quot;: {
    &quot;LogGroupF5B46931&quot;: {
      &quot;Type&quot;: &quot;AWS::Logs::LogGroup&quot;,
      &quot;Properties&quot;: {
        &quot;RetentionInDays&quot;: 731
      },
      &quot;UpdateReplacePolicy&quot;: &quot;Retain&quot;,
      &quot;DeletionPolicy&quot;: &quot;Retain&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/LogGroup/Resource&quot;
      }
    },
    &quot;Bucket83908E77&quot;: {
      &quot;Type&quot;: &quot;AWS::S3::Bucket&quot;,
      &quot;Properties&quot;: {},
      &quot;UpdateReplacePolicy&quot;: &quot;Retain&quot;,
      &quot;DeletionPolicy&quot;: &quot;Retain&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/Bucket/Resource&quot;
      }
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Additional Resources&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;https://dev.to/aws/aws-cdk-in-action-may-2025-empowered-deployments-governance-and-community-1gdb&lt;/li&gt;
&lt;li&gt;https://dev.to/aws-builders/enforcing-compliance-with-aws-cdk-aspects-1goo&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>Cleanup Resources from Ephemeral Stacks in AWS CDK with Aspects and Property Injectors</title><link>https://johanneskonings.dev/blog/2025-06-18-aws-application-signals-slo-cdk/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2025-06-18-aws-application-signals-slo-cdk/</guid><description>When developing features with ephemeral CDK stacks, the resource retention configuration needs to differ from test and production stacks. This article explains how to use CDK Aspects and Property Injectors to automatically clean up resources from ephemeral stacks.</description><pubDate>Sat, 14 Jun 2025 08:15:18 GMT</pubDate><content:encoded>&lt;ul&gt;
&lt;li&gt;https://github.com/aws/aws-cdk-rfcs/issues/673&lt;/li&gt;
&lt;li&gt;https://developer.mamezou-tech.com/en/blogs/2024/12/15/aws-app-signals-lambda/&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Overview&lt;/h2&gt;
&lt;p&gt;When using AWS CDK, Aspects allow us to apply a &lt;code&gt;RemovalPolicy.DESTROY&lt;/code&gt; to all resources within an ephemeral stack. This works well for many resources but isn&apos;t sufficient for S3 buckets that require objects to be automatically deleted before the bucket itself can be removed.
With the newer Property Injectors feature, we can centrally set the &lt;code&gt;autoDeleteObjects&lt;/code&gt; property for S3 buckets based on the stack type (e.g., ephemeral vs. production). This enables the use of the same CDK code for test, production, and ephemeral stacks while ensuring appropriate cleanup or retention.&lt;/p&gt;
&lt;p&gt;CDK Property Injectors, in general, are described &lt;a href=&quot;https://dev.to/aws/aws-cdk-in-action-may-2025-empowered-deployments-governance-and-community-1gdb&quot;&gt;here&lt;/a&gt;.
CDK Aspects, particularly for setting the removal policy, are detailed &lt;a href=&quot;https://dev.to/aws-builders/enforcing-compliance-with-aws-cdk-aspects-1goo&quot;&gt;here&lt;/a&gt;.&lt;/p&gt;
&lt;h2&gt;Implementation&lt;/h2&gt;
&lt;p&gt;This post will demonstrate an example using a CloudWatch Log Group and an S3 Bucket. For long-lived stacks (like test and production), both resources should be retained even if the stack is destroyed. For ephemeral stacks, this default retention behavior will be overridden to ensure complete cleanup.&lt;/p&gt;
&lt;h2&gt;Aspects&lt;/h2&gt;
&lt;p&gt;Aspects are ideal for applying broad changes across constructs in a stack. Here, we&apos;ll use an Aspect to set the &lt;code&gt;RemovalPolicy&lt;/code&gt; for all resources.&lt;/p&gt;
&lt;p&gt;The Aspect looks like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { CfnResource, type IAspect, RemovalPolicy } from &quot;aws-cdk-lib&quot;; // Combined RemovalPolicy import
import type { IConstruct } from &quot;constructs&quot;;

export class DeletionPolicySetter implements IAspect {
  constructor(private readonly policy: RemovalPolicy) {}
  visit(node: IConstruct): void {
    if (node instanceof CfnResource) {
      node.applyRemovalPolicy(this.policy);
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;It will be applied in your CDK application like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Aspects.of(app).add(new DeletionPolicySetter(RemovalPolicy.DESTROY));
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Property Injection&lt;/h2&gt;
&lt;p&gt;While Aspects handle the general removal policy, S3 buckets need the &lt;code&gt;autoDeleteObjects&lt;/code&gt; property set to &lt;code&gt;true&lt;/code&gt; for automatic cleanup. Property Injectors are perfect for such targeted modifications.&lt;/p&gt;
&lt;p&gt;The Property Injector for S3 buckets looks like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import {
  InjectionContext,
  IPropertyInjector,
  RemovalPolicy,
} from &quot;aws-cdk-lib&quot;;
import { Bucket, BucketProps } from &quot;aws-cdk-lib/aws-s3&quot;;

export class BucketAutoDeletionSetter implements IPropertyInjector {
  public readonly constructUniqueId: string;

  constructor(private readonly autoDeleteObjects: boolean) {
    this.constructUniqueId = Bucket.PROPERTY_INJECTION_ID;
  }

  public inject(
    originalProps: BucketProps,
    _context: InjectionContext,
  ): BucketProps {
    return {
      ...originalProps,
      autoDeleteObjects: this.autoDeleteObjects,
      // If autoDeleteObjects is true, RemovalPolicy must be DESTROY.
      // Otherwise, retain the original policy.
      removalPolicy: this.autoDeleteObjects
        ? RemovalPolicy.DESTROY
        : originalProps.removalPolicy,
    };
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If &lt;code&gt;autoDeleteObjects&lt;/code&gt; is set to &lt;code&gt;true&lt;/code&gt;, the &lt;code&gt;removalPolicy&lt;/code&gt; must also be set to &lt;code&gt;RemovalPolicy.DESTROY&lt;/code&gt;; otherwise, the CDK will throw a validation error during synthesis.&lt;/p&gt;
&lt;p&gt;It will be applied in your CDK application like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;PropertyInjectors.of(app).add(new BucketAutoDeletionSetter(true));
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Example Stack Definition&lt;/h2&gt;
&lt;p&gt;The CDK constructs for the Log Group and the S3 Bucket are instantiated within a stack as follows:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { Stack, StackProps } from &quot;aws-cdk-lib&quot;;
import type { Construct } from &quot;constructs&quot;;
import { LogGroup, LogGroupProps } from &quot;aws-cdk-lib/aws-logs&quot;;
import { Bucket, BucketProps } from &quot;aws-cdk-lib/aws-s3&quot;;

export type StackMainProps = StackProps; // Can be extended with custom props

export class StackMain extends Stack {
  constructor(scope: Construct, id: string, props?: StackMainProps) {
    super(scope, id, props);

    // By default, LogGroup and Bucket have RemovalPolicy.RETAIN if not specified otherwise.
    // For production/test, this is desired. For ephemeral, Aspects/Injectors will override.
    new LogGroup(this, &quot;LogGroup&quot;); // Consider adding default props if needed, e.g., retention

    new Bucket(this, &quot;Bucket&quot;);
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;By default, &lt;code&gt;LogGroup&lt;/code&gt; and &lt;code&gt;Bucket&lt;/code&gt; constructs (when not further configured) often default to a &lt;code&gt;RemovalPolicy&lt;/code&gt; of &lt;code&gt;RETAIN&lt;/code&gt; or have specific behaviors (like S3 buckets not deleting if non-empty). This is generally what we want for test and production stacks. The Aspects and Property Injectors will override these for ephemeral stacks.&lt;/p&gt;
&lt;h2&gt;App Configuration&lt;/h2&gt;
&lt;p&gt;The &lt;code&gt;app.ts&lt;/code&gt; will check an environment variable, &lt;code&gt;EPHEMERAL_STACKS&lt;/code&gt;. If this variable is set to &lt;code&gt;&quot;true&quot;&lt;/code&gt;, it will apply the Aspects and Property Injectors via the &lt;code&gt;ephemeralStacksCleanup&lt;/code&gt; function to configure the stacks for automatic cleanup. This variable is typically set in CI/CD pipelines for feature branches or locally by developers.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;#!/usr/bin/env node
import type { Environment } from &quot;aws-cdk-lib&quot;;
import { App, Aspects, PropertyInjectors, RemovalPolicy } from &quot;aws-cdk-lib&quot;;
import { DeletionPolicySetter } from &quot;../lib/aspects/DeletionPolicySetter&quot;;
import { AwsSolutionsChecks } from &quot;cdk-nag&quot;; // Assuming cdk-nag is used for compliance checks
import { StackMain } from &quot;../lib/stacks/stack&quot;;
import { BucketAutoDeletionSetter } from &quot;../lib/propertyInjectors/BucketAutoDeletionSetter&quot;;

const app = new App();

const env: Environment = {
  account: process.env.APP_ACCOUNT,
  region: process.env.APP_REGION,
};

// Function to apply cleanup settings for ephemeral stacks
const ephemeralStacksCleanup = (targetApp: App) =&amp;gt; {
  // Pass app to scope Aspects/Injectors
  Aspects.of(targetApp).add(new DeletionPolicySetter(RemovalPolicy.DESTROY));
  PropertyInjectors.of(targetApp).add(new BucketAutoDeletionSetter(true));
};

const appMain = (isEphemeral: boolean) =&amp;gt; {
  if (isEphemeral) {
    ephemeralStacksCleanup(app); // Apply to the current app instance
  }

  new StackMain(app, &quot;StackMain&quot;, { env }); // Pass env to the stack

  // Apply general compliance checks (e.g., cdk-nag) after all other modifications
  Aspects.of(app).add(new AwsSolutionsChecks());
};

// Determine if building for ephemeral stacks
appMain(process.env.EPHEMERAL_STACKS === &quot;true&quot;);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;CloudFormation Results&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Ephemeral Stacks:&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;When &lt;code&gt;EPHEMERAL_STACKS&lt;/code&gt; is true, the generated CloudFormation will include &lt;code&gt;DeletionPolicy: Delete&lt;/code&gt; and auto-delete configurations for S3:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
  &quot;Resources&quot;: {
    &quot;LogGroupF5B46931&quot;: {
      &quot;Type&quot;: &quot;AWS::Logs::LogGroup&quot;,
      &quot;Properties&quot;: {
        &quot;RetentionInDays&quot;: 731 // Default retention, can also be configured via props or aspects
      },
      &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;, // This is also influenced by the RemovalPolicy
      &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/LogGroup/Resource&quot;
      }
    },
    &quot;Bucket83908E77&quot;: {
      &quot;Type&quot;: &quot;AWS::S3::Bucket&quot;,
      &quot;Properties&quot;: {
        &quot;Tags&quot;: [
          {
            &quot;Key&quot;: &quot;aws-cdk:auto-delete-objects&quot;,
            &quot;Value&quot;: &quot;true&quot;
          }
          // Other bucket properties...
        ]
      },
      &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;, // This is also influenced by the RemovalPolicy
      &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/Bucket/Resource&quot;
      }
    },
    &quot;BucketPolicyE9A3008A&quot;: {
      &quot;Type&quot;: &quot;AWS::S3::BucketPolicy&quot;,
      &quot;Properties&quot;: {
        &quot;Bucket&quot;: {
          &quot;Ref&quot;: &quot;Bucket83908E77&quot;
        },
        &quot;PolicyDocument&quot;: {
          &quot;Statement&quot;: [
            {
              &quot;Action&quot;: [
                &quot;s3:DeleteObject*&quot;,
                &quot;s3:GetBucket*&quot;,
                &quot;s3:List*&quot;,
                &quot;s3:PutBucketPolicy&quot;
              ],
              &quot;Effect&quot;: &quot;Allow&quot;,
              &quot;Principal&quot;: {
                &quot;AWS&quot;: {
                  &quot;Fn::GetAtt&quot;: [
                    &quot;CustomS3AutoDeleteObjectsCustomResourceProviderRole3B1BD092&quot;,
                    &quot;Arn&quot;
                  ]
                }
              },
              &quot;Resource&quot;: [
                {
                  &quot;Fn::GetAtt&quot;: [&quot;Bucket83908E77&quot;, &quot;Arn&quot;]
                },
                {
                  &quot;Fn::Join&quot;: [
                    &quot;&quot;,
                    [
                      {
                        &quot;Fn::GetAtt&quot;: [&quot;Bucket83908E77&quot;, &quot;Arn&quot;]
                      },
                      &quot;/*&quot;
                    ]
                  ]
                }
              ]
            }
          ],
          &quot;Version&quot;: &quot;2012-10-17&quot;
        }
      },
      &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;,
      &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/Bucket/Policy/Resource&quot;
      }
    },
    &quot;BucketAutoDeleteObjectsCustomResourceBAFD23C2&quot;: {
      &quot;Type&quot;: &quot;Custom::S3AutoDeleteObjects&quot;,
      &quot;Properties&quot;: {
        &quot;ServiceToken&quot;: {
          &quot;Fn::GetAtt&quot;: [
            &quot;CustomS3AutoDeleteObjectsCustomResourceProviderHandler9D90184F&quot;,
            &quot;Arn&quot;
          ]
        },
        &quot;BucketName&quot;: {
          &quot;Ref&quot;: &quot;Bucket83908E77&quot;
        }
      },
      &quot;DependsOn&quot;: [&quot;BucketPolicyE9A3008A&quot;],
      &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;,
      &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/Bucket/AutoDeleteObjectsCustomResource/Default&quot;
      }
    },
    &quot;CustomS3AutoDeleteObjectsCustomResourceProviderRole3B1BD092&quot;: {
      &quot;Type&quot;: &quot;AWS::IAM::Role&quot;,
      &quot;Properties&quot;: {
        &quot;AssumeRolePolicyDocument&quot;: {
          &quot;Version&quot;: &quot;2012-10-17&quot;,
          &quot;Statement&quot;: [
            {
              &quot;Action&quot;: &quot;sts:AssumeRole&quot;,
              &quot;Effect&quot;: &quot;Allow&quot;,
              &quot;Principal&quot;: {
                &quot;Service&quot;: &quot;lambda.amazonaws.com&quot;
              }
            }
          ]
        },
        &quot;ManagedPolicyArns&quot;: [
          {
            &quot;Fn::Sub&quot;: &quot;arn:${AWS::Partition}:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole&quot;
          }
        ]
      },
      &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;,
      &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/Custom::S3AutoDeleteObjectsCustomResourceProvider/Role&quot;
      }
    },
    &quot;CustomS3AutoDeleteObjectsCustomResourceProviderHandler9D90184F&quot;: {
      &quot;Type&quot;: &quot;AWS::Lambda::Function&quot;,
      &quot;Properties&quot;: {
        &quot;Code&quot;: {
          &quot;S3Bucket&quot;: {
            &quot;Fn::Sub&quot;: &quot;cdk-hnb659fds-assets-${AWS::AccountId}-${AWS::Region}&quot;
          },
          &quot;S3Key&quot;: &quot;faa95a81ae7d7373f3e1f242268f904eb748d8d0fdd306e8a6fe515a1905a7d6.zip&quot;
        },
        &quot;Timeout&quot;: 900,
        &quot;MemorySize&quot;: 128,
        &quot;Handler&quot;: &quot;index.handler&quot;,
        &quot;Role&quot;: {
          &quot;Fn::GetAtt&quot;: [
            &quot;CustomS3AutoDeleteObjectsCustomResourceProviderRole3B1BD092&quot;,
            &quot;Arn&quot;
          ]
        },
        &quot;Runtime&quot;: &quot;nodejs22.x&quot;,
        &quot;Description&quot;: {
          &quot;Fn::Join&quot;: [
            &quot;&quot;,
            [
              &quot;Lambda function for auto-deleting objects in &quot;,
              {
                &quot;Ref&quot;: &quot;Bucket83908E77&quot;
              },
              &quot; S3 bucket.&quot;
            ]
          ]
        }
      },
      &quot;DependsOn&quot;: [
        &quot;CustomS3AutoDeleteObjectsCustomResourceProviderRole3B1BD092&quot;
      ],
      &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;,
      &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/Custom::S3AutoDeleteObjectsCustomResourceProvider/Handler&quot;,
        &quot;aws:asset:path&quot;: &quot;asset.faa95a81ae7d7373f3e1f242268f904eb748d8d0fdd306e8a6fe515a1905a7d6&quot;,
        &quot;aws:asset:property&quot;: &quot;Code&quot;
      }
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The test and production stacks will have the same resources but with the removal policy set to &lt;code&gt;RemovalPolicy.RETAIN&lt;/code&gt; and the S3 bucket without the auto deletion.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
  &quot;Resources&quot;: {
    &quot;LogGroupF5B46931&quot;: {
      &quot;Type&quot;: &quot;AWS::Logs::LogGroup&quot;,
      &quot;Properties&quot;: {
        &quot;RetentionInDays&quot;: 731
      },
      &quot;UpdateReplacePolicy&quot;: &quot;Retain&quot;,
      &quot;DeletionPolicy&quot;: &quot;Retain&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/LogGroup/Resource&quot;
      }
    },
    &quot;Bucket83908E77&quot;: {
      &quot;Type&quot;: &quot;AWS::S3::Bucket&quot;,
      &quot;Properties&quot;: {},
      &quot;UpdateReplacePolicy&quot;: &quot;Retain&quot;,
      &quot;DeletionPolicy&quot;: &quot;Retain&quot;,
      &quot;Metadata&quot;: {
        &quot;aws:cdk:path&quot;: &quot;StackMain/Bucket/Resource&quot;
      }
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Additional Resources&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;https://dev.to/aws/aws-cdk-in-action-may-2025-empowered-deployments-governance-and-community-1gdb&lt;/li&gt;
&lt;li&gt;https://dev.to/aws-builders/enforcing-compliance-with-aws-cdk-aspects-1goo&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>Suppress cdk-nag findings for custom resource singleton lambda globally</title><link>https://johanneskonings.dev/blog/2025-01-12-aws-cdk-nag-custom-resource-singleton-suppression/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2025-01-12-aws-cdk-nag-custom-resource-singleton-suppression/</guid><description>This post describes how to suppress cdk-nag findings that are caused by the custom resource singleton Lambda function globally, so that findings which are out of your own control are not reported.</description><pubDate>Sun, 12 Jan 2025 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Overview&lt;/h2&gt;
&lt;p&gt;When using AWS CDK with custom resources, CDK creates a singleton Lambda function that handles all custom resource operations. This Lambda function can trigger several cdk-nag findings, even if you&apos;re not directly using custom resources in your stack. This article explains how to properly suppress these findings globally.&lt;/p&gt;
&lt;h2&gt;Understanding the Problem&lt;/h2&gt;
&lt;h3&gt;How CDK Uses Custom Resources&lt;/h3&gt;
&lt;p&gt;CDK uses a shared Lambda function to handle all custom resources in your stack. Here&apos;s a typical example of creating a custom resource:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;new AwsCustomResource(this, &quot;GetParameter&quot;, {
  onUpdate: {
    service: &quot;SSM&quot;,
    action: &quot;GetParameter&quot;,
    parameters: {
      Name: &quot;my-parameter&quot;,
      WithDecryption: true,
    },
    physicalResourceId: PhysicalResourceId.of(Date.now().toString()),
  },
  functionName: &quot;GetParameter&quot;,
  policy: AwsCustomResourcePolicy.fromSdkCalls({
    resources: AwsCustomResourcePolicy.ANY_RESOURCE,
  }),
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Generated CloudFormation Components&lt;/h3&gt;
&lt;p&gt;When CDK synthesizes this code, it creates several CloudFormation resources:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;The custom resource itself&lt;/li&gt;
&lt;li&gt;An IAM policy for the custom resource&lt;/li&gt;
&lt;li&gt;An IAM role for the singleton Lambda function&lt;/li&gt;
&lt;li&gt;The singleton Lambda function&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code&gt;  # The custom resource, which calling the singleton lambda function
  &quot;GetParameter42B0A00E&quot;: {
   &quot;Type&quot;: &quot;Custom::AWS&quot;,
   &quot;Properties&quot;: {
    &quot;ServiceToken&quot;: {
     &quot;Fn::GetAtt&quot;: [
      &quot;AWS679f53fac002430cb0da5b7982bd22872D164C4C&quot;,
      &quot;Arn&quot;
     ]
    },
    &quot;Create&quot;: &quot;{\&quot;service\&quot;:\&quot;SSM\&quot;,\&quot;action\&quot;:\&quot;GetParameter\&quot;,\&quot;parameters\&quot;:{\&quot;Name\&quot;:\&quot;my-parameter\&quot;,\&quot;WithDecryption\&quot;:true},\&quot;physicalResourceId\&quot;:{\&quot;id\&quot;:\&quot;1736701660301\&quot;}}&quot;,
    &quot;Update&quot;: &quot;{\&quot;service\&quot;:\&quot;SSM\&quot;,\&quot;action\&quot;:\&quot;GetParameter\&quot;,\&quot;parameters\&quot;:{\&quot;Name\&quot;:\&quot;my-parameter\&quot;,\&quot;WithDecryption\&quot;:true},\&quot;physicalResourceId\&quot;:{\&quot;id\&quot;:\&quot;1736701660301\&quot;}}&quot;,
    &quot;InstallLatestAwsSdk&quot;: false
   },
   &quot;DependsOn&quot;: [
    &quot;GetParameterCustomResourcePolicyD8E5D455&quot;
   ],
   &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;,
   &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
   &quot;Metadata&quot;: {
    &quot;aws:cdk:path&quot;: &quot;StackMain/GetParameter/Resource/Default&quot;
   }
  },
  # The policy which is attached to the singleton lambda function role
  &quot;GetParameterCustomResourcePolicyD8E5D455&quot;: {
   &quot;Type&quot;: &quot;AWS::IAM::Policy&quot;,
   &quot;Properties&quot;: {
    &quot;PolicyDocument&quot;: {
     &quot;Statement&quot;: [
      {
       &quot;Action&quot;: &quot;ssm:GetParameter&quot;,
       &quot;Effect&quot;: &quot;Allow&quot;,
       &quot;Resource&quot;: &quot;*&quot;
      }
     ],
     &quot;Version&quot;: &quot;2012-10-17&quot;
    },
    &quot;PolicyName&quot;: &quot;GetParameterCustomResourcePolicyD8E5D455&quot;,
    &quot;Roles&quot;: [
     {
      &quot;Ref&quot;: &quot;AWS679f53fac002430cb0da5b7982bd2287ServiceRoleC1EA0FF2&quot;
     }
    ]
   },
   &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;,
   &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
   &quot;Metadata&quot;: {
    &quot;aws:cdk:path&quot;: &quot;StackMain/GetParameter/CustomResourcePolicy/Resource&quot;
   }
  },
  # The role for the singleton lambda function
  # Causing the finding AWSSolution-IAM4, because of the used managed policy
  &quot;AWS679f53fac002430cb0da5b7982bd2287ServiceRoleC1EA0FF2&quot;: {
   &quot;Type&quot;: &quot;AWS::IAM::Role&quot;,
   &quot;Properties&quot;: {
    &quot;AssumeRolePolicyDocument&quot;: {
     &quot;Statement&quot;: [
      {
       &quot;Action&quot;: &quot;sts:AssumeRole&quot;,
       &quot;Effect&quot;: &quot;Allow&quot;,
       &quot;Principal&quot;: {
        &quot;Service&quot;: &quot;lambda.amazonaws.com&quot;
       }
      }
     ],
     &quot;Version&quot;: &quot;2012-10-17&quot;
    },
    &quot;ManagedPolicyArns&quot;: [
     {
      &quot;Fn::Join&quot;: [
       &quot;&quot;,
       [
        &quot;arn:&quot;,
        {
         &quot;Ref&quot;: &quot;AWS::Partition&quot;
        },
        &quot;:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole&quot;
       ]
      ]
     }
    ]
   },
   &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;,
   &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
   &quot;Metadata&quot;: {
    &quot;aws:cdk:path&quot;: &quot;StackMain/AWS679f53fac002430cb0da5b7982bd2287/ServiceRole/Resource&quot;
   }
  },
  # The lambda function which is used for all custom resources
  &quot;AWS679f53fac002430cb0da5b7982bd22872D164C4C&quot;: {
   &quot;Type&quot;: &quot;AWS::Lambda::Function&quot;,
   &quot;Properties&quot;: {
    &quot;Code&quot;: {
     &quot;S3Bucket&quot;: {
      &quot;Fn::Sub&quot;: &quot;cdk-hnb659fds-assets-${AWS::AccountId}-${AWS::Region}&quot;
     },
     &quot;S3Key&quot;: &quot;ce2f3595a340d6c519a65888ef97e3b9b64f053f83608e32cc28162e22d7d99a.zip&quot;
    },
    &quot;FunctionName&quot;: &quot;GetParameter&quot;,
    &quot;Handler&quot;: &quot;index.handler&quot;,
    &quot;Role&quot;: {
     &quot;Fn::GetAtt&quot;: [
      &quot;AWS679f53fac002430cb0da5b7982bd2287ServiceRoleC1EA0FF2&quot;,
      &quot;Arn&quot;
     ]
    },
    &quot;Runtime&quot;: {
     &quot;Fn::FindInMap&quot;: [
      &quot;LatestNodeRuntimeMap&quot;,
      {
       &quot;Ref&quot;: &quot;AWS::Region&quot;
      },
      &quot;value&quot;
     ]
    },
    &quot;Timeout&quot;: 120
   },
   &quot;DependsOn&quot;: [
    &quot;AWS679f53fac002430cb0da5b7982bd2287ServiceRoleC1EA0FF2&quot;
   ],
   &quot;UpdateReplacePolicy&quot;: &quot;Delete&quot;,
   &quot;DeletionPolicy&quot;: &quot;Delete&quot;,
   &quot;Metadata&quot;: {
    &quot;aws:cdk:path&quot;: &quot;StackMain/AWS679f53fac002430cb0da5b7982bd2287/Resource&quot;,
    &quot;aws:asset:path&quot;: &quot;asset.ce2f3595a340d6c519a65888ef97e3b9b64f053f83608e32cc28162e22d7d99a&quot;,
    &quot;aws:asset:is-bundled&quot;: false,
    &quot;aws:asset:property&quot;: &quot;Code&quot;
   }
  },
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This will be the linked resources if there a two custom resources in the stack.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./custom-resources.png&quot; alt=&quot;Diagramm Cloudforamtion Template&quot; /&gt;&lt;/p&gt;
&lt;h3&gt;cdk-nag Findings&lt;/h3&gt;
&lt;p&gt;This configuration triggers three main findings from cdk-nag:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;[Error at /StackMain/ApplicationsSignals/GetParameter/CustomResourcePolicy/Resource] AwsSolutions-IAM5[Resource::*]: The IAM entity contains wildcard permissions and does not have a cdk-nag rule suppression with evidence for those permission.

[Error at /StackMain/AWS679f53fac002430cb0da5b7982bd2287/ServiceRole/Resource] AwsSolutions-IAM4[Policy::arn:&amp;lt;AWS::Partition&amp;gt;:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole]: The IAM user, role, or group uses AWS managed policies.

[Warning at /StackMain/AWS679f53fac002430cb0da5b7982bd2287/Resource] CdkNagValidationFailure[AwsSolutions-L1]: &apos;AwsSolutions-L1&apos; threw an error during validation.
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Why Resource-Level Suppression Isn&apos;t Enough&lt;/h3&gt;
&lt;p&gt;A simple resource-level suppression like this only handles the &lt;code&gt;AwsSolutions-IAM5&lt;/code&gt; finding:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;NagSuppressions.addResourceSuppressions(
  customResource,
  [
    {
      id: &quot;AwsSolutions-IAM5&quot;,
      reason: &quot;Policy Creation derived from SdkCalls are OK&quot;,
    },
  ],
  true,
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This doesn&apos;t work for the other findings because the singleton Lambda function is attached to the root of the stack and requires a different suppression approach.&lt;/p&gt;
&lt;h2&gt;The Solution&lt;/h2&gt;
&lt;h3&gt;Overview&lt;/h3&gt;
&lt;p&gt;To properly suppress all findings related to the custom resource singleton Lambda, we need to:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Identify all possible paths where the custom resource creates resources&lt;/li&gt;
&lt;li&gt;Check if these paths exist in our stack&lt;/li&gt;
&lt;li&gt;Apply suppressions only to existing paths&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Implementation&lt;/h3&gt;
&lt;p&gt;Here&apos;s the complete solution:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const suppressions: NagPackSuppression[] = [
  {
    id: &quot;AwsSolutions-IAM4&quot;,
    reason: &quot;Custom resource singleton lambda&quot;,
  },
  {
    id: &quot;AwsSolutions-L1&quot;,
    reason: &quot;Custom resource singleton lambda&quot;,
  },
];

const stack = Stack.of(this);
// The UUID is a fixed value used by CDK for custom resources
const customResourceId = `AWS${AwsCustomResource.PROVIDER_FUNCTION_UUID.split(&quot;-&quot;).join(&quot;&quot;)}`;
const stackName = stack.stackName;

// Define all possible paths that could trigger cdk-nag findings
const customResourceSuppressPaths = new Set([
  `/${stackName}/${customResourceId}/ServiceRole/Resource`,
  `/${stackName}/${customResourceId}/Resource`,
]);

// Get all existing paths in the stack
const allExistingPaths = new Set(
  stack.node.findAll().map((node) =&amp;gt; `/${node.node.path}`),
);

// Apply suppressions only to paths that exist
for (const path of customResourceSuppressPaths) {
  if (allExistingPaths.has(path)) {
    NagSuppressions.addResourceSuppressionsByPath(
      stack,
      path,
      suppressions,
      true,
    );
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Benefits&lt;/h3&gt;
&lt;p&gt;This solution:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Handles all cdk-nag findings related to the singleton Lambda&lt;/li&gt;
&lt;li&gt;Works whether you&apos;re directly using custom resources or not&lt;/li&gt;
&lt;li&gt;Prevents errors from trying to suppress non-existent paths&lt;/li&gt;
&lt;li&gt;Can be implemented centrally in your stack&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Additional Resources&lt;/h2&gt;
&lt;p&gt;If you&apos;re using a custom &lt;a href=&quot;https://github.com/cdklabs/cdk-nag/blob/main/docs/NagPack.md&quot;&gt;NagPack&lt;/a&gt;, you can find implementation examples &lt;a href=&quot;https://github.com/JohannesKonings/cdk-nag-custom-nag-pack?tab=readme-ov-file#custom-resource&quot;&gt;here&lt;/a&gt;.&lt;/p&gt;
</content:encoded></item><item><title>Configure AWS CloudWatch Application Signals Transaction Search with CDK</title><link>https://johanneskonings.dev/blog/2025-01-02-aws-application-signals-config-transaction-search-cdk/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2025-01-02-aws-application-signals-config-transaction-search-cdk/</guid><description>Learn how to enable and configure AWS CloudWatch Application Signals Transaction Search using the AWS CDK, including setting up the required IAM policies and X-Ray configurations.</description><pubDate>Tue, 07 Jan 2025 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Update 2025-06-18: Provisioning with CloudFormation&lt;/h2&gt;
&lt;p&gt;Recently, AWS added native CloudFormation support for provisioning Application Signals Transaction Search. You can find the official documentation here:&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch-Transaction-Search-Cloudformation.html&quot;&gt;https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch-Transaction-Search-Cloudformation.html&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;Here is an example of how to implement it in the AWS CDK:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { CfnTransactionSearchConfig } from &quot;aws-cdk-lib/aws-xray&quot;;
import { CfnResourcePolicy } from &quot;aws-cdk-lib/aws-logs&quot;;

const { account, partition, region } = Stack.of(this);

const transactionSearchAccess = new CfnResourcePolicy(
  this,
  &quot;XRayLogResourcePolicy&quot;,
  {
    policyName: &quot;TransactionSearchAccess&quot;,
    policyDocument: JSON.stringify({
      Version: &quot;2012-10-17&quot;,
      Statement: [
        {
          Sid: &quot;TransactionSearchXRayAccess&quot;,
          Effect: &quot;Allow&quot;,
          Principal: {
            Service: &quot;xray.amazonaws.com&quot;,
          },
          Action: &quot;logs:PutLogEvents&quot;,
          Resource: [
            `arn:${partition}:logs:${region}:${account}:log-group:aws/spans:*`,
            `arn:${partition}:logs:${region}:${account}:log-group:/aws/application-signals/data:*`,
          ],
          Condition: {
            ArnLike: {
              &quot;aws:SourceArn&quot;: `arn:${partition}:xray:${region}:${account}:*`,
            },
            StringEquals: {
              &quot;aws:SourceAccount&quot;: account,
            },
          },
        },
      ],
    }),
  },
);

const transactionSearchConfig = new CfnTransactionSearchConfig(
  this,
  &quot;XRayTransactionSearchConfig&quot;,
  {
    indexingPercentage: 100,
  },
);

transactionSearchConfig.node.addDependency(transactionSearchAccess);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;⚠️ To avoid the following error, you must first update the X-Ray segment destination to &lt;code&gt;XRay&lt;/code&gt; before creating the Transaction Search Config again with CloudFormation:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;aws xray update-trace-segment-destination --destination XRay
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Without this step, you may encounter this error:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;1:24:42 PM | CREATE_FAILED        | AWS::XRay::TransactionSearchConfig             | ApplicationsSignal...archConfigF88EF961
Resource handler returned message: &quot;null&quot; (RequestToken: 94079df2-4a8f-2771-a3e4-03eb0ccbc436, HandlerErrorCode: AlreadyExists)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;em&gt;(The rest of this post describes the original method using custom resources, which is still useful for understanding the underlying API calls.)&lt;/em&gt;&lt;/p&gt;
&lt;h2&gt;Use Case&lt;/h2&gt;
&lt;p&gt;This guide is for you if you want to use &lt;a href=&quot;https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch-Application-Monitoring-Sections.html&quot;&gt;AWS CloudWatch Application Signals Transaction Search&lt;/a&gt; and your Infrastructure as Code (IaC) is based on the AWS CDK.&lt;/p&gt;
&lt;h2&gt;Setup&lt;/h2&gt;
&lt;p&gt;This is how the Transaction Search page looks when it&apos;s not yet configured:&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./application-signals-transaction-search-without-config.png&quot; alt=&quot;application signals transaction search without config&quot; /&gt;&lt;/p&gt;
&lt;p&gt;And this is how it appears in the X-Ray settings:&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./application-signals-transaction-search-without-xray-config.png&quot; alt=&quot;application signals transaction search without config X-Ray&quot; /&gt;&lt;/p&gt;
&lt;p&gt;The official AWS documentation outlines the steps to enable Transaction Search here: &lt;a href=&quot;https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch-Transaction-Search-getting-started.html#w24aac24c21c13b9&quot;&gt;https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch-Transaction-Search-getting-started.html#w24aac24c21c13b9&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;While the documentation provides AWS CLI commands, this post will show you how to implement the same configuration using AWS CDK Custom Resources for a true Infrastructure as Code approach.&lt;/p&gt;
&lt;p&gt;The complete CDK implementation using custom resources looks like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const { account, region } = Stack.of(this);

// https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch-Transaction-Search-getting-started.html#w24aac24c21c13b9
const applicationSignalsTransactionSearchLogsResourcePolicy =
  new AwsCustomResource(
    this,
    &quot;ApplicationSignalsTransactionSearchLogsResourcePolicy&quot;,
    {
      onCreate: {
        service: &quot;@aws-sdk/client-cloudwatch-logs&quot;,
        action: &quot;PutResourcePolicy&quot;,
        parameters: {
          policyName: &quot;ApplicationSignalsTransactionSearchLogsResourcePolicy&quot;,
          policyDocument: JSON.stringify({
            Version: &quot;2012-10-17&quot;,
            Statement: [
              {
                Sid: &quot;TransactionSearchXRayAccess&quot;,
                Effect: &quot;Allow&quot;,
                Principal: {
                  Service: &quot;xray.amazonaws.com&quot;,
                },
                Action: [&quot;logs:PutLogEvents&quot;, &quot;logs:CreateLogStream&quot;],
                Resource: [
                  `arn:aws:logs:${region}:${account}:log-group:aws/spans:*`,
                  `arn:aws:logs:${region}:${account}:log-group:/aws/application-signals/data:*`,
                ],
                Condition: {
                  ArnLike: {
                    &quot;aws:SourceArn&quot;: `arn:aws:xray:${region}:${account}:*`,
                  },
                  StringEquals: {
                    &quot;aws:SourceAccount&quot;: account,
                  },
                },
              },
            ],
          }),
        },
        physicalResourceId: PhysicalResourceId.of(
          &quot;ApplicationSignalsTransactionSearchLogsResourcePolicy&quot;,
        ),
      },
      policy: AwsCustomResourcePolicy.fromSdkCalls({
        resources: AwsCustomResourcePolicy.ANY_RESOURCE,
      }),
    },
  );
const applicationSignalsTransactionSearchXraySegmentDestination =
  new AwsCustomResource(
    this,
    &quot;ApplicationSignalsTransactionSearchXraySegmentDestination&quot;,
    {
      onCreate: {
        service: &quot;@aws-sdk/client-xray&quot;,
        action: &quot;UpdateTraceSegmentDestination&quot;,
        parameters: {
          Destination: &quot;CloudWatchLogs&quot;,
        },
        physicalResourceId: PhysicalResourceId.of(
          &quot;ApplicationSignalsTransactionSearchXraySegmentDestination&quot;,
        ),
      },
      installLatestAwsSdk: true,
      policy: AwsCustomResourcePolicy.fromStatements([
        new PolicyStatement({
          effect: Effect.ALLOW,
          actions: [&quot;logs:PutRetentionPolicy&quot;],
          resources: [
            `arn:aws:logs:${region}:${account}:log-group:aws/spans:log-stream:`,
          ],
        }),
        new PolicyStatement({
          effect: Effect.ALLOW,
          actions: [&quot;xray:UpdateTraceSegmentDestination&quot;],
          resources: [&quot;*&quot;],
        }),
      ]),
    },
  );
applicationSignalsTransactionSearchXraySegmentDestination.node.addDependency(
  applicationSignalsTransactionSearchLogsResourcePolicy,
);
const applicationSignalsTransactionSearchXrayIndexRule = new AwsCustomResource(
  this,
  &quot;ApplicationSignalsTransactionSearchXrayIndexRule&quot;,
  {
    onCreate: {
      service: &quot;@aws-sdk/client-xray&quot;,
      action: &quot;UpdateIndexingRule&quot;,
      parameters: {
        Name: &quot;Default&quot;,
        Rule: {
          Probabilistic: {
            DesiredSamplingPercentage: 100,
          },
        },
      },
      physicalResourceId: PhysicalResourceId.of(
        &quot;ApplicationSignalsTransactionSearchXrayIndexRule&quot;,
      ),
    },
    installLatestAwsSdk: true,
    policy: AwsCustomResourcePolicy.fromSdkCalls({
      resources: AwsCustomResourcePolicy.ANY_RESOURCE,
    }),
  },
);
NagSuppressions.addResourceSuppressions(
  [
    applicationSignalsTransactionSearchXraySegmentDestination,
    applicationSignalsTransactionSearchLogsResourcePolicy,
    applicationSignalsTransactionSearchXrayIndexRule,
  ],
  [
    {
      id: &quot;AwsSolutions-IAM5&quot;,
      reason: &quot;CDK managed policy&quot;,
    },
  ],
  true,
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;It consists of the following steps:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Update the CloudWatch Logs Resource Policy.&lt;/li&gt;
&lt;li&gt;Update the X-Ray settings.&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;Update the CloudWatch Logs Resource Policy&lt;/h3&gt;
&lt;p&gt;The official documentation found &lt;a href=&quot;https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch-Transaction-Search-getting-started.html#w24aac24c21c13b9&quot;&gt;here&lt;/a&gt; is slightly misleading in a couple of areas. First, the &lt;code&gt;aws:SourceArn&lt;/code&gt; condition should use the &lt;code&gt;xray&lt;/code&gt; service principal (&lt;code&gt;arn:partition:xray:region:account-id:*&lt;/code&gt;), not &lt;code&gt;logs&lt;/code&gt;. Second, the &lt;code&gt;Action&lt;/code&gt; requires both &lt;code&gt;logs:PutLogEvents&lt;/code&gt; and &lt;code&gt;logs:CreateLogStream&lt;/code&gt; permissions.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const applicationSignalsTransactionSearchLogsResourcePolicy =
  new AwsCustomResource(
    this,
    &quot;ApplicationSignalsTransactionSearchLogsResourcePolicy&quot;,
    {
      onCreate: {
        service: &quot;@aws-sdk/client-cloudwatch-logs&quot;,
        action: &quot;PutResourcePolicy&quot;,
        parameters: {
          policyName: &quot;ApplicationSignalsTransactionSearchLogsResourcePolicy&quot;,
          policyDocument: JSON.stringify({
            Version: &quot;2012-10-17&quot;,
            Statement: [
              {
                Sid: &quot;TransactionSearchXRayAccess&quot;,
                Effect: &quot;Allow&quot;,
                Principal: {
                  Service: &quot;xray.amazonaws.com&quot;,
                },
                Action: [&quot;logs:PutLogEvents&quot;, &quot;logs:CreateLogStream&quot;],
                Resource: [
                  `arn:aws:logs:${region}:${account}:log-group:aws/spans:*`,
                  `arn:aws:logs:${region}:${account}:log-group:/aws/application-signals/data:*`,
                ],
                Condition: {
                  ArnLike: {
                    &quot;aws:SourceArn&quot;: `arn:aws:xray:${region}:${account}:*`,
                  },
                  StringEquals: {
                    &quot;aws:SourceAccount&quot;: account,
                  },
                },
              },
            ],
          }),
        },
        physicalResourceId: PhysicalResourceId.of(
          &quot;ApplicationSignalsTransactionSearchLogsResourcePolicy&quot;,
        ),
      },
      policy: AwsCustomResourcePolicy.fromSdkCalls({
        resources: AwsCustomResourcePolicy.ANY_RESOURCE,
      }),
    },
  );
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Update the X-Ray Settings&lt;/h3&gt;
&lt;p&gt;The two API calls for X-Ray are mostly straightforward. The only nuance is that the &lt;code&gt;UpdateTraceSegmentDestination&lt;/code&gt; and &lt;code&gt;UpdateIndexingRule&lt;/code&gt; actions require a recent version of the AWS SDK, as they were introduced in v3.698.0. Therefore, we must set &lt;code&gt;installLatestAwsSdk: true&lt;/code&gt; on the custom resource.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const applicationSignalsTransactionSearchXraySegmentDestination =
  new AwsCustomResource(
    this,
    &quot;ApplicationSignalsTransactionSearchXraySegmentDestination&quot;,
    {
      onCreate: {
        service: &quot;@aws-sdk/client-xray&quot;,
        action: &quot;UpdateTraceSegmentDestination&quot;,
        parameters: {
          Destination: &quot;CloudWatchLogs&quot;,
        },
        physicalResourceId: PhysicalResourceId.of(
          &quot;ApplicationSignalsTransactionSearchXraySegmentDestination&quot;,
        ),
      },
      installLatestAwsSdk: true,
      policy: AwsCustomResourcePolicy.fromStatements([
        new PolicyStatement({
          effect: Effect.ALLOW,
          actions: [&quot;logs:PutRetentionPolicy&quot;],
          resources: [
            `arn:aws:logs:${region}:${account}:log-group:aws/spans:log-stream:`,
          ],
        }),
        new PolicyStatement({
          effect: Effect.ALLOW,
          actions: [&quot;xray:UpdateTraceSegmentDestination&quot;],
          resources: [&quot;*&quot;],
        }),
      ]),
    },
  );
applicationSignalsTransactionSearchXraySegmentDestination.node.addDependency(
  applicationSignalsTransactionSearchLogsResourcePolicy,
);
const applicationSignalsTransactionSearchXrayIndexRule = new AwsCustomResource(
  this,
  &quot;ApplicationSignalsTransactionSearchXrayIndexRule&quot;,
  {
    onCreate: {
      service: &quot;@aws-sdk/client-xray&quot;,
      action: &quot;UpdateIndexingRule&quot;,
      parameters: {
        Name: &quot;Default&quot;,
        Rule: {
          Probabilistic: {
            DesiredSamplingPercentage: 100,
          },
        },
      },
      physicalResourceId: PhysicalResourceId.of(
        &quot;ApplicationSignalsTransactionSearchXrayIndexRule&quot;,
      ),
    },
    installLatestAwsSdk: true,
    policy: AwsCustomResourcePolicy.fromSdkCalls({
      resources: AwsCustomResourcePolicy.ANY_RESOURCE,
    }),
  },
);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Result&lt;/h2&gt;
&lt;p&gt;After deploying the CDK stack and waiting a few minutes, Transaction Search will be enabled and visible in the AWS Management Console.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./result-transaction-search-xray-settings.png&quot; alt=&quot;application signals transaction search enabled in X-Ray settings&quot; /&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./result-transaction-search.png&quot; alt=&quot;application signals transaction search enabled in CloudWatch&quot; /&gt;&lt;/p&gt;
</content:encoded></item><item><title>AWS Application Signals for Node.js Lambda with CDK</title><link>https://johanneskonings.dev/blog/2025-01-02-aws-application-signals-config-lambda-cdk/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2025-01-02-aws-application-signals-config-lambda-cdk/</guid><description>How to configure AWS CloudWatch Application Signals centrally for an AWS account and for individual Node.js Lambdas using the AWS CDK.</description><pubDate>Thu, 02 Jan 2025 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Update 2025-06-18: Provisioning with CloudFormation&lt;/h2&gt;
&lt;p&gt;It can now also be configured using CloudFormation.
https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-applicationsignals-discovery.html&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { CfnDiscovery } from &quot;aws-cdk-lib/aws-applicationsignals&quot;;

new CfnDiscovery(this, &quot;ApplicationSignalsDiscovery&quot;);
&lt;/code&gt;&lt;/pre&gt;
&lt;blockquote&gt;
&lt;p&gt;[!NOTE]
With the &lt;code&gt;CfnDiscovery&lt;/code&gt; resource now available, the following sections detailing the custom resource implementation are for informational purposes only. They show the workaround that was needed before native CloudFormation support was added.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;Use case&lt;/h2&gt;
&lt;p&gt;This guide shows how to use &lt;a href=&quot;https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch-Application-Monitoring-Sections.html&quot;&gt;AWS CloudWatch Application Signals&lt;/a&gt; for your Node.js Lambda functions when using the AWS CDK for Infrastructure as Code (IaC).&lt;/p&gt;
&lt;h2&gt;Setup&lt;/h2&gt;
&lt;p&gt;In a new AWS account, the Management Console shows two steps to set up CloudWatch Application Signals.
Step 1 is an account-wide setup.
Step 2 is necessary for each Lambda function.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./application-signals-config-steps.png&quot; alt=&quot;application signals config steps&quot; /&gt;&lt;/p&gt;
&lt;h3&gt;Account-wide setup&lt;/h3&gt;
&lt;p&gt;To start discovery, the service-linked role &lt;code&gt;application-signals.cloudwatch.amazonaws.com&lt;/code&gt; must be created: https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/using-service-linked-roles.html#service-linked-role-signals.
The AWS CDK does not have a high-level construct for this. However, we can use the AWS SDK via a custom resource to achieve this.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const serviceLinkeRoleArnApplicationSignals = `arn:aws:iam::${Stack.of(this).account}:role/aws-service-role/application-signals.cloudwatch.amazonaws.com/AWSServiceRoleForCloudWatchApplicationSignals`;
const applicationSignalsStartDiscovery = new AwsCustomResource(
  this,
  &quot;ApplicationSignalsStartDiscovery&quot;,
  {
    onCreate: {
      service: &quot;@aws-sdk/client-application-signals&quot;,
      action: &quot;StartDiscovery&quot;,
      physicalResourceId: PhysicalResourceId.of(
        &quot;ApplicationSignalsStartDiscovery&quot;,
      ),
    },
    // fromSdkCalls didn&apos;t work, that&apos;s why the policy is set manually
    // policy: AwsCustomResourcePolicy.fromSdkCalls({ resources: AwsCustomResourcePolicy.ANY_RESOURCE }),
    policy: AwsCustomResourcePolicy.fromStatements([
      new PolicyStatement({
        effect: Effect.ALLOW,
        actions: [&quot;iam:CreateServiceLinkedRole&quot;],
        resources: [serviceLinkeRoleArnApplicationSignals],
      }),
      new PolicyStatement({
        effect: Effect.ALLOW,
        actions: [&quot;application-signals:StartDiscovery&quot;],
        resources: [&quot;*&quot;],
      }),
    ]),
  },
);

const customResourceId = `AWS${AwsCustomResource.PROVIDER_FUNCTION_UUID.replaceAll(&quot;-&quot;, &quot;&quot;)}`;
NagSuppressions.addResourceSuppressionsByPath(
  Stack.of(this),
  [
    `/${Stack.of(this).stackName}/${customResourceId}/ServiceRole/Resource`,
    `/${Stack.of(this).stackName}/${customResourceId}/Resource`,
  ],
  [
    {
      id: &quot;AwsSolutions-L1&quot;,
      reason: &quot;CDK managed lambda function&quot;,
    },
    {
      id: &quot;AwsSolutions-IAM4&quot;,
      reason: &quot;CDK managed policy&quot;,
    },
    {
      id: &quot;AwsSolutions-IAM5&quot;,
      reason: &quot;CDK managed policy&quot;,
    },
  ],
  true,
);

NagSuppressions.addResourceSuppressions(
  applicationSignalsStartDiscovery,
  [
    {
      id: &quot;AwsSolutions-IAM5&quot;,
      reason: &quot;CDK managed policy&quot;,
    },
  ],
  true,
);
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Each Lambda function&lt;/h3&gt;
&lt;p&gt;As described &lt;a href=&quot;https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch-Application-Signals-Enable-Lambda.html&quot;&gt;here&lt;/a&gt;, each Lambda needs the environment variable &lt;code&gt;AWS_LAMBDA_EXEC_WRAPPER&lt;/code&gt; with the value &lt;code&gt;/opt/otel-instrument&lt;/code&gt; and the &lt;code&gt;AWSOpenTelemetryDistroJs&lt;/code&gt; layer with the respective ARN.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const LAMBDA_APPLICATION_SIGNALS_LAYER_ARN =
  &quot;arn:aws:lambda:us-east-1:615299751070:layer:AWSOpenTelemetryDistroJs:5&quot;;
const LAMBDA_APPLICATION_SIGNALS_ENV = {
  AWS_LAMBDA_EXEC_WRAPPER: &quot;/opt/otel-instrument&quot;,
};

const lambda = new NodejsFunction(this, id, {
  runtime: Runtime.NODEJS_22_X,
  timeout: Duration.seconds(10),
  environment: props.enableApplicationSignals
    ? LAMBDA_APPLICATION_SIGNALS_ENV
    : {},
});
lambda.role?.addManagedPolicy(
  ManagedPolicy.fromAwsManagedPolicyName(
    &quot;CloudWatchLambdaApplicationSignalsExecutionRolePolicy&quot;,
  ),
);
NagSuppressions.addResourceSuppressions(
  lambda,
  [
    {
      id: &quot;AwsSolutions-IAM4&quot;,
      reason: &quot;CDK managed policy&quot;,
    },
  ],
  true,
);
const layerApplicationSignals = LayerVersion.fromLayerVersionArn(
  this,
  &quot;LambdaApplicationSignalsLayer&quot;,
  LAMBDA_APPLICATION_SIGNALS_LAYER_ARN,
);

lambda.addLayers(layerApplicationSignals);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The Lambda function implementation can then look like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const handler = async (event: undefined, context: undefined) =&amp;gt; {
  console.log(&quot;lambda was called...&quot;);
  return {
    statusCode: 200,
    body: JSON.stringify({
      message: &quot;Hello from Lambda!&quot;,
    }),
  };
};
module.exports = { handler };
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;⚠️ The &lt;a href=&quot;https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch-Application-Signals-supportmatrix.html#CloudWatch-Application-Signals-supportmatrix-nodejs&quot;&gt;documentation&lt;/a&gt; currently recommends using CommonJS (CJS) instead of ECMAScript Modules (ESM).&lt;/p&gt;
&lt;p&gt;For CommonJS, some details need to be considered, such as how the handler is exported: https://github.com/aws-observability/aws-otel-lambda/issues/284#issuecomment-1465465790&lt;/p&gt;
&lt;h2&gt;Result&lt;/h2&gt;
&lt;p&gt;After the setup and a few invocations of the Lambda function, the CloudWatch Application Signals will be visible in the Management Console. This might take a few minutes.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./result.png&quot; alt=&quot;application signals result&quot; /&gt;&lt;/p&gt;
</content:encoded></item><item><title>Calling IAM authenticated API Gateway with different HTTP clients</title><link>https://johanneskonings.dev/blog/2024-12-28-api-gw-sigv4-different-api-clients/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2024-12-28-api-gw-sigv4-different-api-clients/</guid><description>This post describes how to call an IAM authenticated API Gateway with different HTTP clients like ky, fetch, and axios via signature v4 requests.</description><pubDate>Sat, 28 Dec 2024 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Use case&lt;/h2&gt;
&lt;p&gt;API Gateways can be secured with IAM authentication. This means that the client must sign the request with the &lt;a href=&quot;https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_sigv.html&quot;&gt;AWS Signature Version 4&lt;/a&gt;. This post describes how to call an IAM authenticated API Gateway with different HTTP clients like &lt;a href=&quot;https://github.com/sindresorhus/ky&quot;&gt;ky&lt;/a&gt;, &lt;a href=&quot;https://nodejs.org/dist/v18.20.5/docs/api/globals.html#fetch&quot;&gt;fetch&lt;/a&gt;, and &lt;a href=&quot;https://github.com/axios/axios&quot;&gt;axios&lt;/a&gt;.&lt;/p&gt;
&lt;h2&gt;Setup&lt;/h2&gt;
&lt;h3&gt;API Gateway&lt;/h3&gt;
&lt;p&gt;The API Gateway looks like this:&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./api-gateway.png&quot; alt=&quot;api gateway&quot; /&gt;&lt;/p&gt;
&lt;p&gt;The authorisation is set to &lt;code&gt;AWS_IAM&lt;/code&gt; additional to an api key.&lt;/p&gt;
&lt;p&gt;The lambda function is a simple one, which returns params, method and body.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;exports.handler = async function (event) {
  const params = event.queryStringParameters;
  const body = event.body;
  const method = event.httpMethod;
  return {
    statusCode: 200,
    body: JSON.stringify({
      message: &quot;API Called&quot;,
      params,
      body,
      method,
    }),
  };
};
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Lambda function for calling the API Gateway&lt;/h3&gt;
&lt;p&gt;Main parts are copied form here: https://github.com/zirkelc/aws-sigv4-fetch by &lt;a href=&quot;https://zirkelc.dev/&quot;&gt;zirkelc&lt;/a&gt;. This project can be used if the HTTP client will be fetch. If ky or axios should be used, the code must be adapted.&lt;/p&gt;
&lt;p&gt;The signing procedure is capsuled in the &lt;code&gt;signV4&lt;/code&gt; function.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { Sha256 } from &quot;@aws-crypto/sha256-js&quot;;
import type { AwsCredentialIdentity, Provider } from &quot;@aws-sdk/types&quot;;
import { HttpRequest } from &quot;@smithy/protocol-http&quot;;
import { SignatureV4 } from &quot;@smithy/signature-v4&quot;;

// for this scope these values can be hard coded
const REGION = &quot;eu-central-1&quot;;
const SERVICE = &quot;execute-api&quot;;

type SigV4Props = {
  readonly credentials?:
    | AwsCredentialIdentity
    | Provider&amp;lt;AwsCredentialIdentity&amp;gt;;
  readonly method: string;
  readonly headers: Record&amp;lt;string, string&amp;gt;;
  readonly body?: BodyInit | undefined | null;
  readonly url: URL;
};

export const signV4 = async (props: SigV4Props) =&amp;gt; {
  if (!props.credentials) {
    throw new Error(&quot;Credentials are required for signing&quot;);
  }

  const signer = new SignatureV4({
    credentials: props.credentials,
    region: REGION,
    service: SERVICE,
    sha256: Sha256,
  });

  const { method, headers, url, body } = props;

  // https://github.com/zirkelc/aws-sigv4-fetch/blob/44d19e270783e8566c2f39eebc1eb83e1b8af3ad/src/create-signed-fetcher.ts#L36C5-L37C32
  // host is required by AWS Signature V4: https://docs.aws.amazon.com/general/latest/gr/sigv4-create-canonical-request.html
  headers[&quot;host&quot;] = url.host;

  const request = new HttpRequest({
    method: method,
    body,
    headers: headers,
    hostname: url.hostname,
    path: url.pathname,
    protocol: url.protocol,
    port: url.port ? Number(url.port) : undefined,
    username: url.username,
    password: url.password,
    fragment: url.hash,
    query: Object.fromEntries(url.searchParams.entries()),
  });

  const signedRequest = await signer.sign(request);

  return signedRequest;
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The call of the API Gateway looks like following. It does mainly two parts: Assuming a role for the credentials and calling the API Gateway with different HTTP clients.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { AssumeRoleCommand, STSClient } from &quot;@aws-sdk/client-sts&quot;;
import ky from &quot;ky&quot;;
import axios from &quot;axios&quot;;
import { signV4 } from &quot;../utils/signV4/signV4&quot;;
// Set the AWS Region.
const REGION = &quot;eu-central-1&quot;;
// Create an AWS STS service client object.
export const client = new STSClient({ region: REGION });
export const handler = async (event: { apiKey: string }) =&amp;gt; {
  try {
    // get the credentials via a role which is allowed to call the API Gateway
    const command = new AssumeRoleCommand({
      // The Amazon Resource Name (ARN) of the role to assume.
      RoleArn: &quot;arn:aws:iam::123456789012:role/ApiAccess&quot;,
      // An identifier for the assumed role session.
      RoleSessionName: &quot;session1&quot;,
      // The duration, in seconds, of the role session. The value specified
      // can range from 900 seconds (15 minutes) up to the maximum session
      // duration set for the role.
      DurationSeconds: 900,
    });
    const response = await client.send(command);
    // console.log(response);
    if (!response.Credentials) {
      throw new Error(&quot;No credentials found in response&quot;);
    }
    if (!response.Credentials.AccessKeyId) {
      throw new Error(&quot;No access key found in response&quot;);
    }
    if (!response.Credentials.SecretAccessKey) {
      throw new Error(&quot;No secret key found in response&quot;);
    }

    // GET ky
    const urlKyGet =
      &quot;https://&amp;lt;&amp;lt;api gw id&amp;gt;&amp;gt;.execute-api.eu-central-1.amazonaws.com/default?client=ky&quot;;
    const signedRequestKyGet = await signV4({
      credentials: {
        accessKeyId: response.Credentials.AccessKeyId,
        secretAccessKey: response.Credentials.SecretAccessKey,
        sessionToken: response.Credentials.SessionToken,
      },
      method: &quot;GET&quot;,
      headers: {
        &quot;Content-Type&quot;: &quot;application/json&quot;,
        &quot;x-api-key&quot;: event.apiKey,
      },
      url: new URL(urlKyGet),
    });

    const responseApiKyGet = await ky(urlKyGet, {
      method: signedRequestKyGet.method,
      headers: signedRequestKyGet.headers,
    });
    const dataKyGet = await responseApiKyGet.json();
    console.log(dataKyGet);
    // GET fetch
    const urlFetchGet =
      &quot;https://&amp;lt;&amp;lt;api gw id&amp;gt;&amp;gt;.execute-api.eu-central-1.amazonaws.com/default?client=fetch&quot;;
    const signedRequestFetchGet = await signV4({
      credentials: {
        accessKeyId: response.Credentials.AccessKeyId,
        secretAccessKey: response.Credentials.SecretAccessKey,
        sessionToken: response.Credentials.SessionToken,
      },
      method: &quot;GET&quot;,
      headers: {
        &quot;Content-Type&quot;: &quot;application/json&quot;,
        &quot;x-api-key&quot;: event.apiKey,
      },
      url: new URL(urlFetchGet),
    });
    const responseApiFetchGet = await fetch(urlFetchGet, {
      method: signedRequestFetchGet.method,
      headers: signedRequestFetchGet.headers,
    });
    const dataFetchGet = await responseApiFetchGet.json();
    console.log(dataFetchGet);
    // GET axios
    const urlAxiosGet =
      &quot;https://&amp;lt;&amp;lt;api gw id&amp;gt;&amp;gt;.execute-api.eu-central-1.amazonaws.com/default?client=axios&quot;;
    const signedRequestAxiosGet = await signV4({
      credentials: {
        accessKeyId: response.Credentials.AccessKeyId,
        secretAccessKey: response.Credentials.SecretAccessKey,
        sessionToken: response.Credentials.SessionToken,
      },
      method: &quot;GET&quot;,
      headers: {
        &quot;Content-Type&quot;: &quot;application/json&quot;,
        &quot;x-api-key&quot;: event.apiKey,
      },
      url: new URL(urlAxiosGet),
    });
    const responseApiAxiosGet = await axios.get(urlAxiosGet, {
      headers: signedRequestAxiosGet.headers,
    });
    const dataAxiosGet = responseApiAxiosGet.data;
    console.log(dataAxiosGet);
    // POST ky
    const urlKyPost =
      &quot;https://&amp;lt;&amp;lt;api gw id&amp;gt;&amp;gt;.execute-api.eu-central-1.amazonaws.com/default&quot;;
    const signedRequestKyPost = await signV4({
      credentials: {
        accessKeyId: response.Credentials.AccessKeyId,
        secretAccessKey: response.Credentials.SecretAccessKey,
        sessionToken: response.Credentials.SessionToken,
      },
      method: &quot;POST&quot;,
      headers: {
        &quot;Content-Type&quot;: &quot;application/json&quot;,
        &quot;x-api-key&quot;: event.apiKey,
      },
      body: JSON.stringify({ client: &quot;ky&quot; }),
      url: new URL(urlKyPost),
    });
    const responseApiKyPost = await ky(urlKyPost, {
      method: signedRequestKyPost.method,
      headers: signedRequestKyPost.headers,
      body: signedRequestKyPost.body as string,
    });
    const dataKyPost = await responseApiKyPost.json();
    console.log(dataKyPost);
    // POST fetch
    const urlFetchPost =
      &quot;https://&amp;lt;&amp;lt;api gw id&amp;gt;&amp;gt;.execute-api.eu-central-1.amazonaws.com/default&quot;;
    const signedRequestFetchPost = await signV4({
      credentials: {
        accessKeyId: response.Credentials.AccessKeyId,
        secretAccessKey: response.Credentials.SecretAccessKey,
        sessionToken: response.Credentials.SessionToken,
      },
      method: &quot;POST&quot;,
      headers: {
        &quot;Content-Type&quot;: &quot;application/json&quot;,
        &quot;x-api-key&quot;: event.apiKey,
      },
      body: JSON.stringify({ client: &quot;fetch&quot; }),
      url: new URL(urlFetchPost),
    });
    const responseApiFetchPost = await fetch(urlFetchPost, {
      method: signedRequestFetchPost.method,
      headers: signedRequestFetchPost.headers,
      body: signedRequestFetchPost.body as string,
    });
    const dataFetchPost = await responseApiFetchPost.json();
    console.log(dataFetchPost);
    // POST axios
    const urlAxiosPost =
      &quot;https://&amp;lt;&amp;lt;api gw id&amp;gt;&amp;gt;.execute-api.eu-central-1.amazonaws.com/default&quot;;
    const signedRequestAxiosPost = await signV4({
      credentials: {
        accessKeyId: response.Credentials.AccessKeyId,
        secretAccessKey: response.Credentials.SecretAccessKey,
        sessionToken: response.Credentials.SessionToken,
      },
      method: &quot;POST&quot;,
      headers: {
        &quot;Content-Type&quot;: &quot;application/json&quot;,
        &quot;x-api-key&quot;: event.apiKey,
      },
      body: JSON.stringify({ client: &quot;axios&quot; }),
      url: new URL(urlAxiosPost),
    });
    const responseApiAxiosPost = await axios.post(
      urlAxiosPost,
      signedRequestAxiosPost.body,
      {
        headers: signedRequestAxiosPost.headers,
      },
    );
    const dataAxiosPost = responseApiAxiosPost.data;
    console.log(dataAxiosPost);
  } catch (error) {
    console.error(error);
  }
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The role has this policy attached and allow to call API Gateways.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
  &quot;Version&quot;: &quot;2012-10-17&quot;,
  &quot;Statement&quot;: [
    {
      &quot;Action&quot;: &quot;execute-api:Invoke&quot;,
      &quot;Resource&quot;: &quot;*&quot;,
      &quot;Effect&quot;: &quot;Allow&quot;
    }
  ]
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The call of the API Gateway is than calling the signV4 function with the credentials and &quot;all information&quot; for the API call.&lt;/p&gt;
&lt;p&gt;e.g. for a GET call&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const urlKyGet =
  &quot;https://&amp;lt;&amp;lt;api gw id&amp;gt;&amp;gt;.execute-api.eu-central-1.amazonaws.com/default?client=ky&quot;;
const signedRequestKyGet = await signV4({
  credentials: {
    accessKeyId: response.Credentials.AccessKeyId,
    secretAccessKey: response.Credentials.SecretAccessKey,
    sessionToken: response.Credentials.SessionToken,
  },
  method: &quot;GET&quot;,
  headers: {
    &quot;Content-Type&quot;: &quot;application/json&quot;,
    &quot;x-api-key&quot;: event.apiKey,
  },
  url: new URL(urlKyGet),
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The return value can be used for different HTTP clients.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const responseApiKyGet = await ky(urlKyGet, {
  method: signedRequestKyGet.method,
  headers: signedRequestKyGet.headers,
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In that way the signing request can be abstracted and the HTTP client can be changed easily.&lt;/p&gt;
&lt;h2&gt;Result&lt;/h2&gt;
&lt;p&gt;The test result of the example lambda function will than look like this:&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./result.png&quot; alt=&quot;result&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Sources&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_sigv.html&quot;&gt;AWS Signature Version 4 for API requests&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/zirkelc/aws-sigv4-fetch/tree/main&quot;&gt;aws-sigv4-fetch&lt;/a&gt; by &lt;a href=&quot;https://zirkelc.dev/&quot;&gt;zirkelc&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/sindresorhus/ky&quot;&gt;ky&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://nodejs.org/dist/v18.20.5/docs/api/globals.html#fetch&quot;&gt;fetch&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/axios/axios&quot;&gt;axios&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>Run vs code on a private AWS ec2 instance without ssh (with AWS CDK examples)</title><link>https://johanneskonings.dev/blog/2024-12-07-aws-vscode-private-ec2-instance/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2024-12-07-aws-vscode-private-ec2-instance/</guid><description>Use the browser based vs code for file handling on a private ec2 instance without ssh</description><pubDate>Sat, 07 Dec 2024 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Use case&lt;/h2&gt;
&lt;p&gt;You have a private EC2 instance in your AWS account and you want to edit files outside of the terminal or just with the help of vs code and vs code extensions on this instance without using SSH.
CDK is already in place and can be used to provision the necessary resources.&lt;/p&gt;
&lt;h2&gt;Setup&lt;/h2&gt;
&lt;p&gt;The following steps are necessary to set up the environment.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Configure SSM Session Manager for the private instance&lt;/li&gt;
&lt;li&gt;Installation of the SSM plugin for the AWS CLI&lt;/li&gt;
&lt;li&gt;Installation of vs code with the &lt;a href=&quot;https://github.com/coder/code-server&quot;&gt;code code-server&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Prerequisites&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;The VPC network setting already exists.&lt;/li&gt;
&lt;li&gt;The private subnet has access to the internet&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Configure SSM Session Manager for the private instance&lt;/h2&gt;
&lt;p&gt;The configuration is described &lt;a href=&quot;https://repost.aws/knowledge-center/ec2-systems-manager-vpc-endpoints&quot;&gt;here&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;The needed VPC endpoints can be created like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const subnetsPrivateIsolated = props.vpc.selectSubnets({
  subnetType: SubnetType.PRIVATE_ISOLATED,
});
props.vpc.addInterfaceEndpoint(&quot;VpcEndpointSsm&quot;, {
  service: InterfaceVpcEndpointAwsService.SSM,
  privateDnsEnabled: true,
  subnets: subnetsPrivateIsolated,
});
props.vpc.addInterfaceEndpoint(&quot;VpcEndpointSsmMessages&quot;, {
  service: InterfaceVpcEndpointAwsService.SSM_MESSAGES,
  privateDnsEnabled: true,
  subnets: subnetsPrivateIsolated,
});
props.vpc.addInterfaceEndpoint(&quot;VpcEndpointEc2Message&quot;, {
  service: InterfaceVpcEndpointAwsService.EC2_MESSAGES,
  privateDnsEnabled: true,
  subnets: subnetsPrivateIsolated,
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Here is a list of AMIs, which has the SSM agent preinstalled: https://docs.aws.amazon.com/systems-manager/latest/userguide/ami-preinstalled-agent.html&lt;/p&gt;
&lt;p&gt;The creation of the instance can than look like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const roleSsm2Ec2 = new Role(this, &quot;RoleSsm2Ec2&quot;, {
  assumedBy: new ServicePrincipal(&quot;ec2.amazonaws.com&quot;),
});
this.roleSsm2Ec2.addManagedPolicy(
  ManagedPolicy.fromAwsManagedPolicyName(&quot;AmazonSSMManagedInstanceCore&quot;),
);

new Instance(this, &quot;Instance&quot;, {
  vpc: props.vpc,
  instanceType: InstanceType.of(InstanceClass.T2, InstanceSize.MICRO),
  machineImage: MachineImage.latestAmazonLinux2023(),
  role: roleSsm2Ec2,
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The attached role is importent for the SSM Session Manager to work.&lt;/p&gt;
&lt;h2&gt;Installation of the SSM plugin for the AWS CLI&lt;/h2&gt;
&lt;p&gt;Before the instance can be accessed via the AWS Systems Manager Session Manager, the AWS CLI needs the SSM plugin.
The installation is described &lt;a href=&quot;https://docs.aws.amazon.com/systems-manager/latest/userguide/session-manager-working-with-install-plugin.html&quot;&gt;here&lt;/a&gt;.&lt;/p&gt;
&lt;h2&gt;Installation of vs code via AWS Systems Manager Session Manager&lt;/h2&gt;
&lt;p&gt;Now the instance can be accessed via the AWS Systems Manager Session Manager.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;aws ssm start-session --target &amp;lt;&amp;lt;instance-id&amp;gt;&amp;gt; --region &amp;lt;&amp;lt;region&amp;gt;&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;With the following commands vs code can than be installed.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;curl -fsSL https://code-server.dev/install.sh | sh
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&quot;./install-code-server.png&quot; alt=&quot;install code-server&quot; /&gt;&lt;/p&gt;
&lt;p&gt;More details here: https://github.com/coder/code-server?tab=readme-ov-file#getting-started&lt;/p&gt;
&lt;p&gt;Now run the code-server with the following command:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;code-server
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&quot;./run-code-server.png&quot; alt=&quot;run code-server&quot; /&gt;&lt;/p&gt;
&lt;p&gt;As you can see, the code-server is running on &lt;code&gt;http://127.0.0.1:8080/&lt;/code&gt; (1.) and the password is in file &lt;code&gt;/home/ssm-user/.config/code-server/config.yaml&lt;/code&gt; (2.).&lt;/p&gt;
&lt;p&gt;That information are needed to connect to the code-server via the browser.&lt;/p&gt;
&lt;h2&gt;Connect to vs code via AWS Systems Manager port forwarding&lt;/h2&gt;
&lt;p&gt;The instance has no public IP and can not be accessed via the browser. But with the help of the AWS Systems Manager port forwarding, the instance can be accessed via the browser.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;aws ssm start-session   --region &amp;lt;&amp;lt;region&amp;gt;&amp;gt; \
                        --target &amp;lt;&amp;lt;instance-id&amp;gt;&amp;gt; \
                        --document-name AWS-StartPortForwardingSession \
                        --parameters &apos;{&quot;portNumber&quot;:[&quot;8080&quot;],&quot;localPortNumber&quot;:[&quot;9999&quot;]}&apos;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;More Details here: https://aws.amazon.com/blogs/aws/new-port-forwarding-using-aws-system-manager-sessions-manager/&lt;/p&gt;
&lt;p&gt;Now the code-server can be accessed via the browser with the following URL: &lt;code&gt;http://localhost:9999/&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Login with the password from the config.yaml file.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./code-server-login.png&quot; alt=&quot;code server login&quot; /&gt;&lt;/p&gt;
&lt;p&gt;Now you can handle the files via vs code 🥳&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./code-server-file-handling.png&quot; alt=&quot;code server file handling&quot; /&gt;&lt;/p&gt;
</content:encoded></item><item><title>Scan all your AWS SSO accounts with Prowler</title><link>https://johanneskonings.dev/blog/2024-11-30-aws-sso-prowler/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2024-11-30-aws-sso-prowler/</guid><description>See how this setup can help to scan all AWS SSO accounts with a mix of Prowler, docker, and aws-sso-util.</description><pubDate>Sat, 30 Nov 2024 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Use case&lt;/h2&gt;
&lt;p&gt;You want to run &lt;a href=&quot;https://docs.prowler.com/projects/prowler-open-source/en/latest/&quot;&gt;prowler&lt;/a&gt; scans for different AWS accounts, which AWS SSO manages.&lt;/p&gt;
&lt;h2&gt;Approach&lt;/h2&gt;
&lt;p&gt;To scan multiple AWS accounts with prowler is described here: https://docs.prowler.com/projects/prowler-open-source/en/latest/tutorials/aws/multiaccount/
Therefore, it&apos;s necessary to have permission to assume a role in every account.
This post describes how to do it with AWS SSO.&lt;/p&gt;
&lt;h2&gt;Setup&lt;/h2&gt;
&lt;p&gt;With a dockerfile, all needed tools are prepared in advance to run the scans.&lt;/p&gt;
&lt;h3&gt;Docker&lt;/h3&gt;
&lt;p&gt;The Dockerfile mainly uses the Prowler image and adds &lt;a href=&quot;https://github.com/benkehoe/aws-sso-util&quot;&gt;aws-sso-util&lt;/a&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;FROM public.ecr.aws/docker/library/alpine:3.20.3 AS builder

RUN apk update &amp;amp;&amp;amp; apk add --no-cache bash

FROM public.ecr.aws/prowler-cloud/prowler:4.6.1

COPY --from=builder /bin/bash /bin/bash

RUN pip install pipx &amp;amp;&amp;amp; \
    pipx install aws-sso-util
    # pipx ensurepath

WORKDIR /workspace

ENTRYPOINT [&quot;/bin/bash&quot;]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The prowler docu for installing with docker is here: https://docs.prowler.com/projects/prowler-open-source/en/latest/#__tabbed_2_3&lt;/p&gt;
&lt;p&gt;After creating the image with &lt;code&gt;docker build -t prowler.&lt;/code&gt;. The container can be created with the following command.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;docker run --entrypoint /bin/bash -it \
--mount type=bind,source=&quot;${PWD}/scripts&quot;,target=/workspace/scripts \
--mount type=bind,source=&quot;${PWD}/.env&quot;,target=/workspace/.env \
--mount type=bind,source=&quot;${PWD}/output&quot;,target=/workspace/output \
--name prowler \
prowler
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;These are the commands to use the container again &lt;code&gt;docker start -a prowler&lt;/code&gt; and &lt;code&gt;docker exec -it prowler /bin/bash&lt;/code&gt;.&lt;/p&gt;
&lt;h2&gt;Initial configuration after container creation&lt;/h2&gt;
&lt;p&gt;The command for running the container mounted the folder &lt;code&gt;scripts&lt;/code&gt; and &lt;code&gt;.env&lt;/code&gt; file to the container.&lt;/p&gt;
&lt;p&gt;One of the scripts is &lt;code&gt;./scripts/create-aws-config.sh&lt;/code&gt; which creates the file &lt;code&gt;~/.aws/config&lt;/code&gt;, with the SSO session values.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;#!/bin/bash

if [ ! -f .env ] ; then
    echo &quot;ERROR: .env file not found!&quot;
    exit 1
else
    source .env
fi
if [ -z &quot;$SSO_START_URL&quot; ] ; then
    echo &quot;ERROR: SSO_START_URL not set in .env file!&quot;
    exit 1
fi
if [ -z &quot;$SSO_SESSION_NAME&quot; ] ; then
    echo &quot;ERROR: SSO_SESSION_NAME not set in .env file!&quot;
    exit 1
fi
if [ -z &quot;$SSO_REGION&quot; ] ; then
    echo &quot;ERROR: REGION not set in .env file!&quot;
    exit 1
fi

aws-sso-util configure populate \
--sso-start-url $SSO_START_URL \
--sso-region $SSO_REGION \
--region $SSO_REGION
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The script needs the following env variables as file &lt;code&gt;.env&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;export SSO_START_URL= # https://&amp;lt;your-aws-account-id&amp;gt;.awsapps.com/start
export SSO_SESSION_NAME= # &amp;lt;your session name, it&apos;s just a name&amp;gt;
export SSO_REGION= # &amp;lt;your region, e.g. us-east-1&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;If you run the script &lt;code&gt;./scripts/create-aws-config.sh&lt;/code&gt; inside the container, the tool &lt;code&gt;aws-sso-util&lt;/code&gt; will ask for the SSO credentials.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./create-aws-config.png&quot; alt=&quot;create aws config&quot; /&gt;&lt;/p&gt;
&lt;p&gt;Open the link in the browser and put in the code.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./authorize-request.png&quot; alt=&quot;authorize request&quot; /&gt;&lt;/p&gt;
&lt;p&gt;Then, allow the access.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./allow-sso-to-access-data.png&quot; alt=&quot;allow sso to access data&quot; /&gt;&lt;/p&gt;
&lt;p&gt;After confirmation, the config file is created.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./aws-config-created.png&quot; alt=&quot;aws config created&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Prowler scans&lt;/h2&gt;
&lt;p&gt;The second script is &lt;code&gt;./scripts/run-prowler.sh&lt;/code&gt; which runs the prowler scan for all accounts.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;#!/bin/bash

if [ ! -f .env ] ; then
    echo &quot;ERROR: .env file not found!&quot;
    exit 1
else
    source .env
fi
if [ -z &quot;$SSO_START_URL&quot; ] ; then
    echo &quot;ERROR: SSO_START_URL not set in .env file!&quot;
    exit 1
fi
if [ -z &quot;$SSO_SESSION_NAME&quot; ] ; then
    echo &quot;ERROR: SSO_SESSION_NAME not set in .env file!&quot;
    exit 1
fi
if [ -z &quot;$SSO_REGION&quot; ] ; then
    echo &quot;ERROR: REGION not set in .env file!&quot;
    exit 1
fi

ROLE_NAME_REGEX=&quot;^AdministratorAccess&quot;
PROWLER_COMAND=&quot;prowler aws -i&quot;

ACCOUNTS=$(aws-sso-util roles --no-header  --separator , --role-name $ROLE_NAME_REGEX --sso-start-url $SSO_START_URL --sso-region $SSO_REGION)

while IFS=&apos;,&apos; read -r PROFILE ACCOUNT ROLE; do
    PROFILE=${PROFILE// /-}  # Replace spaces with hyphens in PROFILE, like aws-sso-util does
    CONFIG_PROFILE=$PROFILE.$ROLE
    echo &quot;Profile: $PROFILE, Account: $ACCOUNT, Role: $ROLE, Config-Profile: $CONFIG_PROFILE&quot;
    $PROWLER_COMAND --profile $CONFIG_PROFILE
done &amp;lt;&amp;lt;&amp;lt; &quot;$ACCOUNTS&quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The value of the variable &lt;code&gt;ROLE_NAME_REGEX&lt;/code&gt; is a regex filter of the role name, which is connected to the AWS SSO.
In this case, only the role name &lt;code&gt;AdministratorAccess&lt;/code&gt; is used.&lt;/p&gt;
&lt;p&gt;Some Accounts are connected with multiple roles.
In this example, all Accounts have the role &lt;code&gt;AdministratorAccess&lt;/code&gt;, so this filter works to scan all listed accounts.
In another scenario, the script must be adapted to filter the right role.&lt;/p&gt;
&lt;p&gt;The variable &lt;code&gt;PROWLER_COMAND&lt;/code&gt; is the command to run the prowler scan.&lt;/p&gt;
&lt;p&gt;The script &lt;code&gt;./scripts/run-prowler.sh&lt;/code&gt; call will look like this for every account.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./prowler-scan.png&quot; alt=&quot;prowler scan&quot; /&gt;&lt;/p&gt;
&lt;p&gt;If the login is expired, the script will ask for the credentials again.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./prowler-scan-relogin.png&quot; alt=&quot;prowler scan relogin&quot; /&gt;&lt;/p&gt;
&lt;p&gt;The scan result is additionally saved in the folder &lt;code&gt;output,&lt;/code&gt; which is mounted to the container.
With this configuration, a JSON and a CSV file are created for every account.&lt;/p&gt;
&lt;p&gt;This example used the Prowler &lt;a href=&quot;https://docs.prowler.com/projects/prowler-open-source/en/latest/tutorials/quick-inventory/&quot;&gt;quick inventory&lt;/a&gt; scan. &lt;a href=&quot;https://docs.prowler.com/projects/prowler-open-source/en/latest/tutorials/prowler-app/&quot;&gt;Here&lt;/a&gt; are more examples for different scans.
E.g. for &lt;a href=&quot;https://docs.prowler.com/projects/prowler-open-source/en/latest/tutorials/compliance/&quot;&gt;compliance scans&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;That&apos;s it. Now you can scan all your AWS accounts with Prowler and AWS SSO 🥳&lt;/p&gt;
</content:encoded></item><item><title>Projen with own batteries</title><link>https://johanneskonings.dev/blog/projen-own-batteries/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/projen-own-batteries/</guid><description>How to setup a projen project to store central decisions to apply across all own repositories</description><pubDate>Thu, 13 Jun 2024 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Use case&lt;/h2&gt;
&lt;p&gt;As described here [Use cdk-notifier to compare changes in pull requests]({{ site.baseurl }}/aws/cdk/2023/12/16/cdk-notifier-feature-stacks.html), the cdk-notifier displays the diff between the feature branch and the main branch.
In case of using tags in the CDK there a two ways to tag resources, which will have different consequences in the diff output of the cdk-notifier.&lt;/p&gt;
&lt;h2&gt;Tagging with Tags.of()&lt;/h2&gt;
&lt;p&gt;The documentation of CDK describes the tagging of resources with the &lt;code&gt;Tags.of()&lt;/code&gt; method: &lt;a href=&quot;https://docs.aws.amazon.com/cdk/v2/guide/tagging.html&quot;&gt;https://docs.aws.amazon.com/cdk/v2/guide/tagging.html&lt;/a&gt;
This could look like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Tags.of(app).add(&quot;branch&quot;, branchName);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/cdk-notifier-examples/blob/746c2b2bc0ecc0ecf3e8f0e6ff771a7430a45d04/src/main.ts#L23&quot;&gt;https://github.com/JohannesKonings/cdk-notifier-examples/blob/746c2b2bc0ecc0ecf3e8f0e6ff771a7430a45d04/src/main.ts#L23&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;The tag will then be added to all resources in the synthesized cloudformation template.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
 &quot;Resources&quot;: {
  &quot;TableCD117FA1&quot;: {
   &quot;Type&quot;: &quot;AWS::DynamoDB::Table&quot;,
   &quot;Properties&quot;: {
    &quot;AttributeDefinitions&quot;: [
     {
      &quot;AttributeName&quot;: &quot;id&quot;,
      &quot;AttributeType&quot;: &quot;S&quot;
     }
    ],
    &quot;BillingMode&quot;: &quot;PAY_PER_REQUEST&quot;,
    &quot;KeySchema&quot;: [
     {
      &quot;AttributeName&quot;: &quot;id&quot;,
      &quot;KeyType&quot;: &quot;HASH&quot;
     }
    ],
    &quot;TableName&quot;: &quot;Table-tags-tags-of&quot;,
    &quot;Tags&quot;: [
     {
      &quot;Key&quot;: &quot;branch&quot;,
      &quot;Value&quot;: &quot;tags-tags-of&quot;
     }
    ]
   },
   ...
  }
 }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Because the tag is in the template, it will then be shown in the diff.&lt;/p&gt;
&lt;p&gt;![diff tag of]({{ site.baseurl }}/img/2024-04-20-cdk-notifier-and-tags/diff-tag-of.png)&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/cdk-notifier-examples/pull/5&quot;&gt;https://github.com/JohannesKonings/cdk-notifier-examples/pull/5&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;Tagging with stack properties&lt;/h2&gt;
&lt;p&gt;The other way is to pass the tags as stack properties (&lt;a href=&quot;https://docs.aws.amazon.com/cdk/api/v2/docs/aws-cdk-lib.Stack.html#tags-1&quot;&gt;https://docs.aws.amazon.com/cdk/api/v2/docs/aws-cdk-lib.Stack.html#tags-1&lt;/a&gt;).
This could look like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;new CdkNotfifierFeatureStackExample(
  app,
  `cdk-notifier-feature-stacks-${branchName}`,
  {
    tags: {
      branch: branchName,
    },
  },
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/cdk-notifier-examples/blob/66874c06b8204b09781e9ad3ab8707590b948000/src/main.ts#L23&quot;&gt;https://github.com/JohannesKonings/cdk-notifier-examples/blob/66874c06b8204b09781e9ad3ab8707590b948000/src/main.ts#L23&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;The tag will then be added to the stack properties and not to the template file.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
 &quot;Resources&quot;: {
  &quot;TableCD117FA1&quot;: {
   &quot;Type&quot;: &quot;AWS::DynamoDB::Table&quot;,
   &quot;Properties&quot;: {
    &quot;AttributeDefinitions&quot;: [
     {
      &quot;AttributeName&quot;: &quot;id&quot;,
      &quot;AttributeType&quot;: &quot;S&quot;
     }
    ],
    &quot;BillingMode&quot;: &quot;PAY_PER_REQUEST&quot;,
    &quot;KeySchema&quot;: [
     {
      &quot;AttributeName&quot;: &quot;id&quot;,
      &quot;KeyType&quot;: &quot;HASH&quot;
     }
    ],
    &quot;TableName&quot;: &quot;Table-tags-stack-properties&quot;,
   },
   ...
  }
 }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In &lt;code&gt;cdk.out&lt;/code&gt; the tags are only in the &lt;code&gt;manifest.json&lt;/code&gt; file.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
  &quot;version&quot;: &quot;36.0.0&quot;,
  &quot;artifacts&quot;: {
    &quot;cdk-notifier-feature-stacks-tags-stack-properties.assets&quot;: {
      &quot;type&quot;: &quot;cdk:asset-manifest&quot;,
      &quot;properties&quot;: {
        &quot;file&quot;: &quot;cdk-notifier-feature-stacks-tags-stack-properties.assets.json&quot;,
        &quot;requiresBootstrapStackVersion&quot;: 6,
        &quot;bootstrapStackVersionSsmParameter&quot;: &quot;/cdk-bootstrap/hnb659fds/version&quot;
      }
    },
    &quot;cdk-notifier-feature-stacks-tags-stack-properties&quot;: {
      &quot;type&quot;: &quot;aws:cloudformation:stack&quot;,
      &quot;environment&quot;: &quot;aws://unknown-account/unknown-region&quot;,
      &quot;properties&quot;: {
        &quot;templateFile&quot;: &quot;cdk-notifier-feature-stacks-tags-stack-properties.template.json&quot;,
        &quot;terminationProtection&quot;: false,
        &quot;tags&quot;: {
          &quot;branch&quot;: &quot;tags-stack-properties&quot;
        },
        &quot;validateOnSynth&quot;: false,
        ...
      }
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Then it will not be shown in the diff, and the cdk-notifier skip the pull request comment.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;check the diff to main
Deploying with stack postfix main
Stack cdk-notifier-feature-stacks-main
Hold on while we create a read-only change set to get a diff with accurate replacement information (use --no-change-set to use a less accurate but faster template-only diff)
There were no differences

✨  Number of stacks with differences: 0

create cdk-notifier report
BRANCH_NAME: tags-stack-properties
GITHUB_OWNER: JohannesKonings
GITHUB_REPO: $(echo JohannesKonings/cdk-notifier-examples | cut -d&apos;/&apos; -f2)
time=&quot;2024-04-20T14:59:48Z&quot; level=info msg=&quot;There is no diff detected for tag id diff-to-main. Skip posting diff.&quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/cdk-notifier-examples/actions/runs/8765869174/job/24057331666#step:6:55&quot;&gt;https://github.com/JohannesKonings/cdk-notifier-examples/actions/runs/8765869174/job/24057331666#step:6:55&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;Conclusion&lt;/h2&gt;
&lt;p&gt;If you want to see the tags in the diff output of the cdk-notifier, you should use the &lt;code&gt;Tags.of()&lt;/code&gt; method to tag the resources.
If not, you can go with the stack properties.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/cdk-notifier-examples&quot;&gt;https://github.com/JohannesKonings/cdk-notifier-examples&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>Consideration about cdk-notifier and Tags</title><link>https://johanneskonings.dev/blog/2024-04-20-cdk-notifier-and-tags/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2024-04-20-cdk-notifier-and-tags/</guid><description>Description of the diff consequence of two ways of tagging in CDK</description><pubDate>Sat, 20 Apr 2024 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Use case&lt;/h2&gt;
&lt;p&gt;As described here &lt;a href=&quot;./cdk-notifier-feature-stacks.html&quot;&gt;Use cdk-notifier to compare changes in pull requests&lt;/a&gt;, the cdk-notifier displays the diff between the feature branch and the main branch.
In case of using tags in the CDK there a two ways to tag resources, which will have different consequences in the diff output of the cdk-notifier.&lt;/p&gt;
&lt;h2&gt;Tagging with Tags.of()&lt;/h2&gt;
&lt;p&gt;The documentation of CDK describes the tagging of resources with the &lt;code&gt;Tags.of()&lt;/code&gt; method: https://docs.aws.amazon.com/cdk/v2/guide/tagging.html
This could look like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Tags.of(app).add(&quot;branch&quot;, branchName);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;https://github.com/JohannesKonings/cdk-notifier-examples/blob/746c2b2bc0ecc0ecf3e8f0e6ff771a7430a45d04/src/main.ts#L23&lt;/p&gt;
&lt;p&gt;The tag will then be added to all resources in the synthesized cloudformation template.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
 &quot;Resources&quot;: {
  &quot;TableCD117FA1&quot;: {
   &quot;Type&quot;: &quot;AWS::DynamoDB::Table&quot;,
   &quot;Properties&quot;: {
    &quot;AttributeDefinitions&quot;: [
     {
      &quot;AttributeName&quot;: &quot;id&quot;,
      &quot;AttributeType&quot;: &quot;S&quot;
     }
    ],
    &quot;BillingMode&quot;: &quot;PAY_PER_REQUEST&quot;,
    &quot;KeySchema&quot;: [
     {
      &quot;AttributeName&quot;: &quot;id&quot;,
      &quot;KeyType&quot;: &quot;HASH&quot;
     }
    ],
    &quot;TableName&quot;: &quot;Table-tags-tags-of&quot;,
    &quot;Tags&quot;: [
     {
      &quot;Key&quot;: &quot;branch&quot;,
      &quot;Value&quot;: &quot;tags-tags-of&quot;
     }
    ]
   },
   ...
  }
 }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Because the tag is in the template, it will then be shown in the diff.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./diff-tag-of.png&quot; alt=&quot;diff tag of&quot; /&gt;&lt;/p&gt;
&lt;p&gt;https://github.com/JohannesKonings/cdk-notifier-examples/pull/5&lt;/p&gt;
&lt;h2&gt;Tagging with stack properties&lt;/h2&gt;
&lt;p&gt;The other way is to pass the tags as stack properties (https://docs.aws.amazon.com/cdk/api/v2/docs/aws-cdk-lib.Stack.html#tags-1).
This could look like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;new CdkNotfifierFeatureStackExample(
  app,
  `cdk-notifier-feature-stacks-${branchName}`,
  {
    tags: {
      branch: branchName,
    },
  },
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;https://github.com/JohannesKonings/cdk-notifier-examples/blob/66874c06b8204b09781e9ad3ab8707590b948000/src/main.ts#L23&lt;/p&gt;
&lt;p&gt;The tag will then be added to the stack properties and not to the template file.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
 &quot;Resources&quot;: {
  &quot;TableCD117FA1&quot;: {
   &quot;Type&quot;: &quot;AWS::DynamoDB::Table&quot;,
   &quot;Properties&quot;: {
    &quot;AttributeDefinitions&quot;: [
     {
      &quot;AttributeName&quot;: &quot;id&quot;,
      &quot;AttributeType&quot;: &quot;S&quot;
     }
    ],
    &quot;BillingMode&quot;: &quot;PAY_PER_REQUEST&quot;,
    &quot;KeySchema&quot;: [
     {
      &quot;AttributeName&quot;: &quot;id&quot;,
      &quot;KeyType&quot;: &quot;HASH&quot;
     }
    ],
    &quot;TableName&quot;: &quot;Table-tags-stack-properties&quot;,
   },
   ...
  }
 }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In &lt;code&gt;cdk.out&lt;/code&gt; the tags are only in the &lt;code&gt;manifest.json&lt;/code&gt; file.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
  &quot;version&quot;: &quot;36.0.0&quot;,
  &quot;artifacts&quot;: {
    &quot;cdk-notifier-feature-stacks-tags-stack-properties.assets&quot;: {
      &quot;type&quot;: &quot;cdk:asset-manifest&quot;,
      &quot;properties&quot;: {
        &quot;file&quot;: &quot;cdk-notifier-feature-stacks-tags-stack-properties.assets.json&quot;,
        &quot;requiresBootstrapStackVersion&quot;: 6,
        &quot;bootstrapStackVersionSsmParameter&quot;: &quot;/cdk-bootstrap/hnb659fds/version&quot;
      }
    },
    &quot;cdk-notifier-feature-stacks-tags-stack-properties&quot;: {
      &quot;type&quot;: &quot;aws:cloudformation:stack&quot;,
      &quot;environment&quot;: &quot;aws://unknown-account/unknown-region&quot;,
      &quot;properties&quot;: {
        &quot;templateFile&quot;: &quot;cdk-notifier-feature-stacks-tags-stack-properties.template.json&quot;,
        &quot;terminationProtection&quot;: false,
        &quot;tags&quot;: {
          &quot;branch&quot;: &quot;tags-stack-properties&quot;
        },
        &quot;validateOnSynth&quot;: false,
        ...
      }
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Then it will not be shown in the diff, and the cdk-notifier skip the pull request comment.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;check the diff to main
Deploying with stack postfix main
Stack cdk-notifier-feature-stacks-main
Hold on while we create a read-only change set to get a diff with accurate replacement information (use --no-change-set to use a less accurate but faster template-only diff)
There were no differences

✨  Number of stacks with differences: 0

create cdk-notifier report
BRANCH_NAME: tags-stack-properties
GITHUB_OWNER: JohannesKonings
GITHUB_REPO: $(echo JohannesKonings/cdk-notifier-examples | cut -d&apos;/&apos; -f2)
time=&quot;2024-04-20T14:59:48Z&quot; level=info msg=&quot;There is no diff detected for tag id diff-to-main. Skip posting diff.&quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;https://github.com/JohannesKonings/cdk-notifier-examples/actions/runs/8765869174/job/24057331666#step:6:55&lt;/p&gt;
&lt;h2&gt;Conclusion&lt;/h2&gt;
&lt;p&gt;If you want to see the tags in the diff output of the cdk-notifier, you should use the &lt;code&gt;Tags.of()&lt;/code&gt; method to tag the resources.
If not, you can go with the stack properties.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/cdk-notifier-examples&quot;&gt;https://github.com/JohannesKonings/cdk-notifier-examples&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>Use cdk-notifier to compare changes in pull requests</title><link>https://johanneskonings.dev/blog/2023-12-16-cdk-notifier-feature-stacks/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2023-12-16-cdk-notifier-feature-stacks/</guid><description>See how cdk-notifier can help to compare which cdk changes will be applied after merging a pull request</description><pubDate>Sat, 16 Dec 2023 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Use case&lt;/h2&gt;
&lt;p&gt;Especially in serverless environments, features will be created with ephemeral stacks, which will be deleted after merging to the main branch. Comparing the cdk diff between the feature branch and the main branch will help to understand which changes will be applied after merging the pull request.&lt;/p&gt;
&lt;p&gt;Some changes, like renaming a dynamodb table or updating more than one dynamodb index, will work if the changes are made before deploying the feature stack but fail if the changes are applied to an existing stack.&lt;/p&gt;
&lt;p&gt;The cdk-notifer will help to identify these changes.&lt;/p&gt;
&lt;h2&gt;cdk-notifier&lt;/h2&gt;
&lt;p&gt;See here for more information about the cdk-notifier:
&lt;a href=&quot;https://github.com/karlderkaefer/cdk-notifier&quot;&gt;https://github.com/karlderkaefer/cdk-notifier&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;GitHub Action&lt;/h2&gt;
&lt;p&gt;This setting has a deployed stack from the main branch, for the feature development a new branch &lt;code&gt;feat1&lt;/code&gt; is created and deployed as a new stack.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./stack-overview.png&quot; alt=&quot;stack-overview&quot; /&gt;&lt;/p&gt;
&lt;p&gt;In the pull request the cdk-notifier action is added to the workflow and create a diff from the &lt;code&gt;feat1&lt;/code&gt; branch files with the BRANCH_NAME env variable for &lt;code&gt;main&lt;/code&gt; to run the diff agianst the &lt;code&gt;main&lt;/code&gt; stack.
The diff output will be added to the pull request as a comment via the cdk-notifier.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;- name: Check diff to main
  run: |
    npm ci  
    echo &quot;check the diff to main&quot;
    BRANCH_NAME=main npx cdk diff --app cdk.out --progress=events &amp;amp;&amp;gt; &amp;gt;(tee cdk.log)
    echo &quot;create cdk-notifier report&quot;
    echo &quot;BRANCH_NAME: $BRANCH_NAME&quot;
    echo &quot;GITHUB_OWNER: $GITHUB_OWNER&quot;
    echo &quot;GITHUB_REPO: $GITHUB_REPO&quot;
    cdk-notifier \
    --owner ${{ env.GITHUB_OWNER }} \
    --repo ${{ env.GITHUB_REPO }} \
    --token ${{ secrets.GITHUB_TOKEN }} \
    --log-file ./cdk.log \
    --tag-id diff-to-main \
    --pull-request-id ${{ env.PULL_REQUEST_ID }} \
    --vcs github \
    --ci circleci \
    --template extendedWithResources
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/cdk-notifier-examples/blob/main/.github/workflows/cdk-diff.yml&quot;&gt;https://github.com/JohannesKonings/cdk-notifier-examples/blob/main/.github/workflows/cdk-diff.yml&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./pipeline-in-pull-request.png&quot; alt=&quot;pipeline in pull request&quot; /&gt;&lt;/p&gt;
&lt;p&gt;The comment looks like this:&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./pr-comment.png&quot; alt=&quot;PR comment&quot; /&gt;
&lt;a href=&quot;https://github.com/JohannesKonings/cdk-notifier-examples/pull/4#issuecomment-1858909147&quot;&gt;https://github.com/JohannesKonings/cdk-notifier-examples/pull/4#issuecomment-1858909147&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;The extended template parameter of the cdk-notifier displays in this setting also a warning that the dynamodb requires a replacement.
In some cases, this could be unintended and be reverted securely in the feature branch before merging.
That is a nice feature besides the overview of the changes, which is itself helpful in making these changes more visible.&lt;/p&gt;
&lt;p&gt;After the merge, the feature stack will be destroyed, and the main stack will be updated.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./pipeline-after-merge.png&quot; alt=&quot;pipeline after merge&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/cdk-notifier-examples&quot;&gt;https://github.com/JohannesKonings/cdk-notifier-examples&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;Follow-up Post&lt;/h2&gt;
&lt;p&gt;For an extended example that adds CloudFormation change set validation alongside &lt;code&gt;cdk diff&lt;/code&gt;, see: &lt;a href=&quot;/blog/2025-12-06-cdk-notifier-feature-stacks-change-set&quot;&gt;cdk-notifier with CloudFormation change sets&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>Use Steampipe to select your AWS resources across SSO accounts with SQL</title><link>https://johanneskonings.dev/blog/2023-09-08-aws-sso-steampipe/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2023-09-08-aws-sso-steampipe/</guid><description>See how this setup can help to discover your AWS resources across all SSO accounts with a mix of Steampipe, docker, bash scripts, and AWS CLI</description><pubDate>Fri, 08 Sep 2023 08:15:18 GMT</pubDate><content:encoded>&lt;h2&gt;Use case&lt;/h2&gt;
&lt;p&gt;You are in charge of several AWS accounts within an AWS Organisation and need to check the resources across these accounts. E.g., to check which are the configured runtimes for the lambdas.&lt;/p&gt;
&lt;h2&gt;Approach with Steampipe&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://steampipe.io/&quot;&gt;Steampipe&lt;/a&gt; is a tool to query data from different providers. Among others, there is a &lt;a href=&quot;https://hub.steampipe.io/plugins/turbot/aws&quot;&gt;plugin for AWS&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;The big plus is that Steampipe provides the ability to query more than one account with a query with &lt;a href=&quot;https://steampipe.io/docs/managing/connections#using-aggregators&quot;&gt;aggregator connection&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;This is how the result will look like for my AWS SSO accounts.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./query-result.png&quot; alt=&quot;query result&quot; /&gt;&lt;/p&gt;
&lt;p&gt;More about Steampipe and AWS: &lt;a href=&quot;https://dev.to/aws-builders/easily-query-your-cloud-inventory-with-steampipe-2af3&quot;&gt;https://dev.to/aws-builders/easily-query-your-cloud-inventory-with-steampipe-2af3&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;Setup&lt;/h2&gt;
&lt;p&gt;It&apos;s necessary to have a link between AWS CLI profiles and Steampipe connection for the AWS SSO accounts that can be recreated without any effects on the local setup, which is created inside a docker image. This &lt;a href=&quot;https://github.com/JohannesKonings/aws-sso-steampipe/blob/main/Dockerfile&quot;&gt;docker image&lt;/a&gt; is based on Steampipe with additional installation of some tools, the AWS CLI and the AWS steampipe plugin.&lt;/p&gt;
&lt;h3&gt;Docker&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;FROM ghcr.io/turbot/steampipe

# Setup prerequisites (as root)
USER root:0
RUN apt-get update -y \
 &amp;amp;&amp;amp; apt-get install -y git curl unzip jq python3-pip python3-venv

# Install AWS CLI
RUN curl &quot;https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip&quot; -o &quot;awscliv2.zip&quot; \
 &amp;amp;&amp;amp; unzip awscliv2.zip \
 &amp;amp;&amp;amp; ./aws/install \
 &amp;amp;&amp;amp; rm -rf awscliv2.zip ./aws

# Install pipx
RUN pip3 install pipx

# Update steampipe to current version
RUN /bin/sh -c &quot;$(curl -fsSL https://steampipe.io/install/steampipe.sh)&quot;

# Install the aws and steampipe plugins for Steampipe (as steampipe user).
USER steampipe:0
RUN steampipe plugin install steampipe aws

# Install aws-sso-util for steampipe user
RUN pipx install aws-sso-util &amp;amp;&amp;amp; pipx ensurepath
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The Steampipe docu is here: &lt;a href=&quot;https://steampipe.io/docs/managing/containers&quot;&gt;https://steampipe.io/docs/managing/containers&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;After creating the image with &lt;code&gt;docker build -t steampipe-query .&lt;/code&gt;. The container can be created with the following command.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;docker run --entrypoint /bin/bash -it \
--mount type=bind,source=&quot;${PWD}/queries&quot;,target=/workspace/queries \
--mount type=bind,source=&quot;${PWD}/scripts&quot;,target=/workspace/scripts \
--mount type=bind,source=&quot;${PWD}/.env&quot;,target=/workspace/.env \
--name steampipe-query \
steampipe-query
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;These are the commands to use the container again &lt;code&gt;docker start -a steampipe-query&lt;/code&gt; and &lt;code&gt;docker exec -it steampipe-query /bin/bash&lt;/code&gt;.&lt;/p&gt;
&lt;h2&gt;Queries&lt;/h2&gt;
&lt;p&gt;One of the mount points was the folder queries, which contain, in this example, the SQL to check the lambda runtime.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;select
  account_id,
  _ctx -&amp;gt;&amp;gt; &apos;connection_name&apos; as connection_name,
  runtime,
  count(*),
  SUM(COUNT(*)) OVER() AS total_count
from
  aws_all.aws_lambda_function
where runtime not in (&apos;nodejs18.x&apos;, &apos;nodejs16.x&apos;, &apos;python3.9&apos;)
group by
  account_id,
  _ctx,
  runtime
order by
  connection_name,
  runtime,
  count;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The command to run this query is &lt;code&gt;steampipe query queries/lambda-runtime.sql&lt;/code&gt;. This will work after the scripts have created the profiles and connections config.&lt;/p&gt;
&lt;h2&gt;Scripts&lt;/h2&gt;
&lt;p&gt;The other mount points are scripts and the env file. The env file contains the necessary env variables for the scripts.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;SSO_START_URL= # https://&amp;lt;your-aws-account-id&amp;gt;.awsapps.com/start
SSO_SESSION_NAME= # &amp;lt;your session name, it&apos;s just a name&amp;gt;
SSO_REGION= # &amp;lt;your region, e.g. us-east-1&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;After setting the env file run the script &lt;code&gt;./scripts/create-aws-config.sh&lt;/code&gt; inside the container, which creates the file ~/.aws/config with SS0 session values using the &lt;a href=&quot;https://github.com/benkehoe/aws-sso-util&quot;&gt;aws-sso-util&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;It will look like this.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./sso-login.png&quot; alt=&quot;sso login&quot; /&gt;&lt;/p&gt;
&lt;p&gt;Open the link in the browser and put in the code.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./authorize-request.png&quot; alt=&quot;authorize request&quot; /&gt;&lt;/p&gt;
&lt;p&gt;Then, allow the access.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./allow-sso-to-access-data.png&quot; alt=&quot;allow sso to access data&quot; /&gt;&lt;/p&gt;
&lt;p&gt;The last step for the setup is to create the connections for Steampipe with the script &lt;code&gt;./scripts/create-steampipe-aws-config.sh&lt;/code&gt; inside the container. This will create a connection for each profile in the AWS config file ~/.aws/config.
Not every role is allowed to query the data, so it&apos;s necessary to set the env variable &lt;code&gt;ALLOWED_ROLES&lt;/code&gt; with the roles allowed to query the data. The roles are comma-separated. E.g.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;ALLOWED_ROLES=&quot;AWSReadOnlyAccess,AWSAdministratorAccess&quot;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;And now it&apos;s possible to run the queries with steampipe 🥳&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/aws-sso-steampipe&quot;&gt;https://github.com/JohannesKonings/aws-sso-steampipe&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;Sources&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/benkehoe/aws-sso-util&quot;&gt;aws-sso-util&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>Example how to use zod with CDK serverless v2</title><link>https://johanneskonings.dev/blog/2023-08-19-cdk-serverless-v2-demo-with-zod/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2023-08-19-cdk-serverless-v2-demo-with-zod/</guid><description>CDK serverless v2 is for using type saftey develepmonet base on schemas like openAPI. This post is a example how to use zod with CDK serverless v2</description><pubDate>Sat, 19 Aug 2023 08:15:18 GMT</pubDate><content:encoded>&lt;p&gt;The &lt;a href=&quot;https://github.com/taimos/cdk-serverless&quot;&gt;AWS CDK Serverless Toolsuite&lt;/a&gt; from &lt;a href=&quot;https://github.com/hoegertn&quot;&gt;Thorsten Hoeger&lt;/a&gt; helps, among others, to deploy an API Gateway from &lt;a href=&quot;https://www.openapis.org/&quot;&gt;OpenApi specs&lt;/a&gt; and a DynamoDb from &lt;a href=&quot;https://doc.onetable.io/&quot;&gt;DynamoDb onetable data modeling&lt;/a&gt;. The advantage is to leverage the type safety from Typscript generated from these files.&lt;/p&gt;
&lt;p&gt;That helps during the development cycle, but a runtime Typescript is Javascript without any type checking. This post is about enhancing this setup with &lt;a href=&quot;https://github.com/colinhacks/zod&quot;&gt;zod&lt;/a&gt; to validate the types during runtime.&lt;/p&gt;
&lt;p&gt;The workflow so far is to create a definition and generate Typescript types from that. Now the workflow has a step before to create a zod schema and derive the definitions from the zod schema.&lt;/p&gt;
&lt;h2&gt;Type checking without zod&lt;/h2&gt;
&lt;p&gt;As you can see, the types are available at development time via the OpenApi spec.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./addTodoTitleType.png&quot; alt=&quot;add Todo title type&quot; /&gt;&lt;/p&gt;
&lt;p&gt;This is because of the defined components in this &lt;a href=&quot;https://github.com/JohannesKonings/cdk-serverless-v2-demo/blob/main/src/definitions/myapi.yaml&quot;&gt;openApi spec&lt;/a&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;components:
  schemas:
    Todo:
      type: object
      required:
        - id
        - state
        - title
        - description
        - lastUpdate
      properties:
        id:
          type: string
        state:
          type: string
        title:
          type: string
        description:
          type: string
        lastUpdate:
          type: string
          format: date-time
    AddTodo:
      type: object
      required:
        - title
        - description
      properties:
        title:
          type: string
        description:
          type: string
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;But that didn&apos;t prevent you from using the false type during runtime.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./addTodoTitleAsNumber.png&quot; alt=&quot;add Todo title as number&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Type checking with zod&lt;/h2&gt;
&lt;p&gt;With a one-line parsing command, zod checks all the types.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./addTodoZodParsing.png&quot; alt=&quot;add Todo zod parsing&quot; /&gt;&lt;/p&gt;
&lt;p&gt;Furthermore, zod has some &lt;a href=&quot;https://github.com/colinhacks/zod#strings&quot;&gt;string-specific validations&lt;/a&gt; that can check if the email is valid.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;notificationsEmail: z.string().email(),
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&quot;./addTodoValidationResult.png&quot; alt=&quot;add Todo validation result&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Implementation&lt;/h2&gt;
&lt;p&gt;To implement that, first, the zod schemas are needed. &lt;a href=&quot;https://github.com/JohannesKonings/cdk-serverless-v2-demo/blob/main/src/zod/schema-todo.ts&quot;&gt;This setup&lt;/a&gt; has three schemas.
One for the API request, one for the API response and one how the data is stored.&lt;/p&gt;
&lt;p&gt;With zod it&apos;s possible to reference existing schema an extend fields or omit some.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import * as z from &quot;zod&quot;;

export const schemaTodoApi = z.object({
  id: z.string().uuid(),
  state: z.enum([&quot;OPEN&quot;, &quot;IN PROGRESS&quot;, &quot;DONE&quot;]).default(&quot;OPEN&quot;),
  title: z.string(),
  finishedInDays: z.number().int().positive(),
  notificationsEmail: z.string().email(),
  description: z.string().optional(),
  lastUpdate: z.string().datetime(),
});

export const schemaAddTodoApi = schemaTodoApi.omit({
  id: true,
  state: true,
  lastUpdate: true,
});

export const schemaTodoDdb = schemaTodoApi
  .extend({
    lastUpdated: z.string().datetime(),
  })
  .omit({ lastUpdate: true });
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;openApi&lt;/h3&gt;
&lt;p&gt;To create the openApi spec I&apos;m using the &lt;code&gt;@asteasolutions/zod-to-openapi&lt;/code&gt; package. zod has listed some more packages &lt;a href=&quot;https://github.com/colinhacks/zod#zod-to-x&quot;&gt;here&lt;/a&gt; which can be used.&lt;/p&gt;
&lt;p&gt;The definition of the apiSpec is now created via Typescript as you can see &lt;a href=&quot;https://github.com/JohannesKonings/cdk-serverless-v2-demo/blob/main/src/zod/openapi.ts&quot;&gt;here&lt;/a&gt; and generate a yaml file.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import fs from &quot;node:fs&quot;;
import {
  extendZodWithOpenApi,
  OpenAPIRegistry,
  OpenApiGeneratorV3,
} from &quot;@asteasolutions/zod-to-openapi&quot;;
import yaml from &quot;js-yaml&quot;;
import * as z from &quot;zod&quot;;
import { schemaAddTodoApi, schemaTodoApi } from &quot;./schema-todo&quot;;

extendZodWithOpenApi(z);

const registry = new OpenAPIRegistry();

const apiKeyComponent = registry.registerComponent(
  &quot;securitySchemes&quot;,
  &quot;api_key&quot;,
  {
    type: &quot;apiKey&quot;,
    name: &quot;x-api-key&quot;,
    in: &quot;header&quot;,
  },
);

registry.register(&quot;Todo&quot;, schemaTodoApi.openapi({}));
registry.register(&quot;AddTodo&quot;, schemaAddTodoApi.openapi({}));

registry.registerPath({
  method: &quot;get&quot;,
  path: &quot;/todos&quot;,
  summary: &quot;return list of todos&quot;,
  tags: [&quot;admin&quot;],
  security: [{ [apiKeyComponent.name]: [] }],
  operationId: &quot;getTodos&quot;,
  responses: {
    200: {
      description: &quot;successful operation&quot;,
      content: {
        &quot;application/json&quot;: {
          schema: {
            type: &quot;array&quot;,
            items: {
              $ref: &quot;#/components/schemas/Todo&quot;,
            },
          },
        },
        &quot;text/calendar&quot;: {
          schema: {
            type: &quot;string&quot;,
          },
        },
      },
    },
  },
});
registry.registerPath({
  method: &quot;post&quot;,
  path: &quot;/todos&quot;,
  summary: &quot;add new todo&quot;,
  tags: [&quot;admin&quot;],
  security: [{ [apiKeyComponent.name]: [] }],
  operationId: &quot;addTodo&quot;,
  requestBody: {
    required: true,
    content: {
      &quot;application/json&quot;: {
        schema: {
          $ref: &quot;#/components/schemas/AddTodo&quot;,
        },
      },
    },
  },
  responses: {
    201: {
      description: &quot;successful operation&quot;,
      content: {
        &quot;application/json&quot;: {
          schema: {
            $ref: &quot;#/components/schemas/Todo&quot;,
          },
        },
      },
    },
    401: {
      description: &quot;you are not logged in&quot;,
      content: {},
    },
    403: {
      description: &quot;you are not authorized to add todos&quot;,
      content: {},
    },
  },
  &quot;x-codegen-request-body-name&quot;: &quot;body&quot;,
});
registry.registerPath({
  method: &quot;post&quot;,
  path: &quot;/todos/{id}&quot;,
  summary: &quot;get a todo by its id&quot;,
  tags: [&quot;admin&quot;],
  security: [{ [apiKeyComponent.name]: [] }],
  operationId: &quot;getTodoById&quot;,
  responses: {
    200: {
      description: &quot;successful operation&quot;,
      content: {
        &quot;application/json&quot;: {
          schema: {
            $ref: &quot;#/components/schemas/Todo&quot;,
          },
        },
      },
    },
    401: {
      description: &quot;you are not logged in&quot;,
      content: {},
    },
    403: {
      description: &quot;you are not authorized to add todos&quot;,
      content: {},
    },
  },
});
registry.registerPath({
  method: &quot;delete&quot;,
  path: &quot;/todos/{id}&quot;,
  summary: &quot;delete a todo&quot;,
  tags: [&quot;admin&quot;],
  security: [{ [apiKeyComponent.name]: [] }],
  operationId: &quot;removeTodo&quot;,
  responses: {
    200: {
      description: &quot;successful operation&quot;,
      content: {},
    },
  },
});

const generator = new OpenApiGeneratorV3(registry.definitions);

const generatorDocument = generator.generateDocument({
  openapi: &quot;3.0.1&quot;,
  info: {
    version: &quot;1.0&quot;,
    title: &quot;Serverless Demo with zod&quot;,
  },
  tags: [
    {
      name: &quot;info&quot;,
    },
    {
      name: &quot;admin&quot;,
    },
  ],
});

const yamlString = yaml.dump(generatorDocument, { indent: 2 });

fs.writeFileSync(&quot;./src/definitions/myapi-zod.yaml&quot;, yamlString);
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;onetable&lt;/h3&gt;
&lt;p&gt;Unfortunately, for onetable didn&apos;t exist a npm package. So the conversion is made from scratch.
&lt;a href=&quot;https://github.com/JohannesKonings/cdk-serverless-v2-demo/blob/main/src/zod/onetable.ts&quot;&gt;This&lt;/a&gt; is how it looks like.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import fs from &quot;node:fs&quot;;
import { z } from &quot;zod&quot;;
import { schemaTodoDdb } from &quot;./schema-todo&quot;;

const modelTodoDdb = {
  PK: {
    type: &quot;string&quot;,
    value: &quot;TODO#${id}&quot;,
  },
  SK: {
    type: &quot;string&quot;,
    value: &quot;TODO#${id}&quot;,
  },
  id: {
    type: &quot;string&quot;,
    required: true,
    generate: &quot;uuid&quot;,
  },
  GSI1PK: {
    type: &quot;string&quot;,
    value: &quot;TODOS&quot;,
  },
  GSI1SK: {
    type: &quot;string&quot;,
    value: &quot;${state}#${title}&quot;,
  },
};

const schemaTodoValues = schemaTodoDdb.keyof().Values;

const modelTodoFields = Object.keys(schemaTodoValues).reduce((acc, key) =&amp;gt; {
  const keyOfSchemaTodoKeyValues = key as keyof typeof schemaTodoValues;
  const shapeType = schemaTodoDdb.shape[keyOfSchemaTodoKeyValues];

  const { type, required, generate, enumValues, defaultValue } =
    deriveAttributes(shapeType);

  return {
    ...acc,
    [key]: {
      type: type,
      required: required,
      generate: generate,
      enum: enumValues,
      default: defaultValue,
    },
  };
}, {});

function deriveAttributes(shapeType: z.ZodType&amp;lt;any, any&amp;gt;) {
  let type = &quot;&quot;;
  let required = false;
  let generate = undefined;
  let enumValues = [] as string[];
  let defaultValue = undefined;

  if (shapeType === undefined) {
    throw new Error(&quot;type is undefined&quot;);
  } else if (shapeType instanceof z.ZodString) {
    type = &quot;string&quot;;
    required = true;
    generate = shapeType.isUUID ? &quot;uuid&quot; : undefined;
  } else if (shapeType instanceof z.ZodNumber) {
    type = &quot;number&quot;;
    required = true;
  } else if (shapeType instanceof z.ZodEnum) {
    required = true;
    type = &quot;string&quot;;
    enumValues = shapeType._def.values;
  } else if (shapeType instanceof z.ZodDefault) {
    required = true;
    defaultValue = shapeType._def.defaultValue();
    const { type: typeInnerType, enumValues: enumInnerType } = deriveAttributes(
      shapeType._def.innerType,
    );
    type = typeInnerType;
    enumValues = enumInnerType as string[];
  } else if (shapeType instanceof z.ZodOptional) {
    required = false;
    const { type: typeInnerType } = deriveAttributes(shapeType._def.innerType);
    type = typeInnerType;
  } else {
    console.log(&quot;shapeType&quot;, shapeType);
    throw new Error(&quot;type is not supported&quot;);
  }
  return {
    type,
    required: required ? true : undefined,
    generate,
    enumValues: enumValues &amp;amp;&amp;amp; enumValues.length &amp;gt; 0 ? enumValues : undefined,
    defaultValue,
  };
}

export const modelTodo = {
  ...modelTodoDdb,
  ...modelTodoFields,
};

const onetable = {
  indexes: {
    primary: {
      hash: &quot;PK&quot;,
      sort: &quot;SK&quot;,
    },
    GSI1: {
      hash: &quot;GSI1PK&quot;,
      sort: &quot;GSI1SK&quot;,
      project: &quot;all&quot;,
    },
    LSI1: {
      type: &quot;local&quot;,
      sort: &quot;lastUpdated&quot;,
      project: [&quot;id&quot;, &quot;lastUpdated&quot;, &quot;title&quot;],
    },
  },
  models: {
    Todo: modelTodo,
  },
  version: &quot;0.1.0&quot;,
  format: &quot;onetable:1.1.0&quot;,
  queries: {},
};

fs.writeFileSync(
  &quot;./src/definitions/mymodel-zod.json&quot;,
  JSON.stringify(onetable, null, 2),
);
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Integration into the file creation workflow&lt;/h3&gt;
&lt;p&gt;The definition files can now be generated based on a zod schema. So that that will happen together with generating the files from the spec the projen.ts file need to be enhanced. This will create two commands before.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const taskDefinitionsCreation = project.addTask(&quot;definitionsCreation&quot;, {
  steps: [
    { exec: &quot;ts-node ./src/zod/openapi.ts&quot; },
    { exec: &quot;ts-node ./src/zod/onetable.ts&quot; },
  ],
});
project.defaultTask!.prependSpawn(taskDefinitionsCreation);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Than the steps look like this.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;  &quot;default&quot;: {
      &quot;name&quot;: &quot;default&quot;,
      &quot;description&quot;: &quot;Synthesize project files&quot;,
      &quot;steps&quot;: [
        {
          &quot;spawn&quot;: &quot;definitionsCreation&quot;
        },
        {
          &quot;exec&quot;: &quot;ts-node --project tsconfig.dev.json .projenrc.ts&quot;
        },
        {
          &quot;spawn&quot;: &quot;generate:api:myapi&quot;
        }
      ]
    },
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Now with the command &lt;code&gt;npm run projen&lt;/code&gt; the definition file are created derived from zod and from that on the workflow is like before.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/cdk-serverless-v2-demo&quot;&gt;https://github.com/JohannesKonings/cdk-serverless-v2-demo&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>Example how to trigger a Dynamodb export and create an Athena saved query with CDK</title><link>https://johanneskonings.dev/blog/2022-10-03-aws-example-ddb-export-athena-query/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2022-10-03-aws-example-ddb-export-athena-query/</guid><description>This post is how to trigger a Dynamodb export and create saved query to create a Athena table from the exported data</description><pubDate>Mon, 03 Oct 2022 08:15:18 GMT</pubDate><content:encoded>&lt;p&gt;In [this]({{ site.baseurl }}/aws/2021/08/27/aws_example_ddb_analytics_cdk/) post is described how to get the data to analyze the changes in the dynamodb data. This post describes how to (semi) automate the export of the dynamodb table data and analyze it with Athena. &lt;a href=&quot;https://aws.amazon.com/de/blogs/aws/new-export-amazon-dynamodb-table-data-to-data-lake-amazon-s3/&quot;&gt;This&lt;/a&gt; post describes how you can do that manually.&lt;/p&gt;
&lt;p&gt;One approach is with a lambda and another approach is with step functions. Both approaches implement the steps for triggering the export to a S3 bucket, create an athena table for that exported data and prepare a namend query for analyzing.&lt;/p&gt;
&lt;p&gt;The data for this example looks like this.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./ddb-export-ddb-data.png&quot; alt=&quot;ddb export ddb data&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;With lambda&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk/blob/main/cdk/lib/ddb-export/ddb-export.lambda-function-ddb-export.ts&quot;&gt;This lambda&lt;/a&gt; triggers the export with via the sdk and create or update a named query.&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk/blob/main/cdk/lib/ddb-export/createTable.sql&quot;&gt;The query&lt;/a&gt; creates the athena table. The export id will be set by the lambda by replacing the &quot;s3location&quot; with something like &lt;code&gt;s3://&amp;lt;&amp;lt;bucket name&amp;gt;&amp;gt;/ddb-exports/AWSDynamoDB/&amp;lt;&amp;lt;ddb-export-id&amp;gt;&amp;gt;/data/&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;CREATE EXTERNAL TABLE ddb_exported_table (
 Item struct&amp;lt;pk:struct&amp;lt;S:string&amp;gt;,
             person:struct&amp;lt;M:struct&amp;lt;
                jobArea:struct&amp;lt;S:string&amp;gt;,
                firstname:struct&amp;lt;S:string&amp;gt;,
                gender:struct&amp;lt;S:string&amp;gt;,
                jobType:struct&amp;lt;S:string&amp;gt;,
                jobDescriptor:struct&amp;lt;S:string&amp;gt;,
                lastname:struct&amp;lt;S:string&amp;gt;
                &amp;gt;&amp;gt;&amp;gt;
)
ROW FORMAT SERDE &apos;org.openx.data.jsonserde.JsonSerDe&apos;
LOCATION &apos;s3Location&apos;
TBLPROPERTIES ( &apos;has_encrypted_data&apos;=&apos;true&apos;);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk/blob/main/cdk/lib/ddb-export/readTable.sql&quot;&gt;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk/blob/main/cdk/lib/ddb-export/readTable.sql&lt;/a&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;SELECT
item.pk.S as pk,
item.person.M.firstname.S as firstname,
item.person.M.lastname.S as lastname,
item.person.M.jobArea.S as jobArea,
item.person.M.gender.S as gender,
item.person.M.jobType.S as jobType,
item.person.M.jobDescriptor.S as jobDescriptor
FROM &quot;db_name&quot;.&quot;table_name&quot;;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;After you started the lambda you have to wait until the export is finished. Then you can run the query for creating the athena table. The lambda has already deleted the old table. After that you can use the prepared query for analyzing.&lt;/p&gt;
&lt;p&gt;A more orchestrated approach is with step function. That&apos;s better for waiting for the results :)&lt;/p&gt;
&lt;h2&gt;With step functions&lt;/h2&gt;
&lt;p&gt;This are the steps, which are orchestrated by the step function.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./ddb-export-sfn.png&quot; alt=&quot;ddb export sfn&quot; /&gt;&lt;/p&gt;
&lt;p&gt;It&apos;s definend &lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk/blob/main/cdk/lib/ddb-export/ddb-export-step-function.ts&quot;&gt;here&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;The step function could be startet with the default values.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./ddb-export-sfn-start-1.png&quot; alt=&quot;ddb export sfn start 1&quot; /&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./ddb-export-sfn-start-2.png&quot; alt=&quot;ddb export sfn start 2&quot; /&gt;&lt;/p&gt;
&lt;p&gt;It takes some minutes to complete.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./ddb-export-sfn-run.png&quot; alt=&quot;ddb export sfn run&quot; /&gt;&lt;/p&gt;
&lt;p&gt;The &quot;recent queries&quot; section list the steps for dropping the old table and create the new one.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./ddb-export-sfn-recent-queries.png&quot; alt=&quot;ddb export sfn athena recent queries&quot; /&gt;&lt;/p&gt;
&lt;p&gt;After it&apos;s finished you can choose the saved query with the name &lt;code&gt;sfn-ddb-export-read-table&lt;/code&gt;. It can be used to query all the data from the dynamodb table and could be adapted to more &quot;complex&quot; queries.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./ddb-export-sfn-athena-query.png&quot; alt=&quot;ddb export sfn athena query&quot; /&gt;&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk&quot;&gt;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>Example how to visualize DynamoDB item changes with Quicksight (S3 source) created with CDK</title><link>https://johanneskonings.dev/blog/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/</guid><description>This post is how to visualize the streamed data changes of a DynamoDb table via Kinesis Data Stream and Kinesis Firehose to S3. Build with CDK.</description><pubDate>Sat, 17 Sep 2022 08:15:18 GMT</pubDate><content:encoded>&lt;p&gt;This post is about how to visualize the DynamoDb data changes with Quicksight. It&apos;s an extension of [this]({{ site.baseurl }}/aws/2021/08/27/aws_example_ddb_analytics_cdk/) post, which describes how to analyze the data with Athena.
The setting for creating the DynamoDb table and putting the data changes to a S3 bucket is the same. Instead of creating an Athena table of the data in the S3 bucket, this data is linked to a data source in Quicksight.&lt;/p&gt;
&lt;h2&gt;Quicksight activation and costs&lt;/h2&gt;
&lt;p&gt;Quicksight needs to be activated before using. It&apos;s enough to use the standard edition for this scenario. The first 30 days are &lt;a href=&quot;https://docs.aws.amazon.com/quicksight/latest/user/signing-up.html&quot;&gt;free&lt;/a&gt;. Costs are listed &lt;a href=&quot;https://aws.amazon.com/quicksight/pricing/&quot;&gt;here&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;![quicksight open service]({{ site.baseurl }}/img/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/quicksight-open-service.png)&lt;/p&gt;
&lt;p&gt;![quicksight sign up]({{ site.baseurl }}/img/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/quicksight-sign-up.png)&lt;/p&gt;
&lt;p&gt;![quicksight choose standard]({{ site.baseurl }}/img/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/quicksight-choose-standard.png)&lt;/p&gt;
&lt;p&gt;Enter a name and an email address&lt;/p&gt;
&lt;p&gt;![quicksight create standard]({{ site.baseurl }}/img/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/quicksight-create-standard.png)&lt;/p&gt;
&lt;h2&gt;Quicksight role&lt;/h2&gt;
&lt;p&gt;In the &lt;a href=&quot;https://docs.aws.amazon.com/quicksight/latest/user/security_iam_service-with-iam.html#security-create-iam-role&quot;&gt;standard edition&lt;/a&gt;, Quicksight uses a standard role that could be configured via the Quicksight console.&lt;/p&gt;
&lt;p&gt;![quicksight permission access to aws services]({{ site.baseurl }}/img/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/quicksight-permission-access-to-aws-services.png)&lt;/p&gt;
&lt;p&gt;Unfortunately, it is not possible to allow for specific KMS keys. For that, we need to add a policy to the role aws-quicksight-service-role-v0.&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk/blob/main/cdk/lib/quicksight/quicksight-role.ts&quot;&gt;This&lt;/a&gt; ads the needed permissions to that role.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { Construct } from &quot;constructs&quot;;
import { aws_iam as iam, aws_s3 as s3 } from &quot;aws-cdk-lib&quot;;

export interface QuicksightRoleProps {
  name: string;
  bucket: s3.IBucket;
}

export class QuicksightRole extends Construct {
  constructor(scope: Construct, id: string, props: QuicksightRoleProps) {
    super(scope, id);

    const quicksightRoleName = &quot;aws-quicksight-service-role-v0&quot;;

    const quicksightRole = iam.Role.fromRoleName(
      this,
      &quot;quicksight-role&quot;,
      quicksightRoleName,
    );

    quicksightRole.attachInlinePolicy(
      new iam.Policy(this, `${props.name}-policy`, {
        statements: [
          new iam.PolicyStatement({
            actions: [&quot;kms:Decrypt&quot;, &quot;s3:GetObject&quot;, &quot;s3:List*&quot;],
            resources: [
              props.bucket.bucketArn,
              `${props.bucket.bucketArn}/*`,
              props.bucket.encryptionKey!.keyArn,
            ],
          }),
        ],
      }),
    );
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Now it&apos;s possible to create a datasource from the S3 bucket.&lt;/p&gt;
&lt;h2&gt;Create a datasource and dataset&lt;/h2&gt;
&lt;p&gt;In Quicksight a data source is the connection to the data and the data set is using this data source and defining how the data will be used.&lt;/p&gt;
&lt;p&gt;Quicksight has a lot of different data sources. We want to use the data from S3.&lt;/p&gt;
&lt;p&gt;![quicksight datasource kinds]({{ site.baseurl }}/img/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/quicksight-datasource-kinds.png)&lt;/p&gt;
&lt;p&gt;Currently, there is no L2 CDK construct for data sources so we need to use the L1 cloud formation.&lt;/p&gt;
&lt;p&gt;In the case of a S3 data source, it&apos;s a link to a manifest file&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const datasource = new quicksightCfn.CfnDataSource(this, &quot;datasource&quot;, {
  name: datasourceName,
  type: &quot;S3&quot;,
  awsAccountId: Stack.of(this).account,
  dataSourceId: datasourceName,
  dataSourceParameters: {
    s3Parameters: {
      manifestFileLocation: {
        bucket: props.bucket.bucketName,
        key: manifestKey,
      },
    },
  },
  permissions: permissionsDatasource,
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This is the definition of the manifest file. More about manifest files &lt;a href=&quot;https://docs.aws.amazon.com/quicksight/latest/user/supported-manifest-file-format.html&quot;&gt;here&lt;/a&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const manifest = {
  fileLocations: [
    {
      URIPrefixes: [`s3://${props.bucket.bucketName}/${props.prefix}/`],
    },
  ],
  globalUploadSettings: {
    format: &quot;JSON&quot;,
  },
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The dataset then defines which fields will be used and has the potential to format these fields.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const dataset = new quicksightCfn.CfnDataSet(this, &quot;dataset&quot;, {
  name: datasetName,
  awsAccountId: Stack.of(this).account,
  dataSetId: datasetName,
  importMode: &quot;SPICE&quot;,
  physicalTableMap: {
    itemChanges: {
      s3Source: {
        dataSourceArn: datasource.attrArn,
        uploadSettings: {
          format: &quot;JSON&quot;,
        },
        inputColumns: [
          {
            name: &quot;awsRegion&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;eventID&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;eventName&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;userIdentity&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;recordFormat&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;tableName&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;dynamodb.ApproximateCreationDateTime&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;dynamodb.Keys.pk.S&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;dynamodb.NewImage.pk.S&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;dynamodb.NewImage.person.M.jobArea.S&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;dynamodb.NewImage.person.M.firstname.S&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;dynamodb.NewImage.person.M.gender.S&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;dynamodb.NewImage.person.M.jobType.S&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;dynamodb.NewImage.person.M.jobDescriptor.S&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;dynamodb.NewImage.person.M.lastname.S&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;dynamodb.SizeBytes&quot;,
            type: &quot;STRING&quot;,
          },
          {
            name: &quot;eventSource&quot;,
            type: &quot;STRING&quot;,
          },
        ],
      },
    },
  },
  logicalTableMap: {
    logicalTableProperty: {
      alias: `${datasetName}-alias`,
      source: { physicalTableId: &quot;itemChanges&quot; },
    },
  },
  permissions: permissionsDataset,
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The whole definition is &lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk/blob/main/cdk/lib/quicksight/quicksight.ts&quot;&gt;here&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://aws-blog.com/2021/09/building-quicksight-datasets-with-cdk-s3.html&quot;&gt;Here&lt;/a&gt; is a post that describes it with Python CDK.&lt;/p&gt;
&lt;h3&gt;permission to see the datasource and dataset&lt;/h3&gt;
&lt;p&gt;Datasources and datasets will be only displayed if your user has permission for that. That is not automatically the case if you deploy it with CDK.
Therefore you need to put your user ARN to the permission. One way to do that is with an environment variable&lt;/p&gt;
&lt;p&gt;&lt;code&gt;QUICKSIGHT_USERNAME=&amp;lt;&amp;lt;Quicksight user name&amp;gt;&amp;gt; npx cdk deploy&lt;/code&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const quicksightUsername = process.env.QUICKSIGHT_USERNAME;
const principalArn = `arn:aws:quicksight:${Stack.of(this).region}:${Stack.of(this).account}:user/default/${quicksightUsername}`;

const permissionsDatasource = [
  {
    principal: principalArn,
    actions: [
      &quot;quicksight:DescribeDataSource&quot;,
      &quot;quicksight:DescribeDataSourcePermissions&quot;,
      &quot;quicksight:PassDataSource&quot;,
      &quot;quicksight:UpdateDataSource&quot;,
      &quot;quicksight:DeleteDataSource&quot;,
      &quot;quicksight:UpdateDataSourcePermissions&quot;,
    ],
  },
];

const permissionsDataset = [
  {
    principal: principalArn,
    actions: [
      &quot;quicksight:DescribeDataSet&quot;,
      &quot;quicksight:DescribeDataSetPermissions&quot;,
      &quot;quicksight:PassDataSet&quot;,
      &quot;quicksight:DescribeIngestion&quot;,
      &quot;quicksight:ListIngestions&quot;,
      &quot;quicksight:UpdateDataSet&quot;,
      &quot;quicksight:DeleteDataSet&quot;,
      &quot;quicksight:CreateIngestion&quot;,
      &quot;quicksight:CancelIngestion&quot;,
      &quot;quicksight:UpdateDataSetPermissions&quot;,
    ],
  },
];
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;You can find the Quicksight username here&lt;/p&gt;
&lt;p&gt;![quicksight username]({{ site.baseurl }}/img/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/quicksight-username.png)&lt;/p&gt;
&lt;h3&gt;Deployment and refresh of the dataset&lt;/h3&gt;
&lt;p&gt;If you deploy this setup the first time, the data does not exist, but the datasource links already to the data. That causes a deployment error.
To avoid this a little dummy data file will be deployed with a SDK call of a CDK custom resource&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const dummyJsonString = JSON.stringify({ dummy: &apos;dummy&apos;}); // Delete after deplyoment
    const customResourcePutObject = new custom_resources.AwsCustomResource(this, &apos;prefix-creation&apos;, { // add -put
      onCreate: {
        service: &apos;S3&apos;,
        action: &apos;putObject&apos;,
        parameters: {
          Bucket: props.bucket.bucketName,
          Key: `${props.prefix}/dummy.json`,
          Body: dummyJsonString,
        },
        physicalResourceId: custom_resources.PhysicalResourceId.of(&apos;prefix-creation&apos;),
      },
      policy: custom_resources.AwsCustomResourcePolicy.fromSdkCalls({ resources: custom_resources.AwsCustomResourcePolicy.ANY_RESOURCE }),
    });
    props.
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;After the datasource is deployed this will be removed&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const customResourceDeleteObject = new custom_resources.AwsCustomResource(
  this,
  &quot;prefix-creation-delete&quot;,
  {
    onCreate: {
      service: &quot;S3&quot;,
      action: &quot;deleteObject&quot;,
      parameters: {
        Bucket: props.bucket.bucketName,
        Key: `${props.prefix}/dummy.json`,
      },
      physicalResourceId:
        custom_resources.PhysicalResourceId.of(&quot;prefix-creation&quot;),
    },
    policy: custom_resources.AwsCustomResourcePolicy.fromSdkCalls({
      resources: custom_resources.AwsCustomResourcePolicy.ANY_RESOURCE,
    }),
  },
);
props.bucket.grantReadWrite(customResourceDeleteObject);
customResourceDeleteObject.node.addDependency(dataset);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;After there is some data in the dynamodb you have to refresh the dataset. This is how it looks like if you create 5 new entries and then modify 1 and refresh again and use this data in an analysis.&lt;/p&gt;
&lt;p&gt;![quicksight refresh dataset]({{ site.baseurl }}/img/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/quicksight-refresh-dataset.png)&lt;/p&gt;
&lt;p&gt;![quicksight dataset imported rows]({{ site.baseurl }}/img/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/quicksight-dataset-imported-rows.png)&lt;/p&gt;
&lt;p&gt;![quicksight dataset analysis]({{ site.baseurl }}/img/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/quicksight-dataset-analysis.png)&lt;/p&gt;
&lt;p&gt;![quicksight analysis inserts]({{ site.baseurl }}/img/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/quicksight-analysis-inserts.png)&lt;/p&gt;
&lt;p&gt;![quicksight analysis modify]({{ site.baseurl }}/img/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/quicksight-analysis-modify.png)&lt;/p&gt;
&lt;h1&gt;Cost Alert 💰&lt;/h1&gt;
&lt;p&gt;⚠️ Don&apos;t forget to delete the Quicksight account after testing.&lt;/p&gt;
&lt;p&gt;![quicksight delete account]({{ site.baseurl }}/img/2022-09-17-aws_example_ddb_analytics_quicksight_cdk/quicksight-delete-account.png)&lt;/p&gt;
&lt;h1&gt;Code&lt;/h1&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk&quot;&gt;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>Example how to create Athena saved queries with CDK</title><link>https://johanneskonings.dev/blog/2022-08-22-aws_example_ddb_analytics_athena_saved_queries_cdk/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2022-08-22-aws_example_ddb_analytics_athena_saved_queries_cdk/</guid><description>This post is about how saved queries are created with CDK. This is useful to have important queries prepared for any users.</description><pubDate>Mon, 22 Aug 2022 08:15:18 GMT</pubDate><content:encoded>&lt;p&gt;In [this]({{ site.baseurl }}/aws/2021/08/27/aws_example_ddb_analytics/) post is a example of a Athena query to get the the current data of the DynamoDb table.&lt;/p&gt;
&lt;p&gt;This post explains how to provide this query with CDK as a saved query in Athena to have the query stored &quot;nearby&quot; the editor and that the query fits to the current deployment regarding the naming of the DB and table.
Another advantage is to have the query under source control.&lt;/p&gt;
&lt;p&gt;This is a extension of [this]({{ site.baseurl }}/aws/2021/10/26/aws_example_ddb_analytics_cdk/) post.&lt;/p&gt;
&lt;p&gt;The saved queries are stored here in the console:&lt;/p&gt;
&lt;p&gt;![athena save queries]({{ site.baseurl }}/img/2022-08-22-aws_example_ddb_analytics_athena_saved_queries_cdk/athena_saved_queries.png)&lt;/p&gt;
&lt;h1&gt;The SQL command&lt;/h1&gt;
&lt;p&gt;This SQL command will be provided as a saved query in Athena. For easier maintenance, the command is in an extra &lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk/blob/main/cdk/lib/saved-queries/ddb-state.sql&quot;&gt;SQL file&lt;/a&gt;.
The DB and the table name are placeholders in the file and will be replaced during the deployment process. That ensures that this SQL command refers to the correct resources, which are also deployed.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;SELECT dynamodb.newimage.pk.s AS pk,
        dynamodb.newimage.person.M.firstname.s AS firstname,
        dynamodb.newimage.person.M.lastname.s AS lastname,
        dynamodb.approximatecreationdatetime AS ts,
        dynamodb.newimage,
        *
FROM &quot;athenaDbName&quot;.&quot;athenaTableName&quot; AS persons1
WHERE (eventname = &apos;INSERT&apos;
        OR eventname = &apos;MODIFY&apos;)
        AND dynamodb.approximatecreationdatetime =
    (SELECT MAX(dynamodb.approximatecreationdatetime)
    FROM &quot;athenaDbName&quot;.&quot;athenaTableName&quot; AS persons2
    WHERE persons2.dynamodb.newimage.pk.s = persons1.dynamodb.newimage.pk.s);
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Saved Query Construct&lt;/h1&gt;
&lt;p&gt;Like the SQL file, the CDK definition of the saved query is in an &lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk/blob/main/cdk/lib/saved-queries/saved-queries.ts&quot;&gt;extra&lt;/a&gt;&lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk/blob/main/cdk/lib/saved-queries/saved-queries.ts&quot;&gt; file&lt;/a&gt; as a CDK construct.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import { Construct } from &quot;constructs&quot;;
import { aws_athena as athenaCfn } from &quot;aws-cdk-lib&quot;;
import * as glue from &quot;@aws-cdk/aws-glue-alpha&quot;;
import { readFileSync } from &quot;fs&quot;;
import { join } from &quot;path&quot;;

export interface SavedQueriesProps {
  glueDb: glue.IDatabase;
  athenaTableName: string;
  athenaWorkgroupName: string;
}

export class SavedQueries extends Construct {
  constructor(scope: Construct, id: string, props: SavedQueriesProps) {
    super(scope, id);

    const getSqlString = (file: string): string =&amp;gt; {
      let personsDdbStateSqlCommand = readFileSync(
        join(__dirname, `${file}`),
        &quot;utf-8&quot;,
      ).toString();
      const athenaDbName = props.glueDb.databaseName;
      let athenaTableName = props.athenaTableName;
      athenaTableName = athenaTableName.replace(/-/g, &quot;_&quot;);
      personsDdbStateSqlCommand = personsDdbStateSqlCommand.replace(
        /athenaDbName/g,
        athenaDbName,
      );
      personsDdbStateSqlCommand = personsDdbStateSqlCommand.replace(
        /athenaTableName/g,
        athenaTableName,
      );
      return personsDdbStateSqlCommand;
    };

    let queryString = getSqlString(&quot;ddb-state.sql&quot;);

    // eslint-disable-next-line no-new
    new athenaCfn.CfnNamedQuery(this, &quot;query-current-ddb-state&quot;, {
      database: props.glueDb.databaseName,
      queryString: queryString,
      description: &quot;query the current state from the ddb table&quot;,
      name: &quot;current-ddb-state&quot;,
      workGroup: props.athenaWorkgroupName,
    });
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Currently, there is no L2 Construct for a named query in CDK, so it is defined as a L1 CFN Resource (&lt;code&gt;CfnNamedQuery&lt;/code&gt;).&lt;/p&gt;
&lt;p&gt;This needs the query as a string. The function &lt;code&gt;getSqlString&lt;/code&gt; converts and transforms the SQL file content to that needed string.&lt;/p&gt;
&lt;p&gt;So that the placeholders are replaced with the resources, which are deployed.&lt;/p&gt;
&lt;p&gt;The Athena table name will be created during the glue crawler process. The convention is that the table name configured prefix of the S3 bucket with an underscore (&quot;_&quot;) instead of dashes (&quot;-&quot;).&lt;/p&gt;
&lt;p&gt;The database name and the workgroup name are passed from the stack to the construct.&lt;/p&gt;
&lt;h1&gt;Insert the saved query construct in the stack&lt;/h1&gt;
&lt;p&gt;For the deployment the saved queries construct will be inserted into the &lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk/blob/main/cdk/lib/cdk-stack.ts#L211&quot;&gt;stack&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;The needed information are passed as props to the construct.&lt;/p&gt;
&lt;p&gt;The saved queries are depending on the workgroup. That has to be defined in CDK. Otherwise, the deployment will fail.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const savedQueries = new SavedQueries(this, &quot;saved-queries&quot;, {
  glueDb: glueDb,
  athenaTableName: firehoseBucketName,
  athenaWorkgroupName: athenaWorkgroup.name,
});

savedQueries.node.addDependency(athenaWorkgroup);
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Result&lt;/h1&gt;
&lt;p&gt;After the deployment, the new query is listed here and can be chosen for query in the editor.&lt;/p&gt;
&lt;p&gt;![athena save query deployed]({{ site.baseurl }}/img/2022-08-22-aws_example_ddb_analytics_athena_saved_queries_cdk/athena_saved_query_deployed.png)&lt;/p&gt;
&lt;p&gt;![athena save queries]({{ site.baseurl }}/img/2022-08-22-aws_example_ddb_analytics_athena_saved_queries_cdk/athena_saved_query_editor.png)&lt;/p&gt;
&lt;h1&gt;Code&lt;/h1&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk&quot;&gt;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>Example how to analyze DynamoDB item changes with Kinesis and Athena created with CDK</title><link>https://johanneskonings.dev/blog/2021-10-26-aws_example_ddb_analytics_cdk/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2021-10-26-aws_example_ddb_analytics_cdk/</guid><description>This post is how stream data changes of a DynamoDb table via Kinesis Data Stream and Kinesis Firehose to S3, and analyze the data with Athena. Build with CDK.</description><pubDate>Tue, 26 Oct 2021 08:15:18 GMT</pubDate><content:encoded>&lt;p&gt;This is the same like described [here]({{ site.baseurl }}/aws/2021/08/27/aws_example_ddb_analytics/), but instead of terraform it&apos;s build with &lt;a href=&quot;https://aws.amazon.com/cdk/&quot;&gt;CDK&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;To bootstrap the project run this command: &lt;code&gt;cdk init app --language typescript&lt;/code&gt;
Further information are &lt;a href=&quot;https://docs.aws.amazon.com/cdk/latest/guide/hello_world.html&quot;&gt;here&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;All the services are in &lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk/blob/main/cdk/lib/cdk-stack.ts&quot;&gt;this&lt;/a&gt; file.&lt;/p&gt;
&lt;h2&gt;Updates&lt;/h2&gt;
&lt;p&gt;2022-09-11: Add prefix for kinesis data firehose S3 destination
2022-08-13: CDK migrated to v2&lt;/p&gt;
&lt;h2&gt;KMS key&lt;/h2&gt;
&lt;p&gt;This creates are KMS key with an alias to encrypt the data in the created services.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const kmsKey = new kms.Key(this, &quot;kmsKey&quot;, {
  enableKeyRotation: true,
});

kmsKey.addAlias(name);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;DynamoDb and Kinesis Data Stream&lt;/h2&gt;
&lt;p&gt;This is the creation of the DynamoDb with the Kinesis Data Stream.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const stream = new kinesis.Stream(this, &quot;Stream&quot;, {
  streamName: `${name}-data-stream`,
  encryption: kinesis.StreamEncryption.KMS,
  encryptionKey: kmsKey,
});

const table = new dynamodb.Table(this, &quot;Table&quot;, {
  tableName: name,
  partitionKey: { name: &quot;pk&quot;, type: dynamodb.AttributeType.STRING },
  billingMode: dynamodb.BillingMode.PAY_PER_REQUEST,
  encryption: dynamodb.TableEncryption.CUSTOMER_MANAGED,
  encryptionKey: kmsKey,
  kinesisStream: stream,
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;That adds to the DynamoDb, a Kinesis Data Stream, and connects it to the DynamoDb.&lt;/p&gt;
&lt;p&gt;![kinesis data stream]({{ site.baseurl }}/img/2021-10-26-aws_example_ddb_analytics_cdk/kinesis_data_stream.png)&lt;/p&gt;
&lt;p&gt;![kinesis data stream ddb]({{ site.baseurl }}/img/2021-10-26-aws_example_ddb_analytics_cdk/kinesis_data_stream_ddb.png)&lt;/p&gt;
&lt;h2&gt;Kinesis Data Firehose and S3 Bucket&lt;/h2&gt;
&lt;p&gt;Kinesis Data Firehose is the connection between the Kinesis Data Stream to the S3 Bucket.&lt;/p&gt;
&lt;p&gt;Unfortunately, Firehose stores the JSONs without a linefeed. Therefore it&apos;s a lambda for conversion is necessary.&lt;/p&gt;
&lt;p&gt;More about that is described in this &lt;a href=&quot;https://medium.com/analytics-vidhya/append-newline-to-amazon-kinesis-firehose-json-formatted-records-with-python-f58498d0177a&quot;&gt;post&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;To have the kinesis firehose data isolated under a &quot;namespace&quot; we use a prefix.&lt;/p&gt;
&lt;p&gt;It looks like this.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const firehoseBucket = new s3.Bucket(this, &quot;firehose-s3-bucket&quot;, {
  bucketName: `${name}-firehose-s3-bucket`,
  encryptionKey: kmsKey,
});

const processor = new lambda.NodejsFunction(this, &quot;lambda-function-processor&quot;, {
  functionName: `${name}-firehose-converter`,
  timeout: cdk.Duration.minutes(2),
  bundling: {
    sourceMap: true,
  },
});

const lambdaProcessor = new LambdaFunctionProcessor(processor, {
  retries: 5,
});

const ddbChangesPrefix = &quot;ddb-changes&quot;;

const s3Destination = new destinations.S3Bucket(firehoseBucket, {
  encryptionKey: kmsKey,
  bufferingInterval: cdk.Duration.seconds(60),
  processor: lambdaProcessor,
  dataOutputPrefix: `${ddbChangesPrefix}/`,
});

const firehoseDeliveryStream = new firehose.DeliveryStream(
  this,
  &quot;Delivery Stream&quot;,
  {
    deliveryStreamName: `${name}-firehose`,
    sourceStream: stream,
    destinations: [s3Destination],
  },
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The delivery of the data to the S3 bucket is buffered. Here are the default values.&lt;/p&gt;
&lt;p&gt;![firehose-buffer]({{ site.baseurl }}/img/2021-10-26-aws_example_ddb_analytics_cdk/firehose_buffer.png)&lt;/p&gt;
&lt;h2&gt;Glue crawler&lt;/h2&gt;
&lt;p&gt;Athena needs a structured table for the SQL queries. The Glue crawler creates this from the data in the S3 bucket below the configured prefix.&lt;/p&gt;
&lt;p&gt;The glue crawler isn&apos;t a L2 construct yet. So it needs a L1 construct. See &lt;a href=&quot;https://blog.phillipninan.com/a-no-nonsense-guide-to-aws-cloud-development-kit-cdk&quot;&gt;here&lt;/a&gt; more about L1 - L3.&lt;/p&gt;
&lt;p&gt;There is already a &lt;a href=&quot;https://github.com/aws/aws-cdk/issues/8863&quot;&gt;github issue&lt;/a&gt; to create a L2 construct for the glue crawler.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const getSecurityConfiguration = new iam.PolicyDocument({
  statements: [
    new iam.PolicyStatement({
      actions: [&quot;glue:GetSecurityConfiguration&quot;],
      resources: [&quot;*&quot;],
    }),
  ],
});

const roleCrawler = new iam.Role(this, &quot;role crawler&quot;, {
  roleName: `${name}-crawler-role`,
  assumedBy: new iam.ServicePrincipal(&quot;glue.amazonaws.com&quot;),
  inlinePolicies: {
    GetSecurityConfiguration: getSecurityConfiguration,
  },
});

const glueDb = new glue.Database(this, &quot;glue db&quot;, {
  databaseName: `${name}-db`,
});

const glueSecurityOptions = new glue.SecurityConfiguration(
  this,
  &quot;glue security options&quot;,
  {
    securityConfigurationName: `${name}-security-options`,
    s3Encryption: {
      mode: glue.S3EncryptionMode.KMS,
    },
  },
);

const crawler = new glue.CfnCrawler(this, &quot;crawler&quot;, {
  name: `${name}-crawler`,
  role: roleCrawler.roleArn,
  targets: {
    s3Targets: [
      {
        path: `s3://${firehoseBucket.bucketName}/${ddbChangesPrefix}`,
      },
    ],
  },
  databaseName: glueDb.databaseName,
  crawlerSecurityConfiguration: glueSecurityOptions.securityConfigurationName,
});

// const glueCrawlerLogArn = `arn:aws:logs:${cdk.Stack.of(this).region}:${cdk.Stack.of(this).account}:log-group:/aws-glue/crawlers:log-stream:${crawler.name}`
const glueCrawlerLogArn = `arn:aws:logs:${cdk.Stack.of(this).region}:${cdk.Stack.of(this).account}:log-group:/aws-glue/crawlers*`; //:log-stream:${crawler.name}`

const glueTableArn = `arn:aws:glue:${cdk.Stack.of(this).region}:${cdk.Stack.of(this).account}:table/${glueDb.databaseName}/*`;

const glueCrawlerArn = `arn:aws:glue:${cdk.Stack.of(this).region}:${cdk.Stack.of(this).account}:crawler/${crawler.name}`;

roleCrawler.addToPolicy(
  new iam.PolicyStatement({
    resources: [
      glueCrawlerLogArn,
      glueTableArn,
      glueDb.catalogArn,
      glueDb.databaseArn,
      kmsKey.keyArn,
      firehoseBucket.bucketArn,
      `${firehoseBucket.bucketArn}/*`,
      glueCrawlerArn,
    ],
    actions: [&quot;logs:*&quot;, &quot;glue:*&quot;, &quot;kms:*&quot;, &quot;S3:*&quot;],
  }),
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;For test purposes, it&apos;s enough to run the crawler before any analysis. Scheduling is also possible.&lt;/p&gt;
&lt;p&gt;![glue-run-crawler]({{ site.baseurl }}/img/2021-10-26-aws_example_ddb_analytics_cdk/glue_run_crawler.png)&lt;/p&gt;
&lt;p&gt;That creates this table, which is accessible by Athena.&lt;/p&gt;
&lt;p&gt;![glue-table]({{ site.baseurl }}/img/2021-10-26-aws_example_ddb_analytics_cdk/glue_table.png)&lt;/p&gt;
&lt;h2&gt;Athena&lt;/h2&gt;
&lt;p&gt;For Athena it needs an S3 bucket for the query results and, for better isolation to other projects, a workgroup.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const athenaQueryResults = new s3.Bucket(this, &quot;query-results&quot;, {
  bucketName: `${name}-query-results`,
  encryptionKey: kmsKey,
});

new athena.CfnWorkGroup(this, &quot;analytics-athena-workgroup&quot;, {
  name: `${name}-workgroup`,
  workGroupConfiguration: {
    resultConfiguration: {
      outputLocation: `s3://${athenaQueryResults.bucketName}`,
      encryptionConfiguration: {
        encryptionOption: &quot;SSE_KMS&quot;,
        kmsKey: kmsKey.keyArn,
      },
    },
  },
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;How to anylyze the data see also [here]({{ site.baseurl }}/aws/2021/08/27/aws_example_ddb_analytics/)&lt;/p&gt;
&lt;h2&gt;Cost Alert 💰&lt;/h2&gt;
&lt;p&gt;⚠️ Don&apos;t forget to destroy after testing. Kinesis Data Streams has &lt;a href=&quot;https://aws.amazon.com/kinesis/data-streams/pricing/&quot;&gt;costs&lt;/a&gt; per hour&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk&quot;&gt;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>Example how to analyze DynamoDB item changes with Kinesis and Athena created with CDK</title><link>https://johanneskonings.dev/blog/2022-08-xx-aws_example_ddb_analytics_parquet_format_cdk/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2022-08-xx-aws_example_ddb_analytics_parquet_format_cdk/</guid><description>This post is how stream data changes of a DynamoDb table via Kinesis Data Stream and Kinesis Firehose to S3, and analyze the data with Athena. Build with CDK.</description><pubDate>Tue, 31 Aug 2021 08:15:18 GMT</pubDate><content:encoded>&lt;p&gt;This is the same like described [here]({{ site.baseurl }}/aws/2021/08/27/aws_example_ddb_analytics/), but instead of terraform it&apos;s build with &lt;a href=&quot;https://aws.amazon.com/cdk/&quot;&gt;CDK&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;To bootrap the project run this command: &lt;code&gt;cdk init app --language typescript&lt;/code&gt;
Further information are &lt;a href=&quot;https://docs.aws.amazon.com/cdk/latest/guide/hello_world.html&quot;&gt;here&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;All the services are in &lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk/blob/main/cdk/lib/cdk-stack.ts&quot;&gt;this&lt;/a&gt; file.&lt;/p&gt;
&lt;h1&gt;KMS key&lt;/h1&gt;
&lt;p&gt;This creates are KMS key with an alias to encrypt the data in the created services.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const kmsKey = new kms.Key(this, &quot;kmsKey&quot;, {
  enableKeyRotation: true,
});

kmsKey.addAlias(name);
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;DynamoDb and Kinesis Data Stream&lt;/h1&gt;
&lt;p&gt;This is the creation of the DynamoDb with the Kinesis Data Stream.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const stream = new kinesis.Stream(this, &quot;Stream&quot;, {
  streamName: `${name}-data-stream`,
  encryption: kinesis.StreamEncryption.KMS,
  encryptionKey: kmsKey,
});

const table = new dynamodb.Table(this, &quot;Table&quot;, {
  tableName: name,
  partitionKey: { name: &quot;pk&quot;, type: dynamodb.AttributeType.STRING },
  billingMode: dynamodb.BillingMode.PAY_PER_REQUEST,
  encryption: dynamodb.TableEncryption.CUSTOMER_MANAGED,
  encryptionKey: kmsKey,
  kinesisStream: stream,
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;That adds to the DynamoDb, a Kinesis Data Stream, and connects it to the DynamoDb.&lt;/p&gt;
&lt;p&gt;![kinesis data stream]({{ site.baseurl }}/img/2021-10-26-aws_example_ddb_analytics_cdk/kinesis_data_stream.png)&lt;/p&gt;
&lt;p&gt;![kinesis data stream ddb]({{ site.baseurl }}/img/2021-10-26-aws_example_ddb_analytics_cdk/kinesis_data_stream_ddb.png)&lt;/p&gt;
&lt;h1&gt;Kinesis Data Firehose and S3 Bucket&lt;/h1&gt;
&lt;p&gt;Kinesis Data Firehose is the connection between the Kinesis Data Stream to the S3 Bucket.&lt;/p&gt;
&lt;p&gt;Unfortunately, Firehose stores the JSONs without a linefeed. Therefore it&apos;s a lambda for conversion is necessary.&lt;/p&gt;
&lt;p&gt;More about that is described in this &lt;a href=&quot;https://medium.com/analytics-vidhya/append-newline-to-amazon-kinesis-firehose-json-formatted-records-with-python-f58498d0177a&quot;&gt;post&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;It looks like this.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const firehoseBucket = new s3.Bucket(this, &quot;firehose-s3-bucket&quot;, {
  bucketName: `${name}-firehose-s3-bucket`,
  encryptionKey: kmsKey,
});

const processor = new lambda.NodejsFunction(this, &quot;lambda-function-processor&quot;, {
  functionName: `${name}-firehose-converter`,
  timeout: cdk.Duration.minutes(2),
  bundling: {
    sourceMap: true,
  },
});

const lambdaProcessor = new LambdaFunctionProcessor(processor, {
  retries: 5,
});

const s3Destination = new destinations.S3Bucket(firehoseBucket, {
  encryptionKey: kmsKey,
  bufferingInterval: cdk.Duration.seconds(60),
  processor: lambdaProcessor,
});

const firehoseDeliveryStream = new firehose.DeliveryStream(
  this,
  &quot;Delivery Stream&quot;,
  {
    deliveryStreamName: `${name}-firehose`,
    sourceStream: stream,
    destinations: [s3Destination],
  },
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The delivery of the data to the S3 bucket is buffered. Here are the default values.&lt;/p&gt;
&lt;p&gt;![firehose-buffer]({{ site.baseurl }}/img/2021-10-26-aws_example_ddb_analytics_cdk/firehose_buffer.png)&lt;/p&gt;
&lt;h1&gt;Glue crawler&lt;/h1&gt;
&lt;p&gt;Athena needs a structured table for the SQL queries. The Glue crawler creates this from the data in the S3 bucket.&lt;/p&gt;
&lt;p&gt;The glue crawler isn&apos;t a L2 construct yet. So it needs a L1 construct. See &lt;a href=&quot;https://blog.phillipninan.com/a-no-nonsense-guide-to-aws-cloud-development-kit-cdk&quot;&gt;here&lt;/a&gt; more about L1 - L3.&lt;/p&gt;
&lt;p&gt;There is already a &lt;a href=&quot;https://github.com/aws/aws-cdk/issues/8863&quot;&gt;github issue&lt;/a&gt; to create a L2 construct for the glue crawler.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const getSecurityConfiguration = new iam.PolicyDocument({
  statements: [
    new iam.PolicyStatement({
      actions: [&quot;glue:GetSecurityConfiguration&quot;],
      resources: [&quot;*&quot;],
    }),
  ],
});

const roleCrawler = new iam.Role(this, &quot;role crawler&quot;, {
  roleName: `${name}-crawler-role`,
  assumedBy: new iam.ServicePrincipal(&quot;glue.amazonaws.com&quot;),
  inlinePolicies: {
    GetSecurityConfiguration: getSecurityConfiguration,
  },
});

const glueDb = new glue.Database(this, &quot;glue db&quot;, {
  databaseName: `${name}-db`,
});

const glueSecurityOptions = new glue.SecurityConfiguration(
  this,
  &quot;glue security options&quot;,
  {
    securityConfigurationName: `${name}-security-options`,
    s3Encryption: {
      mode: glue.S3EncryptionMode.KMS,
    },
  },
);

const crawler = new glue.CfnCrawler(this, &quot;crawler&quot;, {
  name: `${name}-crawler`,
  role: roleCrawler.roleArn,
  targets: {
    s3Targets: [
      {
        path: `s3://${firehoseBucket.bucketName}`,
      },
    ],
  },
  databaseName: glueDb.databaseName,
  crawlerSecurityConfiguration: glueSecurityOptions.securityConfigurationName,
});

// const glueCrawlerLogArn = `arn:aws:logs:${cdk.Stack.of(this).region}:${cdk.Stack.of(this).account}:log-group:/aws-glue/crawlers:log-stream:${crawler.name}`
const glueCrawlerLogArn = `arn:aws:logs:${cdk.Stack.of(this).region}:${cdk.Stack.of(this).account}:log-group:/aws-glue/crawlers*`; //:log-stream:${crawler.name}`

const glueTableArn = `arn:aws:glue:${cdk.Stack.of(this).region}:${cdk.Stack.of(this).account}:table/${glueDb.databaseName}/*`;

const glueCrawlerArn = `arn:aws:glue:${cdk.Stack.of(this).region}:${cdk.Stack.of(this).account}:crawler/${crawler.name}`;

roleCrawler.addToPolicy(
  new iam.PolicyStatement({
    resources: [
      glueCrawlerLogArn,
      glueTableArn,
      glueDb.catalogArn,
      glueDb.databaseArn,
      kmsKey.keyArn,
      firehoseBucket.bucketArn,
      `${firehoseBucket.bucketArn}/*`,
      glueCrawlerArn,
    ],
    actions: [&quot;logs:*&quot;, &quot;glue:*&quot;, &quot;kms:*&quot;, &quot;S3:*&quot;],
  }),
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;For test purposes, it&apos;s enough to run the crawler before any analysis. Scheduling is also possible.&lt;/p&gt;
&lt;p&gt;![glue-run-crawler]({{ site.baseurl }}/img/2021-10-26-aws_example_ddb_analytics_cdk/glue_run_crawler.png)&lt;/p&gt;
&lt;p&gt;That creates this table, which is accessible by Athena.&lt;/p&gt;
&lt;p&gt;![glue-table]({{ site.baseurl }}/img/2021-10-26-aws_example_ddb_analytics_cdk/glue_table.png)&lt;/p&gt;
&lt;h1&gt;Athena&lt;/h1&gt;
&lt;p&gt;For Athena it needs an S3 bucket for the query results and, for better isolation to other projects, a workgroup.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const athenaQueryResults = new s3.Bucket(this, &quot;query-results&quot;, {
  bucketName: `${name}-query-results`,
  encryptionKey: kmsKey,
});

new athena.CfnWorkGroup(this, &quot;analytics-athena-workgroup&quot;, {
  name: `${name}-workgroup`,
  workGroupConfiguration: {
    resultConfiguration: {
      outputLocation: `s3://${athenaQueryResults.bucketName}`,
      encryptionConfiguration: {
        encryptionOption: &quot;SSE_KMS&quot;,
        kmsKey: kmsKey.keyArn,
      },
    },
  },
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;How to anylyze the data see also [here]({{ site.baseurl }}/aws/2021/08/27/aws_example_ddb_analytics/)&lt;/p&gt;
&lt;h1&gt;Cost Alert 💰&lt;/h1&gt;
&lt;p&gt;⚠️ Don&apos;t forget to destroy after testing. Kinesis Data Streams has &lt;a href=&quot;https://aws.amazon.com/kinesis/data-streams/pricing/&quot;&gt;costs&lt;/a&gt; per hour&lt;/p&gt;
&lt;h1&gt;Code&lt;/h1&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk&quot;&gt;https://github.com/JohannesKonings/test-aws-dynamodb-athena-cdk&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>Example how to analyze DynamoDB item changes with Kinesis and Athena created with Terraform</title><link>https://johanneskonings.dev/blog/2021-08-27-aws_example_ddb_analytics/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2021-08-27-aws_example_ddb_analytics/</guid><description>This post is how stream data changes of a DynamoDb table via Kinesis Data Stream and Kinesis Firehose to S3, and analyze the data with Athena</description><pubDate>Fri, 27 Aug 2021 08:15:18 GMT</pubDate><content:encoded>&lt;h1&gt;Why?&lt;/h1&gt;
&lt;p&gt;The data of a DynamoDb table is not so easy to analyze as a &lt;a href=&quot;https://aws.amazon.com/rds/&quot;&gt;RDS&lt;/a&gt; with e.g., the &lt;a href=&quot;https://www.pgadmin.org/&quot;&gt;pgAdmin&lt;/a&gt;.
It will be somehow possible with scan operation but it&apos;s in the most cases &lt;a href=&quot;https://dynobase.dev/dynamodb-scan/&quot;&gt;not recommented&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;Another possibility is the &lt;a href=&quot;https://aws.amazon.com/blogs/aws/new-export-amazon-dynamodb-table-data-to-data-lake-amazon-s3/&quot;&gt;export to S3 functionylity&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;In this post, it&apos;s described to use streams. Since 11/2020, it is also possible &lt;a href=&quot;https://aws.amazon.com/about-aws/whats-new/2020/11/now-you-can-use-amazon-kinesis-data-streams-to-capture-item-level-changes-in-your-amazon-dynamodb-table/&quot;&gt;to use kinesis data streams&lt;/a&gt; for such a case.&lt;/p&gt;
&lt;p&gt;That also allows to analyze changes and use it for audits.&lt;/p&gt;
&lt;p&gt;A example with DynamoDb streams are here:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://www.youtube.com/watch?v=7QFUEh-FYYE&quot;&gt;https://www.youtube.com/watch?v=7QFUEh-FYYE&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://www.youtube.com/watch?v=17AmrTqn0GY&quot;&gt;https://www.youtube.com/watch?v=17AmrTqn0GY&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h1&gt;Architecture&lt;/h1&gt;
&lt;p&gt;&lt;img src=&quot;https://raw.githubusercontent.com/JohannesKonings/test-aws-dynamodb-athena-tf/main/diagrams/overview.png&quot; alt=&quot;architecture&quot; /&gt;&lt;/p&gt;
&lt;p&gt;The lambda is sending fake person data to DynamoDb. The integration of the Kinesis Data Stream into the DynamoDb is connected to the Kinesis Firehose, which sends the changes partitioned to the S3 bucket.&lt;/p&gt;
&lt;p&gt;The Glue crawler will recognize the data structure and create a table, which can be accessed from Athena to analyze the data.&lt;/p&gt;
&lt;p&gt;Let&apos;s see the certain building blocks&lt;/p&gt;
&lt;h1&gt;Lambda (for data creation)&lt;/h1&gt;
&lt;p&gt;The &lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-tf/blob/main/terraform/lambda.tf&quot;&gt;Lambda&lt;/a&gt; is created with a module from &lt;a href=&quot;serverless.tf&quot;&gt;serverless.tf&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;The source code is &lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-tf/blob/main/terraform/src/persons-loader/index.js&quot;&gt;here&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;The number of created persons depends on the test event.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
  &quot;batchSize&quot;: 5
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;DynamoDb and Kinesis Data Stream&lt;/h1&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-tf/blob/main/terraform/dynamodb.tf&quot;&gt;This&lt;/a&gt; is the creation of the DynamoDb with the Kinesis Data Stream.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;resource &quot;aws_dynamodb_table&quot; &quot;aws_dynamodb_table&quot; {
  name         = var.TABLE_NAME
  billing_mode = &quot;PAY_PER_REQUEST&quot;
  hash_key     = &quot;pk&quot;

  attribute {
    name = &quot;pk&quot;
    type = &quot;S&quot;
  }
}

resource &quot;aws_kinesis_stream&quot; &quot;aws_kinesis_stream&quot; {
  name            = &quot;${var.TABLE_NAME}-data-stream&quot;
  shard_count     = 1
  encryption_type = &quot;KMS&quot;
  kms_key_id      = aws_kms_key.aws_kms_key.arn
}

resource &quot;aws_dynamodb_kinesis_streaming_destination&quot; &quot;aws_dynamodb_kinesis_streaming_destination&quot; {
  stream_arn = aws_kinesis_stream.aws_kinesis_stream.arn
  table_name = aws_dynamodb_table.aws_dynamodb_table.name
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;That adds to the DynamoDb, a Kinesis Data Stream, and connects it to the DynamoDb.&lt;/p&gt;
&lt;p&gt;![kinesis data stream]({{ site.baseurl }}/img/2021-08-27-aws_example_ddb_analytics/kinesis_data_stream.png)&lt;/p&gt;
&lt;p&gt;![kinesis data stream ddb]({{ site.baseurl }}/img/2021-08-27-aws_example_ddb_analytics/kinesis_data_stream_ddb.png)&lt;/p&gt;
&lt;h1&gt;Kinesis Data Firehose and S3 Bucket&lt;/h1&gt;
&lt;p&gt;Kinesis Data Firehose is the connection between the Kinesis Data Stream to the S3 Bucket.&lt;/p&gt;
&lt;p&gt;Unfortunately, Firehose stores the JSONs without a linefeed. Therefore it&apos;s a lambda for conversion is necessary.&lt;/p&gt;
&lt;p&gt;More about that is described in this &lt;a href=&quot;https://medium.com/analytics-vidhya/append-newline-to-amazon-kinesis-firehose-json-formatted-records-with-python-f58498d0177a&quot;&gt;post&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;Besides policy configuration, it looks like this.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;resource &quot;aws_kinesis_firehose_delivery_stream&quot; &quot;aws_kinesis_firehose_delivery_stream&quot; {
  name        = local.firehose-name
  destination = &quot;extended_s3&quot;

  kinesis_source_configuration {
    kinesis_stream_arn = aws_kinesis_stream.aws_kinesis_stream.arn
    role_arn           = aws_iam_role.aws_iam_role.arn
  }

  extended_s3_configuration {
    role_arn   = aws_iam_role.aws_iam_role.arn
    bucket_arn = aws_s3_bucket.aws_s3_bucket.arn

    processing_configuration {
      enabled = &quot;true&quot;

      processors {
        type = &quot;Lambda&quot;

        parameters {
          parameter_name  = &quot;LambdaArn&quot;
          parameter_value = &quot;${module.lambda_function_persons_firehose_converter.lambda_function_arn}:$LATEST&quot;
        }
      }
    }

    cloudwatch_logging_options {
      enabled         = true
      log_group_name  = aws_cloudwatch_log_group.aws_cloudwatch_log_group_firehose.name
      log_stream_name = aws_cloudwatch_log_stream.aws_cloudwatch_log_stream_firehose.name
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Details are &lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-tf/blob/main/terraform/kinesis_firehose.tf&quot;&gt;here&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;The delivery of the data to the S3 bucket is buffered. Here are the default values.&lt;/p&gt;
&lt;p&gt;![firehose-buffer]({{ site.baseurl }}/img/2021-08-27-aws_example_ddb_analytics/firehose_buffer.png)&lt;/p&gt;
&lt;h1&gt;Glue crawler&lt;/h1&gt;
&lt;p&gt;Athena needs a structured table for the SQL queries. The Glue crawler creates this from the data in the S3 bucket.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;resource &quot;aws_glue_crawler&quot; &quot;aws_glue_crawler&quot; {
  database_name = aws_glue_catalog_database.aws_glue_bookings_database.name
  name          = local.glue-crawler-name
  role          = aws_iam_role.aws_iam_role_glue_crawler.arn

  configuration = jsonencode(
    {
      &quot;Version&quot; : 1.0
      CrawlerOutput = {
        Partitions = { AddOrUpdateBehavior = &quot;InheritFromTable&quot; }
      }
    }
  )

  s3_target {
    path = &quot;s3://${aws_s3_bucket.aws_s3_bucket.bucket}&quot;
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Details &lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-tf/blob/main/terraform/glue.tf&quot;&gt;here&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;For test purposes, it&apos;s enough to run the crawler before any analysis. Scheduling is also possible.&lt;/p&gt;
&lt;p&gt;![glue-run-crawler]({{ site.baseurl }}/img/2021-08-27-aws_example_ddb_analytics/glue_run_crawler.png)&lt;/p&gt;
&lt;p&gt;That creates this table, which is accessible by Athena.&lt;/p&gt;
&lt;p&gt;![glue-table]({{ site.baseurl }}/img/2021-08-27-aws_example_ddb_analytics/glue_table.png)&lt;/p&gt;
&lt;h1&gt;Athena&lt;/h1&gt;
&lt;p&gt;For Athena it needs an S3 bucket for the query results and, for better isolation to other projects, a workgroup.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;locals {
  athena-query-results-s3-name = &quot;${var.TABLE_NAME}-query-results&quot;
  athena-workgroup-name        = &quot;${var.TABLE_NAME}-workgroup&quot;
}
resource &quot;aws_s3_bucket&quot; &quot;aws_s3_bucket_bookings_query_results&quot; {
  bucket = local.athena-query-results-s3-name
  acl    = &quot;private&quot;

  versioning {
    enabled = true
  }

  server_side_encryption_configuration {
    rule {
      apply_server_side_encryption_by_default {
        kms_master_key_id = aws_kms_key.aws_kms_key.arn
        sse_algorithm     = &quot;aws:kms&quot;
      }
    }
  }
}

resource &quot;aws_athena_workgroup&quot; &quot;aws_athena_workgroup&quot; {
  name = local.athena-workgroup-name

  configuration {
    enforce_workgroup_configuration    = true
    publish_cloudwatch_metrics_enabled = true

    result_configuration {
      output_location = &quot;s3://${aws_s3_bucket.aws_s3_bucket_bookings_query_results.bucket}/output/&quot;

      encryption_configuration {
        encryption_option = &quot;SSE_KMS&quot;
        kms_key_arn       = aws_kms_key.aws_kms_key.arn
      }
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Details &lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-tf/blob/main/terraform/glue.tf&quot;&gt;here&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;Analysis&lt;/h2&gt;
&lt;p&gt;First select the new workgroup.&lt;/p&gt;
&lt;p&gt;![athena-workgroup]({{ site.baseurl }}/img/2021-08-27-aws_example_ddb_analytics/athena_workgroup.png)&lt;/p&gt;
&lt;p&gt;And than the new Database.&lt;/p&gt;
&lt;p&gt;![athena-database]({{ site.baseurl }}/img/2021-08-27-aws_example_ddb_analytics/athena_database.png)&lt;/p&gt;
&lt;h3&gt;Query example&lt;/h3&gt;
&lt;p&gt;DynamoDb sends the changes of an item as INSERT, MODIFY or REMOVE. To the current data of the table, this Query will work.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;SELECT dynamodb.newimage.pk.s AS pk,
         dynamodb.newimage.person.M.firstname.s AS firstname,
         dynamodb.newimage.person.M.lastname.s AS lastname,
         dynamodb.approximatecreationdatetime AS ts,
         dynamodb.newimage,
         *
FROM &quot;persons-db&quot;.&quot;persons_firehose_s3_bucket&quot; AS persons1
WHERE (eventname = &apos;INSERT&apos;
        OR eventname = &apos;MODIFY&apos;)
        AND dynamodb.approximatecreationdatetime =
    (SELECT MAX(dynamodb.approximatecreationdatetime)
    FROM &quot;persons-db&quot;.&quot;persons_firehose_s3_bucket&quot; AS persons2
    WHERE persons2.dynamodb.newimage.pk.s = persons1.dynamodb.newimage.pk.s);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;![athena-ddb]({{ site.baseurl }}/img/2021-08-27-aws_example_ddb_analytics/athena_ddb.png)&lt;/p&gt;
&lt;h1&gt;Cost Alert 💰&lt;/h1&gt;
&lt;p&gt;⚠️ Don&apos;t forget to destroy after testing. Kinesis Data Streams has &lt;a href=&quot;https://aws.amazon.com/kinesis/data-streams/pricing/&quot;&gt;costs&lt;/a&gt; per hour&lt;/p&gt;
&lt;h1&gt;Code&lt;/h1&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/test-aws-dynamodb-athena-tf&quot;&gt;https://github.com/JohannesKonings/test-aws-dynamodb-athena-tf&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>GitHub actions example for automatic release drafts and changelog.md creation</title><link>https://johanneskonings.dev/blog/2021-02-28-github-automatic-releases-and-changelog/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2021-02-28-github-automatic-releases-and-changelog/</guid><description>This post is how to define your release draft via labels in pull requests and the update of the changelog.md after publishing a release</description><pubDate>Sun, 28 Feb 2021 08:15:18 GMT</pubDate><content:encoded>&lt;h1&gt;What are GitHub actions?&lt;/h1&gt;
&lt;p&gt;That is well described in this &lt;a href=&quot;https://dev.to/github/what-are-github-actions-3pml&quot;&gt;post&lt;/a&gt; by &lt;a href=&quot;https://dev.to/bdougieyo&quot;&gt;Brian Douglas&lt;/a&gt;.
He has also an entire &lt;a href=&quot;https://dev.to/github/28-days-of-github-action-tips-4opg&quot;&gt;post series&lt;/a&gt; about tips around GitHub actions.&lt;/p&gt;
&lt;h1&gt;Which problem should be solved?&lt;/h1&gt;
&lt;p&gt;There are different solutions to create automatic releases based on certain criteria. Again &lt;a href=&quot;https://dev.to/bdougieyo&quot;&gt;Brian Douglas&lt;/a&gt; pointed out some possibilities in this &lt;a href=&quot;https://dev.to/github/generate-semantic-release-with-github-actions-2lll&quot;&gt;post&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;For &lt;a href=&quot;https://github.com/abap-observability-tools&quot;&gt;this&lt;/a&gt; open-source project, the requirement was to determine the release structure via labels at the pull request. This was preferred over &lt;a href=&quot;https://www.conventionalcommits.org/en/v1.0.0/&quot;&gt;conventional commits&lt;/a&gt;.
Also, not every merged pull request should automatically trigger a new release. Therefore, a draft is the right way to collect the changes and publish a version whenever needed.&lt;/p&gt;
&lt;p&gt;Addionatylly to the GitHub releases, a changelog.md helps see the version history, for example, directly in the code editor.
That changelog.md should be updated every time a release is published.&lt;/p&gt;
&lt;p&gt;The combination &lt;a href=&quot;https://github.com/release-drafter/release-drafter&quot;&gt;Release Drafter&lt;/a&gt; and &lt;a href=&quot;https://github.com/github-tools/github-release-notes&quot;&gt;gren&lt;/a&gt; is the approach to solve the problem.&lt;/p&gt;
&lt;h1&gt;Configure Release Drafter&lt;/h1&gt;
&lt;p&gt;To configure &lt;a href=&quot;https://github.com/release-drafter/release-drafter&quot;&gt;Release Drafter&lt;/a&gt; in the default way, it needs two files and the according labels.&lt;/p&gt;
&lt;p&gt;This &lt;a href=&quot;https://github.com/abap-observability-tools/abap-log-exporter/blob/main/.github/release-drafter.yml&quot;&gt;template&lt;/a&gt; describes the structure of the release draft and the needed labels.
The full path is &lt;code&gt;.github/release-drafter.yml&lt;/code&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;name-template: &quot;v$RESOLVED_VERSION 🌈&quot;
tag-template: &quot;v$RESOLVED_VERSION&quot;
categories:
  - title: &quot;🚀 Features&quot;
    labels:
      - &quot;feature&quot;
      - &quot;enhancement&quot;
  - title: &quot;🐛 Bug Fixes&quot;
    labels:
      - &quot;fix&quot;
      - &quot;bugfix&quot;
      - &quot;bug&quot;
  - title: &quot;🧰 Maintenance&quot;
    label: &quot;chore&quot;
  - title: &quot;🧺 Miscellaneous&quot; #Everything except ABAP
    label: &quot;misc&quot;
change-template: &quot;- $TITLE @$AUTHOR (#$NUMBER)&quot;
change-title-escapes: &apos;\&amp;lt;*_&amp;amp;&apos; # You can add # and @ to disable mentions, and add ` to disable code blocks.
version-resolver:
  major:
    labels:
      - &quot;major&quot;
  minor:
    labels:
      - &quot;minor&quot;
  patch:
    labels:
      - &quot;patch&quot;
  default: patch
template: |
  ## Changes
  $CHANGES
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The GitHub actions &lt;a href=&quot;https://github.com/abap-observability-tools/abap-log-exporter/blob/main/.github/workflows/release-drafter.yml&quot;&gt;configuration&lt;/a&gt; like this:&lt;/p&gt;
&lt;p&gt;&lt;code&gt;github/workflows/release-drafter.yml&lt;/code&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;name: Release Drafter

on:
  push:
    # branches to consider in the event; optional, defaults to all
    branches:
      - main

jobs:
  update_release_draft:
    runs-on: ubuntu-latest
    steps:
      # Drafts your next Release notes as Pull Requests are merged into &quot;master&quot;
      - uses: release-drafter/release-drafter@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Configure gren&lt;/h1&gt;
&lt;p&gt;The releases are published manually at certain times. This trigger &lt;a href=&quot;https://github.com/abap-observability-tools/abap-log-exporter/blob/main/.github/workflows/update-changelog.yml&quot;&gt;this&lt;/a&gt; configuration.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;name: &quot;update changelog&quot;
on:
  release:
    types: [published]

jobs:
  update-changelog:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Update changelog
        run: |
          npm install github-release-notes
          export GREN_GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }}
          npm run overrideChangelog
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v3
        with:
          commit-message: update changelog
          title: Update Changelog
          body: Update changelog to reflect release changes
          branch: update-changelog
          base: main
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The command &lt;code&gt;&quot;overrideChangelog&quot;: &quot;gren changelog --override&quot;&lt;/code&gt; from the &lt;a href=&quot;https://github.com/abap-observability-tools/abap-log-exporter/blob/main/package.json&quot;&gt;package.json&lt;/a&gt; update then the changelog.md.&lt;/p&gt;
&lt;p&gt;Because of the main branch protection, it&apos;s not possible to push the changes directly back. This will do this via a pull request with the GitHub action &lt;a href=&quot;https://github.com/marketplace/actions/create-pull-request&quot;&gt;create-pull-request&lt;/a&gt;.&lt;/p&gt;
&lt;h1&gt;How it looks like&lt;/h1&gt;
&lt;p&gt;The collection of the changes in a release draft.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;release-draft.png&quot; alt=&quot;release-draft&quot; /&gt;&lt;/p&gt;
&lt;p&gt;The labels in a pull request.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./label-pull-request.png&quot; alt=&quot;label-pull-request&quot; /&gt;&lt;/p&gt;
&lt;p&gt;The result in the GitHub release.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./minor-enhancement.png&quot; alt=&quot;minor-enhancement&quot; /&gt;&lt;/p&gt;
&lt;p&gt;The result in the CHANGELOG.md.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./changelog.png&quot; alt=&quot;changelog&quot; /&gt;&lt;/p&gt;
&lt;h1&gt;Code&lt;/h1&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/abap-observability-tools&quot;&gt;https://github.com/abap-observability-tools&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>How to setup AWS Billing metrics in Grafana Cloud via Terraform</title><link>https://johanneskonings.dev/blog/2020-12-19-aws-billing-metrics-grafana-cloud-and-terraform/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2020-12-19-aws-billing-metrics-grafana-cloud-and-terraform/</guid><description>Terraform setup to display AWS billing metrics in Grafana Cloud with S3 Backend and CI/CD pipeline</description><pubDate>Sat, 19 Dec 2020 08:15:18 GMT</pubDate><content:encoded>&lt;h1&gt;Why Terraform?&lt;/h1&gt;
&lt;p&gt;In this &lt;a href=&quot;./aws_billing_metrics_and_grafana_cloud/&quot;&gt;post&lt;/a&gt; I described how to display AWS Billing metrics in Grafana Cloud. Therefore it was necessary to create manually the data source and the dashboard.
With Terraform, you can describe the setup as code and benefit from the full advantages of &lt;a href=&quot;https://en.wikipedia.org/wiki/Infrastructure_as_code&quot;&gt;IaC&lt;/a&gt;.&lt;/p&gt;
&lt;h1&gt;Terraform&lt;/h1&gt;
&lt;p&gt;&lt;a href=&quot;https://www.terraform.io/&quot;&gt;Terraform&lt;/a&gt; is a tool for infrastructure as code and works with many different &lt;a href=&quot;https://www.terraform.io/docs/providers/index.html#lists-of-terraform-providers&quot;&gt;provider&lt;/a&gt;.
Terraform comes with a CLI for the deployments.&lt;/p&gt;
&lt;h1&gt;Grafana Provider and Dashboard declaration&lt;/h1&gt;
&lt;p&gt;For this use case, you need a Grafana data source and a Grafana dashboard. These configurations have to defined in a .tf file like this &lt;a href=&quot;https://github.com/JohannesKonings/aws-grafana-billing-dashboard/blob/main/grafana.tf&quot;&gt;one&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;At first the &lt;a href=&quot;https://registry.terraform.io/providers/grafana/grafana/latest/docs&quot;&gt;provider&lt;/a&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;provider &quot;grafana&quot; {
  url  = var.grafana_url
  auth = var.grafana_api_key
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Then the &lt;a href=&quot;https://registry.terraform.io/providers/grafana/grafana/latest/docs/resources/data_source&quot;&gt;data source&lt;/a&gt; and &lt;a href=&quot;https://registry.terraform.io/providers/grafana/grafana/latest/docs/resources/dashboard&quot;&gt;dashboard&lt;/a&gt;.
The dashboard section links to the file &lt;a href=&quot;https://github.com/JohannesKonings/aws-grafana-billing-dashboard/blob/main/dashboards/aws-billing.json&quot;&gt;dashboards/aws-billing.json&lt;/a&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;resource &quot;grafana_data_source&quot; &quot;cloudwatch&quot; {
  type = &quot;cloudwatch&quot;
  name = &quot;CloudWatch&quot;

  json_data {
    default_region = var.region
    auth_type      = &quot;keys&quot;
  }

  secure_json_data {
    access_key = var.access_key_grafana
    secret_key = var.secret_key_grafana
  }
}

resource &quot;grafana_dashboard&quot; &quot;metrics&quot; {
  config_json = file(&quot;dashboards/aws-billing.json&quot;)
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;For security reasons and flexible sharing of the template, the parameters for secrets and variables like region are in a .env file. This is the &lt;a href=&quot;https://github.com/JohannesKonings/aws-grafana-billing-dashboard/blob/main/.env_template&quot;&gt;template&lt;/a&gt; for that.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# The URL of the Grafana instance
export TF_VAR_grafana_url=
# The API key of the Grafana instance
export TF_VAR_grafana_api_key=
# IAM user like described here: https://grafana.com/docs/grafana/latest/datasources/cloudwatch/#iam-policies
export TF_VAR_access_key_grafana=
export TF_VAR_secret_key_grafana=
# Default region of the data source
export TF_VAR_region=
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The declaration of Terraform variables looks like that.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;variable &quot;grafana_url&quot; {}
variable &quot;grafana_api_key&quot; {}
variable &quot;access_key_grafana&quot; {}
variable &quot;secret_key_grafana&quot; {}
variable &quot;region&quot; {}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In this case it&apos;s in the file &lt;a href=&quot;https://github.com/JohannesKonings/aws-grafana-billing-dashboard/blob/main/variables.tf&quot;&gt;variable.tf&lt;/a&gt; like described &lt;a href=&quot;https://learn.hashicorp.com/tutorials/terraform/azure-variables&quot;&gt;here&lt;/a&gt;.&lt;/p&gt;
&lt;h1&gt;Grafana API Key&lt;/h1&gt;
&lt;p&gt;Terraform can &quot;communicate&quot; with Grafana via an API key.
Navigate to this URL &quot;https://&amp;lt;&amp;lt;Grafana instance&amp;gt;&amp;gt;/org/apikeys&quot; and create on with the role &quot;Admin&quot;.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./grafana_api_key_creation.png&quot; alt=&quot;grafana api key creation&quot; /&gt;&lt;/p&gt;
&lt;p&gt;Put the API key into the .env file.&lt;/p&gt;
&lt;h1&gt;Usage of the env file&lt;/h1&gt;
&lt;p&gt;Before the creation of the S3 Backend and the deployment run the command &lt;code&gt;source .env&lt;/code&gt;.&lt;/p&gt;
&lt;h1&gt;Terraform AWS S3 backend&lt;/h1&gt;
&lt;p&gt;This setup so far works for the first deployment. Changes and a redeployment lead to an error because the resource already exists.
Therefore it&apos;s necessary to extend the setup with a &lt;a href=&quot;https://www.terraform.io/docs/backends/index.html&quot;&gt;Terraform backend&lt;/a&gt;.
In this example, it&apos;s a &lt;a href=&quot;https://www.terraform.io/docs/backends/types/s3.html&quot;&gt;S3 backend&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;Unfortunately, it&apos;s not possible to use variables here. This is discussed in this &lt;a href=&quot;https://github.com/hashicorp/terraform/issues/13022&quot;&gt;issue&lt;/a&gt; with some approaches for workarounds. I use this &lt;a href=&quot;https://github.com/hashicorp/terraform/issues/13022#issuecomment-482014961&quot;&gt;one&lt;/a&gt;, more or less.&lt;/p&gt;
&lt;p&gt;Concrete I put a &lt;a href=&quot;https://github.com/JohannesKonings/aws-grafana-billing-dashboard/blob/main/scripts/terraformInit.sh&quot;&gt;script&lt;/a&gt; around the command &lt;code&gt;terraform init&lt;/code&gt;. This script can use the environment variables and create a terraform file for the backend.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;#!/bin/sh
cat &amp;gt; ./backend.tf &amp;lt;&amp;lt; EOF
terraform {
  backend &quot;s3&quot; {
    bucket = &quot;${TF_VAR_s3_bucket_name}&quot;
    key    = &quot;${TF_VAR_backend_key}&quot;
    region = &quot;${TF_VAR_region}&quot;
  }
}
EOF

terraform init -input=false
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This &lt;a href=&quot;https://github.com/JohannesKonings/aws-grafana-billing-dashboard/blob/main/scripts/createS3BackendBucket.sh&quot;&gt;script&lt;/a&gt; creates the bucket.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;#!/bin/sh

aws s3api create-bucket --bucket $TF_VAR_s3_bucket_name --region $TF_VAR_region
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;For the backend, it needs an IAM user. This &lt;a href=&quot;https://github.com/JohannesKonings/aws-grafana-billing-dashboard/blob/main/scripts/createUser4S3BackendBucket.sh&quot;&gt;script&lt;/a&gt; creates the user and return access and secret key. Put that into the .env file.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# Name of the Terrafrom S3 backend for state handling
export TF_VAR_s3_bucket_name=
# Name of the state file
export TF_VAR_backend_key=terraform.tfstate
# IAM user credentials to access S3 and write the state file
export AWS_ACCESS_KEY_ID=
export AWS_SECRET_ACCESS_KEY=
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code&gt;#!/bin/sh

aws iam create-user --user-name terraform_state

aws iam create-access-key --user-name terraform_state
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This &lt;a href=&quot;https://github.com/JohannesKonings/aws-grafana-billing-dashboard/blob/main/scripts/createAndAttachS3BackendBucketPolicy.sh&quot;&gt;script&lt;/a&gt; creates and attach the missing policy.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;#!/bin/bash

cat &amp;gt; ./scripts/policy &amp;lt;&amp;lt; EOF
{
  &quot;Version&quot;: &quot;2012-10-17&quot;,
  &quot;Statement&quot;: [
    {
      &quot;Effect&quot;: &quot;Allow&quot;,
      &quot;Action&quot;: &quot;s3:ListBucket&quot;,
      &quot;Resource&quot;: &quot;arn:aws:s3:::${TF_VAR_s3_bucket_name}&quot;
    },
    {
      &quot;Effect&quot;: &quot;Allow&quot;,
      &quot;Action&quot;: [&quot;s3:GetObject&quot;, &quot;s3:PutObject&quot;],
      &quot;Resource&quot;: &quot;arn:aws:s3:::${TF_VAR_s3_bucket_name}/*&quot;
    }
  ]
}
EOF

aws iam create-policy --policy-name terraform_state --policy-document file://scripts/policy

ARN=$(aws iam list-policies --query &apos;Policies[?PolicyName==`terraform_state`].Arn&apos; --output text)

aws iam attach-user-policy --policy-arn $ARN --user-name terraform_state
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Deployment commands&lt;/h1&gt;
&lt;p&gt;Once the S3 backend is created, you&apos;re a few commands away from the deployment.&lt;/p&gt;
&lt;p&gt;At first, the initialization of Terraform, which is wrapped in a script.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;sh scripts/terraformInit.sh&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;For the next commands, the Terraform CLI is sufficient.&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://www.terraform.io/docs/commands/validate.html&quot;&gt;validate:&lt;/a&gt; &lt;code&gt;terraform validate&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://www.terraform.io/docs/commands/plan.html&quot;&gt;plan:&lt;/a&gt; &lt;code&gt;terraform plan&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://www.terraform.io/docs/commands/apply.html&quot;&gt;apply:&lt;/a&gt; &lt;code&gt;terraform apply&lt;/code&gt;&lt;/p&gt;
&lt;h1&gt;Grafana Dasboard changes&lt;/h1&gt;
&lt;p&gt;The dashboard can now be changed directly via the JSON file in the folder dashboards. The easier way is to do that manually in Grafana and copy the changed JSON via the share functionality.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./share_grafana_dashboard.png&quot; alt=&quot;share grafana dashboard&quot; /&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./grafana_dashboard_export_view_json.png&quot; alt=&quot;grafana dashboard export view json&quot; /&gt;&lt;/p&gt;
&lt;p&gt;Overwrite the file aws-billing.json with the JSON from Grafana and redeploy.&lt;/p&gt;
&lt;h1&gt;CI/CD pipeline&lt;/h1&gt;
&lt;p&gt;The local deployment is also possible with a CI/CD pipeline. In &lt;a href=&quot;https://github.com/JohannesKonings/aws-grafana-billing-dashboard/blob/main/.github/workflows/deploy2grafana.yml&quot;&gt;this example&lt;/a&gt; it&apos;s with GitHub actions.
Instead of the .env file, the variables and credentials coming from GitHub secrets.&lt;/p&gt;
&lt;h1&gt;Code&lt;/h1&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/aws-grafana-billing-dashboard&quot;&gt;https://github.com/JohannesKonings/aws-grafana-billing-dashboard&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>AWS Amplify Admin UI user management instead of self sign up</title><link>https://johanneskonings.dev/blog/2020-12-06-amplify_admin_ui_user_management/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2020-12-06-amplify_admin_ui_user_management/</guid><description>Description of the change from a self sign up to user management from an admin</description><pubDate>Sun, 06 Dec 2020 08:15:18 GMT</pubDate><content:encoded>&lt;p&gt;In my web app, I use the authentification &lt;a href=&quot;https://docs.amplify.aws/ui/auth/authenticator/q/framework/react&quot;&gt;UI Compontents&lt;/a&gt; of Amplify. Without any configuration, this UI component comes with a signup link so that any person who knows the URL can signup for themself. However, this web app is for a certain group of users. That&apos;s why I want to create users who are allowed to sign in.&lt;/p&gt;
&lt;p&gt;And this is now possible in an easy way via the new Amplify Admin UI.&lt;/p&gt;
&lt;h1&gt;The Amplify Admin UI&lt;/h1&gt;
&lt;p&gt;The Amplify Admin UI has a big set of functions. You can check the &lt;a href=&quot;https://docs.amplify.aws/console/adminui/intro&quot;&gt;docs&lt;/a&gt; for more details or test it in a &lt;a href=&quot;https://sandbox.amplifyapp.com/getting-started&quot;&gt;sandbox&lt;/a&gt;.
This &lt;a href=&quot;https://dev.to/aws-builders/aws-amplify-admin-ui-45bm&quot;&gt;post&lt;/a&gt; also gives an excellent overview.&lt;/p&gt;
&lt;h1&gt;Hide the signup link&lt;/h1&gt;
&lt;p&gt;At first, I had to hide the signup link to avoid that everyone can signup.&lt;/p&gt;
&lt;p&gt;![signin with signup]({{ site.baseurl }}/img/2020-12-06-amplify_admin_ui_user_management/signin_with_signup.png)&lt;/p&gt;
&lt;p&gt;To hide the signup link is just one line.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;AmplifyAuthenticator&amp;gt;
  &amp;lt;AmplifySignIn hideSignUp=&quot;true&quot; slot=&quot;sign-in&quot; /&amp;gt;
&amp;lt;/AmplifyAuthenticator&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;More options are described in the &lt;a href=&quot;https://docs.amplify.aws/ui/auth/authenticator/q/framework/react#sign-in&quot;&gt;docs&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;Then it looks like that.&lt;/p&gt;
&lt;p&gt;![signin without signup]({{ site.baseurl }}/img/2020-12-06-amplify_admin_ui_user_management/signin_without_signup.png)&lt;/p&gt;
&lt;h1&gt;Create a new user via Amplify Admin UI&lt;/h1&gt;
&lt;p&gt;If a new user is needed, you can create on via the Amplify Admin UI.
That you can use the Admin UI, it is just a click in the Amplify &lt;a href=&quot;https://console.aws.amazon.com/amplify/home&quot;&gt;Console&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;![enabled admin ui]({{ site.baseurl }}/img/2020-12-06-amplify_admin_ui_user_management/enabled_admin_ui.png)&lt;/p&gt;
&lt;p&gt;In the Admin UI, it&apos;s in the section user management. Click on &quot;create user&quot;.&lt;/p&gt;
&lt;p&gt;![user management create user]({{ site.baseurl }}/img/2020-12-06-amplify_admin_ui_user_management/user_management_create_user.png)&lt;/p&gt;
&lt;p&gt;Type in the user data. The user will get an email with the username and temporary password.&lt;/p&gt;
&lt;p&gt;![user management user data]({{ site.baseurl }}/img/2020-12-06-amplify_admin_ui_user_management/user_management_user_data.png)&lt;/p&gt;
&lt;h1&gt;User sign in process&lt;/h1&gt;
&lt;p&gt;Then the user can log in with his username and temporary password.&lt;/p&gt;
&lt;p&gt;![user signin tempory password]({{ site.baseurl }}/img/2020-12-06-amplify_admin_ui_user_management/user_signin_tempory_password.png)&lt;/p&gt;
&lt;p&gt;At the next screen, the user has to create a new password.&lt;/p&gt;
&lt;p&gt;![user signin new password]({{ site.baseurl }}/img/2020-12-06-amplify_admin_ui_user_management/user_signin_new_password.png)&lt;/p&gt;
&lt;p&gt;Two screens are left. One for the email verification...&lt;/p&gt;
&lt;p&gt;![user signin verify]({{ site.baseurl }}/img/2020-12-06-amplify_admin_ui_user_management/user_signin_verify.png)&lt;/p&gt;
&lt;p&gt;...and the last one for the verification code.&lt;/p&gt;
&lt;p&gt;![user signin verification code]({{ site.baseurl }}/img/2020-12-06-amplify_admin_ui_user_management/user_signin_verification_code.png)&lt;/p&gt;
&lt;p&gt;The last step is not necessary if the admin marks the email as verified.&lt;/p&gt;
&lt;p&gt;![admin verify email]({{ site.baseurl }}/img/2020-12-06-amplify_admin_ui_user_management/admin_verify_email.png)&lt;/p&gt;
&lt;p&gt;That&apos;s it 🎉&lt;/p&gt;
&lt;p&gt;![user after signin]({{ site.baseurl }}/img/2020-12-06-amplify_admin_ui_user_management/user_after_signin.png)&lt;/p&gt;
&lt;h1&gt;Invite User to the Admin UI&lt;/h1&gt;
&lt;p&gt;Now with the Admin UI, it is also possible to invite users as admins. Even without access to the AWS account.
See details &lt;a href=&quot;https://docs.amplify.aws/console/adminui/access-management&quot;&gt;here&lt;/a&gt;.&lt;/p&gt;
&lt;h1&gt;Code&lt;/h1&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/fff-badminton&quot;&gt;https://github.com/JohannesKonings/fff-badminton&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>How to display AWS billing metrics in Grafana Cloud</title><link>https://johanneskonings.dev/blog/2020-11-27-aws_billing_metrics_and_grafana_cloud/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2020-11-27-aws_billing_metrics_and_grafana_cloud/</guid><description>Configuration of Grafana Cloud to display AWS billing metrics</description><pubDate>Fri, 27 Nov 2020 08:15:18 GMT</pubDate><content:encoded>&lt;h1&gt;Why Grafana for AWS billing metrics?&lt;/h1&gt;
&lt;p&gt;&lt;a href=&quot;https://grafana.com/&quot;&gt;Grafana&lt;/a&gt; is well known for observability dashboards, which can be composed of many data sources.&lt;/p&gt;
&lt;p&gt;With AWS CloudWatch metrics, it is possible to do more or less the same, but it is designed more for one account and more for the AWS metrics.&lt;/p&gt;
&lt;p&gt;In Grafana, it&apos;s easy to use different data sources so that you can have your billing metrics on the same dashboard as your other technical and business metrics from elsewhere.&lt;/p&gt;
&lt;h1&gt;Configuration of the Grafana Cloud instance&lt;/h1&gt;
&lt;p&gt;Here &lt;a href=&quot;https://grafana.com/products/cloud/&quot;&gt;https://grafana.com/products/cloud/&lt;/a&gt; you can sign up for a Grafana Cloud account.&lt;/p&gt;
&lt;p&gt;It&apos;s free for this use case (and many more :))&lt;/p&gt;
&lt;p&gt;![Grafana Cloud pricing]({{ site.baseurl }}/img/2020-11-27-aws_billing_metrics_and_grafana_cloud/grafana_cloud_pricing.png)&lt;/p&gt;
&lt;p&gt;After login, create a stack with the starter plan.&lt;/p&gt;
&lt;p&gt;![Grafana Cloud add stack]({{ site.baseurl }}/img/2020-11-27-aws_billing_metrics_and_grafana_cloud/grafana_cloud_add_stack.png)&lt;/p&gt;
&lt;p&gt;![Grafana Cloud starter plan]({{ site.baseurl }}/img/2020-11-27-aws_billing_metrics_and_grafana_cloud/grafana_cloud_starter_plan.png)&lt;/p&gt;
&lt;p&gt;Then log in to your instance.&lt;/p&gt;
&lt;p&gt;![Grafana Cloud instance login]({{ site.baseurl }}/img/2020-11-27-aws_billing_metrics_and_grafana_cloud/grafana_cloud_instance_login.png)&lt;/p&gt;
&lt;h1&gt;Creation of an IAM user for the CloudWatch data source&lt;/h1&gt;
&lt;p&gt;The CloudWatch data source needs an access key and a secret key. Therefore is an IAM user needed. As policy can be this used:&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://grafana.com/docs/grafana/latest/datasources/cloudwatch/#iam-policies&quot;&gt;https://grafana.com/docs/grafana/latest/datasources/cloudwatch/#iam-policies&lt;/a&gt;&lt;/p&gt;
&lt;h1&gt;Configuration of the CloudWatch Datasource&lt;/h1&gt;
&lt;p&gt;The next step is to add the CloudWatch data source to your Grafana cloud instance.&lt;/p&gt;
&lt;p&gt;![Grafana Cloud add datasource]({{ site.baseurl }}/img/2020-11-27-aws_billing_metrics_and_grafana_cloud/grafana_cloud_add_datasource.png)&lt;/p&gt;
&lt;p&gt;![Grafana Cloud select cloudwatch datasource]({{ site.baseurl }}/img/2020-11-27-aws_billing_metrics_and_grafana_cloud/grafana_cloud_select_cloudwatch_datasource.png)&lt;/p&gt;
&lt;p&gt;As for credentials, use the access key and secret key from the IAM user.&lt;/p&gt;
&lt;p&gt;![Grafana Cloud configuration cloudwatch datasource]({{ site.baseurl }}/img/2020-11-27-aws_billing_metrics_and_grafana_cloud/grafana_cloud_configuration_cloudwatch_datasource.png)&lt;/p&gt;
&lt;h1&gt;Creation of a dashboard with a graph panel&lt;/h1&gt;
&lt;p&gt;Now you can use the data source in the panels of a dashboard.&lt;/p&gt;
&lt;p&gt;![Grafana Cloud create dashboard]({{ site.baseurl }}/img/2020-11-27-aws_billing_metrics_and_grafana_cloud/grafana_cloud_create_dashboard.png)&lt;/p&gt;
&lt;p&gt;![Grafana Cloud add panel]({{ site.baseurl }}/img/2020-11-27-aws_billing_metrics_and_grafana_cloud/grafana_cloud_add_panel.png)&lt;/p&gt;
&lt;p&gt;This is a panel configuration example for the costs of all linked accounts.&lt;/p&gt;
&lt;p&gt;![Grafana Cloud accounts panel]({{ site.baseurl }}/img/2020-11-27-aws_billing_metrics_and_grafana_cloud/grafana_cloud_accounts_panel.png)&lt;/p&gt;
&lt;p&gt;This should be considered:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;If the CloudWatch data source is not the default one, switch to the CloudWatch data source.&lt;/li&gt;
&lt;li&gt;Billing metrics are all in the region us-east-1&lt;/li&gt;
&lt;li&gt;The time frame &quot;this month&quot; seems the right choice for the monthly costs :)&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;And ready to go 🎉&lt;/p&gt;
&lt;p&gt;![Grafana Cloud created dashboard]({{ site.baseurl }}/img/2020-11-27-aws_billing_metrics_and_grafana_cloud/grafana_cloud_created_dashboard.png)&lt;/p&gt;
&lt;h1&gt;Usage of a prepared dashboard&lt;/h1&gt;
&lt;p&gt;Here &lt;a href=&quot;https://grafana.com/grafana/dashboards&quot;&gt;https://grafana.com/grafana/dashboards&lt;/a&gt; is a big list of official and community dashboards you can import to Grafana.&lt;/p&gt;
&lt;p&gt;You can import a URL or ID from the list or, e.g., this &lt;a href=&quot;https://grafana.com/grafana/dashboards/13446&quot;&gt;https://grafana.com/grafana/dashboards/13446&lt;/a&gt; Dashboard in that way.&lt;/p&gt;
&lt;p&gt;![Grafana Cloud import dashboard]({{ site.baseurl }}/img/2020-11-27-aws_billing_metrics_and_grafana_cloud/grafana_cloud_import_dashboard.png)&lt;/p&gt;
&lt;p&gt;The selection of the data source is required.&lt;/p&gt;
&lt;p&gt;![Grafana Cloud import dashboard2]({{ site.baseurl }}/img/2020-11-27-aws_billing_metrics_and_grafana_cloud/grafana_cloud_import_dashboard2.png)&lt;/p&gt;
&lt;p&gt;And ready to go 🎉&lt;/p&gt;
&lt;p&gt;![Grafana Cloud imported dashboard]({{ site.baseurl }}/img/2020-11-27-aws_billing_metrics_and_grafana_cloud/grafana_cloud_imported_dashboard.png)&lt;/p&gt;
&lt;h1&gt;more dashboards&lt;/h1&gt;
&lt;p&gt;Monitoring Artist has many more cloud watch dashboards: &lt;a href=&quot;https://grafana.com/orgs/monitoringartist&quot;&gt;https://grafana.com/orgs/monitoringartist&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>Quick example to get the average of items from different arrays</title><link>https://johanneskonings.dev/blog/2020-10-19-example_react_average_of_items_in_different_arrays/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2020-10-19-example_react_average_of_items_in_different_arrays/</guid><description>Example how to calculate the average of different arrays Javascript (map, reduce, concat, ...)</description><pubDate>Mon, 19 Oct 2020 08:15:18 GMT</pubDate><content:encoded>&lt;h1&gt;⚠ Disclaimer&lt;/h1&gt;
&lt;p&gt;This is a quick example how to calculate the average of different items from different arrays. If there are better solutions, please let me know.&lt;/p&gt;
&lt;h1&gt;Overview&lt;/h1&gt;
&lt;p&gt;The data basis are three fruit baskets with different kinds of fruits an the number ot items in the basket. With a React webpage the several baskets can be choosed and the average of the items will be calculated.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const fruitBaskets = [
  {
    name: &quot;fruitBasket1&quot;,
    fruitBasket: [
      { fruitName: &quot;Apple&quot;, count: 5 },
      { fruitName: &quot;Banana&quot;, count: 3 },
      { fruitName: &quot;Strawberry&quot;, count: 9 },
      { fruitName: &quot;Lemon&quot;, count: 7 }
    ]
  },
  {
    name: &quot;fruitBasket2&quot;,
    fruitBasket: [
      { fruitName: &quot;Apple&quot;, count: 5 },
      { fruitName: &quot;Banana&quot;, count: 8 },
      { fruitName: &quot;Lemon&quot;, count: 3 }
    ]
  },
  {
    name: &quot;fruitBasket3&quot;,
    fruitBasket: [
      { fruitName: &quot;Apple&quot;, count: 5 },
      { fruitName: &quot;Banana&quot;, count: 3 },
      { fruitName: &quot;Orange&quot;, count: 3 },
      { fruitName: &quot;Lemon&quot;, count: 9 },
      { fruitName: &quot;Strawberry&quot;, count: 5 }
    ]
  }
];
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Implementation&lt;/h1&gt;
&lt;p&gt;The implementation looks as follows:&lt;/p&gt;
&lt;h2&gt;The webpage - App.js&lt;/h2&gt;
&lt;p&gt;The &lt;code&gt;App.js&lt;/code&gt; contains the selection of the fruit baskets via a dropdown and a table for the results. The coding is &lt;a href=&quot;https://stackblitz.com/edit/example-react-average-of-items-in-different-arrays?file=src%2FApp.js&quot;&gt;here&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;![app.js]({{ site.baseurl }}/img/2020-10-19-example_react_average_of_items_in_different_arrays/react_frui_basket_selection.png)&lt;/p&gt;
&lt;h2&gt;The calculation - data.js&lt;/h2&gt;
&lt;p&gt;The &lt;code&gt;data.js&lt;/code&gt; contains the data of the fruit baskets and the calculation input data. The coding is &lt;a href=&quot;https://stackblitz.com/edit/example-react-average-of-items-in-different-arrays?file=src%2Fdata.js&quot;&gt;here&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;The calculation has three steps and is described with the baskets 1 and 2 and is in this format.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;[
  [
    { &quot;fruitName&quot;: &quot;Apple&quot;, &quot;count&quot;: 5 },
    { &quot;fruitName&quot;: &quot;Banana&quot;, &quot;count&quot;: 3 },
    { &quot;fruitName&quot;: &quot;Strawberry&quot;, &quot;count&quot;: 9 },
    { &quot;fruitName&quot;: &quot;Lemon&quot;, &quot;count&quot;: 7 }
  ],
  [
    { &quot;fruitName&quot;: &quot;Apple&quot;, &quot;count&quot;: 5 },
    { &quot;fruitName&quot;: &quot;Banana&quot;, &quot;count&quot;: 8 },
    { &quot;fruitName&quot;: &quot;Lemon&quot;, &quot;count&quot;: 3 }
  ]
]
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;merge&lt;/h3&gt;
&lt;p&gt;Basket 1 and 2 are selected. This two arrays will be merged with this coding.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;selectedFruitBaskets.forEach((selectedFruitBasket) =&amp;gt; {
  const found = fruitBaskets.find(
    (fruitBasket) =&amp;gt; fruitBasket.name === selectedFruitBasket.name,
  );
  fruits.push(found.fruitBasket);
});

const basketCounts = fruits.length;

const mergedBasket = [].concat(...fruits);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;After&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;[
  { &quot;fruitName&quot;: &quot;Apple&quot;, &quot;count&quot;: 5 },
  { &quot;fruitName&quot;: &quot;Banana&quot;, &quot;count&quot;: 3 },
  { &quot;fruitName&quot;: &quot;Strawberry&quot;, &quot;count&quot;: 9 },
  { &quot;fruitName&quot;: &quot;Lemon&quot;, &quot;count&quot;: 7 },
  { &quot;fruitName&quot;: &quot;Apple&quot;, &quot;count&quot;: 5 },
  { &quot;fruitName&quot;: &quot;Banana&quot;, &quot;count&quot;: 8 },
  { &quot;fruitName&quot;: &quot;Lemon&quot;, &quot;count&quot;: 3 }
]
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;sum&lt;/h3&gt;
&lt;p&gt;At this step the number of fruits of each kind will be summed&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const basketSum = mergedBasket.reduce((acc, curr) =&amp;gt; {
  if (!acc[curr.fruitName]) {
    acc[curr.fruitName] = { ...curr, countInBaskets: 1, sum: curr.count };
    return acc;
  }
  acc[curr.fruitName].countInBaskets += 1;
  acc[curr.fruitName].sum += curr.count;

  return acc;
}, {});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;After&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
  &quot;Apple&quot;: {
    &quot;count&quot;: 5,
    &quot;countInBaskets&quot;: 2,
    &quot;fruitName&quot;: &quot;Apple&quot;,
    &quot;sum&quot;: 10
  },
  &quot;Banana&quot;: {
    &quot;count&quot;: 3,
    &quot;countInBaskets&quot;: 2,
    &quot;fruitName&quot;: &quot;Banana&quot;,
    &quot;sum&quot;: 11
  },
  &quot;Lemon&quot;: {
    &quot;count&quot;: 7,
    &quot;countInBaskets&quot;: 2,
    &quot;fruitName&quot;: &quot;Lemon&quot;,
    &quot;sum&quot;: 10
  },
  &quot;Strawberry&quot;: {
    &quot;count&quot;: 9,
    &quot;countInBaskets&quot;: 1,
    &quot;fruitName&quot;: &quot;Strawberry&quot;,
    &quot;sum&quot;: 9
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;average&lt;/h3&gt;
&lt;p&gt;After the summation the average can be calulated.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;const basketAverage = Object.keys(basketSum).map((fruitName) =&amp;gt; {
  const item = basketSum[fruitName];
  return {
    fruitName: item.fruitName,
    averageCountOverall: item.sum / basketCounts,
    averageCountWithMinOne: item.sum / item.countInBaskets,
    sum: item.sum,
  };
});
return basketAverage;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;After&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;[
  {
    &quot;averageCountOverall&quot;: 5,
    &quot;averageCountWithMinOne&quot;: 5,
    &quot;fruitName&quot;: &quot;Apple&quot;,
    &quot;sum&quot;: 10
  },
  {
    &quot;averageCountOverall&quot;: 5.5,
    &quot;averageCountWithMinOne&quot;: 5.5,
    &quot;fruitName&quot;: &quot;Banana&quot;,
    &quot;sum&quot;: 11
  },
  {
    &quot;averageCountOverall&quot;: 4.5,
    &quot;averageCountWithMinOne&quot;: 9,
    &quot;fruitName&quot;: &quot;Strawberry&quot;,
    &quot;sum&quot;: 9
  },
  {
    &quot;averageCountOverall&quot;: 5,
    &quot;averageCountWithMinOne&quot;: 5,
    &quot;fruitName&quot;: &quot;Lemon&quot;,
    &quot;sum&quot;: 10
  }
]
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Result&lt;/h1&gt;
&lt;p&gt;![result]({{ site.baseurl }}/img/2020-10-19-example_react_average_of_items_in_different_arrays/result.gif)&lt;/p&gt;
&lt;h1&gt;Coding&lt;/h1&gt;
&lt;p&gt;See the Coding on GitHub or StackBlitz:&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/example-react-average-of-items-in-different-arrays&quot;&gt;GitHub&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://stackblitz.com/edit/example-react-average-of-items-in-different-arrays&quot;&gt;StackBlitz&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>Quick example: pass data to a React component and get the changed value back</title><link>https://johanneskonings.dev/blog/2020-08-31-example_react_component_props_events/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/2020-08-31-example_react_component_props_events/</guid><description>An quick example how to pass data to componenents and get data from components</description><pubDate>Mon, 31 Aug 2020 08:15:18 GMT</pubDate><content:encoded>&lt;h1&gt;⚠ Disclaimer&lt;/h1&gt;
&lt;p&gt;This is a quick example how I pass data to a React component and get the changed data back. If there are better solutions, please let me know.&lt;/p&gt;
&lt;h1&gt;Overview&lt;/h1&gt;
&lt;p&gt;In different cases it&apos;s neccessary to pass data to a component and get the data back. In this quick example there are two child components. One is used to determine the height of increase in the count. The other one is used to increase the count via a button click with the height of increase from the other component.&lt;/p&gt;
&lt;p&gt;![result]({{ site.baseurl }}/img/2020-08-31-example_react_component_props_events/components_overview.png)&lt;/p&gt;
&lt;h1&gt;Implementation&lt;/h1&gt;
&lt;p&gt;The implementation looks as follows:&lt;/p&gt;
&lt;h2&gt;App.js&lt;/h2&gt;
&lt;p&gt;The &lt;code&gt;App.js&lt;/code&gt; contains the two child components &lt;code&gt;CounterSteps.js&lt;/code&gt; and &lt;code&gt;Button.js&lt;/code&gt;.
From the &lt;code&gt;CounterSteps.js&lt;/code&gt; the &lt;code&gt;App.js&lt;/code&gt; get the height of the increase via a event and store it in the &lt;code&gt;counterSteps&lt;/code&gt; state. The &lt;code&gt;counterSteps&lt;/code&gt; value will be passed to &lt;code&gt;Button.js&lt;/code&gt;. After each time the Button was pressed the &lt;code&gt;App.js&lt;/code&gt; get the value back.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import React, { useState } from &quot;react&quot;;
import CounterSteps from &quot;./CounterSteps&quot;;
import Button from &quot;./Button&quot;;
import &quot;./style.css&quot;;

export default function App() {
  const [counterSteps, setCounterSteps] = useState(0);
  const [count, setCount] = useState(0);

  const handleCurrentInput = (currentInput) =&amp;gt; {
    setCounterSteps(currentInput);
  };

  const handleCounterIncreased = (counterSteps) =&amp;gt; {
    const newCount = count + parseInt(counterSteps);
    setCount(newCount);
  };

  return (
    &amp;lt;div&amp;gt;
      &amp;lt;h1&amp;gt;Hello StackBlitz!&amp;lt;/h1&amp;gt;
      &amp;lt;p&amp;gt;current counterStepsInput: {counterSteps}&amp;lt;/p&amp;gt;
      &amp;lt;p&amp;gt;current count: {count}&amp;lt;/p&amp;gt;
      &amp;lt;CounterSteps onCurrentInput={handleCurrentInput} /&amp;gt;
      &amp;lt;Button
        counterSteps={counterSteps}
        onCounterIncreased={handleCounterIncreased}
      /&amp;gt;
    &amp;lt;/div&amp;gt;
  );
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;CounterSteps.js&lt;/h2&gt;
&lt;p&gt;In the &lt;code&gt;CounterSteps.js&lt;/code&gt; is a input field. Every change of the value will be passed via a event to the parent component.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import React, { useState } from &quot;react&quot;;
import PropTypes from &quot;prop-types&quot;;
import &quot;./style.css&quot;;

export default function CounterSteps(props) {
  const [count, setCount] = useState(0);
  const { onCurrentInput } = props;

  const handleInput = (event) =&amp;gt; {
    onCurrentInput(event.target.value);
  };

  return (
    &amp;lt;div&amp;gt;
      &amp;lt;p&amp;gt;
        &amp;lt;input
          type=&quot;number&quot;
          name=&quot;counterSteps&quot;
          placeholder=&quot;counterSteps&quot;
          onKeyDown={(e) =&amp;gt; /[\+\-\.\,]$/.test(e.key) &amp;amp;&amp;amp; e.preventDefault()}
          onInput={handleInput}
        /&amp;gt;
      &amp;lt;/p&amp;gt;
    &amp;lt;/div&amp;gt;
  );
}

CounterSteps.propTypes = {
  onCurrentInput: PropTypes.func,
};
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Button.js&lt;/h2&gt;
&lt;p&gt;The &lt;code&gt;Button.js&lt;/code&gt; receive the height of the inccrease from the parent component. A Button click calls the event and pass the height of increase back. In the &lt;code&gt;App.js&lt;/code&gt; the total count is calculated.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import React, { useState, useEffect } from &quot;react&quot;;
import PropTypes from &quot;prop-types&quot;;
import &quot;./style.css&quot;;

export default function Button(props) {
  const [counterSteps, setCounterSteps] = useState(0);
  const { onCounterIncreased } = props;

  useEffect(() =&amp;gt; {
    setCounterSteps(props.counterSteps);
  }, [props]);

  const increaseCount = () =&amp;gt; {
    onCounterIncreased(counterSteps);
  };

  return (
    &amp;lt;div&amp;gt;
      &amp;lt;button onClick={increaseCount}&amp;gt;increase counter&amp;lt;/button&amp;gt;
    &amp;lt;/div&amp;gt;
  );
}

Button.propTypes = {
  onCounterIncreased: PropTypes.func,
};
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Result&lt;/h1&gt;
&lt;p&gt;![result]({{ site.baseurl }}/img/2020-08-31-example_react_component_props_events/result.gif)&lt;/p&gt;
&lt;h1&gt;Coding&lt;/h1&gt;
&lt;p&gt;See the Coding on GitHub or StackBlitz:&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/example-react-component-props-events&quot;&gt;GitHub&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://stackblitz.com/edit/example-react-component-props-events&quot;&gt;StackBlitz&lt;/a&gt;&lt;/p&gt;
</content:encoded></item><item><title>AWS Amplify Console: How I changed master branchenv to main</title><link>https://johanneskonings.dev/blog/aws-amplify-console-changed-master-branchenv-main/</link><guid isPermaLink="true">https://johanneskonings.dev/blog/aws-amplify-console-changed-master-branchenv-main/</guid><description>Change of the master branch end amplfiy env to main and migration of the env data</description><pubDate>Tue, 18 Aug 2020 08:15:18 GMT</pubDate><content:encoded>&lt;p&gt;As part of #BlackLivesMatter there was a discussion to rename git branch master to main. How to do that in git and GitHub and some more about the background is described here:
&lt;a href=&quot;http://www.kapwing.com/blog/how-to-rename-your-master-branch-to-main-in-git/&quot;&gt;http://www.kapwing.com/blog/how-to-rename-your-master-branch-to-main-in-git/&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;Together with the master branch I had a master env, which was connected to the git master branch in the amplify console. I also wanted to rename this master env.&lt;/p&gt;
&lt;h1&gt;AWS Amplify&lt;/h1&gt;
&lt;p&gt;After the master branch was renamed to main, the corresponding env have to be also renamed from master to main.&lt;/p&gt;
&lt;h2&gt;Create new env&lt;/h2&gt;
&lt;p&gt;Check If you are in the master env with &lt;code&gt;amplify status&lt;/code&gt;. If not checkout the master env with &lt;code&gt;amplify env checkout master&lt;/code&gt;.
Run the command &lt;code&gt;amplify add env main&lt;/code&gt; and &lt;code&gt;amplify push&lt;/code&gt;. After that, you can connect the main branch with the main env in the amplify console.&lt;/p&gt;
&lt;h2&gt;Connection of main branch with main env in the Amplify Console&lt;/h2&gt;
&lt;p&gt;The new main branch and the new main env can now be connected in the amplify console.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./amplify_console_connection_branch_to_env1.png&quot; alt=&quot;Amplify Console connection branch to env&quot; /&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./amplify_console_connection_branch_to_env2.png&quot; alt=&quot;Amplify Console connection branch to env&quot; /&gt;&lt;/p&gt;
&lt;p&gt;Afterwards the production branch must be changed to main.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./amplify_console_production_branch.png&quot; alt=&quot;Amplify Console production branch&quot; /&gt;&lt;/p&gt;
&lt;h1&gt;Migration&lt;/h1&gt;
&lt;p&gt;The new env main is now ready to use, but the data is missing. That must be migrated from the master env to the main env. In my case, it was Cognito users, DynamoDB data and S3 profile photos.&lt;/p&gt;
&lt;h2&gt;Cognito&lt;/h2&gt;
&lt;p&gt;There are some approaches to export und import cognito users:&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-user-pools-import-users.html&quot;&gt;https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-user-pools-import-users.html&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://medium.com/collaborne-engineering/migrate-aws-cognito-user-pools-ff2a91a745a2&quot;&gt;https://medium.com/collaborne-engineering/migrate-aws-cognito-user-pools-ff2a91a745a2&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;However, since it is a small internal app, it is sufficient for users to register again 😊&lt;/p&gt;
&lt;h2&gt;DynamoDb&lt;/h2&gt;
&lt;p&gt;The DynamoDB data is more important. The easiest way to migrate the data was with a shell script and the aws cli:&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/JohannesKonings/fff-badminton/blob/main/AmplifyEnvMigration/dynamodb-migration.sh&quot;&gt;dynamodb-migration.sh&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;S3&lt;/h2&gt;
&lt;p&gt;As described here the users can upload new profile pictures:
&lt;a href=&quot;https://dev.to/johanneskonings/aws-amplify-storage-react-example-for-avatar-pictures-273o&quot;&gt;AWS Amplify Storage: React example for Avatar pictures&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;That profile pictures can also be copied by a script &lt;a href=&quot;https://github.com/JohannesKonings/fff-badminton/blob/main/AmplifyEnvMigration/s3-migration.sh&quot;&gt;s3-migration.sh&lt;/a&gt;. But it&apos;s necessary to rename the folder name to the new identity pool identity browser id, that the pictures will be found.&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./identity_browser.png&quot; alt=&quot;identity browser&quot; /&gt;&lt;/p&gt;
&lt;p&gt;I fixed that manually 😊&lt;/p&gt;
</content:encoded></item></channel></rss>